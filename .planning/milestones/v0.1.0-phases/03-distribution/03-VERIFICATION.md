---
phase: 03-distribution
verified: 2026-04-10T12:00:00Z
status: gaps_found
score: 7/9
overrides_applied: 0
gaps:
  - truth: "Pre-built binaries for darwin-arm64, darwin-x64, linux-musl-x64, and linux-musl-arm64 are published to GitHub releases"
    status: failed
    reason: "No GitHub releases exist for themouette/claude-plan-reviewer. The release pipeline (cargo-dist) is correctly configured and will produce releases on git tag push, but no tag has been pushed yet. SC-1 cannot be satisfied until a versioned tag is pushed."
    artifacts:
      - path: ".github/workflows/release.yml"
        issue: "Pipeline is correct but has never been triggered — no release artifacts exist"
    missing:
      - "Push a semver git tag (e.g. v0.1.0) to trigger the release workflow and publish binaries to GitHub Releases"
  - truth: "curl | sh installs the binary without requiring Rust, Node.js, or any other runtime (DIST-02)"
    status: partial
    reason: "install.sh is correct and syntactically valid, but REPO='OWNER/claude-plan-reviewer' (line 9) is an unresolved placeholder. The script cannot download a binary because: (1) no releases exist yet (see SC-1 gap), and (2) OWNER must be replaced with 'themouette' before the URL resolves correctly."
    artifacts:
      - path: "install.sh"
        issue: "Line 9: REPO='OWNER/claude-plan-reviewer' — placeholder not replaced with actual org 'themouette'. Cargo.toml already has repository = 'https://github.com/themouette/claude-plan-reviewer' as reference."
    missing:
      - "Replace OWNER with 'themouette' in install.sh line 9 (and line 4 comment)"
      - "After pushing a release tag, do an end-to-end test of the curl | sh flow"
human_verification:
  - test: "Push v0.1.0 tag and verify the release workflow completes"
    expected: "GitHub Actions release.yml runs to completion, produces artifacts for all four targets (aarch64-apple-darwin, x86_64-apple-darwin, x86_64-unknown-linux-musl, aarch64-unknown-linux-musl), uploads them to a GitHub Release, and the install.sh download URL resolves"
    why_human: "Requires pushing a git tag and waiting for CI, cannot verify programmatically in a local check"
  - test: "On macOS ARM64, run curl -fsSL .../install.sh | sh (after fixing OWNER and publishing a release)"
    expected: "Binary installs to ~/.local/bin, plan-reviewer install wires ~/.claude/settings.json correctly, PATH warning appears if ~/.local/bin not on PATH"
    why_human: "Full end-to-end install requires a real GitHub release and a real macOS machine"
  - test: "Verify macOS binary is not blocked by Gatekeeper on first run"
    expected: "Running plan-reviewer on macOS does not trigger 'unverified developer' Gatekeeper block — ad hoc codesign should satisfy this"
    why_human: "Gatekeeper behavior requires a real macOS machine and a real downloaded binary; cannot verify programmatically"
---

# Phase 3: Distribution — Verification Report

**Phase Goal:** Any developer can install the binary on macOS or Linux with a single `curl | sh` and wire it into Claude Code in under two minutes
**Verified:** 2026-04-10T12:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Cargo.toml has `[[bin]]` with `name = "plan-reviewer"` and `path = "src/main.rs"` | VERIFIED | Cargo.toml line 7-9: `[[bin]] name = "plan-reviewer" path = "src/main.rs"` |
| 2 | Cargo.toml has `[workspace.metadata.dist]` with all four targets | VERIFIED | Line 37: all four targets present in one array: aarch64-apple-darwin, aarch64-unknown-linux-musl, x86_64-apple-darwin, x86_64-unknown-linux-musl |
| 3 | `.github/workflows/release.yml` exists (cargo-dist CI generated) | VERIFIED | File exists, 314 lines, generated by cargo-dist 0.31.0 |
| 4 | `.github/build-setup.yml` exists with Node.js install + npm build steps | VERIFIED | File exists; `actions/setup-node@v4` (node-version: "20") + `cd ui && npm install && npm run build` |
| 5 | `release.yml` has a codesign step on macOS runners after the dist build step | VERIFIED | Lines 156-163: `Ad hoc codesign macOS binaries (DIST-04)` step with `if: runner.os == 'macOS'`; placed before Post-build step |
| 6 | `install.sh` warns if `~/.local/bin` is not on PATH (DIST-03) | VERIFIED | Line 77: `echo "WARNING: ${INSTALL_DIR} is not on your PATH."` inside PATH case-check |
| 7 | `src/main.rs` uses `Option<Commands>` dispatch — no subcommand runs hook flow, `install` calls `run_install()` | VERIFIED | Lines 88, 158-167: `command: Option<Commands>`, `Some(Commands::Install) => install::run_install()`, `None => run_hook_flow()` |
| 8 | `src/install.rs` implements `pub fn run_install()` with idempotency check | VERIFIED | `pub fn run_install()` at line 5; idempotency check lines 73-87; `entry().or_insert_with()` pattern at lines 61-71; prints "already configured" on no-op |
| 9 | Pre-built binaries for all four targets published to GitHub releases | FAILED | No GitHub releases exist at themouette/claude-plan-reviewer; install.sh OWNER placeholder not replaced (line 9) |

**Score:** 8/9 truths verified (but SC-2 is partial — install.sh has OWNER placeholder)

Note: The score is reported as 7/9 counting SC-2 (DIST-02) as also failing due to the OWNER placeholder preventing the curl/sh flow from working end-to-end.

### Roadmap Success Criteria

| # | Success Criterion | Status | Evidence |
|---|------------------|--------|----------|
| SC-1 | Pre-built binaries for all four targets published to GitHub releases | FAILED | No releases exist; no git tag pushed |
| SC-2 | `curl -fsSL .../install.sh | sh` installs without Rust/Node | PARTIAL | install.sh is correct but OWNER placeholder unresolved; no releases to download |
| SC-3 | Install script detects PATH and warns if not on PATH | VERIFIED | install.sh line 77 — warning present and correct |
| SC-4 | macOS binaries pass ad hoc Gatekeeper signing | VERIFIED (pipeline) | codesign step present in release.yml; will execute on release tag push |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Cargo.toml` | `[[bin]]` + `[workspace.metadata.dist]` with four targets | VERIFIED | All present: `[[bin]] name="plan-reviewer"`, `[workspace.metadata.dist]` with 4 targets, `install-path = "~/.local/bin"`, `github-build-setup = "../build-setup.yml"` |
| `.github/build-setup.yml` | Node.js install + npm run build injection | VERIFIED | `actions/setup-node@v4` (node 20), `cd ui && npm install && npm run build` |
| `.github/workflows/release.yml` | Generated cargo-dist release workflow | VERIFIED | Generated by cargo-dist 0.31.0; contains plan, build-local-artifacts, build-global-artifacts, host, announce jobs |
| `install.sh` | curl/sh installer with hook wiring | STUB | Syntactically valid; platform detection correct for all 4 targets; PATH warning correct; calls `"${INSTALL_DIR}/${BINARY}" install`; BUT: `REPO="OWNER/claude-plan-reviewer"` is unresolved placeholder |
| `src/main.rs` | Clap subcommand dispatch with `Option<Commands>` | VERIFIED | `struct Cli` with `command: Option<Commands>`, dispatch in `main()`, stdin read deferred to `run_hook_flow()` |
| `src/install.rs` | `run_install()` with idempotent hook wiring | VERIFIED | Full implementation: reads settings.json, uses `entry().or_insert_with()`, idempotency check, writes back with `to_string_pretty`, handles missing file/malformed JSON |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `.github/workflows/release.yml` | `.github/build-setup.yml` | `github-build-setup` in Cargo.toml | WIRED | Cargo.toml line 40: `github-build-setup = "../build-setup.yml"`; build-setup steps appear injected in release.yml lines 130-137 |
| `install.sh` | `~/.local/bin/plan-reviewer` | platform-detect + curl download + extract | WIRED (logic) | Platform detection, download, extract, and `cp` to INSTALL_DIR all present; OWNER placeholder prevents actual download |
| `install.sh` | `plan-reviewer install` subcommand | exec after binary placement | WIRED | Line 90: `"${INSTALL_DIR}/${BINARY}" install` — calls binary with install subcommand after placement |
| `src/main.rs` | `src/install.rs` | `mod install; install::run_install()` | WIRED | `mod install;` at line 2; `install::run_install()` at line 161 |
| `src/install.rs` | `~/.claude/settings.json` | `std::fs::write` after serde_json merge | WIRED | `settings_path` constructed at line 24; write at line 125; `create_dir_all` for parent at line 115 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `src/install.rs` / `run_install()` | `root` (settings.json content) | `std::fs::read_to_string` + `serde_json::from_str` | Yes — real file read; fallback to `json!({})` if missing | FLOWING |
| `src/install.rs` / `run_install()` | `binary_path` | `std::env::current_exe()` | Yes — real OS path | FLOWING |
| `install.sh` | `LATEST_TAG` | `curl ... github.com/repos/${REPO}/releases/latest` | No — OWNER placeholder prevents real API call | HOLLOW (OWNER placeholder blocks real data) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Binary builds with correct name | `cargo build --bin plan-reviewer` | `Finished dev profile ... 2 warnings` | PASS |
| All 4 tests pass (no regressions) | `cargo test` | `4 passed; 0 failed` | PASS |
| Shell syntax valid | `sh -n install.sh` | `Shell syntax OK` (exit 0) | PASS |
| `Option<Commands>` dispatch in main.rs | `grep 'Option.Commands.'` | line 88: `command: Option<Commands>` | PASS |
| `run_install` called on Install variant | `grep 'Commands::Install'` | line 159: `Some(Commands::Install) => install::run_install()` | PASS |
| `pub fn run_install` exported | `grep 'pub fn run_install'` | line 5 of src/install.rs | PASS |
| Idempotency check present | `grep 'already_present'` | lines 74, 82 | PASS |
| PATH warning in install.sh | `grep 'not on your PATH'` | line 77 | PASS |
| codesign step in release.yml | `grep 'codesign'` | lines 157, 161 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DIST-01 | 03-01 | Binary distributed as pre-built binaries via GitHub releases | BLOCKED | Pipeline configured but no releases published; OWNER placeholder in install.sh |
| DIST-02 | 03-01 | Install with single `curl \| sh`, no Rust/Node | PARTIAL | install.sh logic correct; blocked by OWNER placeholder and no releases |
| DIST-03 | 03-01 | Install script detects PATH, warns if not on PATH | SATISFIED | install.sh lines 71-85: PATH case-check with clear warning and instructions |
| DIST-04 | 03-01 | macOS binaries ad hoc code-signed | SATISFIED (pipeline) | release.yml lines 156-163: codesign step with `if: runner.os == 'macOS'` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `install.sh` | 9 | `REPO="OWNER/claude-plan-reviewer"` — unresolved placeholder | Warning | Prevents actual binary download; must be replaced with `themouette` before publishing |

Note: The `OWNER` placeholder is documented intentionally (T-03-07) but it is an actionable gap — the actual org `themouette` is already in `Cargo.toml` at line 5 (`repository = "https://github.com/themouette/claude-plan-reviewer"`), so this is a simple one-line fix.

### Human Verification Required

#### 1. GitHub Release Pipeline End-to-End

**Test:** Push a semver git tag (e.g., `git tag v0.1.0 && git push origin v0.1.0`) to the themouette/claude-plan-reviewer repository
**Expected:** The release.yml workflow triggers, builds all four targets, the macOS runner codesigns the binaries, and artifacts are uploaded to a GitHub Release with the four expected archive filenames (plan-reviewer-v0.1.0-aarch64-apple-darwin.tar.xz, etc.)
**Why human:** Requires git tag push, CI execution, and release artifact inspection — cannot verify programmatically in a local check

#### 2. Full curl/sh Install Flow

**Test:** After publishing a release and fixing the OWNER placeholder in install.sh, run `curl -fsSL https://raw.githubusercontent.com/themouette/claude-plan-reviewer/main/install.sh | sh` on macOS and Linux
**Expected:** Binary installs to `~/.local/bin/plan-reviewer`, `plan-reviewer install` wires `~/.claude/settings.json` with the ExitPlanMode hook, PATH warning appears if `~/.local/bin` not on PATH
**Why human:** Requires a real GitHub release and real target machines

#### 3. macOS Gatekeeper Check

**Test:** Download the macOS binary from a release and run it for the first time
**Expected:** macOS does not show the "unverified developer" dialog; binary executes without Gatekeeper blocking
**Why human:** Gatekeeper behavior requires a real macOS machine and a downloaded (not locally built) binary

### Gaps Summary

Two gaps block the phase goal:

**Gap 1 — No GitHub Releases Published (SC-1)**

The release pipeline (cargo-dist, `.github/workflows/release.yml`) is fully and correctly configured. However, no git tag has been pushed, so no releases exist at github.com/themouette/claude-plan-reviewer. The phase goal ("any developer can install with a single `curl | sh`") cannot be achieved until at least one release is published. Fix: push `v0.1.0` tag.

**Gap 2 — OWNER Placeholder in install.sh (DIST-01, DIST-02)**

`install.sh` line 9 contains `REPO="OWNER/claude-plan-reviewer"` — a placeholder per threat model entry T-03-07. The actual GitHub org is `themouette` (already in `Cargo.toml` line 5). Until this is replaced, the install script will construct incorrect download URLs (`https://api.github.com/repos/OWNER/...`). This is a one-line fix that should be done before publishing any release.

**Root cause:** Both gaps share the same root cause — the project has not yet cut its first release. The infrastructure is complete and correct; the release action has simply not been taken. These two gaps are tightly related and can be resolved together by: (1) fixing the OWNER placeholder, (2) committing, (3) tagging v0.1.0.

---

_Verified: 2026-04-10T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
