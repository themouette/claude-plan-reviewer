---
phase: 05-integration-architecture
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified: ["src/main.rs"]
autonomous: true
gap_closure: true
requirements: ["INTEG-05"]

must_haves:
  truths:
    - "Running `plan-reviewer --help` shows the supported integrations (claude, gemini, opencode) in the top-level about text"
  artifacts:
    - path: "src/main.rs"
      provides: "Updated CLI about text mentioning supported integrations"
      contains: "claude, gemini, opencode"
  key_links: []
---

<objective>
Fix the top-level CLI `about` text so `plan-reviewer --help` shows the supported integrations.

Purpose: UAT test 2 found that `plan-reviewer --help` only says "Claude Code plan reviewer hook binary" without listing supported integrations. The install/uninstall subcommand help is correct, but the top-level help omits the integration roster.
Output: Updated `src/main.rs` with integration list in the clap `about` attribute.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-architecture/05-01-SUMMARY.md
@.planning/phases/05-integration-architecture/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI about text with integration roster</name>
  <files>src/main.rs</files>
  <action>
On line 89 of src/main.rs, change the clap `about` attribute from:

```rust
#[command(version, about = "Claude Code plan reviewer hook binary")]
```

to:

```rust
#[command(version, about = "Plan reviewer hook binary (supports: claude, gemini, opencode)")]
```

This removes the Claude-specific naming from the top-level help since the tool now supports multiple integrations. The parenthetical lists exactly the three IntegrationSlug variants defined in src/integrations/mod.rs.

After editing, run `cargo fmt` and `cargo clippy -- -D warnings` to satisfy the pre-commit hook.
  </action>
  <verify>
    <automated>cargo build --quiet && cargo run -- --help 2>&1 | grep -q "supports: claude, gemini, opencode" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Running `plan-reviewer --help` displays "Plan reviewer hook binary (supports: claude, gemini, opencode)" in the about line. All existing tests still pass (`cargo test`). Clippy and fmt are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This change modifies a static help string only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | - | - | - | Static string change; no security surface affected |
</threat_model>

<verification>
1. `cargo build` succeeds
2. `cargo run -- --help` output contains "supports: claude, gemini, opencode"
3. `cargo test` passes (all 13 tests)
4. `cargo clippy -- -D warnings` clean
5. `cargo fmt --check` clean
</verification>

<success_criteria>
- `plan-reviewer --help` top-level output mentions all three supported integrations: claude, gemini, opencode
- No regression in existing tests
- UAT test 2 gap is closed
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-architecture/05-02-SUMMARY.md`
</output>
