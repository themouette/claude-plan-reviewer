---
phase: 07.2-insert-phase-a-from-planning-research-integration-plugin-rew
plan: 04
type: execute
wave: 3
gap_closure: true
depends_on:
  - 07.2-03
files_modified:
  - ui/src/App.tsx
autonomous: true
requirements:
  - INTEG-05
must_haves:
  truths:
    - "Help tab no longer mentions manual settings.json hook editing for install or disable"
    - "Help tab shows `plan-reviewer install claude|gemini|opencode` as the install method"
    - "Help tab shows `plan-reviewer uninstall claude|gemini|opencode` as the uninstall method"
    - "Help tab retains the --no-browser tip"
    - "Existing UI tests (if any) still pass after the change"
  artifacts:
    - path: "ui/src/App.tsx"
      provides: "Updated HelpView with plugin-model install/uninstall instructions"
      contains: "plan-reviewer install"
  key_links: []
---

<objective>
Update the webapp Help tab to reflect the new plugin-directory install model.

The existing HelpView in ui/src/App.tsx documents the old bare settings.json hook approach: manual JSON editing to enable, manual settings.json surgery to disable, `rm $(which claude-plan-reviewer)` to uninstall. This is now wrong — plan-reviewer has `install` and `uninstall` subcommands.

Output: Rewritten HelpView JSX that documents `plan-reviewer install <integration>` / `plan-reviewer uninstall <integration>` and removes the manual JSON-editing instructions.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<current_help_content>
The existing HelpView function (ui/src/App.tsx lines 180–262) contains:

1. Title: "claude-plan-reviewer"
2. Description: "Intercepts Claude Code's ExitPlanMode hook..."
3. Section "Disabling the hook" — instructs manual settings.json editing with raw JSON block
4. Section "Skipping the browser" — `claude-plan-reviewer --no-browser` ← keep this
5. Section "Uninstalling" — `rm $(which claude-plan-reviewer)` then remove hook from settings.json

Sections 3 and 5 are outdated. Section 2's description is still accurate but the binary name
has changed from `claude-plan-reviewer` to `plan-reviewer` (check Cargo.toml for confirmation).
</current_help_content>

<new_model>
After Phase 07.2, installation works via subcommands:
  - `plan-reviewer install claude`    — writes plugin dir + settings.json registration
  - `plan-reviewer install gemini`    — writes extension dir (auto-discovered, no settings.json)
  - `plan-reviewer install opencode`  — writes plugin .mjs file

Uninstallation works via:
  - `plan-reviewer uninstall claude`
  - `plan-reviewer uninstall gemini`
  - `plan-reviewer uninstall opencode`

The binary is invoked as `plan-reviewer` (check Cargo.toml [[bin]] name for the exact binary name).
</new_model>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite HelpView with plugin-model instructions</name>
  <files>ui/src/App.tsx</files>
  <read_first>
    - ui/src/App.tsx (lines 180–262 — the HelpView function to rewrite)
    - Cargo.toml (confirm the binary name under [[bin]] or [package] name)
    - src/main.rs (lines 250–290 — confirm Install/Uninstall subcommand names and integration args)
  </read_first>
  <action>
Rewrite the `HelpView` function body (the returned JSX only — keep all style constants unchanged).

**New section structure:**

1. **Title + description** — Keep the title. Update description to mention all three supported integrations (Claude Code, Gemini CLI, OpenCode).

2. **Installing** (new section, replaces nothing — this was absent):
   ```
   plan-reviewer install claude
   plan-reviewer install gemini
   plan-reviewer install opencode
   ```
   Add a brief note per integration explaining what it does:
   - Claude: registers a plugin directory in `~/.local/share/plan-reviewer/claude-plugin/` and adds entries to `~/.claude/settings.json`
   - Gemini: writes an extension directory at `~/.gemini/extensions/plan-reviewer/` (auto-discovered by Gemini CLI, no settings.json needed)
   - OpenCode: installs a plugin file at `~/.config/opencode/plugins/plan-reviewer-opencode.mjs`

3. **Uninstalling** (replaces old "Uninstalling" section):
   ```
   plan-reviewer uninstall claude
   plan-reviewer uninstall gemini
   plan-reviewer uninstall opencode
   ```
   Remove the old `rm $(which claude-plan-reviewer)` + manual settings.json surgery instructions entirely.

4. **Skipping the browser** — Keep this section. Update binary name from `claude-plan-reviewer` to the correct binary name from Cargo.toml.

5. **Remove entirely**: The old "Disabling the hook" section with the raw JSON block.

**Style:** Keep the existing `codeStyle`, `inlineCode`, `h2Style`, `pStyle` constants and use them consistently. Keep `maxWidth: '640px'` wrapper div. No new style variables needed.

**Do not** modify anything outside the `HelpView` function's return statement.
  </action>
  <verify>
    <automated>cd /Users/julien.muetton/Projects/lab/claude-plan-reviewer && npm --prefix ui run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/App.tsx contains "plan-reviewer install"
    - ui/src/App.tsx contains "plan-reviewer uninstall"
    - ui/src/App.tsx does NOT contain `rm $(which` (old uninstall instruction gone)
    - ui/src/App.tsx does NOT contain `"hooks": {` inside the HelpView JSX (old raw JSON block gone)
    - ui/src/App.tsx does NOT contain `settings.json` inside the HelpView JSX (manual settings editing gone)
    - ui/src/App.tsx contains `--no-browser` (tip preserved)
    - npm run build exits 0 (TypeScript + Vite build passes)
  </acceptance_criteria>
  <done>HelpView updated to document plan-reviewer install/uninstall subcommands for all three integrations. Manual settings.json instructions removed. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm --prefix ui run build` — exits 0 (no TypeScript errors, Vite build succeeds)
2. `grep 'plan-reviewer install' ui/src/App.tsx` — found
3. `grep 'plan-reviewer uninstall' ui/src/App.tsx` — found
4. `grep -c 'rm \$(which' ui/src/App.tsx` — returns 0 (old uninstall removed)
5. `grep -c '"hooks": {' ui/src/App.tsx` — returns 0 or only outside HelpView JSX
</verification>

<success_criteria>
- HelpView accurately describes install/uninstall via CLI subcommands for all three integrations
- No manual settings.json editing instructions remain in the Help tab
- The --no-browser tip is preserved
- UI builds cleanly with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-insert-phase-a-from-planning-research-integration-plugin-rew/07.2-04-SUMMARY.md`
</output>
