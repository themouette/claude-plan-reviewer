---
phase: 07.2-insert-phase-a-from-planning-research-integration-plugin-rew
plan: "04"
subsystem: frontend-ui
tags: [help-view, ui, documentation, install, uninstall]
dependency_graph:
  requires: []
  provides: [updated-help-view]
  affects: [ui/src/App.tsx]
tech_stack:
  added: []
  patterns: [react-tsx]
key_files:
  created: []
  modified:
    - ui/src/App.tsx
decisions:
  - "Binary name updated from claude-plan-reviewer to plan-reviewer in all HelpView text"
  - "Removed 'Disabling the hook' section (manual settings.json surgery) entirely"
  - "Removed 'Uninstalling' section using rm \$(which ...) — replaced with uninstall subcommand"
  - "Added 'Installing' section with per-integration descriptions for all three integrations"
metrics:
  duration: "~5 minutes"
  completed_date: "2026-04-11"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 1
---

# Phase 07.2 Plan 04: Update HelpView with Plugin-Model Install/Uninstall Instructions Summary

**One-liner:** Rewrote HelpView in App.tsx to document `plan-reviewer install/uninstall <integration>` for Claude Code, Gemini CLI, and OpenCode, removing all manual settings.json editing instructions.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Rewrite HelpView with plugin-model instructions | eac658f | ui/src/App.tsx |

## What Was Built

The `HelpView` function in `ui/src/App.tsx` was rewritten to reflect the new plugin-directory install model introduced in Phase 07.2:

**Removed:**
- "Disabling the hook" section with raw JSON block (`"hooks": { "PermissionRequest": [...] }`)
- Manual settings.json editing instructions (global and project-local paths)
- "Uninstalling" section using `rm $(which claude-plan-reviewer)` and manual settings.json surgery

**Added:**
- "Installing" section with `plan-reviewer install claude/gemini/opencode` commands
- Per-integration description: Claude (plugin dir + settings.json), Gemini (auto-discovered extension dir), OpenCode (plugin .mjs file)
- "Uninstalling" section with `plan-reviewer uninstall claude/gemini/opencode` commands

**Updated:**
- Title from `claude-plan-reviewer` to `plan-reviewer`
- Description expanded to mention all three supported integrations
- "Skipping the browser" section binary name updated from `claude-plan-reviewer --no-browser` to `plan-reviewer --no-browser`

## Verification Results

All acceptance criteria passed:
- `grep -c 'plan-reviewer install' ui/src/App.tsx` → 3
- `grep -c 'plan-reviewer uninstall' ui/src/App.tsx` → 3
- `grep -c 'rm \$(which' ui/src/App.tsx` → 0
- `grep -c '"hooks": {' ui/src/App.tsx` → 0
- `grep -c '\-\-no-browser' ui/src/App.tsx` → 2
- `npm --prefix ui run build` → exits 0 (TypeScript + Vite build passes)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — HelpView is documentation-only UI with no data stubs.

## Threat Flags

None — purely frontend documentation change; no new network endpoints, auth paths, or data access.

## Self-Check: PASSED

- File exists: ui/src/App.tsx — FOUND (modified)
- Commit eac658f — FOUND in git log
