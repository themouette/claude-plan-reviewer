---
phase: 26.1-commit-navigation-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/code-review/hooks/useDiff.ts
  - ui/src/code-review/hooks/useDiff.test.ts
  - ui/src/code-review/CodeReviewApp.tsx
  - ui/src/code-review/CommitDrawer.tsx
  - ui/src/code-review/CommitDrawer.test.ts
  - ui/src/code-review/DiffPane.tsx
  - ui/src/code-review/DiffPane.test.ts
autonomous: true
requirements:
  - COMMIT-01
  - COMMIT-02
  - COMMIT-03
  - COMMIT-04

must_haves:
  truths:
    - "fetchCommitDiffOnce rejects SHA strings that do not match /^[0-9a-f]{7,40}$/i before any network call (CR-01)"
    - "fetchFilteredBranchDiff returns { files: [], error: 'all fetches failed' } when every per-SHA fetch fails; returns { files: [...], error: null } when at least one succeeds (WR-02)"
    - "seededRef sentinel in CodeReviewApp prevents re-seeding checkedCommitShas after the user deselects all commits (CR-02)"
    - "Keyboard ArrowLeft/ArrowRight handler in CodeReviewApp returns early when findIndex returns -1 (WR-01)"
    - "CommitRow <li> elements include role='button', tabIndex={0}, and onKeyDown handler that fires onCommitClick on Enter (WR-03)"
    - "DiffPane shows a short-SHA fallback strip (activeCommitSha.slice(0,7)) when viewMode==='commit' and activeCommitSha is not found in commits (WR-04)"
    - "All vitest tests in useDiff.test.ts, CommitDrawer.test.ts, and DiffPane.test.ts pass (npm test exit 0)"
  artifacts:
    - path: "ui/src/code-review/hooks/useDiff.ts"
      provides: "SHA-validated fetchCommitDiffOnce and FetchDiffResult-returning fetchFilteredBranchDiff"
      contains: "SHA_RE"
    - path: "ui/src/code-review/hooks/useDiff.test.ts"
      provides: "New CR-01 invalid-SHA test, new WR-02 all-fail test, updated fetchFilteredBranchDiff tests using result.files"
      contains: "invalid sha"
    - path: "ui/src/code-review/CodeReviewApp.tsx"
      provides: "seededRef sentinel + findIndex -1 guard"
      contains: "seededRef"
    - path: "ui/src/code-review/CommitDrawer.tsx"
      provides: "Keyboard-accessible CommitRow <li> (role=button, tabIndex, onKeyDown)"
      contains: "role=\"button\""
    - path: "ui/src/code-review/DiffPane.tsx"
      provides: "Short-SHA fallback strip when activeCommit is null"
      contains: "activeCommitSha.slice(0, 7)"
    - path: "ui/src/code-review/CommitDrawer.test.ts"
      provides: "Source assertions for role=button, tabIndex=0, onKeyDown / Enter handler"
      contains: "role=\"button\""
    - path: "ui/src/code-review/DiffPane.test.ts"
      provides: "Source assertion for activeCommitSha.slice(0, 7) fallback"
      contains: "activeCommitSha.slice(0, 7)"
  key_links:
    - from: "ui/src/code-review/hooks/useDiff.ts (dispatch in useDiff hook)"
      to: "fetchFilteredBranchDiff result"
      via: "branch-union arm returns result directly"
      pattern: "return fetchFilteredBranchDiff"
    - from: "ui/src/code-review/CodeReviewApp.tsx (seed effect)"
      to: "seededRef.current"
      via: "guard check before scheduling setCheckedCommitShas"
      pattern: "seededRef.current"
    - from: "ui/src/code-review/CodeReviewApp.tsx (keydown effect)"
      to: "findIndex result"
      via: "early return when idx === -1"
      pattern: "if \\(idx === -1\\) return"
    - from: "ui/src/code-review/CommitDrawer.tsx (<li>)"
      to: "onCommitClick"
      via: "onKeyDown Enter handler"
      pattern: "e.key === 'Enter'"
    - from: "ui/src/code-review/DiffPane.tsx (title strip)"
      to: "activeCommitSha"
      via: "fallback render when activeCommit === null"
      pattern: "activeCommitSha.slice"
---

<objective>
Apply targeted patches for the 2 blockers (CR-01, CR-02) and 4 warnings (WR-01..WR-04) identified in `.planning/phases/26-commit-navigation/26-REVIEW.md`. Per D-04, all fixes live in a single consolidated plan with TDD-first ordering for the two issues that have testable behavioral contracts (CR-01 invalid-SHA guard, WR-02 all-fail return).

Purpose: Close the code-review gap in Phase 26's commit navigation feature before Phase 27 (Inline Comments) begins. Security (CR-01 path-traversal guard), correctness (CR-02 re-seed bug, WR-01 -1 guard, WR-02 silent-failure), and accessibility (WR-03 keyboard nav, WR-04 unknown-SHA fallback) all land together because they share the same 4 source files and the same test surface.

Output:
- `ui/src/code-review/hooks/useDiff.ts` (CR-01 SHA guard + WR-02 return-type change to `FetchDiffResult`)
- `ui/src/code-review/hooks/useDiff.test.ts` (2 new tests + 3 updated tests per D-01b/D-03)
- `ui/src/code-review/CodeReviewApp.tsx` (CR-02 `seededRef` + WR-01 `-1` guard + branch-union arm reads `result.files`/`result.error`)
- `ui/src/code-review/CommitDrawer.tsx` (WR-03 `role="button"`, `tabIndex={0}`, `onKeyDown`)
- `ui/src/code-review/CommitDrawer.test.ts` (source assertions for the 3 new attributes)
- `ui/src/code-review/DiffPane.tsx` (WR-04 short-SHA fallback strip)
- `ui/src/code-review/DiffPane.test.ts` (source assertion for `activeCommitSha.slice(0, 7)`)
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-commit-navigation-bug-fixes/26.1-CONTEXT.md
@.planning/phases/26.1-commit-navigation-bug-fixes/26.1-PATTERNS.md
@.planning/phases/26-commit-navigation/26-REVIEW.md

<!-- HANDOFF spec for every fix (file paths, line numbers, exact patches) -->
@.planning/HANDOFF.json

<interfaces>
<!-- The shared FetchDiffResult interface that WR-02 standardizes on. Extracted from ui/src/code-review/hooks/useDiff.ts lines 6-11. -->

From ui/src/code-review/hooks/useDiff.ts:
- `export type DoFetch = (url: string) => Promise<Response>`
- `export interface FetchDiffResult { files: FileDiff[]; error: string | null }`
- `fetchDiffOnce(doFetch, contextLines?): Promise<FetchDiffResult>` — error strings used today: `'fetch failed'`, `'network error'`.
- `fetchCommitDiffOnce(sha, doFetch, contextLines?): Promise<FetchDiffResult>` — CR-01 adds a new error string: `'invalid sha'`.
- `fetchFilteredBranchDiff(shas, doFetch, contextLines?): Promise<FileDiff[]>` — BEFORE WR-02. AFTER WR-02 the return type becomes `Promise<FetchDiffResult>` with new error string `'all fetches failed'`.

From ui/src/code-review/CodeReviewApp.tsx — the `useDiff({ selector })` call at line 31 returns `{ files, loading, error, refetch }`. The branch-union arm in `useDiff`'s internal `dispatch` (useDiff.ts lines 137-140) currently wraps `fetchFilteredBranchDiff`'s array result into `{ files, error: null }`; WR-02 removes this wrapping (return the result directly) so the all-fail error reaches the caller's `error` state.

CodeReviewApp consumers of branch-union results (state.error display in DiffPane.tsx) require zero changes — they already render `error` strings from the hook.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for CR-01 (invalid-SHA guard) and WR-02 (all-fail return)</name>
  <files>ui/src/code-review/hooks/useDiff.test.ts</files>
  <read_first>
    - ui/src/code-review/hooks/useDiff.test.ts (current test file — preserve existing structure, add new cases inside existing `describe` blocks)
    - ui/src/code-review/hooks/useDiff.ts (current `fetchCommitDiffOnce` and `fetchFilteredBranchDiff` bodies — confirms today's behavior the new tests must drive to fail until Task 2 lands)
    - .planning/phases/26.1-commit-navigation-bug-fixes/26.1-PATTERNS.md (sections "useDiff.test.ts — D-03" and "Shared Patterns / Injectable doFetch seam")
    - .planning/phases/26.1-commit-navigation-bug-fixes/26.1-CONTEXT.md (decisions D-01, D-01b, D-03)
  </read_first>
  <behavior>
    - Test 1 (CR-01): `fetchCommitDiffOnce('../branch', doFetch)` MUST return `{ files: [], error: 'invalid sha' }` AND a tracking flag (e.g. `let called = false` mutated by the injected `doFetch` closure) MUST remain `false` after the call returns — proving the guard ran before any network call.
    - Test 2 (WR-02 all-fail): `fetchFilteredBranchDiff(['sha1', 'sha2'], doFetch)` where `doFetch` always resolves to `new Response(null, { status: 500 })` MUST return `{ files: [], error: 'all fetches failed' }`.
    - Existing `fetchFilteredBranchDiff` tests at lines 115-145 MUST be updated for D-01b (return-type change): `expect(result).toHaveLength(N)` becomes `expect(result.files).toHaveLength(N)` at line 123 (`toHaveLength(2)`) and line 133 (`toHaveLength(1)`). The third existing test at lines 136-144 (`appends ?context=N to every per-sha URL`) reads `capturedUrls` only — no `result` shape change — leave its assertions untouched.
    - Other valid SHA test inputs in `describe('fetchCommitDiffOnce')` (e.g. `'abc123'`, `'sha1'`, `'sha2'`) MUST continue to pass — the SHA regex `/^[0-9a-f]{7,40}$/i` matches them. Do NOT change input strings in existing passing tests.
    - After Task 1, running `npm test -- ui/src/code-review/hooks/useDiff.test.ts` MUST show the two NEW tests failing (or, for the updated `result.files` assertions, also failing because the production type is still `FileDiff[]`). Existing `fetchDiffOnce` and single-call `fetchCommitDiffOnce` tests MUST still pass.
  </behavior>
  <action>
    Add two new `it(...)` cases and update three existing assertions in `ui/src/code-review/hooks/useDiff.test.ts`.

    Concrete identifiers:
    - New test inside `describe('fetchCommitDiffOnce')` named exactly `'rejects an invalid SHA without calling doFetch'`. Use a `let called = false` flag mutated inside an injected `doFetch` closure. Call `fetchCommitDiffOnce('../branch', doFetch)`. Assert `called === false`, `result.files.length === 0`, `result.error === 'invalid sha'`.
    - New test inside `describe('fetchFilteredBranchDiff')` named exactly `"returns { files: [], error: 'all fetches failed' } when all SHAs return 500"`. Inject `doFetch = () => Promise.resolve(new Response(null, { status: 500 }))`. Call `fetchFilteredBranchDiff(['sha1', 'sha2'], doFetch)`. Assert `result.files.length === 0` and `result.error === 'all fetches failed'`.
    - Update the existing tests at lines ~123 and ~133: change `expect(result).toHaveLength(2)` → `expect(result.files).toHaveLength(2)`; change `expect(result).toHaveLength(1)` → `expect(result.files).toHaveLength(1)`. Add `expect(result.error).toBeNull()` to both for completeness, matching the `FetchDiffResult` contract.
    - Use the injectable-closure spy pattern (`let capturedUrl = ''` / `let called = false`) per 26.1-PATTERNS.md "Shared Patterns / Injectable doFetch seam" — do NOT introduce `vi.fn()` or `@testing-library/react`.
    - Keep all existing imports (`describe, it, expect`, `fetchDiffOnce, fetchCommitDiffOnce, fetchFilteredBranchDiff`, `FileDiff`).

    Do NOT modify `useDiff.ts` in this task — these tests must fail against current production code, then pass after Task 2.
  </action>
  <verify>
    <automated>cd ui && npm test -- src/code-review/hooks/useDiff.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/hooks/useDiff.test.ts` contains the literal string `'rejects an invalid SHA without calling doFetch'`.
    - `ui/src/code-review/hooks/useDiff.test.ts` contains the literal string `'all fetches failed'`.
    - `ui/src/code-review/hooks/useDiff.test.ts` contains the literal string `'invalid sha'`.
    - `grep -c "expect(result).toHaveLength" ui/src/code-review/hooks/useDiff.test.ts` returns `0` (all changed) for the three lines updated in `describe('fetchFilteredBranchDiff')`. `expect(result.files).toHaveLength` MUST appear at least twice.
    - `cd ui && npm test -- src/code-review/hooks/useDiff.test.ts` exits with a NON-zero status (failing tests prove the contract is not yet implemented). Specifically the two new tests AND the two updated tests fail; the existing `fetchDiffOnce` and `fetchCommitDiffOnce` tests still pass.
    - No `vi.fn()` import or call appears anywhere in `useDiff.test.ts`.
  </acceptance_criteria>
  <done>
    New tests written; running the test file shows the new CR-01 / WR-02 / `result.files` cases failing and all pre-existing tests still passing. No production code changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CR-01, WR-02, CR-02, WR-01 (useDiff.ts + CodeReviewApp.tsx)</name>
  <files>ui/src/code-review/hooks/useDiff.ts, ui/src/code-review/CodeReviewApp.tsx</files>
  <read_first>
    - ui/src/code-review/hooks/useDiff.ts (entire file — Task 2 changes `fetchCommitDiffOnce` and `fetchFilteredBranchDiff`, plus the `dispatch` branch-union arm)
    - ui/src/code-review/CodeReviewApp.tsx (entire file — Task 2 adds `seededRef`, updates the seed effect at lines 67-74, adds the `-1` guard at the keydown effect at lines 78-87)
    - ui/src/code-review/hooks/useDiff.test.ts (the test cases written in Task 1 are the contract this task must satisfy)
    - .planning/phases/26.1-commit-navigation-bug-fixes/26.1-PATTERNS.md (sections "useDiff.ts — CR-01 + WR-02" and "CodeReviewApp.tsx — CR-02 + WR-01")
    - .planning/phases/26.1-commit-navigation-bug-fixes/26.1-CONTEXT.md (decisions D-01, D-02)
    - .planning/HANDOFF.json (canonical fix specs)
  </read_first>
  <behavior>
    - After Task 2, ALL existing tests in `useDiff.test.ts` continue to pass AND the two new tests from Task 1 pass AND the three updated `result.files` assertions pass — `npm test -- src/code-review/hooks/useDiff.test.ts` exits 0.
    - `fetchCommitDiffOnce('../branch', doFetch)` returns `{ files: [], error: 'invalid sha' }` without calling `doFetch`.
    - `fetchCommitDiffOnce('abc1234', doFetch)` (valid lowercase 7-char hex) and `fetchCommitDiffOnce('A1B2C3D4', doFetch)` (valid uppercase 8-char hex) both pass the regex and proceed to `doFetch`.
    - `fetchFilteredBranchDiff([sha1, sha2], doFetchAllFail)` where every response is 500 returns `{ files: [], error: 'all fetches failed' }`.
    - `fetchFilteredBranchDiff([sha1, sha2], doFetchOneOk)` where exactly one SHA returns 200 returns `{ files: [...], error: null }` with the successful sha's files.
    - `CodeReviewApp` mounts cleanly; `seededRef` is initialized via `useRef(false)`; the seed effect sets `seededRef.current = true` exactly once on the transition from `commits.length === 0` to `commits.length > 0`. After the user unchecks every commit (`checkedCommitShas = []`), the seed effect does NOT re-fire even if `commits.length` is unchanged. (Validated by reading the modified source — guard becomes `if (seededRef.current || commits.length === 0) return`.)
    - The keyboard handler in CodeReviewApp returns early when `commits.findIndex(...)` returns `-1` — no crash and no `setActiveCommitSha` call when `activeCommitSha` does not match any loaded commit.
  </behavior>
  <action>
    Edit `ui/src/code-review/hooks/useDiff.ts`:
    1. CR-01 — In `fetchCommitDiffOnce` (after the function signature opening at line 60, before the `const url` assignment at line 61), insert a guard block:
       - Declare `const SHA_RE = /^[0-9a-f]{7,40}$/i` as a function-scoped const (or hoist to module scope — either acceptable; if hoisting, place it directly after the `FetchDiffResult` interface block, lines 8-11).
       - Immediately after, add `if (!SHA_RE.test(sha)) { return { files: [], error: 'invalid sha' } }`.
       - The error string MUST be the literal `'invalid sha'`. Do NOT change any other behavior in `fetchCommitDiffOnce`.
    2. WR-02 — Replace `fetchFilteredBranchDiff` (lines 82-95):
       - Change return type from `Promise<FileDiff[]>` to `Promise<FetchDiffResult>`.
       - Replace the `Promise.all(...)` body with `Promise.allSettled(shas.map((sha) => fetchCommitDiffOnce(sha, doFetch, contextLines)))`.
       - Compute `const allFailed = settled.every((r) => r.status === 'rejected' || r.value.error !== null)`.
       - If `allFailed` is true, return `{ files: [], error: 'all fetches failed' }`. The error string MUST be the literal `'all fetches failed'`.
       - Otherwise, return `{ files: settled.flatMap((r) => (r.status === 'fulfilled' ? r.value.files : [])), error: null }`.
    3. WR-02 caller — Update the `dispatch` branch-union arm at lines 137-140:
       - Before: assigns `unionFiles` and returns `{ files: unionFiles, error: null }`.
       - After: `return fetchFilteredBranchDiff(selector.shas, doFetch, contextLines)` — pass the result through unchanged so the all-fail error string reaches the hook's `error` state.

    Edit `ui/src/code-review/CodeReviewApp.tsx`:
    4. CR-02 — Add a new ref declaration near the existing `diffPaneRef` at line 20: `const seededRef = useRef(false)`. Re-use the already-imported `useRef` from line 1.
    5. CR-02 — Replace the seed effect at lines 67-74:
       - Guard becomes `if (seededRef.current || commits.length === 0) return` (single-line early return; do NOT also keep the `checkedCommitShas.length === 0` check — the sentinel replaces it per D-02).
       - On entry past the guard, set `seededRef.current = true` BEFORE scheduling the `setTimeout` to ensure idempotency even if React invokes the effect twice in StrictMode.
       - Continue using `setTimeout(() => setCheckedCommitShas(commits.map((c) => c.sha)), 0)` and the matching `clearTimeout` cleanup — preserves the existing react-hooks/set-state-in-effect compliance.
       - Remove the `eslint-disable-next-line react-hooks/exhaustive-deps` comment if the deps array `[commits.length]` is sufficient under the new code (verify by re-running lint at the end of Task 2; restore the disable if lint complains).
    6. WR-01 — Inside the keyboard handler in the effect at lines 78-87, immediately after `const idx = commits.findIndex(...)` add `if (idx === -1) return` as a new line. The existing two arrow-key branches (`ArrowLeft && idx > 0` and `ArrowRight && idx < commits.length - 1`) remain unchanged below the guard.

    Do NOT add or remove any imports beyond what is already present. Do NOT change any other lines in either file. Do NOT touch CommitDrawer or DiffPane in this task.
  </action>
  <verify>
    <automated>cd ui && npm test -- src/code-review/hooks/useDiff.test.ts 2>&1 | tail -20 && cd ui && npm test -- src/code-review/CodeReviewApp.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "SHA_RE" ui/src/code-review/hooks/useDiff.ts` returns at least one match; the regex literal `/^[0-9a-f]{7,40}$/i` appears in the file.
    - `grep -F "'invalid sha'" ui/src/code-review/hooks/useDiff.ts` returns at least one match.
    - `grep -F "'all fetches failed'" ui/src/code-review/hooks/useDiff.ts` returns at least one match.
    - `grep -F "Promise.allSettled" ui/src/code-review/hooks/useDiff.ts` returns at least one match.
    - `grep -c "Promise<FileDiff\[\]>" ui/src/code-review/hooks/useDiff.ts` returns `0` (the `fetchFilteredBranchDiff` return type no longer references `FileDiff[]` directly as its `Promise<...>`).
    - `grep -F "return fetchFilteredBranchDiff" ui/src/code-review/hooks/useDiff.ts` matches the new `dispatch` branch-union return statement.
    - `grep -F "seededRef" ui/src/code-review/CodeReviewApp.tsx` returns at least three matches (declaration + guard + assignment).
    - `grep -F "seededRef.current || commits.length === 0" ui/src/code-review/CodeReviewApp.tsx` returns one match.
    - `grep -F "if (idx === -1) return" ui/src/code-review/CodeReviewApp.tsx` returns one match.
    - `cd ui && npm test -- src/code-review/hooks/useDiff.test.ts` exits 0.
    - `cd ui && npm test -- src/code-review/CodeReviewApp.test.ts` exits 0 (no regressions in pre-existing CodeReviewApp tests).
  </acceptance_criteria>
  <done>
    `useDiff.ts` and `CodeReviewApp.tsx` carry the four production fixes (CR-01, WR-02, CR-02, WR-01); all `useDiff.test.ts` tests pass; CodeReviewApp tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement WR-03 (CommitDrawer keyboard a11y) and WR-04 (DiffPane unknown-SHA fallback) with source-assertion tests</name>
  <files>ui/src/code-review/CommitDrawer.tsx, ui/src/code-review/CommitDrawer.test.ts, ui/src/code-review/DiffPane.tsx, ui/src/code-review/DiffPane.test.ts</files>
  <read_first>
    - ui/src/code-review/CommitDrawer.tsx (specifically the `<li>` at lines 109-123 inside the commit-list `ol`)
    - ui/src/code-review/CommitDrawer.test.ts (entire file — confirms the source-assertion test pattern using `readFileSync`)
    - ui/src/code-review/DiffPane.tsx (the per-commit title strip section — search for the JSX block that currently renders `{activeCommit.short_sha}` and `activeCommit.message`; PATTERNS.md references lines 280-308)
    - ui/src/code-review/DiffPane.test.ts (entire file — source-assertion test pattern)
    - .planning/phases/26.1-commit-navigation-bug-fixes/26.1-PATTERNS.md (sections "CommitDrawer.tsx — WR-03" and "DiffPane.tsx — WR-04" plus "CSS variable tokens")
    - .planning/phases/26.1-commit-navigation-bug-fixes/26.1-CONTEXT.md (specifics for WR-03 and WR-04)
  </read_first>
  <behavior>
    - The `<li>` element rendered inside the `commits.map(...)` loop in `CommitDrawer.tsx` carries three new attributes alongside the existing `onClick`: `role="button"`, `tabIndex={0}`, and an `onKeyDown` handler that calls `onCommitClick(commit.sha)` when `e.key === 'Enter'`.
    - The existing `onClick={() => onCommitClick(commit.sha)}` handler is preserved unchanged.
    - In `DiffPane.tsx`, when `viewMode === 'commit'` AND `activeCommitSha !== null` AND `activeCommit === null` (lookup miss), a fallback title strip renders containing `activeCommitSha.slice(0, 7)` as the displayed text. When `activeCommit !== null`, the existing title strip renders unchanged. When `viewMode === 'branch'` OR `activeCommitSha === null`, neither strip renders.
    - The fallback strip reuses the same container styles (`var(--color-surface)` background, `var(--color-border)` bottom border, `padding: '8px 16px'`, `flexShrink: 0`) and `var(--color-text-primary)` for the SHA text — matching the existing strip's visual rhythm per PATTERNS.md "CSS variable tokens".
    - `cd ui && npm test` exits 0 across the entire UI test suite after this task.
  </behavior>
  <action>
    Edit `ui/src/code-review/CommitDrawer.tsx`:
    1. WR-03 — Modify the `<li>` opening tag at line 109 to add three attributes alongside the existing `key`, `onClick`, and `style` props:
       - `role="button"`
       - `tabIndex={0}`
       - `onKeyDown={(e) => { if (e.key === 'Enter') onCommitClick(commit.sha) }}`
       The new `onKeyDown` handler MUST use the literal comparison `e.key === 'Enter'` (string literal `'Enter'` with single quotes to match project quoting convention visible in the existing file).
       Preserve the existing `onClick={() => onCommitClick(commit.sha)}` exactly as-is.

    Edit `ui/src/code-review/CommitDrawer.test.ts`:
    2. Append three new `it(...)` cases inside the existing `describe('CommitDrawer', ...)` block, using the same `source` constant already declared at line 7 (`const source = readFileSync(...)`). Test names:
       - `'CommitRow li has role="button" and tabIndex={0} for keyboard accessibility'` — assert `expect(source).toContain('role="button"')` and `expect(source).toContain('tabIndex={0}')`.
       - `'CommitRow li has onKeyDown handler for Enter key'` — assert `expect(source).toContain("e.key === 'Enter'")` and `expect(source).toContain('onKeyDown')`.
       - (Optional third — kept narrow): no third needed; the two above cover the three new attributes.

    Edit `ui/src/code-review/DiffPane.tsx`:
    3. WR-04 — Locate the existing per-commit title strip (the JSX block guarded by `viewMode === 'commit' && activeCommitSha !== null && activeCommit !== null` — if the current guard is shorter, expand it to include the explicit `activeCommit !== null` check so the fallback block is mutually exclusive). Immediately after that JSX block, add a second sibling block:
       - Guard: `viewMode === 'commit' && activeCommitSha !== null && activeCommit === null`.
       - Content: a `<div>` wrapper with the existing strip's container styles (`background: 'var(--color-surface)'`, `borderBottom: '1px solid var(--color-border)'`, `padding: '8px 16px'`, `flexShrink: 0`) containing a child `<div>` styled `fontSize: 14, fontWeight: 600, color: 'var(--color-text-primary)'` and rendering `{activeCommitSha.slice(0, 7)}`.
       - The literal expression `activeCommitSha.slice(0, 7)` MUST appear verbatim in the source so the upcoming source-assertion test can detect it.
       - If the current per-commit strip guard does NOT explicitly check `activeCommit !== null` today, add that check so the two strips never both render at once.

    Edit `ui/src/code-review/DiffPane.test.ts`:
    4. Append one new `it(...)` case inside the existing `describe('DiffPane', ...)` block, reusing the existing `source` constant declared at line 7. Test name:
       - `'renders fallback short SHA when activeCommit lookup returns null (WR-04)'` — assert `expect(source).toContain('activeCommitSha.slice(0, 7)')`.

    Do NOT add any new imports to either component or test file beyond what is already present.
  </action>
  <verify>
    <automated>cd ui && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'role="button"' ui/src/code-review/CommitDrawer.tsx` returns at least one match.
    - `grep -F 'tabIndex={0}' ui/src/code-review/CommitDrawer.tsx` returns at least one match.
    - `grep -F 'onKeyDown' ui/src/code-review/CommitDrawer.tsx` returns at least one match.
    - `grep -F "e.key === 'Enter'" ui/src/code-review/CommitDrawer.tsx` returns at least one match.
    - `grep -F "onClick={() => onCommitClick(commit.sha)}" ui/src/code-review/CommitDrawer.tsx` still returns one match (preserved).
    - `grep -F "activeCommitSha.slice(0, 7)" ui/src/code-review/DiffPane.tsx` returns at least one match.
    - `grep -F "activeCommit === null" ui/src/code-review/DiffPane.tsx` returns at least one match (the new fallback guard).
    - `grep -F "activeCommitSha.slice(0, 7)" ui/src/code-review/DiffPane.test.ts` returns at least one match.
    - `grep -F 'role="button"' ui/src/code-review/CommitDrawer.test.ts` returns at least one match.
    - `grep -F "e.key === 'Enter'" ui/src/code-review/CommitDrawer.test.ts` returns at least one match.
    - `cd ui && npm test` exits 0 with zero failures across the entire UI test suite.
    - `cd ui && npm run lint` exits 0.
    - `cd ui && npm run build` exits 0 (TypeScript type-checks succeed — confirms the `Promise<FetchDiffResult>` change in Task 2 is consumed correctly everywhere).
  </acceptance_criteria>
  <done>
    `CommitDrawer.tsx` `<li>` is keyboard-accessible; `DiffPane.tsx` shows a fallback SHA strip when the active commit isn't found; both new source-assertion tests pass; full UI suite green; ESLint and TypeScript clean.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, the following must all hold simultaneously:

1. `cd ui && npm test` — exits 0; every test in `useDiff.test.ts`, `CodeReviewApp.test.ts`, `CommitDrawer.test.ts`, and `DiffPane.test.ts` passes.
2. `cd ui && npm run lint` — exits 0; no new ESLint violations introduced (in particular no `react-hooks/exhaustive-deps` or `react-hooks/set-state-in-effect` regression).
3. `cd ui && npm run build` — exits 0; the `FetchDiffResult` return-type change in `fetchFilteredBranchDiff` does not break any consumer.
4. `cargo test` — still passes (sanity check; no Rust changes expected but the workspace test suite should not regress from a `cargo fmt`/`clippy` perspective if anything Rust was touched accidentally).
5. Source-level grep gates (filtered against comment-only matches where relevant):
   - `grep -c "Promise<FileDiff\\[\\]>" ui/src/code-review/hooks/useDiff.ts | { read n; [ "$n" = "0" ]; }` (the return-type change is complete).
   - `grep -v '^//' ui/src/code-review/hooks/useDiff.ts | grep -F "'invalid sha'"` succeeds.
   - `grep -v '^//' ui/src/code-review/hooks/useDiff.ts | grep -F "'all fetches failed'"` succeeds.
   - `grep -F "seededRef.current" ui/src/code-review/CodeReviewApp.tsx` succeeds.
   - `grep -F "if (idx === -1) return" ui/src/code-review/CodeReviewApp.tsx` succeeds.
   - `grep -F 'role="button"' ui/src/code-review/CommitDrawer.tsx` succeeds.
   - `grep -F "activeCommitSha.slice(0, 7)" ui/src/code-review/DiffPane.tsx` succeeds.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Rust server | URL paths built from user-controlled commit SHA values cross from browser JS into the server's HTTP route matcher; any unsanitized value risks `/api/diff/commit/{sha}` becoming `/api/diff/commit/../branch` or similar path-tampering string |
| React state → DOM | `activeCommitSha` from app state flows into the `DiffPane` title strip text content; no HTML injection risk because React escapes, but unknown values must not crash the render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.1-01 | Tampering | `fetchCommitDiffOnce(sha, doFetch)` URL construction in `ui/src/code-review/hooks/useDiff.ts` | mitigate | CR-01: validate `sha` against `/^[0-9a-f]{7,40}$/i` BEFORE interpolating into `/api/diff/commit/${sha}` URL. Reject non-matching input with `{ files: [], error: 'invalid sha' }`. Implemented in Task 2; failing test in Task 1 enforces the guard. |
| T-26.1-02 | Denial of Service | `fetchFilteredBranchDiff` silent-failure path | mitigate | WR-02: when every per-SHA fetch fails, return an explicit error string `'all fetches failed'` so the caller's error state surfaces in the UI instead of silently showing an empty diff. Implemented in Task 2; failing test in Task 1 enforces the contract. |
| T-26.1-03 | Information Disclosure | Stale `seededRef` re-seeding (CR-02) | accept-then-mitigate | One-shot `seededRef` sentinel prevents inadvertent re-seeding of `checkedCommitShas` after the user explicitly empties the set. No data leakage, but user intent could otherwise be silently overridden. Implemented in Task 2. |
| T-26.1-04 | Repudiation | Keyboard navigation crash from `-1` index (WR-01) | mitigate | Guard `if (idx === -1) return` in the keydown effect prevents `commits[idx - 1]` / `commits[idx + 1]` from throwing on an out-of-range index when `activeCommitSha` is stale. Implemented in Task 2. |
| T-26.1-05 | Elevation of Privilege | n/a | n/a | No auth surface in this phase |
| T-26.1-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this phase — all changes are within existing source files; the package legitimacy gate does not apply. |
</threat_model>

<success_criteria>
The plan is complete when:

1. All six fixes (CR-01, CR-02, WR-01, WR-02, WR-03, WR-04) are implemented per the 26.1-CONTEXT.md decisions and 26.1-PATTERNS.md analogs.
2. Every `must_haves.truths` entry is independently verifiable via the corresponding grep/test command in `<acceptance_criteria>`.
3. The complete UI test suite (`cd ui && npm test`) passes with zero failures.
4. `cd ui && npm run lint` and `cd ui && npm run build` both exit 0.
5. The ROADMAP success-criteria checklist for Phase 26.1 (items 1-6) is fully satisfied — each criterion maps to a specific code change in one of the three tasks.
6. No file outside the seven listed in `files_modified` was modified.
</success_criteria>

<output>
Create `.planning/phases/26.1-commit-navigation-bug-fixes/26.1-01-SUMMARY.md` when done, following `@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md`.
</output>
