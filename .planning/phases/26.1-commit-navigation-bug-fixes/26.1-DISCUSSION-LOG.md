# Phase 26.1: Commit Navigation — Bug Fixes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-24
**Phase:** 26.1-commit-navigation-bug-fixes
**Areas discussed:** WR-02 signature change, CR-02 sentinel reset behavior, Testing coverage scope, Plan structure

---

## WR-02 Signature Change

| Option | Description | Selected |
|--------|-------------|----------|
| Return FetchDiffResult | Reuse existing `{ files, error }` interface — consistent with the other two fetch functions. Uses `Promise.allSettled`. | ✓ |
| Throw on all-fail | Let errors throw when all parallel fetches fail. No type change but inconsistent with rest of file. | |

**User's choice:** Return FetchDiffResult
**Notes:** User first asked "what kind of error are we talking, shouldn't HTTP error codes be used instead?" — clarified that HTTP errors are already detected by `fetchCommitDiffOnce` via `res.ok`; the problem is `fetchFilteredBranchDiff` silently discards those with `.then(r => r.files).catch(() => [])`. No new HTTP semantics needed, just stop discarding the already-detected error signals.

---

## CR-02 Sentinel Reset Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| One-and-done — never reset | `seededRef` fires once on initial load, never resets. Simple; app has no retry-fetch today. | ✓ |
| Reset on empty commits — re-seeds after retry | Reset `seededRef` when commits become empty again. Handles retry scenarios. More complex. | |

**User's choice:** One-and-done — never reset
**Notes:** No retry-fetch mechanism exists in the app today, so the more complex path is out of scope.

---

## Testing Coverage Scope

| Option | Description | Selected |
|--------|-------------|----------|
| CR-01 — SHA validation (new test) | `fetchCommitDiffOnce` rejects invalid SHA without calling `doFetch`. | ✓ |
| WR-02 — all-fail error case (new test) | `fetchFilteredBranchDiff` returns `{ files:[], error }` when every SHA returns 500. | ✓ |
| Update existing fetchFilteredBranchDiff tests | Required — 3 existing tests check `result.length`, must become `result.files.length`. | ✓ |

**User's choice:** All three selected
**Notes:** WR-01 (one-liner guard), WR-03 (markup), WR-04 (rendering), and CR-02 (React component effect) are applied as targeted patches with no new tests — no component test infrastructure exists for CodeReviewApp today.

---

## Plan Structure

| Option | Description | Selected |
|--------|-------------|----------|
| One plan — TDD + fix together | Single 26.1-01-PLAN.md: failing tests first, then all 6 fixes, then verify. | ✓ |
| Two plans — TDD plan then fix plan | 26.1-01 writes tests; 26.1-02 implements fixes. Matches Phase 26 wave pattern. | |

**User's choice:** One plan — TDD + fix together
**Notes:** Fixes are small and self-contained. One consolidated plan is appropriate.

---

## Claude's Discretion

- Exact SHA regex form (`/^[0-9a-f]{7,40}$/i` is specified in HANDOFF.json — no deviation needed)
- Error string wording for WR-02 all-fail case
- Whether CR-01 validation is inline in `fetchCommitDiffOnce` or a helper

## Deferred Ideas

None — discussion stayed within phase scope.
