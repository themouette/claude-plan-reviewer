---
phase: 17-foundation-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - ui/src/reviewer-v2/types.ts
  - ui/src/reviewer-v2/utils/connectivity.ts
  - ui/src/reviewer-v2/utils/connectivity.test.ts
  - ui/src/reviewer-v2/utils/offlineLabels.ts
  - ui/src/reviewer-v2/utils/serializeAnnotations.ts
  - ui/src/reviewer-v2/utils/offlineLabels.test.ts
  - ui/src/reviewer-v2/hooks/useHeartbeat.ts
  - ui/src/reviewer-v2/hooks/useHeartbeat.test.ts
  - ui/src/reviewer-v2/hooks/useAnnotations.ts
  - ui/src/reviewer-v2/hooks/useAnnotations.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
tags:
  - reviewer-v2
  - isolation
  - heartbeat
  - annotations
  - useReducer
  - subtree

must_haves:
  truths:
    - "All v2 code lives exclusively under `ui/src/reviewer-v2/` (ARCH-01)"
    - "No file under `ui/src/reviewer-v2/` contains an import path beginning with `../`"
    - "`reviewer-v2/hooks/useHeartbeat.ts` polls `/api/ping` independently from `ui/src/hooks/useHeartbeat.ts` (ARCH-02)"
    - "`reviewer-v2/hooks/useAnnotations.ts` exposes a pure `annotationReducer` that can be unit-tested without React"
    - "`annotationReducer` handles add, edit, and remove actions correctly"
    - "`npm run lint` passes — the ESLint coupling rule does not flag any newly created v2 file"
    - "`npm test` passes — including new tests for `annotationReducer` and the v2 `useHeartbeat` copy"
  artifacts:
    - path: "ui/src/reviewer-v2/types.ts"
      provides: "Minimal v2 Annotation type and AnnotationAction discriminated union"
      contains: "interface Annotation"
    - path: "ui/src/reviewer-v2/utils/connectivity.ts"
      provides: "Copy of ConnectivityStatus, HeartbeatState, nextHeartbeatState, initialHeartbeatState"
      contains: "nextHeartbeatState"
    - path: "ui/src/reviewer-v2/utils/offlineLabels.ts"
      provides: "Copies of buildClipboardPayload and shouldUseClipboard (the two functions v2 will need in Phase 22)"
      contains: "buildClipboardPayload"
    - path: "ui/src/reviewer-v2/hooks/useHeartbeat.ts"
      provides: "v2-owned useHeartbeat hook with runHeartbeatTick exported helper (verbatim copy of v1, repointed imports)"
      contains: "runHeartbeatTick"
    - path: "ui/src/reviewer-v2/hooks/useAnnotations.ts"
      provides: "useAnnotations hook + exported pure annotationReducer per D-09/D-10"
      contains: "annotationReducer"
    - path: "ui/src/reviewer-v2/utils/connectivity.test.ts"
      provides: "Test that v2 connectivity reducer behaves identically to v1"
    - path: "ui/src/reviewer-v2/hooks/useAnnotations.test.ts"
      provides: "Tests for add/edit/remove reducer cases per D-09"
    - path: "ui/src/reviewer-v2/hooks/useHeartbeat.test.ts"
      provides: "Test that runHeartbeatTick in v2 copy behaves identically to v1 (basic smoke test)"
  key_links:
    - from: "ui/src/reviewer-v2/hooks/useHeartbeat.ts"
      to: "ui/src/reviewer-v2/utils/connectivity.ts"
      via: "relative import within v2 subtree"
      pattern: "from './\\.\\./utils/connectivity'"
    - from: "ui/src/reviewer-v2/hooks/useAnnotations.ts"
      to: "ui/src/reviewer-v2/types.ts"
      via: "relative import within v2 subtree"
      pattern: "from './\\.\\./types'"
    - from: "ui/src/reviewer-v2/utils/offlineLabels.ts"
      to: "ui/src/reviewer-v2/utils/serializeAnnotations.ts"
      via: "relative import within v2 subtree"
      pattern: "from './serializeAnnotations'"
---

<objective>
Scaffold the `ui/src/reviewer-v2/` subtree: copy the utilities v2 needs (connectivity state machine, offline-labels helpers), copy `useHeartbeat` verbatim with imports repointed to the local copy, define the minimal v2 `Annotation` type per D-11, and build the `useAnnotations` reducer hook per D-09/D-10. All code stays inside the subtree — zero `../` imports — so the ESLint coupling rule from Plan 01 catches any accidental escape.

Purpose: ARCH-01 (isolation) is satisfied by making the v2 subtree self-contained; ARCH-02 (independent heartbeat) is satisfied by giving v2 its own copy of `useHeartbeat`. The reducer pattern matches the existing codebase convention (pure function exported for testing, hook wrapper for runtime) so v2 tests follow the same DI pattern as v1 tests with no `@testing-library/react`.

Output: A populated `reviewer-v2/` subtree with types, utilities, hooks, and tests. No file in this plan imports from outside the subtree. All new tests pass; the ESLint coupling rule still passes on the new files.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-foundation-isolation/17-CONTEXT.md
@.planning/phases/17-foundation-isolation/17-RESEARCH.md
@.planning/phases/17-foundation-isolation/17-01-PLAN.md

# Source files to copy from (read each in full once)
@ui/src/hooks/useHeartbeat.ts
@ui/src/utils/connectivity.ts
@ui/src/utils/offlineLabels.ts
@ui/src/utils/serializeAnnotations.ts
@ui/src/types.ts

# Existing tests to model new tests on
@ui/src/utils/connectivity.test.ts
@ui/src/hooks/useHeartbeat.test.ts

<interfaces>
<!-- Exports the v2 copies must reproduce. Read each source file once; do not re-read. -->

From ui/src/utils/connectivity.ts:
- `export type ConnectivityStatus = 'online' | 'offline'`
- `export interface HeartbeatState { status: ConnectivityStatus; failCount: number }`
- `export type HeartbeatEvent = { type: 'success' } | { type: 'failure' }`
- `export const initialHeartbeatState: HeartbeatState`
- `export function nextHeartbeatState(state, event): HeartbeatState` — 3-failure threshold, success resets failCount

From ui/src/hooks/useHeartbeat.ts:
- `export interface HeartbeatTickContext { doFetch, isVisible, isCancelled, getNextGeneration, getCurrentGeneration, getAbortController, setAbortController, getState, setState, onStatus, timeoutMs }`
- `export async function runHeartbeatTick(ctx: HeartbeatTickContext): Promise<void>`
- `export function useHeartbeat(): ConnectivityStatus`
- Polls `/api/ping`; 5000ms interval; 3000ms timeout; pauses on `document.visibilityState !== 'visible'`; generation guard against superseded ticks; AbortSignal.any composition

From ui/src/utils/offlineLabels.ts:
- `export type ClipboardDecision = 'allow' | 'deny'`
- `export function buildClipboardPayload(decision, denyText, overallComment, annotations): string`
- `export function shouldUseClipboard(status: ConnectivityStatus): boolean`
- (Other exports — OFFLINE_BANNER_LINE_*, approveButtonLabel, denyButtonLabel, submitDenialButtonLabel — are NOT needed in v2 per CONTEXT D-04 "copy the parts v2 needs")
- Internally calls `serializeAnnotations(denyText, overallComment, annotations)` from `../types` — v2 must also copy `serializeAnnotations.ts` because offlineLabels depends on it.

From ui/src/utils/serializeAnnotations.ts:
- `export function serializeAnnotations(denyText: string, overallComment: string, annotations: Annotation[]): string`
- Imports `Annotation` from `../types`.

From ui/src/types.ts (reference only — v2 defines its own minimal type per D-11):
- v1 Annotation = { id: string; type: 'comment'|'delete'|'replace'; anchorText: string; comment: string; replacement: string }
- v2 minimal per D-11/RESEARCH A2 = { id: string; anchorText: string; comment: string; type: 'comment'|'delete'|'replace' } — NOTE: omits `replacement`. The v2 `serializeAnnotations` copy must accept this shape; either keep the v1 logic (read `a.replacement` which will be `undefined` for the replace branch) by defaulting to '' OR strip the replace branch — RESEARCH does not mandate either. Recommended: keep v1's logic, treat the type field as the discriminant and default `replacement` and `comment` to empty strings if missing, so the v2 reducer's later extensions in Phase 21 can extend the type without rewriting serialization.
</interfaces>

<patterns>
Codebase pattern: "Exported pure reducer + hook wrapper" (from `ui/src/hooks/useHeartbeat.ts` and `ui/src/utils/connectivity.ts`):
1. Define the pure `nextState(state, action): State` function at module top, exported.
2. Define a hook that calls `useReducer(reducer, initial)` and returns typed action dispatchers.
3. Tests import the pure reducer directly — never use `renderHook` or `@testing-library/react`.

This is the pattern `useAnnotations` MUST follow per D-09.

Codebase pattern: "Dependency-injected runHeartbeatTick" (from `ui/src/hooks/useHeartbeat.ts`):
- Hook body calls `runHeartbeatTick(ctx)` with a context object that wires real `fetch`, `document.visibilityState`, refs, etc.
- Tests build a fake `ctx` and call `runHeartbeatTick` directly with synthetic refs.
- The v2 copy must preserve this structure exactly.
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy connectivity, serializeAnnotations, offlineLabels into the v2 subtree</name>
  <files>ui/src/reviewer-v2/utils/connectivity.ts, ui/src/reviewer-v2/utils/connectivity.test.ts, ui/src/reviewer-v2/utils/serializeAnnotations.ts, ui/src/reviewer-v2/utils/offlineLabels.ts, ui/src/reviewer-v2/utils/offlineLabels.test.ts, ui/src/reviewer-v2/types.ts</files>
  <read_first>
    - ui/src/utils/connectivity.ts (full file — source for verbatim copy)
    - ui/src/utils/serializeAnnotations.ts (full file — source for copy; note import of `Annotation` from `../types`)
    - ui/src/utils/offlineLabels.ts (full file — source; only `buildClipboardPayload`, `shouldUseClipboard`, and the `ClipboardDecision` type need to land in v2)
    - ui/src/utils/connectivity.test.ts (full file — model for v2 test)
    - ui/src/utils/offlineLabels.test.ts (full file — used to identify the buildClipboardPayload / shouldUseClipboard test cases worth copying)
    - ui/src/types.ts (full file — reference for the v1 Annotation shape; v2 type is DERIVED, not imported)
    - .planning/phases/17-foundation-isolation/17-CONTEXT.md (D-04, D-11)
    - .planning/phases/17-foundation-isolation/17-RESEARCH.md (Assumption A2 — v2 Annotation type fields)
  </read_first>
  <behavior>
    - `ui/src/reviewer-v2/utils/connectivity.ts` is byte-for-byte identical to `ui/src/utils/connectivity.ts` (no imports to repoint — that file has none).
    - `ui/src/reviewer-v2/types.ts` exports a v2-minimal `Annotation` interface with fields `{ id: string; anchorText: string; comment: string; type: 'comment' | 'delete' | 'replace' }`, and exports `AnnotationAction` as a discriminated union with three members: `{ type: 'add'; annotation: Annotation }`, `{ type: 'edit'; id: string; comment: string }`, `{ type: 'remove'; id: string }`.
    - `ui/src/reviewer-v2/utils/serializeAnnotations.ts` is a copy of `ui/src/utils/serializeAnnotations.ts` with its `Annotation` import re-pointed from `../types` to `../types` (same relative path — files are in the same relative position; the import line stays `import type { Annotation } from '../types'`). The v2 minimal Annotation type lacks a `replacement` field, so the `replace` switch branch reading `a.replacement` must default missing values to `''`: write `(a.replacement ?? '').replace(/\n/g, '\n> ')`.
    - `ui/src/reviewer-v2/utils/offlineLabels.ts` is a partial copy of `ui/src/utils/offlineLabels.ts` containing ONLY the `ClipboardDecision` type, `buildClipboardPayload`, and `shouldUseClipboard`. The banner string constants and the `approveButtonLabel`/`denyButtonLabel`/`submitDenialButtonLabel` helpers are NOT copied — those belong to v1 banner UI. Imports re-point from `./connectivity` (same name in the same v2 utils folder) and `./serializeAnnotations` and `../types`.
    - `ui/src/reviewer-v2/utils/connectivity.test.ts` mirrors the 5-test suite in `ui/src/utils/connectivity.test.ts`, imported from `./connectivity` (relative import inside the v2 subtree — allowed by ESLint rule).
    - `ui/src/reviewer-v2/utils/offlineLabels.test.ts` exercises `buildClipboardPayload` with two cases: (a) decision='allow' with no annotations and no overallComment returns exactly `'{"behavior":"allow"}'`; (b) decision='deny' with denyText='blocked' and one annotation returns a JSON string parseable as `{ behavior: 'deny', message: <non-empty string containing 'blocked'> }`.
    - Every test file uses `describe`/`it`/`expect` imported from `vitest`. No `@testing-library/react`. No imports beginning with `../../`.
    - All new files compile under the existing `tsc -b` (no `any`, no `@ts-ignore`).
  </behavior>
  <action>
    Create the directory `ui/src/reviewer-v2/utils/` (and parents).

    Create `ui/src/reviewer-v2/utils/connectivity.ts` as a verbatim copy of `ui/src/utils/connectivity.ts`. No code changes — there are no imports to repoint.

    Create `ui/src/reviewer-v2/types.ts` exporting:
    - `type AnnotationType = 'comment' | 'delete' | 'replace'` (re-derived locally; NOT imported from `../types`).
    - `interface Annotation { id: string; anchorText: string; comment: string; type: AnnotationType }` per D-11 and RESEARCH A2.
    - `type AnnotationAction = { type: 'add'; annotation: Annotation } | { type: 'edit'; id: string; comment: string } | { type: 'remove'; id: string }` — the discriminated union the reducer in Task 2 will switch on.

    Create `ui/src/reviewer-v2/utils/serializeAnnotations.ts` as a copy of `ui/src/utils/serializeAnnotations.ts`. The single import `import type { Annotation } from '../types'` keeps the same relative path (the file's position relative to `types.ts` is identical in v2). Modify the `replace` branch from `a.replacement.replace(/\n/g, '\n> ')` to `(a.replacement ?? '').replace(/\n/g, '\n> ')` because the v2 Annotation type omits `replacement`. The `typeLabel`, `serializeAnnotations` function bodies otherwise stay identical. Cast `a.replacement` via `(a as Annotation & { replacement?: string }).replacement` to satisfy strict mode, or extend the type locally with an optional `replacement?: string` field — pick whichever produces zero `tsc -b` errors.

    Create `ui/src/reviewer-v2/utils/offlineLabels.ts` containing ONLY:
    - `import type { ConnectivityStatus } from './connectivity'`
    - `import { serializeAnnotations } from './serializeAnnotations'`
    - `import type { Annotation } from '../types'`
    - `export type ClipboardDecision = 'allow' | 'deny'`
    - `export function buildClipboardPayload(decision, denyText, overallComment, annotations): string` with byte-for-byte logic from `ui/src/utils/offlineLabels.ts`.
    - `export function shouldUseClipboard(status): boolean` returning `status === 'offline'`.
    - Do NOT copy `OFFLINE_BANNER_LINE_1`, `OFFLINE_BANNER_LINE_2`, `OFFLINE_APPROVE_LABEL`, `OFFLINE_DENY_LABEL`, `OFFLINE_SUBMIT_DENIAL_LABEL`, `approveButtonLabel`, `denyButtonLabel`, or `submitDenialButtonLabel`.

    Create `ui/src/reviewer-v2/utils/connectivity.test.ts` mirroring the 5 tests in `ui/src/utils/connectivity.test.ts`, importing from `./connectivity`. Test names and assertions identical.

    Create `ui/src/reviewer-v2/utils/offlineLabels.test.ts` with two tests:
    - `describe('buildClipboardPayload (v2 copy)')` containing:
      - `it('allow with no notes returns exactly {"behavior":"allow"}')` — call with decision='allow', empty denyText, empty overallComment, empty annotations array; assert `expect(result).toBe('{"behavior":"allow"}')`.
      - `it('deny serializes message including denyText')` — call with decision='deny', denyText='blocked', empty overallComment, and a single annotation `{ id: '1', anchorText: 'foo', comment: 'fix this', type: 'comment' }`; parse the result with `JSON.parse`; assert `parsed.behavior === 'deny'` and `parsed.message.includes('blocked')` and `parsed.message.includes('fix this')`.

    Run `cd ui && npm test` and verify the new tests pass alongside existing ones.

    Run `cd ui && npm run lint` and verify no `no-restricted-imports` errors fire — all imports in new files are either `./foo` or `../types` (where `../types` resolves to `src/reviewer-v2/types.ts`, INSIDE the subtree). The ESLint rule blocks paths starting with `../` followed by anything outside the subtree, but `../types` from inside `utils/` lands at `src/reviewer-v2/types.ts` which IS inside the subtree. NOTE: The ESLint `no-restricted-imports` `group: ['../**']` pattern is structural — it matches based on the literal import string, not on where the path resolves. Therefore `../types` inside `src/reviewer-v2/utils/serializeAnnotations.ts` WILL be flagged by the rule even though it stays inside the subtree. To avoid this, the v2 serializeAnnotations and offlineLabels files MUST import the v2 Annotation type via a path that does NOT begin with `../`. The cleanest solution is to add an `index.ts` re-export inside `src/reviewer-v2/types/` OR keep `types.ts` at the root and use a TypeScript baseUrl/path alias OR — simplest — colocate the type import by making `types.ts` referenced via a different mechanism. PER THE CONSTRAINT: change the structure so that `serializeAnnotations.ts` and `offlineLabels.ts` live at the v2 root (next to `types.ts`) OR import `Annotation` from `'./types'` instead of `'../types'`. Recommendation: move `serializeAnnotations.ts` to `ui/src/reviewer-v2/serializeAnnotations.ts` and `offlineLabels.ts` to `ui/src/reviewer-v2/offlineLabels.ts` so they sit alongside `types.ts` — and keep `connectivity.ts` in `utils/` since it has no imports. Update `must_haves.artifacts` references mentally; the **files_modified** frontmatter list above already reflects the original `utils/` layout — if you choose the flat layout instead, the practical outcome is the same: zero `../` imports anywhere.

    FINAL DIRECTORY LAYOUT (recommended to avoid ESLint rule triggering):
    - `ui/src/reviewer-v2/types.ts`
    - `ui/src/reviewer-v2/serializeAnnotations.ts` (imports `'./types'`)
    - `ui/src/reviewer-v2/offlineLabels.ts` (imports `'./connectivity'`, `'./serializeAnnotations'`, `'./types'`)
    - `ui/src/reviewer-v2/connectivity.ts`
    - `ui/src/reviewer-v2/connectivity.test.ts`
    - `ui/src/reviewer-v2/offlineLabels.test.ts`

    Update files_modified semantics accordingly if you take this layout. The `utils/` and `hooks/` folder distinction stops being meaningful when avoiding `../` imports; flat layout is acceptable under D-04 ("directory structure within reviewer-v2/ beyond the top-level hooks/ and utils/ subdirectories" is Claude's discretion).
  </action>
  <acceptance_criteria>
    - `ui/src/reviewer-v2/types.ts` exists and `grep -c "interface Annotation" ui/src/reviewer-v2/types.ts` returns 1.
    - `ui/src/reviewer-v2/types.ts` defines `AnnotationAction` with three union arms (`'add'`, `'edit'`, `'remove'`).
    - `ui/src/reviewer-v2/connectivity.ts` (or `ui/src/reviewer-v2/utils/connectivity.ts`) exists and exports `nextHeartbeatState` and `initialHeartbeatState` and `ConnectivityStatus`.
    - `ui/src/reviewer-v2/offlineLabels.ts` (or under `utils/`) exports `buildClipboardPayload` and `shouldUseClipboard` and does NOT export `OFFLINE_BANNER_LINE_1`.
    - `ui/src/reviewer-v2/serializeAnnotations.ts` (or under `utils/`) exists and exports `serializeAnnotations`.
    - Every file under `ui/src/reviewer-v2/` has zero import statements beginning with `'../'` — verify with `grep -rE "from '\\.\\./" ui/src/reviewer-v2/ | grep -v -E "from '\\.\\./types'" | wc -l` returns 0, AND ALSO `grep -rE "from '\\.\\./types'" ui/src/reviewer-v2/` is empty (because `../types` is itself blocked by the ESLint pattern).
    - `cd ui && npm run lint` exits 0 — the coupling rule does not fire on any new file.
    - `cd ui && npm test` exits 0; both new test files report their tests as passing; total test count increases by at least 7 (5 connectivity tests + 2 offlineLabels tests).
    - `buildClipboardPayload('allow', '', '', [])` returns exactly the string `'{"behavior":"allow"}'`.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && grep -q 'interface Annotation' src/reviewer-v2/types.ts && grep -q 'AnnotationAction' src/reviewer-v2/types.ts && grep -rq 'export function buildClipboardPayload' src/reviewer-v2/ && grep -rq 'export function shouldUseClipboard' src/reviewer-v2/ && grep -rq 'export function nextHeartbeatState' src/reviewer-v2/ && ! grep -rE "from '\\.\\./" src/reviewer-v2/ && npm run lint && npm test</automated>
  </verify>
  <done>The v2 subtree contains type definitions, connectivity state machine, serializeAnnotations, and offlineLabels — all isolated. No `../` imports. Tests pass. ESLint coupling rule does not flag any new file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Copy useHeartbeat into v2 and create useAnnotations reducer hook with tests</name>
  <files>ui/src/reviewer-v2/hooks/useHeartbeat.ts, ui/src/reviewer-v2/hooks/useHeartbeat.test.ts, ui/src/reviewer-v2/hooks/useAnnotations.ts, ui/src/reviewer-v2/hooks/useAnnotations.test.ts</files>
  <read_first>
    - ui/src/hooks/useHeartbeat.ts (full file — verbatim copy source; preserve generation guard, AbortSignal.any composition, visibility pause)
    - ui/src/hooks/useHeartbeat.test.ts (model for v2 useHeartbeat test — pick 1–2 representative cases for the smoke test; full porting is overkill since the reducer is already covered in Task 1)
    - ui/src/utils/connectivity.test.ts (model — same DI pattern)
    - .planning/phases/17-foundation-isolation/17-CONTEXT.md (D-07, D-08, D-09, D-10)
    - .planning/phases/17-foundation-isolation/17-RESEARCH.md (Pattern 3 useAnnotations example)
    - ui/src/reviewer-v2/types.ts (just created by Task 1 — reference for Annotation + AnnotationAction shape)
    - ui/src/reviewer-v2/connectivity.ts (or .../utils/connectivity.ts — wherever Task 1 placed it; the useHeartbeat copy imports from here)
  </read_first>
  <behavior>
    - `ui/src/reviewer-v2/hooks/useHeartbeat.ts` is a verbatim copy of `ui/src/hooks/useHeartbeat.ts` with ONE change: the import `from '../utils/connectivity'` becomes `from '../connectivity'` (if Task 1 placed connectivity at the v2 root) OR `from '../utils/connectivity'` if Task 1 kept the `utils/` subfolder. Either way, the import path resolves to the v2 connectivity copy — and the literal path must not match the ESLint `../**` pattern. Since `../connectivity` and `../utils/connectivity` both begin with `../`, the ESLint rule WILL flag them. The fix: place `useHeartbeat.ts` at the v2 root (next to `connectivity.ts`) so the import is `from './connectivity'`, OR add an explicit ESLint override that exempts the v2 internal relative imports — D-05 wording forbids this. Per the same constraint discussed in Task 1, the RECOMMENDED layout is FLAT: `ui/src/reviewer-v2/useHeartbeat.ts`, `ui/src/reviewer-v2/useAnnotations.ts`, alongside `connectivity.ts`, `types.ts`, etc. — every internal import is `'./X'`. The `files_modified` field above lists the `hooks/` subdirectory layout as a default; if you choose flat layout, the practical outcome and frontmatter intent are unchanged.
    - The v2 `useHeartbeat` polls `/api/ping` on a 5000ms interval, aborts after 3000ms, pauses on `document.visibilityState !== 'visible'`, requires 3 consecutive failures before transitioning to 'offline'. Behavior is byte-identical to v1.
    - `runHeartbeatTick` is exported from the v2 copy for direct unit testing.
    - `ui/src/reviewer-v2/hooks/useAnnotations.ts` exports a pure reducer `annotationReducer(state, action): AnnotationState` and a hook `useAnnotations()` that calls `useReducer(annotationReducer, initialAnnotationState)` and returns `{ annotations, addAnnotation, editAnnotation, removeAnnotation }`.
    - `annotationReducer` add: appends `action.annotation` to the annotations array.
    - `annotationReducer` edit: maps over annotations and replaces `comment` on the one matching `action.id`; non-matching annotations are returned unchanged.
    - `annotationReducer` remove: filters out the annotation matching `action.id`.
    - Test 1 (add): empty state → dispatch add → expect `state.annotations.length === 1` and `state.annotations[0].id` matches.
    - Test 2 (add then edit): add an annotation then dispatch edit on its id with a new comment → expect the comment field updated and id/anchorText/type unchanged.
    - Test 3 (add then remove): add an annotation then dispatch remove with its id → expect `state.annotations.length === 0`.
    - Test 4 (edit non-existent id): dispatch edit with id not in the array → state is structurally equivalent (no error, no mutation).
    - Test 5 (remove non-existent id): dispatch remove with id not in the array → state is structurally equivalent (no error, no mutation).
    - v2 useHeartbeat smoke test (1 test): create a synthetic `HeartbeatTickContext` matching the v1 test harness; call `runHeartbeatTick` with a fake fetch returning 200 OK; assert `statuses` array remains empty (no transition needed since initial state is 'online' and success keeps it online — the v1 implementation only calls `onStatus` on transitions).
    - All test files import from `vitest` and use `describe`/`it`/`expect`. No `@testing-library/react`. No imports beginning with `../`.
  </behavior>
  <action>
    Decision: ADOPT THE FLAT V2 LAYOUT so every internal import is `'./X'` and the ESLint coupling rule never fires on legitimate intra-subtree code. The directory tree at the end of this task:
    - `ui/src/reviewer-v2/types.ts` (from Task 1)
    - `ui/src/reviewer-v2/connectivity.ts` (from Task 1)
    - `ui/src/reviewer-v2/connectivity.test.ts` (from Task 1)
    - `ui/src/reviewer-v2/serializeAnnotations.ts` (from Task 1)
    - `ui/src/reviewer-v2/offlineLabels.ts` (from Task 1)
    - `ui/src/reviewer-v2/offlineLabels.test.ts` (from Task 1)
    - `ui/src/reviewer-v2/useHeartbeat.ts` (new — this task)
    - `ui/src/reviewer-v2/useHeartbeat.test.ts` (new — this task)
    - `ui/src/reviewer-v2/useAnnotations.ts` (new — this task)
    - `ui/src/reviewer-v2/useAnnotations.test.ts` (new — this task)

    NOTE: This deviates from the `files_modified` frontmatter list above (which uses `utils/` and `hooks/` subdirectories per D-04 default). D-04 grants discretion over subdirectories within `reviewer-v2/`. The flat layout is required by the ESLint coupling rule's literal-string matching of `../**`. Update the actual artifacts to match this layout.

    Create `ui/src/reviewer-v2/useHeartbeat.ts` as a copy of `ui/src/hooks/useHeartbeat.ts`. The only change: `from '../utils/connectivity'` becomes `from './connectivity'`. Preserve every other line including:
    - `POLL_INTERVAL_MS = 5000` and `REQUEST_TIMEOUT_MS = 3000` constants.
    - The `HeartbeatTickContext` interface export.
    - The full `runHeartbeatTick` function including the visibility guard, abort controller cleanup, `AbortSignal.any` composition, the generation token logic for superseded ticks, the `try { await ctx.doFetch(signal) } catch {}` pattern that treats any error as a failure (Pitfall #1 in v1 — Chromium AbortError vs Firefox/Safari TimeoutError), and the `next.status !== prev.status` Object.is bail-out before calling `onStatus`.
    - The `useHeartbeat` hook that calls `runHeartbeatTick` inside a `useEffect` with `let cancelled = false`, `let generation = 0`, a `tick` async function, a `visibilitychange` listener that fires an immediate tick on resume, and a cleanup that aborts the in-flight controller and clears the interval.

    Create `ui/src/reviewer-v2/useHeartbeat.test.ts` with a single smoke test that proves the v2 copy is wired correctly. Import `runHeartbeatTick` and `HeartbeatTickContext` from `'./useHeartbeat'` and `initialHeartbeatState`, `ConnectivityStatus`, `HeartbeatState` from `'./connectivity'`. Build a minimal harness identical in shape to `ui/src/hooks/useHeartbeat.test.ts` (state ref, abort ref, generation counter, cancelled flag, visible flag, statuses array). Single test: `it('v2 copy: successful fetch keeps status online without calling onStatus')` — call `runHeartbeatTick` with `doFetch: () => Promise.resolve(new Response(null, { status: 200 }))`; await one tick; assert `state.value.status === 'online'` and `statuses.length === 0` (no transition).

    Create `ui/src/reviewer-v2/useAnnotations.ts` with:
    - `import { useReducer } from 'react'`
    - `import type { Annotation, AnnotationAction } from './types'`
    - `export interface AnnotationState { annotations: Annotation[] }`
    - `export const initialAnnotationState: AnnotationState = { annotations: [] }`
    - `export function annotationReducer(state, action): AnnotationState` — three switch branches per the behavior spec above. Use `[...state.annotations, action.annotation]` for add, `.map` with conditional `{ ...a, comment: action.comment }` for edit, and `.filter` for remove.
    - `export function useAnnotations()` — calls `useReducer(annotationReducer, initialAnnotationState)`; returns `{ annotations: state.annotations, addAnnotation: (annotation) => dispatch({ type: 'add', annotation }), editAnnotation: (id, comment) => dispatch({ type: 'edit', id, comment }), removeAnnotation: (id) => dispatch({ type: 'remove', id }) }`.

    Create `ui/src/reviewer-v2/useAnnotations.test.ts` with 5 tests covering add/edit/remove + edit-missing-id + remove-missing-id per the behavior spec. Import the reducer and types directly from `'./useAnnotations'` and `'./types'`. Each test calls `annotationReducer(state, action)` synchronously — no React renderer, no `useAnnotations` hook invocation.

    After all files are created, run from the `ui/` directory:
    1. `npm run lint` — must exit 0; no `no-restricted-imports` violations on the v2 subtree.
    2. `npm test` — must exit 0; total test count increases by at least 6 (5 reducer tests + 1 heartbeat smoke test).
    3. `npx tsc -b --noEmit` (or `npm run build` if you prefer) — must exit 0; no TypeScript errors in any v2 file.

    IF Task 1 placed files under `utils/` instead of the flat layout, this task must MOVE them to the flat layout (delete the `utils/` subdirectory and place files at the v2 root). Confirm with the user only if absolutely necessary; flat layout is required for correctness per the ESLint rule analysis above.
  </action>
  <acceptance_criteria>
    - `ui/src/reviewer-v2/useHeartbeat.ts` exists; `grep -c "runHeartbeatTick" ui/src/reviewer-v2/useHeartbeat.ts` returns >= 2 (export + use); contains `POLL_INTERVAL_MS = 5000` and `REQUEST_TIMEOUT_MS = 3000`.
    - `ui/src/reviewer-v2/useHeartbeat.ts` contains `from './connectivity'` (the only import that previously was `from '../utils/connectivity'`).
    - `ui/src/reviewer-v2/useAnnotations.ts` exports `annotationReducer`, `initialAnnotationState`, `AnnotationState`, and `useAnnotations`.
    - `ui/src/reviewer-v2/useAnnotations.test.ts` exists; contains at least 5 `it(` calls covering add / edit / remove / edit-missing / remove-missing.
    - `ui/src/reviewer-v2/useHeartbeat.test.ts` exists; contains at least 1 `it(` and imports `runHeartbeatTick` from `'./useHeartbeat'`.
    - `ui/src/reviewer-v2/` contains NO subdirectory unless that subdirectory is also free of `../` imports (flat layout is the recommended final state).
    - Run `grep -rE "from '\\.\\./" ui/src/reviewer-v2/` — returns no matches (no `../` imports anywhere under the v2 subtree).
    - `cd ui && npm run lint` exits 0.
    - `cd ui && npm test` exits 0 and the new tests pass.
    - `cd ui && npx tsc -b --noEmit` exits 0.
    - Verify ARCH-02 isolation behaviorally: `grep -l "fetch.*'/api/ping'" ui/src/hooks/useHeartbeat.ts ui/src/reviewer-v2/useHeartbeat.ts` lists BOTH files — confirming two independent pollers exist that share only the backend endpoint, not internal state.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && grep -q "runHeartbeatTick" src/reviewer-v2/useHeartbeat.ts && grep -q "POLL_INTERVAL_MS = 5000" src/reviewer-v2/useHeartbeat.ts && grep -q "from './connectivity'" src/reviewer-v2/useHeartbeat.ts && grep -q "annotationReducer" src/reviewer-v2/useAnnotations.ts && grep -q "initialAnnotationState" src/reviewer-v2/useAnnotations.ts && ! grep -rE "from '\\.\\./" src/reviewer-v2/ && npm run lint && npm test && npx tsc -b --noEmit</automated>
  </verify>
  <done>The v2 subtree owns its own `useHeartbeat` (verbatim copy, repointed to local connectivity) and `useAnnotations` (reducer + hook per D-09/D-10). All tests pass under jsdom. The ESLint coupling rule does not flag any v2 file. `tsc -b --noEmit` passes. ARCH-01 and ARCH-02 are mechanically satisfied — every v2 file is inside the subtree, every internal import is `./X`, and v2 has its own independent heartbeat poller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v1 subtree → v2 subtree | The ESLint coupling rule from Plan 01 enforces the rule that v2 files cannot import from outside `src/reviewer-v2/`. The CONTEXT D-06 direction (v1 may import from v2) is unenforced by lint but uninteresting in Phase 17 (no v1 file imports from v2 yet). |
| Browser fetch → server `/api/ping` | The v2 `useHeartbeat` copy shares the `/api/ping` endpoint with v1; the endpoint contract is owned by the Rust server (Phase 12). No new server attack surface introduced by adding a second client poller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | v2 subtree imports outside its boundary | mitigate | ESLint coupling rule from Plan 01 fires on any `../` import. Task verify steps include `grep -rE "from '\\.\\./" src/reviewer-v2/` returning no matches. The flat directory layout adopted in this plan eliminates `../` imports entirely. |
| T-17-02-02 | Denial of Service | Two heartbeat pollers (v1 + v2) hitting `/api/ping` from the same browser tab | accept | At any given moment only one of `<App />` or `<ReviewerV2 />` is mounted (per Plan 03's main.tsx routing branch); the inactive component does not run its hooks. Even in the degenerate case of both pollers running, 2 × 1 request per 5s = ~24/min — well under any reasonable rate limit for a 127.0.0.1-only local server. Endpoint is stateless (Phase 12 SC #4). |
| T-17-02-03 | Information Disclosure | `buildClipboardPayload` v2 copy could diverge from v1 and produce a non-byte-identical clipboard payload | mitigate | Task 1 copies `serializeAnnotations` byte-for-byte with one well-scoped change (defaulting `a.replacement` to `''`); `buildClipboardPayload` is otherwise verbatim. The CONTEXT decision (`Clipboard JSON byte-identical to build_opencode_output; must not drift`) is preserved structurally. Phase 22 will exercise the full clipboard payload contract. |
| T-17-02-04 | Spoofing | Network | accept | Loopback-only (127.0.0.1); no auth surface; out of scope. |
| T-17-02-SC | Tampering | npm install of new packages | accept | This plan adds ZERO new packages — only copies existing source files. No supply-chain attack surface introduced. |
</threat_model>

<verification>
After both tasks complete:

1. `cd ui && npm test` exits 0; new v2 tests pass alongside existing tests.
2. `cd ui && npm run lint` exits 0; the ESLint coupling rule fires on zero v2 files.
3. `cd ui && npx tsc -b --noEmit` exits 0; no TypeScript errors.
4. `grep -rE "from '\\.\\./" ui/src/reviewer-v2/` returns nothing (zero `../` imports under the v2 subtree).
5. `ls ui/src/reviewer-v2/` shows: `types.ts`, `connectivity.ts`, `connectivity.test.ts`, `serializeAnnotations.ts`, `offlineLabels.ts`, `offlineLabels.test.ts`, `useHeartbeat.ts`, `useHeartbeat.test.ts`, `useAnnotations.ts`, `useAnnotations.test.ts` (flat layout, no subdirectories required — D-04 grants discretion).
6. Both `ui/src/hooks/useHeartbeat.ts` and `ui/src/reviewer-v2/useHeartbeat.ts` exist as independent files — confirmation that v2 owns its own copy per D-07/D-08.

Plan 17-03 depends on this work: it will import `useHeartbeat` and `useAnnotations` from `'./useHeartbeat'` and `'./useAnnotations'` inside the new `ReviewerV2.tsx`.
</verification>

<success_criteria>
ARCH-01 satisfied: All v2 code lives exclusively under `ui/src/reviewer-v2/`. No file outside imports from inside (no consumers exist yet; the rule is mechanically enforced by ESLint from Plan 01). No `../` imports anywhere under the v2 subtree — proved by grep + lint.

ARCH-02 satisfied: `ui/src/reviewer-v2/useHeartbeat.ts` is an independent copy that polls `/api/ping` and exposes `ConnectivityStatus` — completely independent of `ui/src/hooks/useHeartbeat.ts`. When Plan 17-03 mounts `<ReviewerV2 />` it will get its own poller via this copy.

`useAnnotations` reducer behaves correctly under add/edit/remove operations and handles missing-id cases without throwing.

The v2 subtree is the only place where v2 code lives; v1 (`App.tsx`, `ui/src/hooks/`, `ui/src/utils/`) is untouched.
</success_criteria>

<output>
On completion, write `.planning/phases/17-foundation-isolation/17-02-SUMMARY.md` per the template.
</output>
