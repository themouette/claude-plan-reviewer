---
phase: 17-foundation-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/package.json
  - ui/package-lock.json
  - ui/vitest.setup.ts
  - ui/vite.config.ts
  - ui/eslint.config.js
autonomous: true
requirements:
  - TEST-02
  - TEST-03
tags:
  - vitest
  - jsdom
  - eslint
  - test-infrastructure
  - coupling-enforcement

must_haves:
  truths:
    - "`npm test` runs in a jsdom environment (DOM globals available in test code)"
    - "Before any test file executes, `IntersectionObserver`, `ResizeObserver`, and `CSS.highlights` are defined as test doubles in the global scope"
    - "A file inside `ui/src/reviewer-v2/` that imports from `../` (any depth) fails `npm run lint` with an error"
    - "A file outside `ui/src/reviewer-v2/` that imports from inside it does NOT trigger the coupling rule (v1 may consume v2, per D-06)"
    - "All 46 existing tests still pass after the jsdom environment switch"
  artifacts:
    - path: "ui/vitest.setup.ts"
      provides: "jsdom mocks for IntersectionObserver, ResizeObserver, CSS.highlights"
      contains: "global.IntersectionObserver"
    - path: "ui/vite.config.ts"
      provides: "Vitest configuration with environment: 'jsdom' and setupFiles"
      contains: "setupFiles"
    - path: "ui/eslint.config.js"
      provides: "reviewer-v2 coupling block scoped to src/reviewer-v2/**"
      contains: "no-restricted-imports"
    - path: "ui/package.json"
      provides: "jsdom dev dependency declared"
      contains: "\"jsdom\""
  key_links:
    - from: "ui/vite.config.ts"
      to: "ui/vitest.setup.ts"
      via: "test.setupFiles array"
      pattern: "setupFiles.*vitest.setup"
    - from: "ui/eslint.config.js"
      to: "ui/src/reviewer-v2/"
      via: "files glob scoping no-restricted-imports"
      pattern: "files.*reviewer-v2"
---

<objective>
Install jsdom and create the test-infrastructure scaffold that every subsequent v2 component test depends on. Add the ESLint coupling rule that enforces ARCH-01 at lint time (TEST-03) so the very first v2 file written is governed by the rule.

Per D-12 and D-13, this work is a hard gate: jsdom mocks (TEST-02) and the ESLint coupling rule (TEST-03) must land before any v2 feature code. This plan delivers both with no v2 component code yet — Plans 02 and 03 build on top of these.

Purpose: TEST-02 prevents tests from breaking when v2 code touches browser-only APIs that jsdom does not implement. TEST-03 makes ARCH-01 mechanically enforced rather than convention-only. Together they form the foundation gate of the v0.6.0 milestone.

Output: jsdom installed as a dev dep; `ui/vitest.setup.ts` registered via `test.setupFiles`; `ui/vite.config.ts` switched to `environment: 'jsdom'`; `ui/eslint.config.js` extended with a `files: ['src/reviewer-v2/**']` block that errors on `../**` imports. Existing 46 tests still pass.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-foundation-isolation/17-CONTEXT.md
@.planning/phases/17-foundation-isolation/17-RESEARCH.md

<interfaces>
<!-- Existing files this plan modifies. Read each in full before editing. -->

ui/package.json — devDependencies block lists eslint@^9.39.4, vitest@^4.1.4, typescript-eslint@^8.58.0, @vitejs/plugin-react, @tailwindcss/vite. jsdom is NOT present and must be added at ^29.1.1.

ui/vite.config.ts — current contents:
- Imports: defineConfig from 'vite', react from '@vitejs/plugin-react', tailwindcss from '@tailwindcss/vite'
- Exports defineConfig({ plugins: [react(), tailwindcss()], base: './' })
- Has NO `test` block — must add it.

ui/eslint.config.js — current contents:
- Imports: js from '@eslint/js', globals, reactHooks from 'eslint-plugin-react-hooks', reactRefresh from 'eslint-plugin-react-refresh', tseslint from 'typescript-eslint', defineConfig + globalIgnores from 'eslint/config'
- defineConfig([globalIgnores(['dist']), { files: ['**/*.{ts,tsx}'], extends: [js.configs.recommended, tseslint.configs.recommended, reactHooks.configs.flat.recommended, reactRefresh.configs.vite], languageOptions: { ecmaVersion: 2020, globals: globals.browser } }])
- The new reviewer-v2 coupling block goes inside the same defineConfig([...]) array as an additional object.

ui/src/utils/connectivity.test.ts (and 3 other existing test files) — currently pass under default Vitest Node environment. After switching to jsdom they must continue to pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install jsdom and wire Vitest to use it with the setup file</name>
  <files>ui/package.json, ui/package-lock.json, ui/vitest.setup.ts, ui/vite.config.ts</files>
  <read_first>
    - ui/package.json (full file — confirm jsdom is absent, identify exact devDependencies block format)
    - ui/vite.config.ts (full file — current shape that the `test` block is added to)
    - .planning/phases/17-foundation-isolation/17-RESEARCH.md (sections: Standard Stack / New Dev Dependency, Pattern 1 with full vitest.setup.ts example, Common Pitfalls #1 #4 #5)
    - .planning/phases/17-foundation-isolation/17-CONTEXT.md (D-12 — the mocks list and the "before any v2 component code" gate)
  </read_first>
  <action>
    Install jsdom as a dev dependency by running `npm install --save-dev jsdom@^29.1.1` from the `ui/` directory (per D-12 and RESEARCH Standard Stack). This adds `"jsdom": "^29.1.1"` to devDependencies in `ui/package.json` and updates `ui/package-lock.json`. Do NOT use `npm install` without `--save-dev`; the package must land in devDependencies.

    Create `ui/vitest.setup.ts` (new file) that registers jsdom polyfills for three browser APIs jsdom does not implement. The file must:
    - `import { vi } from 'vitest'` at the top.
    - Assign `global.IntersectionObserver` to `vi.fn().mockImplementation(() => ({ observe: vi.fn(), unobserve: vi.fn(), disconnect: vi.fn(), root: null, rootMargin: '', thresholds: [] }))`.
    - Assign `global.ResizeObserver` to `vi.fn().mockImplementation(() => ({ observe: vi.fn(), unobserve: vi.fn(), disconnect: vi.fn() }))`.
    - Define `CSS.highlights` as a `new Map()` with the defensive guards from Pitfall #4: first check `typeof CSS === 'undefined'` and define a `CSS = {}` shim on `global` if so; then check `(CSS as { highlights?: unknown }).highlights` and assign `new Map()` only when missing. The two casts in RESEARCH Pattern 1 (`global as unknown as { CSS: object }` and `CSS as { highlights: unknown }`) are required to keep the TypeScript strict mode in the existing tsconfig happy without `any`.

    Modify `ui/vite.config.ts` to add a `test` property to the defineConfig argument with:
    - `environment: 'jsdom'` (per D-12 — required for the mocks file to be useful; Pitfall #1 in RESEARCH).
    - `setupFiles: ['./vitest.setup.ts']` (the relative path resolves from the `ui/` directory where vite.config.ts lives).
    - Do NOT add `globals: true` and do NOT add a triple-slash `/// <reference types="vitest" />` directive — per RESEARCH "State of the Art" both are unnecessary in Vitest 4 with `defineConfig` from `vite`.
    - Keep the existing `plugins: [react(), tailwindcss()]` and `base: './'` properties intact.

    After the file changes, run `cd ui && npm test` once to verify all 46 existing tests still pass under the jsdom environment (Pitfall #5 — should pass since existing tests are pure functions, but confirm before exiting).
  </action>
  <acceptance_criteria>
    - `ui/package.json` devDependencies contains a `"jsdom"` entry with version starting `"^29"` (e.g. `"jsdom": "^29.1.1"`).
    - `ui/package-lock.json` contains a `node_modules/jsdom` entry (verify with `grep -q '"node_modules/jsdom"' ui/package-lock.json`).
    - `ui/vitest.setup.ts` exists and `grep -c "global.IntersectionObserver" ui/vitest.setup.ts` returns >= 1.
    - `ui/vitest.setup.ts` contains `global.ResizeObserver` assignment.
    - `ui/vitest.setup.ts` contains `CSS` and `highlights` references with the defensive `typeof CSS === 'undefined'` guard.
    - `ui/vite.config.ts` contains `environment: 'jsdom'` and `setupFiles` referencing `./vitest.setup.ts`.
    - `cd ui && npm test` exits 0 and reports the same number of tests passing as before this plan (46 expected at time of writing).
    - `cd ui && npm test` output does NOT include any `ReferenceError` for `IntersectionObserver`, `ResizeObserver`, or `CSS.highlights`.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && grep -q '"jsdom"' package.json && grep -q '"node_modules/jsdom"' package-lock.json && grep -q 'global.IntersectionObserver' vitest.setup.ts && grep -q 'environment.*jsdom' vite.config.ts && npm test</automated>
  </verify>
  <done>jsdom is a declared dev dependency. `ui/vitest.setup.ts` registers the three required jsdom polyfills. `ui/vite.config.ts` uses environment: 'jsdom' and loads the setup file. All existing tests pass under the new environment.</done>
</task>

<task type="auto">
  <name>Task 2: Add the reviewer-v2 ESLint coupling block and verify it blocks ../** imports</name>
  <files>ui/eslint.config.js</files>
  <read_first>
    - ui/eslint.config.js (full file — current structure with defineConfig([...]) array)
    - .planning/phases/17-foundation-isolation/17-RESEARCH.md (sections: Pattern 2 with the verified `no-restricted-imports` block, Common Pitfalls #2 #3, Open Question #1)
    - .planning/phases/17-foundation-isolation/17-CONTEXT.md (D-05 — files: ['**/reviewer-v2/**']; D-06 — coupling direction is one-way; specifics — block both `../**` and `./../**` per CONTEXT spec)
  </read_first>
  <action>
    Modify `ui/eslint.config.js` to append a new config object inside the existing `defineConfig([...])` array. The new object must:
    - Set `files: ['src/reviewer-v2/**']` (per Pitfall #2 — glob is relative to the `ui/` directory where `eslint.config.js` lives, not to `src/`).
    - Set `rules: { 'no-restricted-imports': ['error', { patterns: [{ group: ['../**'], message: 'reviewer-v2/ files must not import from outside the subtree. Copy the utility into reviewer-v2/utils/ or reviewer-v2/hooks/.' }] }] }` (per Pattern 2 verified against eslint.org docs).
    - Use the single pattern `'../**'` per Pitfall #3 and the CONTEXT specifics note — `**` crosses directory separators in gitignore-style globs, so this blocks `../foo`, `../../foo`, `../../../foo`, etc. Do NOT use only `'../*'` (which would only block one level).
    - Be a sibling of the existing `{ files: ['**/*.{ts,tsx}'], extends: [...] }` block, NOT nested inside it (Anti-Patterns to Avoid #2 in RESEARCH).

    The new block does NOT need `languageOptions`, `extends`, or any plugin reference — it only adds the `no-restricted-imports` rule and inherits parser settings from the file-wide TypeScript block via ESLint flat-config matching rules.

    After the edit, smoke-test the rule live (Open Question #1) by:
    1. Run `cd ui && npm run lint` once with no v2 files yet — must exit 0 (rule fires only on `src/reviewer-v2/**` matches; nothing exists yet, so no errors).
    2. Create a temporary file at `ui/src/reviewer-v2/__lint_test__.ts` containing a single line `import App from '../../App'`. Run `cd ui && npm run lint` — it MUST exit non-zero with a `no-restricted-imports` violation referencing the message about "reviewer-v2/ files must not import from outside the subtree".
    3. DELETE `ui/src/reviewer-v2/__lint_test__.ts` after confirming the violation. Re-run `cd ui && npm run lint` — must exit 0 again.
    4. Repeat steps 2–3 with `import { foo } from '../utils/foo'` to confirm the single-level-up case also fails.

    Do not commit the temporary `__lint_test__.ts` file. The final state must contain the new ESLint block and no test artifacts.
  </action>
  <acceptance_criteria>
    - `ui/eslint.config.js` contains `files: ['src/reviewer-v2/**']` in exactly one new config block.
    - `ui/eslint.config.js` contains `'no-restricted-imports'` with `'error'` severity.
    - `ui/eslint.config.js` contains the pattern `'../**'` inside a `group` array.
    - `cd ui && npm run lint` exits 0 with no v2 files present (clean state).
    - When a deliberate violating file is created at `ui/src/reviewer-v2/__lint_test__.ts` with content `import App from '../../App'`, `npm run lint` exits non-zero and the output contains the substring `no-restricted-imports` or the rule's custom message about "reviewer-v2/".
    - The same violation appears for a single-level-up import (`'../utils/foo'`).
    - After deleting the test file, `npm run lint` exits 0 again.
    - No new files committed: `ui/src/reviewer-v2/` directory either does not exist or contains only files created by later plans, not lint test artifacts.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && grep -q "files:.*'src/reviewer-v2/\*\*'" eslint.config.js && grep -q 'no-restricted-imports' eslint.config.js && grep -q "'../\*\*'" eslint.config.js && npm run lint && mkdir -p src/reviewer-v2 && printf "import App from '../../App'\n" > src/reviewer-v2/__lint_test__.ts && ! npm run lint > /tmp/lint_out_17_01 2>&1 && grep -q 'no-restricted-imports' /tmp/lint_out_17_01 && rm src/reviewer-v2/__lint_test__.ts && rmdir src/reviewer-v2 2>/dev/null; npm run lint</automated>
  </verify>
  <done>The ESLint coupling block is present in `ui/eslint.config.js`, scoped to `src/reviewer-v2/**`. The rule fires with an error on any `../**` import (including multi-level). Lint passes on a clean repo with no v2 files. No lint test artifacts remain in the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | jsdom is fetched from the npm registry at install time; package contents are executed during test runs |
| ESLint config → developer workflow | The coupling rule mediates which imports are allowed for files under `src/reviewer-v2/`; misconfiguration would silently allow forbidden imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | npm install of `jsdom` | mitigate | jsdom is verified in RESEARCH Package Legitimacy Audit as a 14-year-old foundational npm package (github.com/jsdom/jsdom, tens of millions of weekly downloads). The slopcheck [SUS] verdict on `crates.io` is a wrong-ecosystem false positive (Rust registry stub, not the npm package). Disposition: install without a blocking human checkpoint per RESEARCH note — this is an ecosystem false positive, not a legitimacy concern. |
| T-17-02 | Information Disclosure | `vitest.setup.ts` shipped to production bundle | accept | Setup file is only loaded via `test.setupFiles` and is not imported by any non-test code; Vite production build does not consume Vitest config and cannot accidentally include the setup file. No PII or secrets are present. |
| T-17-03 | Elevation of Privilege | Misconfigured ESLint coupling rule allows `../**` imports silently | mitigate | Task 2 includes a live smoke-test step that creates a deliberately violating file and confirms `npm run lint` exits non-zero before deleting it. The verify command runs the same probe automatically. The rule cannot ship in a state that fails to catch violations. |
| T-17-04 | Repudiation | None applicable | accept | No multi-user actions, no log forging surface — single-user local tooling. |
| T-17-SC | Tampering | npm install supply chain | mitigate | Only one new package (jsdom@^29) — verified by RESEARCH Package Legitimacy Audit as authoritative (github.com/jsdom/jsdom). No `[ASSUMED]` or `[SUS]` packages on npm for this phase; no blocking human checkpoint required per RESEARCH guidance. |
</threat_model>

<verification>
After both tasks complete:

1. `cd ui && npm test` exits 0; all existing tests pass under jsdom; no `ReferenceError` for `IntersectionObserver`, `ResizeObserver`, or `CSS.highlights` appears in the output.
2. `cd ui && npm run lint` exits 0 on the clean repo.
3. Creating a deliberate `import App from '../../App'` inside any newly created `src/reviewer-v2/` file produces a `no-restricted-imports` error.
4. `ui/vitest.setup.ts` is the only newly created file in `ui/`; `ui/vite.config.ts` and `ui/eslint.config.js` are modified in place.
5. The `ui/src/reviewer-v2/` directory does NOT yet exist at the end of this plan — that directory is created in Plan 02 with the actual v2 subtree contents.

The successor plan (17-02) depends on this work: jsdom-environment test runs are required for `useAnnotations` and `useHeartbeat` v2 copy tests, and the ESLint coupling rule must already be live before any v2 file is committed.
</verification>

<success_criteria>
TEST-02 satisfied: `vitest.setup.ts` includes jsdom mocks for `IntersectionObserver`, `ResizeObserver`, and `CSS.highlights`, registered via `test.setupFiles` in `ui/vite.config.ts`. The setup file runs before any test module.

TEST-03 satisfied: An ESLint `no-restricted-imports` rule in `ui/eslint.config.js` enforces ARCH-01 automatically — any `../**` import from inside `src/reviewer-v2/` is a lint error.

Existing tests still green: all 46 previously-passing tests continue to pass.
</success_criteria>

<output>
On completion, write `.planning/phases/17-foundation-isolation/17-01-SUMMARY.md` per the template.
</output>
