---
phase: 06.1-integration-test-harness
plan: 01
subsystem: cli
tags: [clap, axum, tokio, port-binding, integration-test]

# Dependency graph
requires: []
provides:
  - "--port <u16> CLI flag on plan-reviewer binary"
  - "start_server accepts port: u16 parameter, binds to 127.0.0.1:{port}"
  - "Integration tests can pass --port <N> to bind server to a known port"
affects:
  - 06.1-02
  - 06.1-03

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Port threading: CLI arg -> run_hook_flow -> async_main -> start_server -> TcpListener::bind"
    - "Port 0 = OS-assigned (default), N = fixed port for testing"

key-files:
  created: []
  modified:
    - src/main.rs
    - src/server.rs

key-decisions:
  - "Default port 0 preserves existing OS-assigned behavior — no breakage for existing users"
  - "Port shadowing: start_server returns actual bound port, which shadows the port parameter — correct behavior when port=0"

patterns-established:
  - "Port 0 = OS-assigned: used as default so existing behavior is unchanged"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-04-10
---

# Phase 06.1 Plan 01: Add --port CLI Flag Summary

**`--port <u16>` flag added to plan-reviewer with default 0 (OS-assigned), threaded from Cli struct through run_hook_flow, async_main, and into start_server's TcpListener::bind**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-10T20:45:00Z
- **Completed:** 2026-04-10T20:58:43Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Added `port: u16` field to `Cli` struct with `default_value_t = 0`
- Threaded port value through entire call chain: `Cli -> run_hook_flow -> async_main -> start_server`
- Updated `start_server` to bind to `format!("127.0.0.1:{}", port)` instead of hardcoded `"127.0.0.1:0"`
- Added 2 TDD tests: `test_cli_port_flag_default_zero` and `test_cli_port_flag_accepts_value`
- All 38 tests pass (36 pre-existing + 2 new); clippy clean

## Task Commits

Each task was committed atomically:

1. **Task 1: Add --port flag to Cli struct and thread through call chain** - `1af7838` (feat)

**Plan metadata:** (docs commit to follow)

_Note: TDD task — tests written before implementation (RED), then implementation made them pass (GREEN)_

## Files Created/Modified

- `src/main.rs` - Added `port: u16` to `Cli` struct; updated `run_hook_flow`, `async_main` signatures; added 2 TDD tests
- `src/server.rs` - Updated `start_server` signature to accept `port: u16`; changed bind from `"127.0.0.1:0"` to `format!("127.0.0.1:{}", port)`

## Decisions Made

- Default `port = 0` preserves OS-assigned behavior — existing users see no change
- Port shadowing in `async_main`: local `port` returned from `start_server` correctly shadows the parameter (actual bound port may differ from requested when requesting 0)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

The worktree was initialized from commit `91f44cb` (older than the required base `a71a49d`). After `git reset --soft` to align the index, the working tree still had old source files. Resolved by running `git checkout HEAD -- src/` to restore all source files to the correct HEAD state, then re-applied port changes cleanly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `--port` flag is ready for integration tests to use
- Plan 06.1-02 (integration test scaffold) can now bind the server to a known port via `--port <N>`
- No blockers

---
*Phase: 06.1-integration-test-harness*
*Completed: 2026-04-10*
