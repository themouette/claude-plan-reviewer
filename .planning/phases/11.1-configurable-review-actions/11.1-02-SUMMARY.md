---
phase: 11.1-configurable-review-actions
plan: "02"
subsystem: frontend-cli
tags: [frontend, react, cli, labels, annotate]
dependency_graph:
  requires: [11.1-01]
  provides: [dynamic Approve/Deny button labels, updated annotate.md with custom labels]
  affects: [ui/src/App.tsx, src/integrations/claude.rs]
tech_stack:
  added: []
  patterns: [React useState + useEffect for non-blocking config fetch, concat! string literal update]
key_files:
  created: []
  modified:
    - ui/src/App.tsx
    - src/integrations/claude.rs
decisions:
  - "fetch /api/config is non-blocking — if it fails or returns non-ok, button labels fall back to 'Approve'/'Deny' defaults"
  - "Submit Denial button text intentionally not changed — it is the confirmation step inside the deny form, not the initial toggle"
  - "pre-existing test failure install_returns_err_when_binary_path_is_none confirmed out-of-scope (existed before this plan)"
metrics:
  duration: ~10min
  completed: "2026-04-12"
  tasks_completed: 2
  files_modified: 2
---

# Phase 11.1 Plan 02: Bind Dynamic Button Labels and Update annotate.md Summary

**One-liner:** Wired /api/config fetch into App.tsx to drive Approve/Deny button labels dynamically, and updated annotate.md content in claude.rs to pass `--approve-label "No issues" --deny-label "Leave feedback"` with matching Step 2 phrasing.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Fetch /api/config on mount and bind dynamic labels to Approve/Deny buttons | 5bf0633 | ui/src/App.tsx |
| 2 | Update annotate.md content and test assertions to use custom labels | ec38a48 | src/integrations/claude.rs |

## Verification Results

- `npm run build` in ui/ — passed (TypeScript compiled, Vite bundle built)
- `cargo test install_creates_annotate_md` — 2 passed (new assertions verified)
- `cargo test -- claude --skip install_returns_err_when_binary_path_is_none` — 32 passed
- `cargo test --test integration -- review` — 6 passed (4 existing + 2 from Plan 01)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — button labels are fully wired from /api/config through to JSX interpolation. annotate.md content is a compile-time constant with the correct labels embedded.

## Pre-existing Failure (Out of Scope)

`install_returns_err_when_binary_path_is_none` in `src/integrations/claude.rs` — confirmed pre-existing before this plan, same as documented in 11.1-01 SUMMARY. Not caused by changes here.

## Threat Flags

None — JSX interpolation of `{approveLabel}` and `{denyLabel}` is auto-escaped by React (T-11.1-03 accepted). annotate.md content is a compile-time Rust concat!() constant (T-11.1-04 accepted).

## Self-Check: PASSED

Files exist:
- ui/src/App.tsx — FOUND
- src/integrations/claude.rs — FOUND

Commits exist:
- 5bf0633 (feat: dynamic button labels) — FOUND
- ec38a48 (feat: annotate.md custom labels) — FOUND
