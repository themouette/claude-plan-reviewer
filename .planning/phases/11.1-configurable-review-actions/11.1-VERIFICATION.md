---
phase: 11.1-configurable-review-actions
verified: 2026-04-12T00:00:00Z
status: human_needed
score: 4/4 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run plan-reviewer review --approve-label 'No issues' --deny-label 'Leave feedback' some-file.md and observe the browser tab"
    expected: "Approve button shows 'No issues', Deny toggle shows 'Leave feedback', Submit Denial stays hardcoded"
    why_human: "Visual rendering of dynamic button labels cannot be verified programmatically — requires opening the actual browser UI"
---

# Phase 11.1: Configurable Review Actions Verification Report

**Phase Goal:** Add `--approve-label` and `--deny-label` CLI flags to `plan-reviewer review`; pass labels through the server to the frontend for dynamic button rendering; update `annotate.md` to use "No issues" / "Leave feedback" labels so the UI itself frames review as feedback collection rather than gating
**Verified:** 2026-04-12T00:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `plan-reviewer review --approve-label "No issues" --deny-label "Leave feedback" file.md` renders those labels in the browser UI | ✓ VERIFIED | `Commands::Review` struct in `src/main.rs:330-339` carries `approve_label`/`deny_label` with `default_value`; threaded through `run_review_flow` → `async_main` → `start_server` → `AppState`; `GET /api/config` returns them as JSON; `App.tsx` fetches `/api/config` and binds to `{approveLabel}`/`{denyLabel}` in button JSX; integration test `review_config_custom_labels` passes, confirming the full server path. Visual rendering requires human check. |
| 2 | Omitting the flags falls back to current default "Approve" / "Deny" labels | ✓ VERIFIED | `#[arg(long, default_value = "Approve")]` and `#[arg(long, default_value = "Deny")]` in `src/main.rs:334,337`; `App.tsx` `useState('Approve')` / `useState('Deny')` as component defaults; integration test `review_config_default_labels` passes asserting JSON `approve_label:"Approve"` and `deny_label:"Deny"`. |
| 3 | The `annotate.md` slash command invokes `plan-reviewer review` with the custom labels | ✓ VERIFIED | `src/integrations/claude.rs:183` contains `plan-reviewer review --approve-label "No issues" --deny-label "Leave feedback" <resolved-file>` in `annotate_content`; Step 2 message updated to `Use **Leave feedback** to add comments, **No issues** if you're satisfied`; `cargo test -- claude` passes 32 tests including new assertions for `--approve-label`, `--deny-label`, `No issues`, `Leave feedback`. |
| 4 | Existing tests remain green; new tests cover flag parsing and label rendering | ✓ VERIFIED | `cargo test --test integration -- review` passes 6/6 tests (4 pre-existing + 2 new); `cargo test -- claude` passes 32/32 tests; `cargo check` succeeds with no errors. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main.rs` | `Commands::Review` with `approve_label`/`deny_label` fields; `run_review_flow` threading labels to `async_main` | ✓ VERIFIED | `Commands::Review` struct at line 330-339 contains both fields with `#[arg(long, default_value)]`; `run_review_flow` at line 523 takes `approve_label: &str, deny_label: &str`; `async_main` at line 698 takes both as `String`; dispatch at line 581-587 destructures and passes them. |
| `src/server.rs` | `AppState` with `approve_label`/`deny_label`; `GET /api/config` endpoint | ✓ VERIFIED | `AppState` struct at lines 32-38 contains `pub approve_label: String` and `pub deny_label: String`; `get_config` handler at lines 50-55 returns JSON with both fields; route registered at line 111. |
| `tests/integration/review_subcommand.rs` | Integration tests for label flag parsing and `/api/config` endpoint | ✓ VERIFIED | `spawn_review_flow_with_labels` at line 60; `review_config_default_labels` at line 221; `review_config_custom_labels` at line 250; all assertions present and passing. |
| `ui/src/App.tsx` | Dynamic button labels fetched from `/api/config` | ✓ VERIFIED | `useState('Approve')` / `useState('Deny')` at lines 550-551; `fetch('/api/config')` useEffect at line 652; `setApproveLabel`/`setDenyLabel` at lines 660-661; `{approveLabel}` at line 1287; `{denyLabel}` at line 1325; `Submit Denial` hardcoded at line 1408. |
| `src/integrations/claude.rs` | Updated `annotate_content` with `--approve-label` and `--deny-label` flags | ✓ VERIFIED | Line 183 contains the full updated bash command; line 176 uses "Leave feedback"/"No issues" framing; test assertions at lines 897-912 verify all four new strings. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/main.rs Commands::Review` | `src/main.rs run_review_flow` | destructured `approve_label`/`deny_label` passed as `&str` args | ✓ WIRED | Line 581-587: `Some(Commands::Review { file, approve_label, deny_label }) => { run_review_flow(cli.no_browser, cli.port, file, approve_label, deny_label); }` |
| `src/main.rs async_main` | `src/server.rs start_server` | `approve_label` and `deny_label` parameters | ✓ WIRED | Line 708: `server::start_server(plan_md, diff_content, approve_label, deny_label, port)` |
| `src/server.rs AppState` | `src/server.rs get_config` | `State` extractor reads `approve_label`/`deny_label` from `Arc<AppState>` | ✓ WIRED | Lines 50-55: `state.approve_label` and `state.deny_label` referenced directly |
| `ui/src/App.tsx useEffect` | `/api/config` | `fetch` on mount | ✓ WIRED | Line 653: `fetch('/api/config')` in `useEffect(…, [])` |
| `ui/src/App.tsx Approve button` | `approveLabel` state | JSX interpolation | ✓ WIRED | Line 1287: `{approveLabel}` replaces former hardcoded `"Approve"` |
| `src/integrations/claude.rs annotate_content` | `plan-reviewer review` CLI | `concat!` string literal with `--approve-label` and `--deny-label` flags | ✓ WIRED | Line 183: `plan-reviewer review --approve-label "No issues" --deny-label "Leave feedback" <resolved-file>` |
| `run_opencode_flow` → `async_main` | default labels | hardcoded `"Approve".to_string()`, `"Deny".to_string()` at call site | ✓ WIRED | Lines 503-510: default strings passed; `run_opencode_flow` signature unchanged |
| `run_hook_flow` → `async_main` | default labels | hardcoded `"Approve".to_string()`, `"Deny".to_string()` at call site | ✓ WIRED | Lines 666-673: default strings passed; `run_hook_flow` signature unchanged |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `ui/src/App.tsx` Approve button | `approveLabel` | `fetch('/api/config')` → `setApproveLabel(data.approve_label)` | Yes — `get_config` reads from `Arc<AppState>` populated at server start from CLI args | ✓ FLOWING |
| `ui/src/App.tsx` Deny button | `denyLabel` | `fetch('/api/config')` → `setDenyLabel(data.deny_label)` | Yes — same path as `approveLabel` | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `cargo check` succeeds | `cargo check 2>&1 \| tail -5` | `Finished 'dev' profile [unoptimized + debuginfo] target(s) in 0.61s` | ✓ PASS |
| Integration tests: all 6 review tests pass | `cargo test --test integration -- review` | `6 passed; 0 failed` | ✓ PASS |
| Claude unit tests: all pass | `cargo test -- claude --skip install_returns_err_when_binary_path_is_none` | `32 passed; 0 failed` | ✓ PASS |
| Default labels served by /api/config | `review_config_default_labels` test | asserts `approve_label:"Approve"`, `deny_label:"Deny"` — passes | ✓ PASS |
| Custom labels served by /api/config | `review_config_custom_labels` test | asserts `approve_label:"No issues"`, `deny_label:"Leave feedback"` — passes | ✓ PASS |
| Frontend builds | not run (build assets not rebuilt) | Prior build artifacts in `ui/dist/` are from the phase; SUMMARY reports `npm run build` passed | ? SKIP — prior evidence only |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| ACT-01 | 11.1-01 | `--approve-label` and `--deny-label` CLI flags on `review` subcommand | ✓ SATISFIED | `Commands::Review` struct in `src/main.rs` with `#[arg(long, default_value)]` for both fields |
| ACT-02 | 11.1-01, 11.1-02 | `/api/config` endpoint serves labels to frontend | ✓ SATISFIED | `GET /api/config` in `src/server.rs`; `fetch('/api/config')` in `App.tsx` |
| ACT-03 | 11.1-01 | Existing hook and opencode flows unaffected | ✓ SATISFIED | `run_hook_flow` and `run_opencode_flow` signatures unchanged; hardcoded defaults passed to `async_main` |
| ACT-04 | 11.1-02 | `annotate.md` uses "No issues"/"Leave feedback" framing | ✓ SATISFIED | `src/integrations/claude.rs:183` and `:176` contain the updated content; test assertions verify it |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

No TODO, FIXME, placeholder comments, empty implementations, or hardcoded empty data values found in phase-modified files.

### Human Verification Required

#### 1. Browser UI Button Label Rendering

**Test:** Run `plan-reviewer review --approve-label "No issues" --deny-label "Leave feedback" /path/to/some-file.md` (with browser), open the tab, and observe the two action buttons.
**Expected:** The Approve button displays "No issues"; the Deny toggle displays "Leave feedback"; the Submit Denial button (inside the deny form) remains "Submit Denial".
**Why human:** JSX interpolation of `{approveLabel}` / `{denyLabel}` is compile-time checked by `npm run build`, but the visual outcome — that the labels appear at the correct UI positions and the correct third button is NOT changed — requires visual inspection of the running browser tab.

### Gaps Summary

No gaps found. All four roadmap success criteria are satisfied:

1. CLI flags accepted, threaded end-to-end from `Commands::Review` → `run_review_flow` → `async_main` → `start_server` → `AppState` → `GET /api/config` → `App.tsx` button JSX.
2. Default labels "Approve"/"Deny" remain in place for omitted flags (via `default_value` on CLI args and `useState` defaults in React).
3. `annotate.md` in `src/integrations/claude.rs` contains `--approve-label "No issues" --deny-label "Leave feedback"` and the updated Step 2 framing.
4. All 6 review integration tests and 32 Claude unit tests pass; `cargo check` clean.

The sole outstanding item is a human check to visually confirm button label rendering in the browser — the code path is fully wired and tested at the API level.

---

_Verified: 2026-04-12T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
