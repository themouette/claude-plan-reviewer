---
phase: 11.1-configurable-review-actions
plan: "01"
subsystem: cli-server
tags: [cli, server, api, labels, review]
dependency_graph:
  requires: []
  provides: [approve_label, deny_label CLI flags, GET /api/config endpoint]
  affects: [src/main.rs, src/server.rs, tests/integration/review_subcommand.rs]
tech_stack:
  added: []
  patterns: [clap default_value fields, axum JSON handler, AppState extension]
key_files:
  created: []
  modified:
    - src/main.rs
    - src/server.rs
    - tests/integration/review_subcommand.rs
decisions:
  - "Used clap default_value (not Option<String>) for labels so downstream code always has a String without unwrapping"
  - "Hardcoded Approve/Deny defaults at async_main call sites in run_opencode_flow and run_hook_flow — no signature changes to those functions"
  - "positional <FILE> arg comes after --approve-label/--deny-label flags in spawn_review_flow_with_labels helper (clap 4 parses named before positional)"
metrics:
  duration: ~15min
  completed: "2026-04-12"
  tasks_completed: 3
  files_modified: 3
---

# Phase 11.1 Plan 01: Add --approve-label/--deny-label Flags and /api/config Endpoint Summary

**One-liner:** Added `--approve-label` and `--deny-label` CLI flags to `plan-reviewer review` subcommand, threaded through `run_review_flow` -> `async_main` -> `start_server`, extended `AppState` with both fields, and added `GET /api/config` endpoint returning JSON label values with defaults "Approve"/"Deny".

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add --approve-label/--deny-label CLI flags and thread through call chain | 3b653c4 | src/main.rs |
| 2 | Extend AppState and add GET /api/config endpoint | 3b653c4 | src/server.rs |
| 3 | Add integration tests for custom labels and /api/config | 8580076 | tests/integration/review_subcommand.rs |
| fix | Update unit test pattern match for extended Commands::Review | dfbadbf | src/main.rs |

## Verification Results

- `cargo build` — passed
- `cargo clippy -- -D warnings` — passed (no warnings)
- `cargo test --bin plan-reviewer -- test_cli` — 8 passed
- `cargo test --test integration -- review` — 6 passed (4 existing + 2 new)
- `plan-reviewer review --help` — shows `--approve-label` and `--deny-label` flags with defaults

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated pre-existing unit test pattern match for Commands::Review**
- **Found during:** Task 3 (full `cargo test` run)
- **Issue:** Existing test `test_cli_review_subcommand_parses` used `Some(Commands::Review { file })` pattern which became non-exhaustive after adding `approve_label` and `deny_label` fields
- **Fix:** Changed to `Some(Commands::Review { file, .. })` using the ignore-remaining-fields syntax
- **Files modified:** src/main.rs (line 280)
- **Commit:** dfbadbf

### Out-of-Scope Pre-existing Failure

One pre-existing test failure found but NOT fixed (outside plan scope):
- `integrations::claude::tests::install_returns_err_when_binary_path_is_none` in `src/integrations/claude.rs`
- This failure existed in commit `6078e0e` before any changes in this plan
- Not caused by this plan's changes; deferred to separate investigation

## Known Stubs

None — all data is wired through from CLI flags to AppState to /api/config response.

## Threat Flags

None — the new `/api/config` endpoint follows the same localhost-only trust model as `/api/plan` and `/api/diff` as documented in the plan's threat model (T-11.1-01, T-11.1-02, both accepted).

## Self-Check: PASSED

Files exist:
- src/main.rs — FOUND
- src/server.rs — FOUND
- tests/integration/review_subcommand.rs — FOUND

Commits exist:
- 3b653c4 (feat: flags + endpoint) — FOUND
- 8580076 (test: integration tests) — FOUND
- dfbadbf (fix: unit test pattern) — FOUND
