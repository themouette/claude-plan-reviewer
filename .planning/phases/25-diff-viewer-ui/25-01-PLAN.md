---
phase: 25-diff-viewer-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - ui/src/code-review/types.ts
  - ui/src/code-review/hooks/useDiff.ts
  - ui/src/code-review/hooks/useDiff.test.ts
  - ui/eslint.config.js
  - src/diff_api.rs
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-04
user_setup: []

must_haves:
  truths:
    - "GET /api/diff/branch (no query param) returns FileDiff[] with default git2 context lines (3) — D-06"
    - "GET /api/diff/branch?context=999 returns FileDiff[] computed with 999 context lines (full file) — D-05"
    - "GET /api/diff/commit/:sha?context=N accepts and applies the same context override"
    - "Invalid ?context input (non-integer, e.g. context=abc) yields HTTP 400, NOT 500 or silently default"
    - "TypeScript FileDiff type literally mirrors the Rust JSON field names (snake_case)"
    - "useDiff exports a testable fetchDiffOnce(doFetch, contextLines?) pure function returning Promise<{ files, error }>"
    - "useDiff React hook fetches on mount, exposes { files, loading, error, refetch }, and refetch(contextLines) re-fetches with ?context=N"
    - "Vitest tests cover: success, HTTP error, network error, refetch flow with contextLines, and contextLines=undefined omits the ?context param"
    - "ESLint rule blocks any import from reviewer-v2/ in code-review/ files"
  artifacts:
    - path: "ui/src/code-review/types.ts"
      provides: "FileDiff interface mirroring the Rust diff_api FileDiff struct"
      contains: "export interface FileDiff"
    - path: "ui/src/code-review/hooks/useDiff.ts"
      provides: "useDiff React hook + fetchDiffOnce pure function for testability"
      contains: "export function useDiff"
    - path: "ui/src/code-review/hooks/useDiff.test.ts"
      provides: "Vitest tests for fetchDiffOnce with injected doFetch"
      contains: "fetchDiffOnce"
    - path: "ui/eslint.config.js"
      provides: "no-restricted-imports rule for src/code-review/** blocking reviewer-v2/ imports"
      contains: "src/code-review/**"
    - path: "src/diff_api.rs"
      provides: "DiffContextQuery struct + ?context=N query param wired into get_diff_branch and get_diff_commit"
      contains: "DiffContextQuery"
  key_links:
    - from: "ui/src/code-review/hooks/useDiff.ts"
      to: "/api/diff/branch"
      via: "fetch with optional ?context= query string"
      pattern: "/api/diff/branch"
    - from: "src/diff_api.rs get_diff_branch"
      to: "try_branch_diff(repo_path, context_lines)"
      via: "DiffOptions::context_lines(u32)"
      pattern: "context_lines"
    - from: "ui/eslint.config.js"
      to: "src/code-review/** files"
      via: "no-restricted-imports rule"
      pattern: "src/code-review/\\*\\*"
---

<objective>
Lay the foundation for the `/code-review` UI: the TypeScript `FileDiff` type that mirrors the Phase 24 backend JSON, the `useDiff` React hook with an injectable `fetchDiffOnce` pure function for tests, the `?context=N` query parameter added to the existing `/api/diff/branch` and `/api/diff/commit/:sha` Rust handlers, and the ESLint isolation rule that blocks `code-review/` from importing `reviewer-v2/`.

Purpose: Establish the data contract (types) and the data access layer (hook + Rust backend extension) that Plans 25-02 (UI components) and 25-03 (route wiring + ARCH-01 cleanup) depend on. Following Phase 24's TDD discipline and CLAUDE.md test coverage rules, the `useDiff` business logic ships with full Vitest coverage from day one. The Rust `?context` param landing in this same plan lets the frontend "Expand All" toggle (D-05) wire up correctly in Plan 25-02 without a second backend round-trip in a later plan.

Output: Compilable TypeScript foundation (types + hook + tests), a compilable Rust backend with `?context` working on both diff endpoints, and an ESLint rule that makes future violations fail `npm run lint`.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-diff-viewer-ui/25-CONTEXT.md
@.planning/phases/25-diff-viewer-ui/25-RESEARCH.md
@.planning/phases/25-diff-viewer-ui/25-PATTERNS.md
@.planning/phases/25-diff-viewer-ui/25-UI-SPEC.md
@.planning/phases/24-backend-diff-api/24-CONTEXT.md
@ui/src/reviewer-v2/useHeartbeat.ts
@ui/src/reviewer-v2/useHeartbeat.test.ts
@ui/src/reviewer-v2/types.ts
@ui/eslint.config.js
@src/diff_api.rs

<interfaces>
<!-- Contracts the executor MUST reproduce verbatim. JSON is snake_case end-to-end -->
<!-- because src/diff_api.rs::FileDiff uses default serde (no rename_all). -->

Rust FileDiff (src/diff_api.rs, already exists — DO NOT modify the struct):
```rust
pub struct FileDiff {
    pub filename: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub previous_filename: Option<String>,
    pub status: String, // "added" | "removed" | "modified" | "renamed" | "copied"
    pub additions: u32,
    pub deletions: u32,
    pub changes: u32,
    pub patch: String, // raw unified diff text, or "[binary file]"
}
```

TypeScript FileDiff (NEW — ui/src/code-review/types.ts):
```typescript
export interface FileDiff {
  filename: string
  previous_filename?: string   // snake_case matches Rust JSON output
  status: 'added' | 'removed' | 'modified' | 'renamed' | 'copied'
  additions: number
  deletions: number
  changes: number
  patch: string                // raw unified diff text, or "[binary file]"
}
```

useDiff public surface (NEW — ui/src/code-review/hooks/useDiff.ts):
```typescript
export interface UseDiffResult {
  files: FileDiff[]
  loading: boolean
  error: string | null
  refetch: (contextLines?: number) => void
}
export function useDiff(): UseDiffResult

// Test-only injection seam — tests call this directly, no renderer needed:
export type DoFetch = (url: string) => Promise<Response>
export interface FetchDiffResult { files: FileDiff[]; error: string | null }
export function fetchDiffOnce(doFetch: DoFetch, contextLines?: number): Promise<FetchDiffResult>
```

Rust DiffContextQuery (NEW — src/diff_api.rs):
```rust
#[derive(serde::Deserialize)]
pub struct DiffContextQuery {
    pub context: Option<u32>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ?context=N query param to both diff endpoints in src/diff_api.rs (D-05)</name>
  <files>src/diff_api.rs</files>
  <read_first>
    - src/diff_api.rs (current full contents — about to modify get_diff_branch, get_diff_commit, try_branch_diff signatures and add DiffContextQuery)
    - .planning/phases/25-diff-viewer-ui/25-RESEARCH.md "Pattern 5: Rust `?context` Query Parameter" and "Integration Points Detail" sections
    - .planning/phases/25-diff-viewer-ui/25-PATTERNS.md "src/diff_api.rs" section (lines ~427-477)
    - .planning/phases/25-diff-viewer-ui/25-CONTEXT.md D-05 and D-06 (context default = 3, expand all = 999)
    - ~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/git2-0.20.4/src/diff.rs line 871 (verify DiffOptions::context_lines(u32) signature)
  </read_first>
  <behavior>
    - Test: get_diff_branch with no ?context returns the SAME FileDiff[] as today (default 3 context lines) — existing tests still pass
    - Test: get_diff_branch?context=999 returns a FileDiff[] whose patch field contains a measurably larger context window than ?context=0 for a multi-line file modification
    - Test: get_diff_branch?context=abc returns HTTP 400 with a JSON error body (axum's Query extractor rejects malformed query)
    - Test: get_diff_commit/:sha?context=N applies the same context override (verified by counting context lines in the returned patch for a commit that modifies a known file)
    - Test: Both endpoints accept ?context=0 (no context) and return parseable JSON
  </behavior>
  <action>
    Modify `src/diff_api.rs` only. Three modifications, in order:

    (1) Add the import `use axum::extract::Query;` next to the existing `use axum::extract::{Path, State};` (consolidate into one `use axum::extract::{Path, Query, State};` line). Add `use serde::Deserialize;` next to the existing `use serde::Serialize;` (consolidate into `use serde::{Deserialize, Serialize};`).

    (2) Add the query struct just below the existing `pub struct CodeReviewState` block (around line 39):
    ```
    #[derive(Deserialize)]
    pub struct DiffContextQuery {
        pub context: Option<u32>,
    }
    ```
    Default for absent param MUST be 3 (git2's default) per D-06.

    (3) Modify `get_diff_branch` signature from `(State(state): State<Arc<CodeReviewState>>)` to `(State(state): State<Arc<CodeReviewState>>, Query(params): Query<DiffContextQuery>)`. axum extractor ordering MUST be `State` first, then `Query` (axum convention). The body becomes:
    ```
    let context_lines = params.context.unwrap_or(3);
    Json(try_branch_diff(&state.repo_path, context_lines).unwrap_or_default())
    ```

    (4) Modify `try_branch_diff` signature from `(repo_path: &std::path::Path) -> Option<Vec<FileDiff>>` to `(repo_path: &std::path::Path, context_lines: u32) -> Option<Vec<FileDiff>>`. Inside the function, after `opts.old_prefix("a/").new_prefix("b/");` add `opts.context_lines(context_lines);` (verified against git2 0.20.4 src/diff.rs line 871: `pub fn context_lines(&mut self, lines: u32) -> &mut DiffOptions`).

    (5) Modify `get_diff_commit` the same way: add `Query(params): Query<DiffContextQuery>` to the signature (after `State` and BEFORE `Path` — but axum requires `State` first, `Path` last; insert `Query` between them: `State(...), Query(params): Query<DiffContextQuery>, Path(sha): Path<String>`). Inside the body, just after the existing `let mut opts = git2::DiffOptions::new();` and `opts.old_prefix("a/").new_prefix("b/");` lines (around current line 327-328), add `let context_lines = params.context.unwrap_or(3); opts.context_lines(context_lines);`.

    (6) Update the existing in-file tests to compile: the test helpers call `do_get(state, "/api/diff/branch")` and that still works (no ?context = default 3). One existing test (`get_diff_branch_returns_empty_array_when_no_base_resolves`) directly calls `try_branch_diff` indirectly via the route — no signature change visible to tests since the handler reads the query param. The only NEW test added in this task is described in Task 2 below; this task is the implementation.

    (7) Run `cargo fmt && cargo clippy -- -D warnings && cargo test --lib diff_api` (per CLAUDE.md Code Quality requirement). All existing diff_api tests MUST still pass. The new behavior tests come in Task 2.

    Do NOT touch `src/plan_review.rs`, `src/server.rs`, `src/main.rs`, or `ui/` in this task. ARCH-01 removals land in Plan 25-03.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -- -D warnings && cargo test --lib diff_api 2>&amp;1 | tail -3 && grep -q 'pub struct DiffContextQuery' src/diff_api.rs && grep -q 'Query(params): Query&lt;DiffContextQuery&gt;' src/diff_api.rs && grep -q 'opts.context_lines(context_lines)' src/diff_api.rs && grep -q 'fn try_branch_diff(repo_path: &amp;std::path::Path, context_lines: u32)' src/diff_api.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo fmt --check` exits 0
    - `cargo clippy -- -D warnings` exits 0
    - `cargo test --lib diff_api` shows the same pre-existing 7 tests still pass with 0 failures
    - `src/diff_api.rs` defines `pub struct DiffContextQuery` with field `pub context: Option<u32>`
    - `src/diff_api.rs` `get_diff_branch` signature contains `State(state): State<Arc<CodeReviewState>>, Query(params): Query<DiffContextQuery>`
    - `src/diff_api.rs` `get_diff_commit` signature contains all three extractors `State`, `Query`, `Path` in that left-to-right order
    - `src/diff_api.rs` calls `opts.context_lines(context_lines)` at least twice (once in try_branch_diff, once in get_diff_commit body)
    - `src/diff_api.rs` `try_branch_diff` signature is `fn try_branch_diff(repo_path: &std::path::Path, context_lines: u32) -> Option<Vec<FileDiff>>`
    - The default fallback `params.context.unwrap_or(3)` literal `3` appears in both handlers
    - No new external crate added; serde + axum + git2 already in Cargo.toml
  </acceptance_criteria>
  <done>
    Rust backend accepts `?context=N` on both `/api/diff/branch` and `/api/diff/commit/:sha`; default remains 3; existing tests pass; clippy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Rust integration tests for ?context behavior on both diff endpoints</name>
  <files>src/diff_api.rs</files>
  <read_first>
    - src/diff_api.rs after Task 1 (so the tests can see DiffContextQuery and the updated signatures)
    - src/diff_api.rs lines 358-682 (existing `#[cfg(test)] mod tests` block — copy fixture helpers and `do_get` patterns)
    - .planning/phases/25-diff-viewer-ui/25-RESEARCH.md "Common Pitfalls" — none specific to context_lines testing, but verify against `make_repo_with_main_and_feature` fixture
  </read_first>
  <behavior>
    - Test: A feature branch with a single file containing 10 lines of context around a modification — fetching `/api/diff/branch?context=0` returns a patch with NO context lines (only +/- lines), while `/api/diff/branch?context=999` returns a patch containing ALL surrounding lines
    - Test: `/api/diff/branch?context=abc` returns HTTP 400 (axum Query rejects non-u32)
    - Test: `/api/diff/branch` with no query param behaves identically to `/api/diff/branch?context=3` (default = 3)
    - Test: `/api/diff/commit/:sha?context=999` accepts the param and returns the SAME shape as without (just more context)
  </behavior>
  <action>
    Append four new `#[tokio::test]` functions to the existing `mod tests` block in `src/diff_api.rs`. Reuse the `make_repo_with_main_and_feature` and `do_get` helpers already defined.

    Test names and behaviors:

    (1) `get_diff_branch_context_param_changes_patch_size`: Create a repo where main has a file with 10 lines of stable content and the feature branch modifies one middle line. Fetch `/api/diff/branch?context=0` and `/api/diff/branch?context=999`. Assert the second response's `patch` field is strictly longer in byte count AND contains at least one line that does NOT appear (literally) in the first response. The patch is in `arr[0]["patch"]` (string).

    (2) `get_diff_branch_invalid_context_returns_400`: Set up `make_repo_with_main_and_feature(&[("x.txt", "a\nb\n")])`, fetch `/api/diff/branch?context=abc`, assert status is `StatusCode::BAD_REQUEST` (axum Query extractor returns 400 on parse failure). Do NOT assert on body content (axum's default body is text/plain).

    (3) `get_diff_branch_default_matches_context_3`: Set up the same fixture, fetch both `/api/diff/branch` (no param) and `/api/diff/branch?context=3`. Assert the patch fields are byte-identical (proves default = 3).

    (4) `get_diff_commit_accepts_context_param`: Set up `make_repo_with_main_and_feature(&[("multi.txt", &"line\n".repeat(10) ), ("multi.txt", &("line\n".repeat(5) + "MOD\n" + &"line\n".repeat(4)))])` (or any two commits where the second modifies a multi-line file). Fetch `/api/diff/commit/{second_oid}?context=999` and assert HTTP 200 with a JSON array length 1.

    All tests use the same fixture-helper-and-do_get pattern as the existing tests; do NOT introduce new helpers. Run `cargo test --lib diff_api` — all 7 existing + 4 new = 11 tests MUST pass.

    Run `cargo fmt && cargo clippy -- -D warnings` after.

    Do NOT modify any non-test code in this task. Task 1 already shipped the implementation.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -- -D warnings && cargo test --lib diff_api 2>&amp;1 | tail -3 && grep -c '#\[tokio::test\]' src/diff_api.rs | grep -q '11' && grep -q 'fn get_diff_branch_context_param_changes_patch_size' src/diff_api.rs && grep -q 'fn get_diff_branch_invalid_context_returns_400' src/diff_api.rs && grep -q 'fn get_diff_branch_default_matches_context_3' src/diff_api.rs && grep -q 'fn get_diff_commit_accepts_context_param' src/diff_api.rs</automated>
  </verify>
  <acceptance_criteria>
    - Four new `#[tokio::test]` functions named exactly as in the action are present in `src/diff_api.rs`
    - `cargo test --lib diff_api` reports 11 tests passing with 0 failures (the 7 pre-existing + 4 new)
    - `cargo fmt --check` and `cargo clippy -- -D warnings` exit 0
    - The `get_diff_branch_invalid_context_returns_400` test asserts `status == StatusCode::BAD_REQUEST`
    - The `get_diff_branch_context_param_changes_patch_size` test asserts the `?context=999` patch is strictly longer (by `.len()`) than the `?context=0` patch
    - The `get_diff_branch_default_matches_context_3` test asserts byte equality between no-param and `?context=3` responses
  </acceptance_criteria>
  <done>
    Backend ?context behavior is locked down by tests; default-=-3 semantics, 400 on invalid input, and Expand-All-=-context=999 round-trip are all verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create FileDiff type, useDiff hook with fetchDiffOnce, and Vitest tests (TDD)</name>
  <files>ui/src/code-review/types.ts, ui/src/code-review/hooks/useDiff.ts, ui/src/code-review/hooks/useDiff.test.ts</files>
  <read_first>
    - ui/src/reviewer-v2/useHeartbeat.ts (full file — copy the dependency-injection idiom: export a pure tick function that tests exercise without a renderer)
    - ui/src/reviewer-v2/useHeartbeat.test.ts (full file — copy the harness/fake-doFetch pattern)
    - ui/src/reviewer-v2/types.ts (one-file pattern: named exports only, no default, no barrel)
    - ui/src/reviewer-v2/hooks/useSectionAnnotationCounts.ts (existing hook in a hooks/ subdirectory — confirms the location pattern)
    - .planning/phases/25-diff-viewer-ui/25-PATTERNS.md "ui/src/code-review/types.ts" and "ui/src/code-review/hooks/useDiff.ts" sections
    - .planning/phases/25-diff-viewer-ui/25-RESEARCH.md "Pattern 2: useDiff Hook Structure" and Assumption A4
    - ui/vitest.setup.ts (jsdom mocks already cover IntersectionObserver — useDiff does not need any new mocks)
  </read_first>
  <behavior>
    - Test: fetchDiffOnce(doFetch) where doFetch returns 200 with `[{filename:'a',status:'added',additions:1,deletions:0,changes:1,patch:'+x',previous_filename:undefined}]` resolves to `{ files: [that one entry], error: null }`
    - Test: fetchDiffOnce(doFetch) where doFetch returns `new Response(null, { status: 500 })` resolves to `{ files: [], error: 'fetch failed' }` (or equivalent non-null error string)
    - Test: fetchDiffOnce(doFetch) where doFetch throws (network error) resolves to `{ files: [], error: 'network error' }` — never throws
    - Test: fetchDiffOnce(doFetch, 999) calls doFetch with a URL containing `?context=999`
    - Test: fetchDiffOnce(doFetch, undefined) calls doFetch with a URL that does NOT contain `?context=` substring
    - Test: fetchDiffOnce(doFetch, 0) calls doFetch with `?context=0` in the URL (zero is a real value, not absent)
    - Test: The Response.json() result is asserted to satisfy the FileDiff[] shape (presence of `filename`, `status`, `additions`, `deletions`, `changes`, `patch`)
  </behavior>
  <action>
    Three NEW files. NO modifications to existing files.

    File A — `ui/src/code-review/types.ts`:
    Export exactly one interface, `FileDiff`, with fields `filename: string`, `previous_filename?: string`, `status: 'added' | 'removed' | 'modified' | 'renamed' | 'copied'`, `additions: number`, `deletions: number`, `changes: number`, `patch: string`. Named export only, no default. No imports. Use `previous_filename` (snake_case) to match the serde JSON output of `src/diff_api.rs::FileDiff` — verified: that struct has no `#[serde(rename_all)]` attribute, so field names are emitted snake_case verbatim.

    File B — `ui/src/code-review/hooks/useDiff.ts`:
    Imports: `import { useEffect, useRef, useState, useCallback } from 'react'` and `import type { FileDiff } from '../types'`.

    Export `export type DoFetch = (url: string) => Promise<Response>`.

    Export `export interface FetchDiffResult { files: FileDiff[]; error: string | null }`.

    Export `export async function fetchDiffOnce(doFetch: DoFetch, contextLines?: number): Promise<FetchDiffResult>`:
    - Build the URL: `const url = contextLines !== undefined ? `/api/diff/branch?context=${contextLines}` : '/api/diff/branch'`. Use `!== undefined` strict check so `contextLines=0` still includes the param (per behavior test above).
    - Call `doFetch(url)` inside try/catch.
    - On success path: if `!res.ok` return `{ files: [], error: 'fetch failed' }`; else parse `await res.json() as FileDiff[]` and return `{ files: data, error: null }`.
    - On catch: return `{ files: [], error: 'network error' }`. Never re-throw.

    Export `export interface UseDiffResult { files: FileDiff[]; loading: boolean; error: string | null; refetch: (contextLines?: number) => void }`.

    Export `export function useDiff(): UseDiffResult`:
    - `const [files, setFiles] = useState<FileDiff[]>([])`
    - `const [loading, setLoading] = useState(true)`
    - `const [error, setError] = useState<string | null>(null)`
    - A `cancelledRef = useRef(false)` to guard setState after unmount.
    - `const refetch = useCallback((contextLines?: number) => { ... }, [])` that sets `setLoading(true)` then calls `fetchDiffOnce(globalThis.fetch.bind(globalThis), contextLines)`, then on resolution (and only if `!cancelledRef.current`) calls `setFiles(result.files); setError(result.error); setLoading(false)`.
    - `useEffect(() => { cancelledRef.current = false; refetch(); return () => { cancelledRef.current = true } }, [refetch])` — fires once on mount, guards against state updates after unmount (avoids React 19 strict-mode double-invoke warnings).
    - Return `{ files, loading, error, refetch }`.

    File C — `ui/src/code-review/hooks/useDiff.test.ts`:
    Imports: `import { describe, it, expect } from 'vitest'` and `import { fetchDiffOnce } from './useDiff'` and `import type { FileDiff } from '../types'`.

    Write exactly the 6 tests enumerated in `<behavior>`. Each test constructs a fake `doFetch` and calls `await fetchDiffOnce(fakeFetch, contextLines?)` then asserts on the result. The "calls doFetch with URL containing ..." tests record the URL the fake `doFetch` was called with (via a closure variable) and assert on it.

    Do NOT use `@testing-library/react` — it is not a dependency and the project explicitly rejects it.

    Verify: `npm test --prefix ui -- useDiff` (or `npm test --prefix ui --run useDiff` for vitest's run mode) passes all 6 new tests. The project script `"test": "vitest run"` is already wired.

    `npm run lint --prefix ui` MUST still pass — the ESLint rule from Task 4 (next task) is not yet in place, so plain `src/code-review/**` imports won't be checked; that's expected. The rule's wiring happens in Task 4.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm test -- --run useDiff 2>&amp;1 | tail -5 &amp;&amp; test -f src/code-review/types.ts &amp;&amp; test -f src/code-review/hooks/useDiff.ts &amp;&amp; test -f src/code-review/hooks/useDiff.test.ts &amp;&amp; grep -q 'export interface FileDiff' src/code-review/types.ts &amp;&amp; grep -q 'export function useDiff' src/code-review/hooks/useDiff.ts &amp;&amp; grep -q 'export async function fetchDiffOnce' src/code-review/hooks/useDiff.ts &amp;&amp; grep -q "fetchDiffOnce" src/code-review/hooks/useDiff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `ui/src/code-review/types.ts` exists and contains the FileDiff interface with all 7 fields named: filename, previous_filename, status, additions, deletions, changes, patch
    - `previous_filename` is optional (`?:`); no other field is optional
    - `status` is a string union literal `'added' | 'removed' | 'modified' | 'renamed' | 'copied'`
    - File `ui/src/code-review/hooks/useDiff.ts` exists and exports `useDiff`, `fetchDiffOnce`, `DoFetch`, `FetchDiffResult`, `UseDiffResult`
    - `fetchDiffOnce` uses `!== undefined` (not `!=` or truthy check) for contextLines so `0` is treated as a real value
    - `fetchDiffOnce` returns the literal error strings `'fetch failed'` (on non-ok response) and `'network error'` (on caught exception)
    - File `ui/src/code-review/hooks/useDiff.test.ts` exists and contains 6 `it(...)` blocks (one per behavior bullet)
    - `npm test --prefix ui -- --run useDiff` exits 0 and reports 6 passing tests under the useDiff suite
    - `npm test --prefix ui` (entire suite) reports 0 failures
    - No `@testing-library/react` import appears in any of the three new files
    - No import from `../reviewer-v2/` appears in any of the three new files
  </acceptance_criteria>
  <done>
    Types and hook are in place with full TDD coverage; the data path is testable without a renderer; downstream plans (25-02, 25-03) can consume `useDiff` and `FileDiff` immediately.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add ESLint isolation rule for src/code-review/** blocking reviewer-v2/ imports</name>
  <files>ui/eslint.config.js</files>
  <read_first>
    - ui/eslint.config.js (current full contents — about to append a new flat-config object)
    - .planning/phases/25-diff-viewer-ui/25-PATTERNS.md "ui/eslint.config.js" section showing the exact rule shape to mirror
    - .planning/phases/25-diff-viewer-ui/25-RESEARCH.md Pitfall 7 (the rule direction must be code-review/ → cannot import reviewer-v2/)
  </read_first>
  <action>
    Modify `ui/eslint.config.js` only. Append a new flat-config object to the array exported by `defineConfig([...])`, immediately after the existing `src/reviewer-v2/**` block (around line 40). The new block:

    ```
    {
      files: ['src/code-review/**'],
      rules: {
        'no-restricted-imports': [
          'error',
          {
            patterns: [
              {
                group: ['../reviewer-v2/**', '*/reviewer-v2/**'],
                message:
                  'code-review/ files must not import from reviewer-v2/. ' +
                  'Copy the utility into code-review/ or extract to shared/.',
              },
            ],
          },
        ],
      },
    },
    ```

    Notes:
    - Use ONLY the `reviewer-v2/` block (not the broader `../**` block). The existing reviewer-v2 rule blocks all `../**`; the code-review rule deliberately blocks reviewer-v2 specifically. The reason: useDiff.ts in `code-review/hooks/` legitimately imports from `../types` (parent dir within code-review/). A blanket `../**` ban would break the hook's `import type { FileDiff } from '../types'` line.
    - The message string mirrors the reviewer-v2 rule's tone.

    Run `npm run lint --prefix ui` — MUST exit 0. The hook file from Task 3 imports `../types` which is OK (still inside `code-review/`), and the test file does not import from reviewer-v2/.

    Then run a regression check: temporarily create `ui/src/code-review/__sentinel.ts` with content `export const x = 1` and `import { useHeartbeat } from '../reviewer-v2/useHeartbeat'` — run `npm run lint --prefix ui`, EXPECT it to FAIL with the no-restricted-imports error. Delete `__sentinel.ts`. (The sentinel check is for the executor's own verification; do NOT leave it committed.)
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run lint 2>&amp;1 | tail -3 &amp;&amp; grep -q "files: \['src/code-review/\*\*'\]" eslint.config.js &amp;&amp; grep -q "'\\*/reviewer-v2/\\*\\*'" eslint.config.js &amp;&amp; ! test -f src/code-review/__sentinel.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ui/eslint.config.js` contains a flat-config object whose `files` field is `['src/code-review/**']`
    - That same object contains `no-restricted-imports` with a `patterns` array referencing both `'../reviewer-v2/**'` and `'*/reviewer-v2/**'`
    - `npm run lint --prefix ui` exits 0 (no violations against the new rule from any current file)
    - Sentinel file `ui/src/code-review/__sentinel.ts` does NOT exist (executor used it for the regression check only and removed it)
    - The existing `src/reviewer-v2/**` rule is unchanged (verified by diffing against the pre-task contents)
  </acceptance_criteria>
  <done>
    ESLint now blocks future violations of the `code-review/ → reviewer-v2/` direction; verified by a temporary sentinel file that failed lint as expected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Rust HTTP server | The frontend fetches `/api/diff/branch?context=N` and `/api/diff/commit/:sha?context=N`. The `?context=N` value is untrusted user input crossing this boundary. |
| Rust server→git2 | The handler reads `context_lines: u32` and passes it directly to `git2::DiffOptions::context_lines(u32)`. No filesystem path or git ref is influenced by the query param. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-CDOS | Denial of service | `?context=N` very large value causes git2 to build a massive in-memory diff buffer | mitigate | The `Option<u32>` extractor caps the type at u32::MAX, but the practical risk is `?context=999` (per D-05 spec) on a large monorepo. Mitigation: keep `Option<u32>` (no smaller cap) but rely on (a) the listener binding to 127.0.0.1 only (existing `start_server`), and (b) the single-user local model. No remote attacker exists. The test `get_diff_branch_context_param_changes_patch_size` exercises the `?context=999` path and confirms it completes. Documented in 25-RESEARCH.md Pitfall 6. |
| T-25-CINV | Tampering | `?context=N` invalid input (`?context=abc`, `?context=-5`, `?context=4294967296`) | mitigate | axum's `Query<DiffContextQuery>` extractor returns HTTP 400 on parse failure for `Option<u32>` — non-hex / negative / overflow inputs ALL fail to deserialize. Asserted by test `get_diff_branch_invalid_context_returns_400` in Task 2. No libgit2 error string is forwarded; the client gets axum's default rejection. |
| T-25-LINK | Tampering / supply-chain | npm/cargo installs | accept | Zero new packages introduced. `serde`, `axum`, `git2` already in Cargo.toml; `react`, `vitest` already in package.json. No `[ASSUMED]`/`[SUS]` packages. Verified against 25-RESEARCH.md "Package Legitimacy Audit" — empty. |
| T-25-FRG | Tampering | ESLint isolation rule could be bypassed | mitigate | The new rule blocks `code-review/ → reviewer-v2/` imports at lint time; Task 4 verifies via a temporary sentinel file that violates the rule (must fail lint), then removes it. `npm run lint` is part of every CI run for this project. |
| T-25-SC | Tampering | cargo install of new crates | accept | No new cargo dependencies in this plan. Package Legitimacy Gate not triggered. |

T-25-CDOS is accepted at the architecture level (D-05 explicitly designed `?context=999` as a 2-state toggle for a single-user local tool); the test in Task 2 verifies the path works end-to-end. T-25-CINV is fully mitigated by axum's typed extractor.
</threat_model>

<verification>
End-to-end verification at the close of this plan:

1. `cargo fmt --check && cargo clippy -- -D warnings && cargo build && cargo test --lib` ALL exit 0
2. `cd ui && npm run lint && npm test && npm run build` ALL exit 0
3. Manual smoke (developer): `curl 'http://127.0.0.1:{port}/api/diff/branch?context=999' | jq 'length'` on a feature branch returns a positive integer; same URL with `?context=0` returns the same length (same number of files) but each `.patch` string is shorter
4. Manual smoke: `curl -i 'http://127.0.0.1:{port}/api/diff/branch?context=abc'` returns `HTTP/1.1 400 Bad Request`
</verification>

<success_criteria>
- The Rust backend accepts `?context=N` (Option<u32>) on both `/api/diff/branch` and `/api/diff/commit/:sha`, defaulting to 3 when absent, returning 400 when malformed.
- 4 new Rust tests exercise the context behavior (sizes change, default = 3, invalid returns 400, commit endpoint accepts the param).
- The TypeScript foundation (`FileDiff` type + `useDiff` hook + `fetchDiffOnce` pure function) is in place and fully tested with 6 Vitest cases.
- ESLint enforces the `code-review/ → cannot import reviewer-v2/` rule.
- `npm test`, `npm run lint`, `cargo test`, `cargo clippy -- -D warnings` ALL pass with 0 failures and 0 warnings.
- No new dependencies (npm or cargo) introduced.
</success_criteria>

<output>
Create `.planning/phases/25-diff-viewer-ui/25-01-SUMMARY.md` describing the artifacts created, the test counts (4 Rust + 6 TypeScript), the FileDiff type contract that 25-02 will consume, and the `?context` URL pattern.
</output>
