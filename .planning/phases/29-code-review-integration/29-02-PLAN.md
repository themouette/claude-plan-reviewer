---
phase: 29-code-review-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - src/integrations/claude.rs
  - tests/integration/install_uninstall.rs
autonomous: true
requirements:
  - INTEG-01
  - INTEG-02
  - INTEG-03
tags:
  - rust
  - claude-code
  - plugin
  - install
  - hooks

must_haves:
  truths:
    - "Running `plan-reviewer install claude` creates `commands/code-review.md` in the Claude plugin directory"
    - "Running `plan-reviewer install claude` writes a `PreToolUse` entry to `hooks/hooks.json` that targets the `plan-reviewer pre-pr-hook` subcommand from Plan 29-01"
    - "The existing `PermissionRequest` (`ExitPlanMode`) hook entry remains intact after the new `PreToolUse` entry is added (Pitfall 5)"
    - "Running `plan-reviewer uninstall claude` removes `commands/code-review.md`, the `PreToolUse` entry, and the slash command (the existing `remove_dir_all(&plugin_dir)` covers this)"
    - "Re-running `plan-reviewer install claude` twice does not duplicate hook entries (idempotency)"
    - "`/code-review` (resolved as `/plan-reviewer:code-review` in Claude Code's plugin namespace) appears in the slash command menu after install"
  artifacts:
    - path: "src/integrations/claude.rs"
      provides: "Extended install() that writes commands/code-review.md + PreToolUse entry in hooks.json"
      contains: "code-review.md"
    - path: "tests/integration/install_uninstall.rs"
      provides: "New integration tests covering code-review.md and PreToolUse install/uninstall"
      contains: "install_claude_creates_commands_code_review_md"
  key_links:
    - from: "src/integrations/claude.rs install()"
      to: "~/.local/share/plan-reviewer/claude-plugin/commands/code-review.md"
      via: "std::fs::write after annotate.md write block"
      pattern: "commands/code-review.md"
    - from: "src/integrations/claude.rs hooks_json literal"
      to: "plan-reviewer pre-pr-hook"
      via: "PreToolUse array with matcher 'Bash' and timeout 600000"
      pattern: "plan-reviewer pre-pr-hook"
    - from: "src/integrations/claude.rs uninstall()"
      to: "all newly written files"
      via: "remove_dir_all(&plugin_dir) — unchanged"
      pattern: "remove_dir_all"
---

<objective>
Extend the existing `ClaudeIntegration::install()` in `src/integrations/claude.rs` to write a `commands/code-review.md` slash command file and add a `PreToolUse` array to `hooks/hooks.json` that invokes `plan-reviewer pre-pr-hook` (the subcommand from Plan 29-01). Confirm the existing `PermissionRequest` array (the `ExitPlanMode` → `plan-reviewer review-hook` entry) survives the change. Confirm uninstall continues to cover the new files via the existing `remove_dir_all(&plugin_dir)` path. Add unit tests in `claude.rs` and integration tests in `tests/integration/install_uninstall.rs` mirroring the existing `annotate.md` tests.

Purpose: Satisfy INTEG-01 (slash command file written by install), INTEG-02 (pre-PR hook entry written by install), and INTEG-03 (uninstall removes both, install/uninstall stay idempotent). This is the final wiring layer that exposes the Plan 29-01 subcommands to Claude Code users.

Output:
- `commands/code-review.md` written by install
- `hooks/hooks.json` contains both `PermissionRequest` and `PreToolUse` arrays
- Unit tests in `claude.rs` covering both new artifacts
- Integration tests in `install_uninstall.rs` mirroring the `annotate.md` pattern
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/29-code-review-integration/29-RESEARCH.md
@.planning/phases/29-code-review-integration/29-PATTERNS.md
@.planning/phases/29-code-review-integration/29-01-PLAN.md
@.planning/phases/29-code-review-integration/29-01-SUMMARY.md

@src/integrations/claude.rs
@tests/integration/install_uninstall.rs

<interfaces>
<!-- Extracted from src/integrations/claude.rs and tests/integration/install_uninstall.rs -->

From src/integrations/claude.rs (existing):
```rust
impl Integration for ClaudeIntegration {
    fn install(&self, ctx: &InstallContext) -> Result<(), String>;       // ~lines 39-365
    fn uninstall(&self, ctx: &InstallContext) -> Result<(), String>;     // ~lines 368-461
    fn is_installed(&self, ctx: &InstallContext) -> bool;
}

// hooks.json write block (lines 111-137) — current contents:
let hooks_json = serde_json::json!({
    "hooks": {
        "PermissionRequest": [
            { "matcher": "ExitPlanMode",
              "hooks": [{"type": "command", "command": "plan-reviewer review-hook"}] }
        ]
    }
});

// commands directory creation + annotate.md write (lines 139-234) — already creates commands_dir.
```

From tests/integration/install_uninstall.rs (existing patterns):
```rust
// install_claude_creates_commands_annotate_md  (lines ~135-174) — direct template.
// install_claude_is_idempotent                (lines ~83-...) — verifies second install doesn't duplicate.
// uninstall_claude_removes_commands_directory (lines ~425-458) — already removes everything via remove_dir_all.
```

From Plan 29-01 (must be complete before this plan runs):
```
- plan-reviewer code-review       — opens browser at /code-review
- plan-reviewer pre-pr-hook       — PreToolUse hook target (filters tool_input.command)
```
</interfaces>

<key_implementation_notes>
1. **`hooks.json` must contain BOTH arrays** (RESEARCH.md Pitfall 5) — the new write must include the existing `PermissionRequest` array AND the new `PreToolUse` array in a single `serde_json::json!` literal. Do NOT read the existing file and patch it — the existing code unconditionally writes a fresh literal; extend the literal.

2. **`PreToolUse` matcher is `"Bash"` (broad)** — filtering happens inside `plan-reviewer pre-pr-hook` (Plan 29-01). Do NOT use the unstable `if` field (RESEARCH.md Open Question 1).

3. **`timeout: 600000`** — 10-minute hook timeout, matching the 540-second server watchdog in `async_main` with 60 seconds of headroom. Claude Code's `PreToolUse` hook timeout is per-hook in milliseconds.

4. **`code-review.md` namespacing (Pitfall 3)** — heading MUST be `# /plan-reviewer:code-review` (with plugin namespace), not `# /code-review`. Asserted in unit test.

5. **`allowed-tools: Bash` (not `Bash(plan-reviewer*)`)** — matches the `annotate.md` pattern. The slash command uses `run_in_background: true` which requires the full Bash tool.

6. **No new dependencies** — confirmed in RESEARCH.md Package Legitimacy Audit.

7. **Idempotency is automatic** — file writes in `install()` are unconditional (always overwrite). The idempotency check only guards settings.json mutations. Existing `install_claude_is_idempotent` test already verifies this; we add an analog for `code-review.md`.

8. **Uninstall is automatic** — `remove_dir_all(&plugin_dir)` at the top of `uninstall()` removes EVERYTHING in the plugin directory including the new file. No uninstall code change required; we add a test asserting the new file is gone after uninstall.
</key_implementation_notes>

<code_review_md_prompt_outline>
The full content of `commands/code-review.md` (verbatim, to be embedded as a `concat!()` literal):

- Frontmatter:
  - `description: Open the code review UI for the current git branch`
  - `allowed-tools: Bash`
- Heading: `# /plan-reviewer:code-review`
- Opening paragraph: "Open the diff viewer for the current git branch in the plan-reviewer browser UI. Use this before creating a PR to review and annotate the changes."
- Step 1 — Launch the review:
  - Instruct Claude to invoke `plan-reviewer code-review` via the Bash tool with `run_in_background: true`
  - Note: "The process opens a local browser tab at `/code-review` showing the current branch diff. It exits when the review is submitted in the browser or when the 540-second server timeout fires."
- Step 2 — Handle the result:
  - "If stdout contains JSON `{\"behavior\":\"allow\"}`: say 'Review complete, proceeding with PR creation.'"
  - "If stdout contains JSON `{\"behavior\":\"deny\",\"message\":\"<feedback>\"}`: say 'Review feedback received: <feedback>'. Treat the message as revision instructions and propose how to address it BEFORE creating the PR."
  - "If no output (empty stdout): say 'The code review process exited without a result.' Ask the user if they want to proceed with PR creation."

The `concat!()` literal must use `\\n` line breaks and `\\\"` escapes for embedded JSON examples. Total length ≈ 70-100 lines of markdown.
</code_review_md_prompt_outline>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend install() — write commands/code-review.md + add PreToolUse to hooks.json</name>
  <files>src/integrations/claude.rs</files>
  <read_first>
    - src/integrations/claude.rs (lines 39-365 for the install() function; lines 111-137 hooks.json write; lines 139-234 annotate.md write; lines 666-693 existing hooks.json unit test; lines 876-971 existing annotate.md unit test; lines 974-1010 install_creates_annotate_md_even_when_already_installed)
    - .planning/phases/29-code-review-integration/29-RESEARCH.md (Pattern 2, Pattern 3, Pattern 4; Pitfalls 3 and 5)
    - .planning/phases/29-code-review-integration/29-PATTERNS.md (claude.rs analog section lines 143-311)
    - .planning/phases/29-code-review-integration/29-01-SUMMARY.md (confirm final subcommand name is `pre-pr-hook`)
  </read_first>
  <behavior>
    - Test 1 (`install_creates_code_review_md_with_expected_content`): after `install()` succeeds in a temp HOME, `<home>/.local/share/plan-reviewer/claude-plugin/commands/code-review.md` exists. Its content contains all of:
      - `# /plan-reviewer:code-review` (plugin-namespaced heading)
      - `description: Open the code review UI for the current git branch`
      - `allowed-tools: Bash`
      - `plan-reviewer code-review` (the bash command to invoke)
      - `run_in_background` (instruction to Claude)
      - `{\"behavior\":\"allow\"}` AND `{\"behavior\":\"deny\"` (decision handling examples)
    - Test 2 (`install_creates_hooks_json_with_pre_tool_use`): after `install()`, `hooks/hooks.json` parses as JSON with `hooks.PreToolUse` as a non-empty array. The first entry has:
      - `matcher == "Bash"`
      - `hooks[0].type == "command"`
      - `hooks[0].command == "plan-reviewer pre-pr-hook"`
      - `hooks[0].timeout == 600000`
    - Test 3 (`install_hooks_json_preserves_exit_plan_mode_after_pre_tool_use_added`): the SAME hooks.json from Test 2 ALSO contains the existing `PermissionRequest` array with `matcher == "ExitPlanMode"` and command `plan-reviewer review-hook` — i.e. Pitfall 5 does not regress.
    - Test 4 (`install_creates_code_review_md_even_when_already_installed`): a second `install()` call on the same HOME still leaves `commands/code-review.md` present and readable, mirroring the existing `install_creates_annotate_md_even_when_already_installed` (lines 974+).
  </behavior>
  <action>
    Step A — Edit the hooks.json literal at `src/integrations/claude.rs:116-125`. Extend it to include a `PreToolUse` array. The final literal:
      ```text
      let hooks_json = serde_json::json!({
          "hooks": {
              "PermissionRequest": [
                  { "matcher": "ExitPlanMode",
                    "hooks": [{"type": "command", "command": "plan-reviewer review-hook"}] }
              ],
              "PreToolUse": [
                  { "matcher": "Bash",
                    "hooks": [{
                        "type": "command",
                        "command": "plan-reviewer pre-pr-hook",
                        "timeout": 600000
                    }] }
              ]
          }
      });
      ```
      Do NOT change the surrounding error-handling or the `write` call. The serializer (`serde_json::to_string_pretty`) handles the new field automatically.

    Step B — Add a `code-review.md` write block immediately after the existing `annotate.md` write (which ends near line 234 with the `println!("plan-reviewer: annotate command written to {}", annotate_path.display());` call). The new block parallels the annotate block:
      - Build `code_review_content` via `concat!()` with the full markdown prompt content described in `<code_review_md_prompt_outline>` above. Use the same escape rules as the annotate block (each line ends with `\\n`; escape `"` inside heredoc as `\\\"`).
      - Construct `let code_review_path = commands_dir.join("code-review.md");`
      - Write with the same `if let Err(e) = std::fs::write(...) { return Err(format!("cannot write {}: {}", ...)); }` pattern
      - Print success: `println!("plan-reviewer: code-review command written to {}", code_review_path.display());`
      Do NOT recreate the `commands_dir` — it was already created for the annotate.md write.

    Step C — Add the four unit tests from the behavior block to the existing `#[cfg(test)] mod tests` in `src/integrations/claude.rs`. Place them next to `install_creates_annotate_md_with_expected_content` (line 876) and `install_creates_hooks_json_with_exit_plan_mode` (line 666). Reuse the existing test setup helpers (`tempdir()`, `InstallContext { home, binary_path: Some(...) }`, `ClaudeIntegration.install(&ctx).unwrap()`).

    Step D — Run `cargo fmt && cargo clippy -- -D warnings` after the edits.
  </action>
  <verify>
    <automated>cargo test --lib install_creates_code_review_md_with_expected_content install_creates_hooks_json_with_pre_tool_use install_hooks_json_preserves_exit_plan_mode_after_pre_tool_use_added install_creates_code_review_md_even_when_already_installed</automated>
  </verify>
  <acceptance_criteria>
    - All four unit tests in the verify block exit 0
    - `grep -n "commands/code-review.md\|code-review command written to" src/integrations/claude.rs` returns ≥ 2 lines
    - `grep -n 'plan-reviewer pre-pr-hook' src/integrations/claude.rs` returns ≥ 1 line
    - `grep -n '"timeout": 600000\|"timeout":600000\|timeout.*600000' src/integrations/claude.rs` returns ≥ 1 line (Bash matcher hook timeout present)
    - `grep -n '"PreToolUse"' src/integrations/claude.rs` returns ≥ 1 line (in the hooks_json literal AND in unit tests)
    - `grep -n '"PermissionRequest"' src/integrations/claude.rs` returns ≥ 2 lines (still present in literal AND in existing/new tests)
    - `grep -n "# /plan-reviewer:code-review" src/integrations/claude.rs` returns 1 line (the slash command heading in code_review_content)
    - `cargo test --lib` exits 0 (no regressions in existing install/uninstall unit tests, including install_creates_hooks_json_with_exit_plan_mode and install_is_idempotent)
    - `cargo build --bin plan-reviewer 2>&1 | grep -c "^error"` returns `0`
    - `cargo clippy --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>install() writes `commands/code-review.md` and a `PreToolUse` array in `hooks/hooks.json` alongside the existing `PermissionRequest` array. Four unit tests pass. No regressions in existing install/uninstall/idempotency tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — install/uninstall round-trip for code-review.md + PreToolUse</name>
  <files>tests/integration/install_uninstall.rs</files>
  <read_first>
    - tests/integration/install_uninstall.rs (lines 1-76 setup; lines 83-130 install_claude_is_idempotent; lines 135-174 install_claude_creates_commands_annotate_md as direct template; lines 425-458 uninstall_claude_removes_commands_directory)
    - .planning/phases/29-code-review-integration/29-PATTERNS.md (integration test section lines 315-381)
  </read_first>
  <behavior>
    - Test 1 (`install_claude_creates_commands_code_review_md`): run `plan-reviewer install claude` in a tmpdir HOME; assert stdout contains "code-review command written to"; assert `<home>/.local/share/plan-reviewer/claude-plugin/commands/code-review.md` exists; assert its content contains `# /plan-reviewer:code-review`, `allowed-tools: Bash`, `plan-reviewer code-review`, `run_in_background`.
    - Test 2 (`install_claude_writes_pre_tool_use_hook_in_hooks_json`): run `plan-reviewer install claude` in a tmpdir HOME; read `<home>/.local/share/plan-reviewer/claude-plugin/hooks/hooks.json`; parse as JSON; assert `hooks.PreToolUse[0].matcher == "Bash"` AND `hooks.PreToolUse[0].hooks[0].command == "plan-reviewer pre-pr-hook"` AND `hooks.PreToolUse[0].hooks[0].timeout == 600000`. Also assert the legacy `hooks.PermissionRequest[0].matcher == "ExitPlanMode"` survives.
    - Test 3 (`uninstall_claude_removes_code_review_md`): run install then uninstall in the same tmpdir HOME; assert `<home>/.local/share/plan-reviewer/claude-plugin/commands/code-review.md` no longer exists (it's removed because the whole plugin dir is removed); assert the uninstall command exits 0.
  </behavior>
  <action>
    Append the three tests to `tests/integration/install_uninstall.rs`. Use the existing helpers (`tempfile::TempDir`, `Command::cargo_bin("plan-reviewer")`, `assert_cmd` predicates). Follow the file's existing style: `drop(home)` at the end of each test to keep TempDir alive.

    Test 1 template (mirror `install_claude_creates_commands_annotate_md` at lines 135-174):
      ```text
      let home = tempfile::TempDir::new().unwrap();
      Command::cargo_bin("plan-reviewer").unwrap()
          .env("HOME", home.path())
          .args(["install", "claude"])
          .assert()
          .success()
          .stdout(predicate::str::contains("code-review command written to"));
      let cr_path = home.path().join(".local/share/plan-reviewer/claude-plugin/commands/code-review.md");
      assert!(cr_path.exists(), "commands/code-review.md must be created");
      let content = std::fs::read_to_string(&cr_path).unwrap();
      assert!(content.contains("# /plan-reviewer:code-review"));
      assert!(content.contains("allowed-tools: Bash"));
      assert!(content.contains("plan-reviewer code-review"));
      assert!(content.contains("run_in_background"));
      drop(home);
      ```

    Test 2 template (mirror `install_claude_creates_hooks_json_with_exit_plan_mode` in unit tests):
      ```text
      let home = tempfile::TempDir::new().unwrap();
      Command::cargo_bin("plan-reviewer").unwrap()
          .env("HOME", home.path())
          .args(["install", "claude"])
          .assert()
          .success();
      let hooks_json_path = home.path().join(".local/share/plan-reviewer/claude-plugin/hooks/hooks.json");
      let content = std::fs::read_to_string(&hooks_json_path).unwrap();
      let json: serde_json::Value = serde_json::from_str(&content).unwrap();
      let pre_tool_use = json["hooks"]["PreToolUse"].as_array().expect("PreToolUse must exist");
      assert_eq!(pre_tool_use[0]["matcher"].as_str(), Some("Bash"));
      assert_eq!(pre_tool_use[0]["hooks"][0]["command"].as_str(), Some("plan-reviewer pre-pr-hook"));
      assert_eq!(pre_tool_use[0]["hooks"][0]["timeout"].as_i64(), Some(600000));
      let perm_req = json["hooks"]["PermissionRequest"].as_array().unwrap();
      assert_eq!(perm_req[0]["matcher"].as_str(), Some("ExitPlanMode"));
      drop(home);
      ```

    Test 3 template (mirror `uninstall_claude_removes_commands_directory`):
      ```text
      let home = tempfile::TempDir::new().unwrap();
      Command::cargo_bin("plan-reviewer").unwrap().env("HOME", home.path()).args(["install","claude"]).assert().success();
      let cr_path = home.path().join(".local/share/plan-reviewer/claude-plugin/commands/code-review.md");
      assert!(cr_path.exists(), "precondition: code-review.md must exist after install");
      Command::cargo_bin("plan-reviewer").unwrap().env("HOME", home.path()).args(["uninstall","claude"]).assert().success();
      assert!(!cr_path.exists(), "code-review.md must be removed by uninstall");
      drop(home);
      ```

    Run `cargo fmt && cargo clippy --tests -- -D warnings` afterward.
  </action>
  <verify>
    <automated>cargo test --test integration install_claude_creates_commands_code_review_md install_claude_writes_pre_tool_use_hook_in_hooks_json uninstall_claude_removes_code_review_md</automated>
  </verify>
  <acceptance_criteria>
    - All three integration tests exit 0
    - `grep -n "install_claude_creates_commands_code_review_md" tests/integration/install_uninstall.rs` returns 1 line
    - `grep -n "install_claude_writes_pre_tool_use_hook_in_hooks_json" tests/integration/install_uninstall.rs` returns 1 line
    - `grep -n "uninstall_claude_removes_code_review_md" tests/integration/install_uninstall.rs` returns 1 line
    - `grep -n "plan-reviewer pre-pr-hook" tests/integration/install_uninstall.rs` returns ≥ 1 line
    - `grep -n "code-review.md" tests/integration/install_uninstall.rs` returns ≥ 3 lines (each test references the path)
    - `cargo test --test integration` exits 0 (full integration suite passes; existing install/uninstall tests including idempotency unchanged)
    - `cargo clippy --tests -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Three integration tests cover the install-creates / install-writes-PreToolUse / uninstall-removes round trip for the new code-review artifacts. Existing integration tests continue to pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green check + ROADMAP success-criteria cross-reference</name>
  <files>(no source changes — verification + summary)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 29 Success Criteria block — lines ~798-816)
    - .planning/phases/29-code-review-integration/29-01-SUMMARY.md (Plan 29-01 completion notes)
    - src/integrations/claude.rs (verify final shape of install() after Task 1)
    - tests/integration/install_uninstall.rs (verify final test count after Task 2)
  </read_first>
  <action>
    Run the full Rust test suite end-to-end and confirm zero regressions:
      ```
      cargo test
      cargo clippy --all-targets -- -D warnings
      cargo fmt --check
      ```

    Manually cross-reference each ROADMAP Phase 29 Success Criterion against the now-implemented behavior and record the mapping in a small notes file or the SUMMARY:

    1. "`plan-reviewer install claude` creates `commands/code-review.md` in the plugin directory and registers a pre-PR hook entry; `/code-review` appears in Claude Code's slash command menu" — covered by Task 1 + Task 2 of THIS plan (29-02). The "appears in menu" claim is verified at the file level (file exists + correct heading); end-user menu verification requires a live Claude Code session (out of scope for automated tests).

    2. "Running `/code-review` (or the agent triggering the pre-PR hook) opens the browser UI at `/code-review` for the current git branch" — covered by Plan 29-01 Task 3 (subcommand smoke test) + Task 1 of this plan (hooks.json wiring).

    3. "`plan-reviewer uninstall claude` removes the slash command file and hook entry; re-running exits 0" — covered by Task 2 Test 3 in this plan + existing `uninstall_claude_idempotent` test (already in the suite).

    4. "The `code-review` subcommand can be invoked directly as `plan-reviewer code-review` to open the review UI without a hook" — covered by Plan 29-01 Task 3 (help + dispatch tests).

    5. "Existing `plan-reviewer install` behavior for annotate/review-hook is unchanged" — covered by the existing `install_creates_annotate_md_with_expected_content`, `install_creates_hooks_json_with_exit_plan_mode`, and `install_claude_is_idempotent` tests continuing to pass (asserted by full-suite green check).

    Do not write a separate verification file unless the SUMMARY needs space.
  </action>
  <verify>
    <automated>cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` exits 0 (zero regressions across the full suite — was 650 tests at Phase 28 close; should now be 650 + new tests from Plans 29-01 and 29-02)
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
    - Each of the five Phase 29 ROADMAP success criteria is mapped to at least one passing test (mapping documented in the SUMMARY)
    - `target/release/plan-reviewer --help | grep -cE "code-review|pre-pr-hook"` returns `2` after `cargo build --release`
  </acceptance_criteria>
  <done>Full Rust suite green. Every Phase 29 success criterion has at least one passing automated test mapped to it. Release build advertises both new subcommands in --help.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem (`~/.local/share/plan-reviewer/`) ← plan-reviewer binary | Install writes files into HOME; uninstall removes them |
| `hooks/hooks.json` ← Claude Code | Claude Code reads this JSON to dispatch hooks; malformed content disables the plugin |
| `commands/code-review.md` ← Claude Code | Slash command prompt loaded into agent context; controls what Claude does when `/plan-reviewer:code-review` is invoked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Tampering | hooks.json write overwrites existing PermissionRequest | mitigate | The new JSON literal includes BOTH arrays (PermissionRequest AND PreToolUse) in a single write. Unit test `install_hooks_json_preserves_exit_plan_mode_after_pre_tool_use_added` (Task 1 Test 3) asserts both arrays present after install. |
| T-29-07 | Tampering | Malformed JSON written to hooks.json | mitigate | `serde_json::to_string_pretty` cannot fail for a well-formed `serde_json::json!` literal; error-handling already in place (returns Err). |
| T-29-08 | Information disclosure | code-review.md leaks sensitive info | accept | Markdown prompt contains no secrets; written to user-owned `~/.local/share/`. No PII or credentials embedded. |
| T-29-09 | Denial of Service | hooks.json with broad `matcher: "Bash"` slows every Bash call | mitigate | The hook command is `plan-reviewer pre-pr-hook` (Plan 29-01) which exits 0 immediately for non-PR commands. Smoke-tested in Plan 29-01 Task 3 (< 2s exit on `npm install`). |
| T-29-10 | Elevation of privilege | Install writes files outside HOME | mitigate | All writes use `home: &str` from `InstallContext`; path joins via `PathBuf::join` (no `..` traversal possible since the segments are hardcoded constants). Existing tests run in `tempfile::TempDir` to verify isolation. |
| T-29-11 | Repudiation | Uninstall silently leaves orphan files | accept | `remove_dir_all(&plugin_dir)` is unconditional and atomic per directory. Uninstall test (Task 2 Test 3) asserts file gone. |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; RESEARCH.md Package Legitimacy Audit confirms zero new dependencies. |
</threat_model>

<verification>
End-to-end verification for the phase as a whole (run after Plan 29-01 + Plan 29-02 both complete):

1. `cargo test` exits 0 (full suite — unit + integration)
2. `cargo clippy --all-targets -- -D warnings` exits 0
3. `cargo fmt --check` exits 0
4. Install round-trip in a real HOME:
   ```text
   plan-reviewer install claude
   test -f ~/.local/share/plan-reviewer/claude-plugin/commands/code-review.md   # exists
   test -f ~/.local/share/plan-reviewer/claude-plugin/commands/annotate.md      # still exists
   jq '.hooks.PreToolUse[0].hooks[0].command' ~/.local/share/plan-reviewer/claude-plugin/hooks/hooks.json
   # → "plan-reviewer pre-pr-hook"
   jq '.hooks.PermissionRequest[0].matcher' ~/.local/share/plan-reviewer/claude-plugin/hooks/hooks.json
   # → "ExitPlanMode"
   plan-reviewer uninstall claude
   test ! -d ~/.local/share/plan-reviewer/claude-plugin                         # removed
   ```
5. Subcommand smoke (from Plan 29-01):
   ```text
   plan-reviewer --help | grep -E "code-review|pre-pr-hook"   # both lines
   echo '{"session_id":"s","cwd":"/tmp","hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"npm install"}}' | plan-reviewer pre-pr-hook
   # exits 0 silently, no stdout
   ```
</verification>

<success_criteria>
- [ ] `install()` writes `commands/code-review.md` with the correct plugin-namespaced heading
- [ ] `install()` writes `hooks.json` with BOTH `PermissionRequest` (existing) and `PreToolUse` (new) arrays
- [ ] `PreToolUse` entry references `plan-reviewer pre-pr-hook` (the Plan 29-01 subcommand) with `timeout: 600000`
- [ ] `uninstall()` removes `commands/code-review.md` (via existing `remove_dir_all`)
- [ ] Idempotency preserved: second `install` does not duplicate hook entries
- [ ] All ROADMAP Phase 29 success criteria (1-5) have at least one passing automated test mapped to them
- [ ] `cargo test` exits 0 with no regressions
- [ ] `cargo clippy --all-targets -- -D warnings` exits 0
- [ ] INTEG-01 satisfied: slash command file written by install
- [ ] INTEG-02 satisfied: pre-PR hook entry written by install
- [ ] INTEG-03 satisfied: uninstall removes both, install/uninstall stay idempotent
</success_criteria>

<output>
Create `.planning/phases/29-code-review-integration/29-02-SUMMARY.md` when done. Include:
- Files modified (src/integrations/claude.rs, tests/integration/install_uninstall.rs)
- Final hooks.json shape (paste a JSON snippet showing both arrays)
- Final code-review.md frontmatter + heading (paste the first 6 lines)
- Test count delta: unit tests added in claude.rs, integration tests added in install_uninstall.rs
- Mapping of ROADMAP success criteria → test names
- Any deviations from this plan
</output>
