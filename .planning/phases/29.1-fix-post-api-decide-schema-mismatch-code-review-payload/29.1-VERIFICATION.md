---
phase: 29.1-fix-post-api-decide-schema-mismatch-code-review-payload
verified: 2026-05-26T12:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 29.1: Fix POST /api/decide Schema Mismatch Verification Report

**Phase Goal:** Fix 422 regression in POST /api/decide when the code-review frontend submits {message?, comments?} (no behavior key); unblock the code-review submit path with no frontend changes
**Verified:** 2026-05-26
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                   | Status     | Evidence                                                                                     |
|----|-----------------------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------|
| 1  | Test `server_cycle_code_review_submit` exists in server_cycle.rs                        | VERIFIED   | Line 283 in tests/integration/server_cycle.rs — `fn server_cycle_code_review_submit()`      |
| 2  | Test POSTs {message, comments} with no `behavior` field to /api/decide                  | VERIFIED   | Line 296: `send_json(json!({ "message": "looks good", "comments": [] }))` — no behavior key |
| 3  | Test asserts HTTP 200 and `json["behavior"] == "allow"` in stdout                       | VERIFIED   | Lines 298-314: assert_eq!(response.status(), 200) + json["behavior"].as_str() == Some("allow") |
| 4  | `post_decide` accepts Json<serde_json::Value> (not Json<Decision>)                      | VERIFIED   | src/plan_review.rs line 44: `Json(body): Json<serde_json::Value>` — grep count 1 occurrence |
| 5  | Json<Decision> extractor removed from post_decide                                       | VERIFIED   | grep -c 'Json<Decision>' src/plan_review.rs returns 0                                        |
| 6  | Key-presence dispatch: behavior absent synthesizes "allow"                              | VERIFIED   | Lines 46-50: body.get("behavior").and_then(|v| v.as_str()).unwrap_or("allow")                |
| 7  | server_cycle_code_review_submit PASSES (GREEN)                                          | VERIFIED   | cargo test server_cycle_code_review_submit: 1 passed, 0 failed (ran live)                    |
| 8  | All 5 pre-existing server_cycle tests continue to pass                                  | VERIFIED   | cargo test server_cycle: 6 passed, 0 failed — all 6 tests green (ran live)                   |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact                              | Expected                                              | Status   | Details                                                          |
|---------------------------------------|-------------------------------------------------------|----------|------------------------------------------------------------------|
| `tests/integration/server_cycle.rs`   | Failing regression test for code-review payload schema | VERIFIED | Contains `spawn_code_review_flow` (line 266) and `server_cycle_code_review_submit` (line 283) |
| `src/plan_review.rs`                  | post_decide handler accepting Json<serde_json::Value>  | VERIFIED | Line 44: `Json(body): Json<serde_json::Value>`; key-presence dispatch lines 46-55; Decision struct, AppState, and router() unchanged |

### Key Link Verification

| From                                         | To             | Via                                                                  | Status  | Details                                                                 |
|----------------------------------------------|----------------|----------------------------------------------------------------------|---------|-------------------------------------------------------------------------|
| `server_cycle.rs::server_cycle_code_review_submit` | `/api/decide`  | ureq::post with {message, comments} — no behavior key                | WIRED   | Line 295-296: ureq::post + send_json without behavior key; asserts 200  |
| `src/plan_review.rs::post_decide`            | `Decision`     | key-presence check: body.get("behavior").and_then(|v| v.as_str())   | WIRED   | Lines 46-55 implement the full dispatch; Decision struct built and sent to channel |

### Behavioral Spot-Checks

| Behavior                                          | Command                                             | Result          | Status |
|---------------------------------------------------|-----------------------------------------------------|-----------------|--------|
| POST {message, comments} returns 200              | cargo test server_cycle_code_review_submit          | 1 passed        | PASS   |
| POST {behavior: allow} still returns 200          | cargo test server_cycle (includes approve_claude)   | 6 passed        | PASS   |
| POST {behavior: deny, message} still returns 200  | cargo test server_cycle (includes deny_claude)      | 6 passed        | PASS   |
| cargo clippy -- -D warnings reports zero warnings | cargo clippy -- -D warnings                         | Finished, clean | PASS   |
| cargo fmt produces no diff                        | cargo fmt -- --check                                | No output (clean) | PASS  |

### Probe Execution

Step 7c: No probes declared in PLAN files. No `scripts/*/tests/probe-*.sh` found.

### Requirements Coverage

No requirement IDs were declared for this phase (requirements field is empty in both PLAN files; ROADMAP lists "TBD"). No REQUIREMENTS.md cross-reference needed.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | — |

Scanned `src/plan_review.rs` and `tests/integration/server_cycle.rs` for TBD, FIXME, XXX, TODO, HACK, PLACEHOLDER, return null, return {}, return []. Zero matches. No stubs, no debt markers.

### Human Verification Required

None. All observable behaviors are programmatically verifiable via integration tests and static code inspection.

### Gaps Summary

No gaps. All must-haves verified against the live codebase.

The handler change is exactly as prescribed:
- `src/plan_review.rs` post_decide signature changed from `Json<Decision>` to `Json<serde_json::Value>` (one occurrence, confirmed by grep)
- Key-presence dispatch correctly synthesizes `behavior: "allow"` when the key is absent
- `Decision` struct, `AppState`, and `router()` are unchanged
- Both plan-review and code-review payload shapes route correctly as confirmed by the full 6-test suite passing

Commits `ca914f6` (Wave 1 — RED test) and `d8ef8c1` (Wave 2 — GREEN handler fix) are present in git history and correspond exactly to the summary claims.

---

_Verified: 2026-05-26_
_Verifier: Claude (gsd-verifier)_
