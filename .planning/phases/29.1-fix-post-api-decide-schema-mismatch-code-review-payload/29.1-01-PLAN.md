---
phase: 29.1-fix-post-api-decide-schema-mismatch-code-review-payload
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/integration/server_cycle.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "A new test `server_cycle_code_review_submit` exists and FAILS with the current handler (RED state)"
    - "The test POSTs {message, comments} with no `behavior` field to /api/decide"
    - "The test asserts HTTP 200 and `json[\"behavior\"] == \"allow\"` in stdout"
    - "All existing server_cycle tests still compile and pass"
  artifacts:
    - path: "tests/integration/server_cycle.rs"
      provides: "Failing regression test for code-review payload schema"
      contains: "server_cycle_code_review_submit"
  key_links:
    - from: "tests/integration/server_cycle.rs::server_cycle_code_review_submit"
      to: "/api/decide"
      via: "ureq::post with {message, comments} — no behavior key"
      pattern: "\"message\".*\"looks good\""
---

<objective>
Add a failing integration test `server_cycle_code_review_submit` that exercises the code-review payload shape against a live `/api/decide` endpoint.

Purpose: Establish the RED state — the test must FAIL before the handler fix (Wave 1) and PASS after it. This test is the regression guard that prevented shipping without confirmation.
Output: `tests/integration/server_cycle.rs` with `spawn_code_review_flow` helper and `server_cycle_code_review_submit` test appended. Test compiles but FAILS with HTTP 422.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-fix-post-api-decide-schema-mismatch-code-review-payload/29.1-RESEARCH.md
@.planning/phases/29.1-fix-post-api-decide-schema-mismatch-code-review-payload/29.1-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add spawn_code_review_flow helper and server_cycle_code_review_submit failing test</name>
  <files>tests/integration/server_cycle.rs</files>

  <read_first>
    - tests/integration/server_cycle.rs — read in full; append after line 261 (end of server_cycle_ping_returns_200)
    - src/plan_review.rs — confirm current handler signature to understand why the test will fail
  </read_first>

  <behavior>
    - spawn_code_review_flow(port: u16) spawns the binary with args ["--no-browser", "--port", &port.to_string(), "code-review"] and Stdio::null() for stdin (code-review subcommand reads no hook input)
    - server_cycle_code_review_submit calls find_free_port(), spawns via spawn_code_review_flow, waits for port with 10-second timeout
    - The POST body is serde_json::json!({ "message": "looks good", "comments": [] }) — no "behavior" key
    - Test asserts response.status() == 200 (will be 422 in RED state — this is the documented regression)
    - After wait_with_output(), test asserts exit code 0 and json["behavior"] == Some("allow")
    - The stdout JSON shape for the code-review subcommand is flat {behavior: "allow"} from build_opencode_output, NOT the nested hookSpecificOutput shape used by plan-review tests
  </behavior>

  <action>
    Append two items after the last test in tests/integration/server_cycle.rs (after line 261):

    1. A helper function `spawn_code_review_flow(port: u16) -> std::process::Child` that mirrors spawn_hook_flow but passes the "code-review" subcommand and uses Stdio::null() for stdin. Use the same Box::leak(Box::new(tempfile::TempDir::new().unwrap())) pattern for HOME isolation.

    2. A test function `#[test] fn server_cycle_code_review_submit()` that:
       - Calls find_free_port() and spawn_code_review_flow(port)
       - Calls wait_for_port(port, Duration::from_secs(10)) with an assertion message including port
       - POSTs to http://127.0.0.1:{port}/api/decide using ureq::post(...).send_json(serde_json::json!({ "message": "looks good", "comments": [] }))
       - Asserts response.status() == 200 with message "expected 200 OK — schema mismatch would return 422"
       - Calls child.wait_with_output() and asserts output.status.success()
       - Parses stdout as serde_json::Value and asserts json["behavior"].as_str() == Some("allow") with message "code-review submit: expected behavior == \"allow\" in stdout"

    Do NOT modify any existing tests or helpers. Do NOT add a "behavior" key to the POST body.

    After writing, verify the test compiles with `cargo test --no-run` and that running it reports a failure (422 or assertion failure) — this confirms RED state. The compile step alone is sufficient to unblock Wave 1.
  </action>

  <verify>
    <automated>cargo test --no-run 2>&amp;1 | grep -v "^$" | tail -5</automated>
    <automated>cargo test server_cycle_code_review_submit 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test --no-run` succeeds (zero compile errors)
    - `cargo test server_cycle_code_review_submit` runs and FAILS (RED state — current handler returns 422, not 200)
    - `cargo test server_cycle_approve_claude server_cycle_deny_claude server_cycle_approve_gemini server_cycle_deny_gemini server_cycle_ping_returns_200` all continue to pass
    - The test file contains `fn spawn_code_review_flow` with Stdio::null() for stdin
    - The test file contains `fn server_cycle_code_review_submit` with a POST body containing "message" and "comments" keys but NO "behavior" key
    - The assert_eq! on response status uses 200 as the expected value (will fail in RED state)
  </acceptance_criteria>

  <done>
    tests/integration/server_cycle.rs compiles clean. server_cycle_code_review_submit exists and FAILS due to the handler returning 422. All pre-existing tests pass. RED state confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → binary subprocess | Integration test spawns a local binary; communication via localhost TCP only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.1-01 | Tampering | test POST body | accept | Test-only code; POST target is localhost; no production code path modified in this plan |
| T-29.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; ureq and serde_json already in Cargo.toml dev-dependencies |
</threat_model>

<verification>
Run after this plan completes:

```
cargo test --no-run
cargo test server_cycle_code_review_submit
cargo test server_cycle_approve_claude server_cycle_deny_claude server_cycle_approve_gemini server_cycle_deny_gemini server_cycle_ping_returns_200
```

Expected: compile clean, new test FAILS (RED), existing tests all PASS.
</verification>

<success_criteria>
The failing test `server_cycle_code_review_submit` exists in tests/integration/server_cycle.rs, compiles without errors, and fails at the HTTP 200 assertion because the current handler returns 422 for a payload without a "behavior" field. All four pre-existing server_cycle tests continue to pass.
</success_criteria>

<output>
Create `.planning/phases/29.1-fix-post-api-decide-schema-mismatch-code-review-payload/29.1-01-SUMMARY.md` when done
</output>
