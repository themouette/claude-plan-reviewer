---
phase: 29.1-fix-post-api-decide-schema-mismatch-code-review-payload
plan: "01"
subsystem: testing
tags: [rust, integration-test, tdd, cargo-test, server-cycle]

# Dependency graph
requires: []
provides:
  - Failing integration test server_cycle_code_review_submit (RED state)
  - spawn_code_review_flow helper for code-review subcommand test spawning
affects:
  - 29.1-02 (handler fix — Wave 1 will make this test pass GREEN)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "spawn_code_review_flow: spawns binary in code-review mode with Stdio::null() stdin (no hook input needed)"
    - "TDD RED phase: test compiles and fails with 422 before handler fix"

key-files:
  created: []
  modified:
    - tests/integration/server_cycle.rs

key-decisions:
  - "Use Stdio::null() for code-review flow stdin — subcommand reads no hook input unlike plan-review flow"
  - "Let ureq propagate the 422 error rather than catching it — the 422 is the regression signal in RED state"

patterns-established:
  - "Code-review integration tests spawn binary with 'code-review' subcommand arg and Stdio::null() stdin"

requirements-completed: []

# Metrics
duration: 2min
completed: "2026-05-26"
---

# Phase 29.1 Plan 01: Add Failing server_cycle_code_review_submit Test (RED)

**Failing integration test confirms {message, comments} POST body without behavior field returns 422 — RED state established for code-review schema mismatch regression guard**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-26T11:18:19Z
- **Completed:** 2026-05-26T11:21:16Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Added `spawn_code_review_flow(port: u16)` helper that spawns the binary in `code-review` mode using `Stdio::null()` for stdin (code-review subcommand reads no hook input)
- Added `server_cycle_code_review_submit` integration test that POSTs `{message, comments}` without a `behavior` key — fails with 422 (RED state) confirming the schema mismatch
- All 5 existing server_cycle tests (approve_claude, deny_claude, approve_gemini, deny_gemini, ping_returns_200) continue to pass

## Task Commits

1. **Task 1: Add spawn_code_review_flow helper and server_cycle_code_review_submit failing test** - `ca914f6` (test)

## Files Created/Modified

- `tests/integration/server_cycle.rs` - Appended spawn_code_review_flow helper and server_cycle_code_review_submit test (55 lines added)

## Decisions Made

- Used `Stdio::null()` for stdin in `spawn_code_review_flow` — the `code-review` subcommand does not read hook input from stdin, unlike the plan-review flow which requires stdin piped with hook JSON
- Let `ureq` surface the 422 as an error (not caught) — the test's `expect("POST /api/decide failed")` panics with `StatusCode(422)`, which clearly documents the regression in RED state
- Removed spurious `mut` on the `child` binding to satisfy clippy `-D warnings`

## Deviations from Plan

None - plan executed exactly as written.

The minor deviation (removing `mut` from `child` binding) was a clippy fix applied proactively to satisfy `cargo clippy -- -D warnings` enforcement from CLAUDE.md. The plan's code example included `let mut child = ...` but `wait_with_output()` takes `self` not `&mut self`, so the `mut` was unused. This is a Rule 1 (auto-fix) correction.

## Issues Encountered

None.

## Next Phase Readiness

- RED state confirmed: `server_cycle_code_review_submit` compiles and fails with 422
- Wave 1 (29.1-02) can now implement the handler fix — when the `post_decide` handler accepts `serde_json::Value` instead of `Json<Decision>`, this test will turn GREEN
- The test is the regression guard: if the fix is ever reverted, CI will catch it

---
*Phase: 29.1-fix-post-api-decide-schema-mismatch-code-review-payload*
*Completed: 2026-05-26*
