---
phase: 29.1-fix-post-api-decide-schema-mismatch-code-review-payload
plan: 02
type: execute
wave: 2
depends_on:
  - 29.1-01
files_modified:
  - src/plan_review.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "POST /api/decide with {message, comments} (no behavior) returns HTTP 200"
    - "POST /api/decide with {behavior: allow} still returns HTTP 200"
    - "POST /api/decide with {behavior: deny, message: ...} still returns HTTP 200 with deny output"
    - "server_cycle_code_review_submit passes (GREEN)"
    - "All pre-existing server_cycle tests continue to pass"
    - "cargo clippy -- -D warnings reports zero warnings"
  artifacts:
    - path: "src/plan_review.rs"
      provides: "post_decide handler accepting serde_json::Value with key-presence dispatch"
      contains: "Json<serde_json::Value>"
  key_links:
    - from: "src/plan_review.rs::post_decide"
      to: "Decision"
      via: "key-presence check: body.get(\"behavior\").and_then(|v| v.as_str())"
      pattern: "body\\.get\\(\"behavior\"\\)"
---

<objective>
Fix the `post_decide` handler in `src/plan_review.rs` to accept `Json<serde_json::Value>` instead of `Json<Decision>`. Discriminate payload shape by key presence: if `behavior` key exists, use its value (plan-review flow); if absent, synthesize `behavior: "allow"` (code-review flow).

Purpose: Unblock the code-review submit path. The code-review frontend sends `{message?, comments?}` — never a `behavior` key. The current typed `Json<Decision>` extractor returns 422 for that shape.
Output: Modified `src/plan_review.rs` with `post_decide` accepting untyped JSON and dispatching correctly for both payload shapes. The `Decision` struct, `AppState`, and router registration are unchanged.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-fix-post-api-decide-schema-mismatch-code-review-payload/29.1-RESEARCH.md
@.planning/phases/29.1-fix-post-api-decide-schema-mismatch-code-review-payload/29.1-PATTERNS.md

<interfaces>
<!-- Extracted from src/plan_review.rs — full file is 67 lines -->

Current Decision struct (lines 14-18):
```rust
#[derive(serde::Deserialize, Debug, Clone)]
pub struct Decision {
    pub behavior: String,        // "allow" or "deny"
    pub message: Option<String>, // required if behavior is "deny"
}
```

Current AppState struct (lines 22-27):
```rust
pub struct AppState {
    pub plan_md: String,
    pub approve_label: String,
    pub deny_label: String,
    pub decision_tx: Mutex<Option<oneshot::Sender<Decision>>>,
}
```

Current post_decide handler (lines 42-54) — THIS IS THE TARGET:
```rust
async fn post_decide(
    State(state): State<Arc<AppState>>,
    Json(body): Json<Decision>,   // <-- requires "behavior" field; 422 for code-review payload
) -> impl IntoResponse {
    let tx = state.decision_tx.lock().unwrap().take();
    match tx {
        Some(tx) => {
            let _ = tx.send(body);
            StatusCode::OK
        }
        None => StatusCode::CONFLICT,
    }
}
```

Router factory (lines 61-67) — NO CHANGE:
```rust
pub fn router(state: Arc<AppState>) -> Router<()> {
    Router::new()
        .route("/api/plan", get(get_plan))
        .route("/api/config", get(get_config))
        .route("/api/decide", post(post_decide))
        .with_state(state)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace post_decide extractor with serde_json::Value and key-presence dispatch</name>
  <files>src/plan_review.rs</files>

  <read_first>
    - src/plan_review.rs — read in full before editing; the file is 67 lines
  </read_first>

  <behavior>
    - POST {behavior: "allow"} → Decision { behavior: "allow", message: None } dispatched to channel → 200
    - POST {behavior: "deny", message: "Needs tests"} → Decision { behavior: "deny", message: Some("Needs tests") } → 200
    - POST {message: "looks good", comments: []} → Decision { behavior: "allow", message: Some("looks good") } → 200
    - POST {} (empty object, no keys) → Decision { behavior: "allow", message: None } → 200
    - POST valid JSON that is not an object (e.g. "null") → 422 from axum extractor (serde_json::Value still rejects non-object when extracted as Value; this is acceptable)
    - Second POST after first (channel already consumed) → 409 (CONFLICT)
  </behavior>

  <action>
    Modify only the `post_decide` function in src/plan_review.rs. Change the function signature from `Json(body): Json<Decision>` to `Json(body): Json<serde_json::Value>`.

    Replace the function body with the key-presence dispatch logic:
    - Extract `behavior` by calling body.get("behavior").and_then(|v| v.as_str())
    - If Some(behavior): construct Decision { behavior: behavior.to_string(), message: body.get("message").and_then(|v| v.as_str()).map(str::to_string) }
    - If None: construct Decision { behavior: "allow".to_string(), message: body.get("message").and_then(|v| v.as_str()).map(str::to_string) }
    - After constructing the Decision, the channel dispatch block is identical to the current handler: let tx = state.decision_tx.lock().unwrap().take(); match tx { Some(tx) => { let _ = tx.send(decision); StatusCode::OK } None => StatusCode::CONFLICT }

    Do NOT change the Decision struct, AppState struct, or router() function.
    Do NOT add any new structs or imports. The serde_json::Value type is accessible via the existing serde_json dependency — no new import line is needed (serde_json::Value is already in scope because serde_json is in Cargo.toml).

    After editing, run cargo fmt to format the file, then cargo clippy -- -D warnings to confirm zero warnings.
  </action>

  <verify>
    <automated>cargo fmt &amp;&amp; cargo clippy -- -D warnings 2>&amp;1 | tail -10</automated>
    <automated>cargo test server_cycle_code_review_submit server_cycle_approve_claude server_cycle_deny_claude server_cycle_approve_gemini server_cycle_deny_gemini server_cycle_ping_returns_200 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/plan_review.rs compiles with zero errors
    - cargo clippy -- -D warnings reports zero warnings
    - cargo fmt produces no diff (file is already formatted after running fmt)
    - `cargo test server_cycle_code_review_submit` PASSES (GREEN state — regression fixed)
    - `cargo test server_cycle_approve_claude` PASSES (plan-review allow flow unchanged)
    - `cargo test server_cycle_deny_claude` PASSES (plan-review deny flow unchanged)
    - `cargo test server_cycle_approve_gemini` PASSES (Gemini allow flow unchanged)
    - `cargo test server_cycle_deny_gemini` PASSES (Gemini deny flow unchanged)
    - `cargo test server_cycle_ping_returns_200` PASSES (unrelated endpoint unchanged)
    - grep -c 'Json<serde_json::Value>' src/plan_review.rs returns 1 (exactly one occurrence — the extractor)
    - grep -c 'Json<Decision>' src/plan_review.rs returns 0 (old typed extractor removed)
    - The Decision struct definition is unchanged (behavior: String, message: Option&lt;String&gt;)
    - The router() function is unchanged
  </acceptance_criteria>

  <done>
    post_decide accepts Json&lt;serde_json::Value&gt;. Both plan-review and code-review payload shapes route correctly. All six integration tests pass. cargo clippy -- -D warnings is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /api/decide | Localhost-only; no network exposure; no auth required for local tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.1-02 | Tampering | post_decide JSON body | mitigate | axum's Json extractor returns 400 for unparseable JSON; keys beyond "behavior" and "message" are silently ignored (no injection surface) |
| T-29.1-03 | Tampering | empty body {} treated as code-review allow | accept | Client-side gate in popover requires message or comments before enabling Send; server accepting {} as "allow" is correct per research Pitfall 4 analysis |
| T-29.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages; change is in-place modification of existing function |
</threat_model>

<verification>
Full test suite after Wave 2:

```
cargo fmt
cargo clippy -- -D warnings
cargo test
```

All three commands must exit 0.

Additional spot checks:
- grep -c 'Json<serde_json::Value>' src/plan_review.rs → 1
- grep -c 'Json<Decision>' src/plan_review.rs → 0
</verification>

<success_criteria>
`cargo test` runs all tests green. `server_cycle_code_review_submit` passes (was failing in Wave 1). All pre-existing server_cycle tests still pass. `cargo clippy -- -D warnings` is clean. The handler change is confined to the `post_decide` function body and signature — no other functions, structs, or the router are modified.
</success_criteria>

<output>
Create `.planning/phases/29.1-fix-post-api-decide-schema-mismatch-code-review-payload/29.1-02-SUMMARY.md` when done
</output>
