---
phase: 22-submit-clipboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/reviewer-v2/offlineLabels.ts
  - ui/src/reviewer-v2/offlineLabels.test.ts
autonomous: true
requirements:
  - SUBMIT-02
tags:
  - reviewer-v2
  - offline
  - banner-copy

must_haves:
  truths:
    - "reviewer-v2/offlineLabels.ts exports OFFLINE_BANNER_LINE_1 and OFFLINE_BANNER_LINE_2 string constants"
    - "OFFLINE_BANNER_LINE_1 equals 'Server connection lost — working offline.' (matches existing utils/offlineLabels.ts so the user-visible string is identical to the v0.5 banner)"
    - "OFFLINE_BANNER_LINE_2 equals 'Clipboard submit is available.' (per UI-SPEC line 338)"
    - "buildClipboardPayload and shouldUseClipboard remain exported with identical signatures and behavior — no regressions"
  artifacts:
    - path: "ui/src/reviewer-v2/offlineLabels.ts"
      provides: "Banner text constants + existing payload utilities — single import source for v2 OfflineBanner"
      exports:
        - "OFFLINE_BANNER_LINE_1"
        - "OFFLINE_BANNER_LINE_2"
        - "buildClipboardPayload"
        - "shouldUseClipboard"
        - "ClipboardDecision"
    - path: "ui/src/reviewer-v2/offlineLabels.test.ts"
      provides: "Vitest coverage asserting constants exist with expected values and prior tests still pass"
  key_links:
    - from: "ui/src/reviewer-v2/offlineLabels.ts"
      to: "consumers in ui/src/reviewer-v2/"
      via: "named exports OFFLINE_BANNER_LINE_1 / OFFLINE_BANNER_LINE_2"
      pattern: "export const OFFLINE_BANNER_LINE_"
---

<objective>
Add the offline-banner text constants `OFFLINE_BANNER_LINE_1` and `OFFLINE_BANNER_LINE_2` to the v2 copy of `offlineLabels.ts` so that the v2 `OfflineBanner` (created in Plan 04) can import them from within the `reviewer-v2/` subtree without violating ARCH-01 (ESLint `no-restricted-imports` blocks any import from outside `reviewer-v2/`).

Purpose: The existing v0.5 banner constants live at `ui/src/utils/offlineLabels.ts` and are imported by `App.tsx`. ARCH-01 prohibits the v2 subtree from importing that path, so the v2 reviewer must own a copy. This is the established Phase 17 pattern (the v2 copies of `buildClipboardPayload`, `shouldUseClipboard`, `serializeAnnotations` already exist).

Output: Two new exported string constants in `reviewer-v2/offlineLabels.ts`, plus extended Vitest tests in `reviewer-v2/offlineLabels.test.ts` that lock in their values and confirm the pre-existing payload helpers remain unchanged.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-submit-clipboard/22-RESEARCH.md
@.planning/phases/22-submit-clipboard/22-UI-SPEC.md
@ui/src/reviewer-v2/offlineLabels.ts
@ui/src/reviewer-v2/offlineLabels.test.ts
@ui/src/utils/offlineLabels.ts

<interfaces>
<!-- Existing v2 offlineLabels exports — must remain intact. -->

From ui/src/reviewer-v2/offlineLabels.ts:
- `export type ClipboardDecision = 'allow' | 'deny'`
- `export function buildClipboardPayload(decision: ClipboardDecision, denyText: string, overallComment: string, annotations: Annotation[]): string`
- `export function shouldUseClipboard(status: ConnectivityStatus): boolean`

Reference values from ui/src/utils/offlineLabels.ts (existing v0.5 banner — values to mirror):
- `OFFLINE_BANNER_LINE_1 = 'Server connection lost — working offline.'`
- `OFFLINE_BANNER_LINE_2 = "When you're done, copy your decision to the clipboard and paste it back into Claude."`

Per UI-SPEC line 338, the v2 banner line 2 value is `'Clipboard submit is available.'` (shorter and more action-focused). The planner has chosen the UI-SPEC value as the source of truth for the v2 banner — the v0.5 banner constant is unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OFFLINE_BANNER_LINE_1 and OFFLINE_BANNER_LINE_2 constants to v2 offlineLabels.ts</name>
  <files>ui/src/reviewer-v2/offlineLabels.ts, ui/src/reviewer-v2/offlineLabels.test.ts</files>
  <read_first>
    - ui/src/reviewer-v2/offlineLabels.ts (current v2 source — preserve existing exports verbatim)
    - ui/src/reviewer-v2/offlineLabels.test.ts (current Vitest test pattern — extend it, do not replace)
    - ui/src/utils/offlineLabels.ts (reference for the existing OFFLINE_BANNER_LINE_1 spelling, including the em-dash)
    - .planning/phases/22-submit-clipboard/22-UI-SPEC.md (lines 337-338 — banner copy contract)
  </read_first>
  <behavior>
    - Test: `OFFLINE_BANNER_LINE_1` is exported and equals exactly `'Server connection lost — working offline.'` (em-dash U+2014, trailing period).
    - Test: `OFFLINE_BANNER_LINE_2` is exported and equals exactly `'Clipboard submit is available.'` (trailing period).
    - Test: `buildClipboardPayload('allow', '', '', [])` still returns `'{"behavior":"allow"}'` exactly (regression guard — existing test from offlineLabels.test.ts must remain passing).
    - Test: `shouldUseClipboard('offline')` still returns `true` and `shouldUseClipboard('online')` still returns `false` (regression guard).
  </behavior>
  <action>
    Add two new exported string constants to `ui/src/reviewer-v2/offlineLabels.ts` at the top of the file (after the existing imports, before `export type ClipboardDecision`):
    - `export const OFFLINE_BANNER_LINE_1 = 'Server connection lost — working offline.'`
    - `export const OFFLINE_BANNER_LINE_2 = 'Clipboard submit is available.'`

    The em-dash in LINE_1 is U+2014 (`—`), matching the existing `ui/src/utils/offlineLabels.ts` spelling. Do NOT change the existing `buildClipboardPayload`, `shouldUseClipboard`, or `ClipboardDecision` exports — they are consumed by Plan 03 (SubmitControls) unchanged.

    Extend `ui/src/reviewer-v2/offlineLabels.test.ts` with a new `describe('banner constants (v2 copy)', ...)` block that includes:
    - `expect(OFFLINE_BANNER_LINE_1).toBe('Server connection lost — working offline.')`
    - `expect(OFFLINE_BANNER_LINE_2).toBe('Clipboard submit is available.')`

    Update the existing imports at the top of the test file to also import the new constants from `./offlineLabels`. Leave the existing `describe('buildClipboardPayload (v2 copy)', ...)` and `describe('shouldUseClipboard (v2 copy)', ...)` blocks untouched — those are the regression guards.

    Do NOT introduce additional helper functions (no `bannerLine1ForStatus`, no `getBannerLines`). The two constants are sufficient — Plan 04 reads them directly.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run reviewer-v2/offlineLabels</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const OFFLINE_BANNER_LINE_1" ui/src/reviewer-v2/offlineLabels.ts` returns 1
    - `grep -c "export const OFFLINE_BANNER_LINE_2" ui/src/reviewer-v2/offlineLabels.ts` returns 1
    - `grep "OFFLINE_BANNER_LINE_1" ui/src/reviewer-v2/offlineLabels.ts` shows the exact literal `'Server connection lost — working offline.'`
    - `grep "OFFLINE_BANNER_LINE_2" ui/src/reviewer-v2/offlineLabels.ts` shows the exact literal `'Clipboard submit is available.'`
    - `cd ui && npm test -- --run reviewer-v2/offlineLabels` exits 0 with both the new constant tests AND the pre-existing `buildClipboardPayload` / `shouldUseClipboard` tests passing
    - `grep -c "export function buildClipboardPayload" ui/src/reviewer-v2/offlineLabels.ts` still returns 1 (existing export untouched)
    - `grep -c "export function shouldUseClipboard" ui/src/reviewer-v2/offlineLabels.ts` still returns 1 (existing export untouched)
  </acceptance_criteria>
  <done>The v2 offlineLabels module exports the two banner constants with their UI-SPEC values; all prior tests still pass; no other v2 file imports the constants yet (Plan 04 will do that).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM ↔ React render tree | Banner strings render into untrusted DOM; both constants are static literals authored in source — no user input flows through them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `OFFLINE_BANNER_LINE_1` / `LINE_2` string literals | accept | Constants are compile-time string literals with no user input, no template substitution, and no dynamic interpolation. React's default text-node escaping eliminates HTML/JS injection risk. |
| T-22-02 | Information Disclosure | Banner copy | accept | Banner text reveals only the connectivity state (server reachable / not reachable). This is by design — the user must know they are offline to use the clipboard fallback. No PII, tokens, or paths are exposed. |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed in this plan. RESEARCH.md "Package Legitimacy Audit" section confirms no new npm dependencies are introduced in Phase 22. |
</threat_model>

<verification>
- Vitest assertions for both new constants and the regression guards for `buildClipboardPayload` / `shouldUseClipboard` pass.
- ESLint (`cd ui && npm run lint`) reports zero new errors — the v2 file still imports only from within the subtree (it already does; this plan adds only literal string exports, no new imports).
- TypeScript compile is implicit in `npm test` (Vitest uses the project tsconfig).
</verification>

<success_criteria>
- Two new exported constants in `ui/src/reviewer-v2/offlineLabels.ts` with the exact values from UI-SPEC.
- All existing v2 offlineLabels tests still pass.
- No other file in the repository is modified.
</success_criteria>

<output>
Create `.planning/phases/22-submit-clipboard/22-01-SUMMARY.md` when done.
</output>
