---
phase: 22-submit-clipboard
plan: 04
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-03
files_modified:
  - ui/src/reviewer-v2/ReviewerV2Shell.tsx
  - ui/src/reviewer-v2/ReviewerV2Shell.test.ts
  - ui/src/reviewer-v2/ReviewerV2.tsx
  - ui/src/index.css
autonomous: false
requirements:
  - SUBMIT-01
  - SUBMIT-02
tags:
  - reviewer-v2
  - wiring
  - offline-banner
  - cleanup

must_haves:
  truths:
    - "ReviewerV2Shell calls useHeartbeat() and captures the ConnectivityStatus return value into a local variable"
    - "ReviewerV2Shell renders an inline OfflineBanner sub-component between the 48px header and the 3-column body row WHEN connectivity === 'offline'"
    - "ReviewerV2Shell mounts SubmitControls in the right slot of the existing 48px header with annotations + connectivity props"
    - "ReviewerV2.tsx no longer calls void useHeartbeat() or void useAnnotations() — both phantom stubs are removed"
    - "ReviewerV2.tsx is a clean pass-through that renders <ReviewerV2Shell />"
    - "index.css contains the .submit-btn transition rule from UI-SPEC §CSS Changes"
    - "Human verifies: in the running app, with 0 comments, Approve is enabled and Send Feedback is disabled (clicking Send Feedback does nothing); with >=1 comment, Approve is disabled and Send Feedback is enabled (clicking Send Feedback opens the popover); approving with 0 comments commits an allow decision; submitting from the popover with >=1 comment commits a deny decision; in offline mode (kill the server) the same actions write the expected JSON to the clipboard"
  artifacts:
    - path: "ui/src/reviewer-v2/ReviewerV2Shell.tsx"
      provides: "v2 shell with SubmitControls mounted in header + OfflineBanner mounted between header and body + useHeartbeat lifted from ReviewerV2.tsx"
    - path: "ui/src/reviewer-v2/ReviewerV2.tsx"
      provides: "Clean ReviewerV2 entry — no void hook calls"
    - path: "ui/src/index.css"
      provides: "Adds .submit-btn transition rule for hover/disabled state animation"
  key_links:
    - from: "ui/src/reviewer-v2/ReviewerV2Shell.tsx"
      to: "ui/src/reviewer-v2/SubmitControls.tsx (Plan 03 output)"
      via: "default import + render in header right slot"
      pattern: "<SubmitControls"
    - from: "ui/src/reviewer-v2/ReviewerV2Shell.tsx"
      to: "ui/src/reviewer-v2/useHeartbeat.ts"
      via: "named import + call returning ConnectivityStatus"
      pattern: "const connectivity = useHeartbeat\\(\\)"
    - from: "ui/src/reviewer-v2/ReviewerV2Shell.tsx (OfflineBanner sub-component)"
      to: "ui/src/reviewer-v2/offlineLabels.ts OFFLINE_BANNER_LINE_1 / LINE_2 (Plan 01 output)"
      via: "named import + text-node rendering"
      pattern: "OFFLINE_BANNER_LINE_1"
---

<objective>
Wire the Phase 22 components into the v2 reviewer:
1. Lift `useHeartbeat()` from `ReviewerV2.tsx` into `ReviewerV2Shell.tsx`, capturing the `ConnectivityStatus` return value (was previously void-discarded).
2. Remove the phantom `void useAnnotations()` from `ReviewerV2.tsx` (Common Pitfall 1 — Shell already owns the annotation reducer).
3. Add an inline `<OfflineBanner>` sub-component in `ReviewerV2Shell.tsx` that renders between the header and the body row when `connectivity === 'offline'`, using the constants from `./offlineLabels` (Plan 01 output).
4. Mount `<SubmitControls annotations={annotations} connectivity={connectivity} />` in the right slot of the existing 48px header (Shell's `<header>` becomes `display: flex; justifyContent: space-between` so the existing "Reviewer v2" label sits left and SubmitControls sits right).
5. Add the `.submit-btn` CSS transition rule to `index.css` per UI-SPEC §CSS Changes.
6. Human verification of the full flow against the success criteria.

Purpose: Without this wiring, SubmitControls (Plan 03) is dead code. This plan is the single integration point that makes SUBMIT-01 and SUBMIT-02 user-observable; it also resolves the technical debt called out in RESEARCH.md Common Pitfalls 1 and 2 (the void hook calls in `ReviewerV2.tsx`).

Output: A user can open `/v2`, add comments, see Approve disable and Send Feedback enable; remove comments, see Approve enable and Send Feedback disable (SUBMIT-01); when enabled, clicking Send Feedback opens a popover with autoFocus textarea; submit succeeds online and shows the inline confirmation; under offline (server killed) the same actions write the correct JSON to the clipboard and show the appropriate confirmation/error state.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-submit-clipboard/22-RESEARCH.md
@.planning/phases/22-submit-clipboard/22-PATTERNS.md
@.planning/phases/22-submit-clipboard/22-UI-SPEC.md
@.planning/phases/22-submit-clipboard/22-01-PLAN.md
@.planning/phases/22-submit-clipboard/22-03-PLAN.md
@ui/src/reviewer-v2/ReviewerV2.tsx
@ui/src/reviewer-v2/ReviewerV2Shell.tsx
@ui/src/reviewer-v2/ReviewerV2Shell.test.ts
@ui/src/reviewer-v2/useHeartbeat.ts
@ui/src/index.css

<interfaces>
<!-- useHeartbeat return type -->

From ui/src/reviewer-v2/useHeartbeat.ts:
- `export function useHeartbeat(): ConnectivityStatus`

<!-- Banner constants from Plan 01 (now in v2 subtree) -->

From ui/src/reviewer-v2/offlineLabels.ts:
- `export const OFFLINE_BANNER_LINE_1: string` // 'Server connection lost — working offline.'
- `export const OFFLINE_BANNER_LINE_2: string` // 'Clipboard submit is available.'

<!-- SubmitControls props contract from Plan 03 -->

From ui/src/reviewer-v2/SubmitControls.tsx:
- `default export: React.FC<{ annotations: Annotation[]; connectivity: ConnectivityStatus }>`

<!-- Existing Shell render structure (current ReviewerV2Shell.tsx lines 31-135) -->

The Shell's current outer structure is:
```
<div className="flex flex-col h-screen overflow-hidden">
  <header style={{ height: 48, ... display: 'flex', alignItems: 'center', paddingLeft: 16, paddingRight: 16 }}>
    <span>Reviewer v2</span>
  </header>
  <div style={{ flex: 1, ..., display: 'flex' }}>
    {/* 3-column body */}
  </div>
</div>
```

The header `<header>` already uses `display: 'flex'; alignItems: 'center'`. We add `justifyContent: 'space-between'` so the existing label sits left and SubmitControls sits right. We insert the OfflineBanner as a sibling between `</header>` and the 3-column `<div>`, ONLY when `connectivity === 'offline'` (so the layout does not reflow when online).

<!-- App.tsx OfflineBanner reference pattern (read-only — do NOT import from this file in v2; replicate the styling inline) -->

From ui/src/App.tsx lines 162-184: `<div role="status">` with `background: var(--color-banner-bg)`, `color: var(--color-banner-text)`, two `<div>` children with the line constants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip ReviewerV2.tsx down to a clean pass-through (Common Pitfalls 1 + 2)</name>
  <files>ui/src/reviewer-v2/ReviewerV2.tsx</files>
  <read_first>
    - ui/src/reviewer-v2/ReviewerV2.tsx (current file with the two void hook calls — must remove both)
    - .planning/phases/22-submit-clipboard/22-RESEARCH.md (lines 211-230 — Common Pitfalls 1 and 2 explain why both must go)
    - .planning/phases/22-submit-clipboard/22-PATTERNS.md (lines 285-310 — final state of ReviewerV2.tsx)
  </read_first>
  <behavior>
    - Test (via grep in acceptance criteria): `ReviewerV2.tsx` no longer imports `useHeartbeat` or `useAnnotations`.
    - Test: `ReviewerV2.tsx` no longer contains `void useHeartbeat()` or `void useAnnotations()`.
    - Test: `ReviewerV2.tsx` default-exports a function that returns `<ReviewerV2Shell />`.
    - Test: the `highlight.js/styles/github-dark.css` import is preserved (needed for syntax highlighting of code blocks in the markdown content pane).
  </behavior>
  <action>
    Rewrite `ui/src/reviewer-v2/ReviewerV2.tsx` to the minimum required content:

    1. Keep the side-effect import `import 'highlight.js/styles/github-dark.css'` at the top (Phase 18 dependency — removing it breaks code-block syntax highlighting).
    2. Remove the `import { useHeartbeat } from './useHeartbeat'` line — useHeartbeat moves into the Shell in Task 2.
    3. Remove the `import { useAnnotations } from './useAnnotations'` line — Shell already calls `useAnnotations` (verified in `ReviewerV2Shell.tsx` line 14); this import was a phantom second reducer instance (Pitfall 1).
    4. Keep `import ReviewerV2Shell from './ReviewerV2Shell'`.
    5. Remove the comment block lines 7-9 ("ARCH-02: ... Phase 22 will wire it to the offline banner.") and the comment block lines 12-13 ("Annotation store mounted here...").
    6. Remove both `void useHeartbeat()` and `void useAnnotations()` lines.
    7. The component body becomes a single-line `return <ReviewerV2Shell />`.
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | grep -i "ReviewerV2\\.tsx" || echo "no ReviewerV2 type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "void useHeartbeat" ui/src/reviewer-v2/ReviewerV2.tsx` returns 0
    - `grep -c "void useAnnotations" ui/src/reviewer-v2/ReviewerV2.tsx` returns 0
    - `grep -c "from './useHeartbeat'" ui/src/reviewer-v2/ReviewerV2.tsx` returns 0
    - `grep -c "from './useAnnotations'" ui/src/reviewer-v2/ReviewerV2.tsx` returns 0
    - `grep -c "highlight.js/styles/github-dark.css" ui/src/reviewer-v2/ReviewerV2.tsx` returns 1 (preserved)
    - `grep -c "from './ReviewerV2Shell'" ui/src/reviewer-v2/ReviewerV2.tsx` returns 1
    - `grep -c "<ReviewerV2Shell" ui/src/reviewer-v2/ReviewerV2.tsx` returns 1
    - File size of `ui/src/reviewer-v2/ReviewerV2.tsx` is < 300 bytes (clean pass-through)
    - `cd ui && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ReviewerV2.tsx is a 4-line clean pass-through: the highlight.js import, the Shell import, the default-export function, the return statement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire SubmitControls + OfflineBanner + useHeartbeat into ReviewerV2Shell.tsx, plus update tests</name>
  <files>ui/src/reviewer-v2/ReviewerV2Shell.tsx, ui/src/reviewer-v2/ReviewerV2Shell.test.ts, ui/src/index.css</files>
  <read_first>
    - ui/src/reviewer-v2/ReviewerV2Shell.tsx (current full file — must preserve the 3-column body, OutlinePane, ContentPane, CommentPane wiring untouched)
    - ui/src/reviewer-v2/ReviewerV2Shell.test.ts (existing assertions must remain green — extend with Phase 22 assertions)
    - ui/src/reviewer-v2/SubmitControls.tsx (Plan 03 output — confirms default-export shape)
    - ui/src/reviewer-v2/offlineLabels.ts (Plan 01 output — confirms OFFLINE_BANNER_LINE_1 / LINE_2 are exported)
    - ui/src/index.css (current CSS — find a safe location to add the .submit-btn rule)
    - .planning/phases/22-submit-clipboard/22-UI-SPEC.md (lines 122-130 header mount position; lines 205-218 OfflineBanner visual spec; lines 312-321 CSS rule)
    - .planning/phases/22-submit-clipboard/22-PATTERNS.md (lines 251-282 — Shell wiring extension)
  </read_first>
  <behavior>
    - Test: `ReviewerV2Shell.tsx` imports `useHeartbeat` from `'./useHeartbeat'`.
    - Test: `ReviewerV2Shell.tsx` imports `SubmitControls` default from `'./SubmitControls'`.
    - Test: `ReviewerV2Shell.tsx` imports `OFFLINE_BANNER_LINE_1` and `OFFLINE_BANNER_LINE_2` from `'./offlineLabels'`.
    - Test: `ReviewerV2Shell.tsx` calls `useHeartbeat()` and stores the result in a const named `connectivity`.
    - Test: `ReviewerV2Shell.tsx` renders `<SubmitControls annotations={annotations} connectivity={connectivity} />` inside the existing `<header>` element.
    - Test: `ReviewerV2Shell.tsx` header style includes `justifyContent: 'space-between'`.
    - Test: `ReviewerV2Shell.tsx` declares an inline `function OfflineBanner` (or `const OfflineBanner =`) sub-component.
    - Test: the OfflineBanner sub-component contains `role="status"`, `var(--color-banner-bg)`, `var(--color-banner-text)`, and renders BOTH `{OFFLINE_BANNER_LINE_1}` and `{OFFLINE_BANNER_LINE_2}`.
    - Test: `ReviewerV2Shell.tsx` conditionally renders `<OfflineBanner />` based on `connectivity === 'offline'`.
    - Test: index.css contains the rule `.submit-btn { ... transition: ... }`.
    - Test: existing ReviewerV2Shell.test.ts assertions remain passing (regression guard for Phases 17-21 wiring).
  </behavior>
  <action>
    Step A — Edit `ui/src/reviewer-v2/ReviewerV2Shell.tsx`:

    1. Add the following imports alongside the existing ones (do NOT remove or reorder the current imports):
       - `import { useHeartbeat } from './useHeartbeat'`
       - `import SubmitControls from './SubmitControls'`
       - `import { OFFLINE_BANNER_LINE_1, OFFLINE_BANNER_LINE_2 } from './offlineLabels'`
    2. Inside the `ReviewerV2Shell` function body, alongside the existing `const { annotations, addAnnotation, editAnnotation, removeAnnotation } = useAnnotations()` line (currently line 14), add:
       - `const connectivity = useHeartbeat()`
    3. Add an inline `function OfflineBanner()` sub-component at the bottom of the file (after `}` of the default export) that mirrors the App.tsx pattern:
       ```
       function OfflineBanner() {
         return (
           <div
             role="status"
             style={{
               background: 'var(--color-banner-bg)',
               color: 'var(--color-banner-text)',
               padding: '8px 16px',
               display: 'flex',
               flexDirection: 'column',
               gap: 4,
               fontSize: 14,
               fontWeight: 400,
               lineHeight: 1.5,
               flexShrink: 0,
             }}
           >
             <div>{OFFLINE_BANNER_LINE_1}</div>
             <div>{OFFLINE_BANNER_LINE_2}</div>
           </div>
         )
       }
       ```
    4. Modify the existing `<header>` element (currently line 34-55):
       - Add `justifyContent: 'space-between'` to its style object (preserve every other style key).
       - Wrap the existing `<span>Reviewer v2</span>` (lines 46-54) inside a `<div>` (so the span keeps its current styling), then add `<SubmitControls annotations={annotations} connectivity={connectivity} />` as the second child of the `<header>`.
       - The `<header>` final structure becomes: `<header style={{ ... justifyContent: 'space-between', ... }}><div><span>Reviewer v2</span></div><SubmitControls annotations={annotations} connectivity={connectivity} /></header>`.
    5. Between `</header>` and the existing 3-column body `<div>` (currently line 58), insert a conditional render:
       - `{connectivity === 'offline' && <OfflineBanner />}`
       - Place it inside the outer `<div className="flex flex-col h-screen overflow-hidden">` so the column-flex layout pushes the banner between header and body without scrolling.
    6. Do NOT modify the existing OutlinePane / ContentPane / CommentPane wiring — every prop they receive today MUST remain exactly the same (regression guard for Phases 17-21).

    Step B — Extend `ui/src/reviewer-v2/ReviewerV2Shell.test.ts`:

    7. Append a NEW `describe('ReviewerV2Shell Phase 22 wiring', ...)` block at the bottom of the file with the following assertions (each via `expect(source).toContain(...)` / `toMatch(...)`):
       - "imports useHeartbeat from './useHeartbeat'" — `expect(source).toMatch(/from ['\"]\\.\\/useHeartbeat['\"]/)`
       - "imports SubmitControls from './SubmitControls'" — `expect(source).toMatch(/import SubmitControls from ['\"]\\.\\/SubmitControls['\"]/)`
       - "imports both banner constants from './offlineLabels'" — `expect(source).toContain('OFFLINE_BANNER_LINE_1')` AND `expect(source).toContain('OFFLINE_BANNER_LINE_2')` AND `expect(source).toMatch(/from ['\"]\\.\\/offlineLabels['\"]/)`
       - "captures useHeartbeat return value as 'connectivity'" — `expect(source).toMatch(/const\\s+connectivity\\s*=\\s*useHeartbeat\\(\\)/)`
       - "renders <SubmitControls annotations={annotations} connectivity={connectivity}" — `expect(source).toMatch(/<SubmitControls\\s+annotations=\\{annotations\\}\\s+connectivity=\\{connectivity\\}/)`
       - "header style declares justifyContent: 'space-between'" — `expect(source).toContain("justifyContent: 'space-between'")`
       - "defines an OfflineBanner sub-component" — `expect(source).toMatch(/function OfflineBanner\\(\\)/)`
       - "OfflineBanner uses role=\"status\"" — assertions on the substring after `function OfflineBanner()` (find the function body via regex and assert `role="status"` is inside)
       - "OfflineBanner renders both banner lines" — `expect(source).toContain('{OFFLINE_BANNER_LINE_1}')` AND `expect(source).toContain('{OFFLINE_BANNER_LINE_2}')`
       - "conditionally renders OfflineBanner on connectivity === 'offline'" — `expect(source).toMatch(/connectivity\\s*===\\s*['\"]offline['\"]\\s*&&\\s*<OfflineBanner/)`
       - "preserves existing OutlinePane / ContentPane / CommentPane wiring" — `expect(source).toContain('<OutlinePane')` AND `expect(source).toContain('<ContentPane')` AND `expect(source).toContain('<CommentPane')`

       Do NOT modify the EXISTING two describe blocks (`describe('ReviewerV2Shell', ...)` and `describe('ReviewerV2Shell editingId + annotationCounts wiring (Phase 21)', ...)`) — those are regression guards. All their assertions must still pass after the Step A changes.

    Step C — Edit `ui/src/index.css`:

    8. Append the following CSS block at the end of the file (or in the Phase-specific section if one exists):
       ```css
       /* Phase 22: SubmitControls button transitions */
       .submit-btn {
         transition: background 0.1s ease, opacity 0.1s ease;
       }
       ```
       Note: SubmitControls.tsx (Plan 03) does NOT currently set `className="submit-btn"` on its buttons — the UI-SPEC introduces this class as an optional hook for hover/disabled transitions. If desired, the executor can add `className="submit-btn"` to the Approve and Send Feedback buttons in `SubmitControls.tsx` for consistency with the CSS rule; otherwise the rule is dormant but still satisfies the UI-SPEC §CSS Changes contract. Since the buttons already animate inline via `onMouseOver` / `onMouseOut`, this CSS rule is a no-op safety net; do NOT modify SubmitControls.tsx to add the className unless `cd ui && npm test -- --run reviewer-v2/SubmitControls` already passes — modifying it would otherwise risk breaking the source-contract assertions already locked in by Plan 03.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run reviewer-v2/ReviewerV2Shell && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const connectivity = useHeartbeat()" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 1
    - `grep -c "import SubmitControls from './SubmitControls'" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 1
    - `grep -c "from './useHeartbeat'" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 1
    - `grep -E "OFFLINE_BANNER_LINE_1.*OFFLINE_BANNER_LINE_2|OFFLINE_BANNER_LINE_2.*OFFLINE_BANNER_LINE_1" ui/src/reviewer-v2/ReviewerV2Shell.tsx` matches at least once (both constants imported)
    - `grep -c "function OfflineBanner" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 1
    - `grep -c "justifyContent: 'space-between'" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 1
    - `grep -E "connectivity\\s*===\\s*'offline'\\s*&&\\s*<OfflineBanner" ui/src/reviewer-v2/ReviewerV2Shell.tsx` matches at least once
    - `grep -c "<SubmitControls annotations={annotations} connectivity={connectivity}" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 1
    - `grep -c "<OutlinePane" ui/src/reviewer-v2/ReviewerV2Shell.tsx` >= 1 (Phase 19 wiring preserved)
    - `grep -c "<ContentPane" ui/src/reviewer-v2/ReviewerV2Shell.tsx` >= 1 (Phase 18 wiring preserved)
    - `grep -c "<CommentPane" ui/src/reviewer-v2/ReviewerV2Shell.tsx` >= 1 (Phase 20 wiring preserved)
    - `grep -c ".submit-btn" ui/src/index.css` >= 1 (UI-SPEC CSS rule added)
    - `grep -c "transition: background" ui/src/index.css` >= 1
    - `cd ui && npm test -- --run reviewer-v2/ReviewerV2Shell` exits 0 — BOTH existing describe blocks AND the new Phase 22 describe block pass
    - `cd ui && npm test` exits 0 (full v2 test suite is green — including Plan 01, 02, 03, and existing Phases 17-21 tests)
    - `cd ui && npm run lint` exits 0 (ARCH-01 ESLint rule still satisfied)
    - `cd ui && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ReviewerV2Shell mounts SubmitControls in the header right slot, conditionally renders OfflineBanner above the body when offline, captures connectivity from useHeartbeat, preserves all existing Phase 17-21 wiring, and the test suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the full SUBMIT-01 + SUBMIT-02 user flow</name>
  <files>(no files — verification only)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (SUBMIT-01 — authoritative source for the two disabled-gate behaviors that Gates 1, 2, and the new Gate 2b verify)
    - .planning/phases/22-submit-clipboard/22-UI-SPEC.md (visual and interaction contract — gates 1-9 check this directly)
    - .planning/phases/22-submit-clipboard/22-RESEARCH.md (Common Pitfalls 3 + 4 — the JSON shape Gate 6 / 7 must verify and the transient-activation requirement Gate 6 implicitly validates)
    - ui/src/reviewer-v2/SubmitControls.tsx (the component under verification — read once to confirm the rendered structure matches expectations)
    - ui/src/reviewer-v2/SubmitPopover.tsx (popover behavior — Gate 8 dismiss flow)
  </read_first>
  <what-built>
    Phase 22 is now wired end-to-end:
    - SubmitControls (Approve + Send Feedback ▾) sits in the top-right of the /v2 header.
    - Approve is HTML-disabled when one or more comments exist; enabled when zero comments.
    - Send Feedback is HTML-disabled when zero comments exist; enabled when one or more comments exist (SUBMIT-01: Ask for changes is disabled when no comments exist).
    - When enabled, Send Feedback opens a popover with an autoFocused optional-message textarea.
    - Online submission POSTs to /api/decide; offline submission writes to the clipboard using buildClipboardPayload.
    - Inline confirmations (Approved / Feedback sent / Copied to clipboard / Clipboard write failed) replace the buttons after submit.
    - OfflineBanner appears between the header and the body when the server is unreachable.
  </what-built>
  <how-to-verify>
    Run the app in dev mode:

    1. From the repo root: `cd ui && npm run dev` (this starts Vite on the default port, typically 5173).
    2. In a separate terminal, start the Rust backend: `cargo run -- review test-plan.md` (or any markdown file). When the browser opens, navigate to `http://127.0.0.1:<port>/v2` (the port is in the Rust log output; the Vite dev server proxies /api/* to it).
    3. **GATE 1 — Approve enabled AND Send Feedback disabled with zero comments (SUBMIT-01 dual-gate):**
       - Open /v2 with no comments in the document.
       - Observe the top-right of the header: a green "Approve" button (fully opaque, pointer cursor on hover) and a grey "Send Feedback ▾" button.
       - Observe the Send Feedback button: it is visibly dimmed (`opacity: 0.4`), with the native browser cursor (not pointer). Hovering it shows the tooltip "Add at least one comment to send feedback".
       - Try clicking Send Feedback — nothing happens. No popover appears. Try Tab-focusing it and pressing Enter — nothing happens.
       - Hover the Approve button — background darkens to `--color-accent-approve-hover`.
       - Click Approve. Within ~500ms, the buttons disappear and the inline "Approved — You can close this tab." confirmation appears. The browser may auto-close the tab; if the browser policy blocks it, the confirmation stays visible.
       - Verify the Rust process printed `{"behavior":"allow"}` to stdout.
    4. **GATE 2 — Approve disabled AND Send Feedback enabled with one or more comments (SUBMIT-01 dual-gate):**
       - Reload /v2 (relaunch the dev server with a fresh plan if needed).
       - Add at least one comment via the existing Phase 21 quick actions (paragraph hover → Comment → type something → submit).
       - Observe the Approve button: visibly dimmed (`opacity: 0.4`), with the native browser cursor (not pointer). Hovering it shows the tooltip "Cannot approve while comments exist".
       - Try clicking Approve — nothing happens. Try Tab-focusing it and pressing Enter — nothing happens.
       - Observe the Send Feedback button: fully opaque, pointer cursor on hover, no tooltip. (Both gates have flipped.)
    5. **GATE 3 — Send Feedback popover, online path:**
       - With at least one comment in place, click "Send Feedback ▾".
       - A popover appears below the button, ~320px wide, with a textarea that has autofocus and the placeholder "Leave a message (optional)".
       - Type an overall note (e.g. "Looks good but tighten the wording.").
       - Press Cmd+Enter (macOS) or Ctrl+Enter (Win/Linux). Alternatively, click the red "Send Feedback" button inside the popover.
       - Within ~500ms, the buttons are replaced by the inline "Feedback sent — You can close this tab." confirmation.
       - Verify the Rust process printed `{"behavior":"deny","message":"# Plan Feedback\\n\\nI've reviewed this plan and have 2 pieces of feedback:\\n\\n## 1. Looks good but tighten the wording.\\n\\n## 2. [COMMENT] Feedback on: \"<anchor text>\"\\n> <comment text>\\n\\n---\\n\\n## Summary\\n\\n- **COMMENT**: 1"}` (or equivalent — the message format is what `serializeAnnotations` produces).
    6. **GATE 4 — Send Feedback with no overall message:**
       - Reload /v2 with at least one comment.
       - Click "Send Feedback ▾", do NOT type anything, click the red "Send Feedback" button.
       - Submission proceeds; the deny message contains only the annotation, no overall section.
    7. **GATE 5 — Offline banner appears when server is unreachable:**
       - With /v2 open, kill the Rust backend process (Ctrl+C in the Rust terminal).
       - Wait up to 15 seconds (3 consecutive failed pings × 5s interval).
       - An amber banner with the text "Server connection lost — working offline." and "Clipboard submit is available." appears between the header and the body.
    8. **GATE 6 — Offline approve writes to clipboard:**
       - With the backend still down and at least zero comments, click Approve (if 0 comments — re-add 0).
       - The "Copied to clipboard — Paste into your Claude conversation." confirmation appears.
       - Open a text editor, paste — the clipboard contains exactly `{"behavior":"allow"}`.
    9. **GATE 7 — Offline ask-for-changes writes to clipboard:**
       - Re-add at least one comment.
       - Click Send Feedback ▾, type a message or leave empty, click Send Feedback inside the popover.
       - The "Copied to clipboard" confirmation appears.
       - Paste into a text editor — the JSON has `"behavior":"deny"` and a `"message"` field containing the serialized annotations (and the overall message if you typed one).
    10. **GATE 8 — Popover dismiss behavior:**
       - Reload /v2 (start the backend again).
       - Add a comment, click Send Feedback ▾.
       - Press Escape — the popover closes without submitting.
       - Click Send Feedback ▾ again, type "test", click somewhere outside the popover — the popover closes without submitting. Click Send Feedback ▾ again — the textarea is empty (state was reset).
    11. **GATE 9 — Existing /v2 functionality regression check:**
       - Verify the outline tree (left column) still tracks the active section as you scroll.
       - Verify hovering a comment in the right column still highlights its anchor in the middle column.
       - Verify clicking the pencil icon on a comment opens the inline edit textarea.
       - Verify deleting a comment via the × icon updates the outline section count.

    If ANY gate fails, describe which gate failed and what went wrong. The orchestrator will route to debug-phase or replan as appropriate.
  </how-to-verify>
  <action>Pause execution and present the verification steps in `<how-to-verify>` to the user. Wait for an explicit approval signal before marking Phase 22 complete. This is a checkpoint:human-verify gate — no autonomous file edits, no automated grep can substitute for the human running through gates 1-9 with the dev server and the Rust backend live.</action>
  <verify>
    <automated>cd ui && npm run dev (background) and `cargo run -- review &lt;some-plan.md&gt;` in another terminal; verification itself is performed by the user reporting results for gates 1-9. No grep / cargo / npm test command can substitute for the full-flow run because gates 5-7 require killing the backend mid-session and gates 1-4 require visual confirmation of disabled state and inline confirmations.</automated>
  </verify>
  <acceptance_criteria>
    - User reports "approved" indicating all 9 gates passed in the live dev environment.
    - Alternative pass form: user reports a specific gate failure with reproducible details (then this task fails and a gap-closure plan is opened).
    - Behavior: With 0 comments, clicking Approve produces `{"behavior":"allow"}` on the Rust stdout (online path) OR on the clipboard (offline path).
    - Behavior (SUBMIT-01): With 0 comments, the Send Feedback button is HTML-disabled — clicking it via mouse or Tab+Enter does NOT open the popover and produces no submission. The button is visibly dimmed (`opacity: 0.4`) and shows the tooltip "Add at least one comment to send feedback" on hover.
    - Behavior (SUBMIT-01): With >= 1 comment, the Approve button is HTML-disabled (clicking it via mouse or Tab+Enter has no effect) AND the Send Feedback button is enabled (clicking it opens the popover).
    - Behavior: Send Feedback (when enabled) opens the popover and the popover's textarea has focus on first open.
    - Behavior: Send Feedback submit produces `{"behavior":"deny","message": ...}` where `message` is the output of `serializeAnnotations(overallMessage, '', annotations)` on the Rust stdout (online) or on the clipboard (offline).
    - Behavior: OfflineBanner appears within 15s of the backend being killed and disappears within 15s of the backend coming back online.
  </acceptance_criteria>
  <done>User explicitly types "approved" in response to the verification prompt. If any gate fails, the user describes the failing gate(s) and observed behavior; the executor opens a gap-closure ticket rather than marking Phase 22 complete.</done>
  <resume-signal>Type "approved" if all 9 gates pass, or describe the failing gate (e.g. "Gate 6 failed: clipboard contained `{\"behavior\":\"allow\",\"notes\":\"\"}` instead of `{\"behavior\":\"allow\"}`").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useHeartbeat return value → connectivity-conditional render | Untrusted (server-down can be spoofed by network failure) but action-only consequence is "switch to clipboard fallback" — no privilege escalation. |
| OfflineBanner text → DOM | Static string literals from `./offlineLabels` (Plan 01 already mitigated this — see T-22-01). |
| SubmitControls props passthrough → child component | `annotations` and `connectivity` flow from Shell state to SubmitControls; the source values are owned by `useAnnotations` and `useHeartbeat` hooks already vetted in Phases 17 and 13. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Spoofing | useHeartbeat → connectivity state | accept | useHeartbeat was hardened in Phase 13 (3 consecutive failures, AbortSignal.timeout, generation guards). The offline mode is a UX fallback, not a security boundary — a spoofed offline state at worst forces the user through the clipboard path, which is also legitimate. |
| T-22-15 | Tampering | OfflineBanner copy | mitigate | Identical to T-22-01 — banner constants are compile-time literals; React text-node escaping prevents injection. |
| T-22-16 | Information Disclosure | OfflineBanner visibility | accept | The banner reveals only the connectivity state (server reachable / not). No tokens, paths, or user data exposed. |
| T-22-17 | Denial of Service | Conditional `<OfflineBanner />` re-render on each heartbeat tick | mitigate | useHeartbeat (Phase 13) uses an Object.is bail-out: setStatus is only called on actual transitions (online↔offline). The conditional render in Shell triggers at most once per transition, not once per 5s tick. |
| T-22-18 | Tampering | Header layout change (justifyContent: space-between) | accept | The change is a static style key; no user input. The "Reviewer v2" label stays in the same position; SubmitControls is appended in the right slot. |
| T-22-19 | Elevation of Privilege | SubmitControls receives annotations + connectivity props | accept | Both props are derived from local React state, not from external input. The flow is: user adds annotation → useAnnotations dispatches → re-render → SubmitControls receives new annotations.length. No privileged operation accessible. |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed in this plan. |
</threat_model>

<verification>
- `cd ui && npm test` passes — full Vitest suite green.
- `cd ui && npm run lint` passes — ARCH-01 ESLint rule still satisfied.
- `cd ui && npx tsc --noEmit` passes — TypeScript compile clean.
- Human verification (Task 3 gates 1-9) all return "approved" — including SUBMIT-01 dual-gate behavior verified in Gate 1 (Send Feedback disabled with zero comments) and Gate 2 (Approve disabled with comments + Send Feedback enabled).
- Manual file size check on `ReviewerV2.tsx` < 300 bytes (clean pass-through).
- Manual grep: no `void useHeartbeat` or `void useAnnotations` anywhere in `ui/src/reviewer-v2/`.
</verification>

<success_criteria>
- /v2 renders the Approve + Send Feedback button group in the top-right of the header.
- Approve is HTML-disabled (with opacity + tooltip) when comments exist.
- Send Feedback is HTML-disabled (with opacity + tooltip) when no comments exist (SUBMIT-01).
- When enabled, Send Feedback opens the popover with autoFocus textarea, dismissable via Escape or outside-click.
- Online submission POSTs the correct JSON to /api/decide (`{ behavior: 'allow' }` or `{ behavior: 'deny', message }`).
- Offline submission writes the byte-identical clipboard JSON using `buildClipboardPayload`.
- OfflineBanner appears between header and body when connectivity transitions to 'offline'.
- All four inline confirmations render with the exact UI-SPEC copy.
- `clipboard_error` state shows a read-only textarea with the JSON for manual copy.
- All existing Phase 17-21 functionality (outline, content, comments, sections, edit/delete) remains intact.
- Human verifier signs off on all 9 gates in Task 3, with Gate 1 explicitly confirming Send Feedback is disabled with zero comments and Gate 2 confirming Approve is disabled with comments.
</success_criteria>

<output>
Create `.planning/phases/22-submit-clipboard/22-04-SUMMARY.md` when done.
</output>
