---
phase: 27-inline-comments
plan: 1
type: tdd
wave: 1
depends_on: []
files_modified:
  - ui/src/code-review/types.ts
  - ui/src/code-review/hooks/useCodeReviewAnnotations.ts
  - ui/src/code-review/hooks/useCodeReviewAnnotations.test.ts
autonomous: true
requirements:
  - COMMENT-03
tags:
  - react
  - reducer
  - tdd
  - code-review

must_haves:
  truths:
    - "reduceAnnotations(state, { type: 'ADD_COMMENT', comment }) returns a new array with the comment appended."
    - "reduceAnnotations(state, { type: 'EDIT_COMMENT', id, text }) updates only the text field of the matching id; other fields unchanged."
    - "reduceAnnotations(state, { type: 'DELETE_COMMENT', id }) removes only the matching id."
    - "EDIT_COMMENT and DELETE_COMMENT with an unknown id return state structurally equivalent to input (no error, no mutation)."
    - "useCodeReviewAnnotations() returns { comments, addComment, editComment, deleteComment } with stable dispatch references across renders."
    - "CodeReviewComment discriminated union compiles with both 'line' and 'file' variants and is exported from types.ts."
  artifacts:
    - path: "ui/src/code-review/types.ts"
      provides: "CodeReviewComment discriminated union type (line | file)"
      contains: "export type CodeReviewComment"
    - path: "ui/src/code-review/hooks/useCodeReviewAnnotations.ts"
      provides: "reduceAnnotations pure function + useCodeReviewAnnotations React hook + CommentAction type"
      exports:
        - "reduceAnnotations"
        - "useCodeReviewAnnotations"
        - "CommentAction"
    - path: "ui/src/code-review/hooks/useCodeReviewAnnotations.test.ts"
      provides: "Pure reducer tests covering ADD_COMMENT, EDIT_COMMENT, DELETE_COMMENT, edit-unknown, delete-unknown, and discriminated-union variants"
      min_lines: 60
  key_links:
    - from: "ui/src/code-review/hooks/useCodeReviewAnnotations.ts"
      to: "ui/src/code-review/types.ts"
      via: "import type { CodeReviewComment }"
      pattern: "import type \\{ CodeReviewComment \\} from '../types'"
    - from: "ui/src/code-review/hooks/useCodeReviewAnnotations.test.ts"
      to: "ui/src/code-review/hooks/useCodeReviewAnnotations"
      via: "import { reduceAnnotations } from './useCodeReviewAnnotations'"
      pattern: "from './useCodeReviewAnnotations'"
---

<objective>
Land the type model and pure reducer for Phase 27 inline comments BEFORE any UI is built (TDD foundation). All comment state lives in a flat `CodeReviewComment[]` array driven by a discriminated union (line vs file). The reducer is exported as a pure function so tests run without React.

Purpose: COMMENT-03 (edit/delete logic) is the single source of truth for mutating comments. Plans 27-02 and 27-03 import these types and the hook — they cannot start until this plan ships green tests.
Output: Three files committed with `npm test` green. Zero React renderer dependencies. Zero new npm packages.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/27-inline-comments/27-CONTEXT.md
@.planning/phases/27-inline-comments/27-RESEARCH.md
@.planning/phases/27-inline-comments/27-PATTERNS.md

@ui/src/code-review/types.ts
@ui/src/reviewer-v2/useAnnotations.ts
@ui/src/reviewer-v2/useAnnotations.test.ts

<interfaces>
<!-- Contracts that plans 27-02 and 27-03 will consume. Implement these exact shapes. -->

From ui/src/code-review/types.ts (append, do not modify existing FileDiff/Commit):

```typescript
export type CodeReviewComment =
  | {
      id: string
      type: 'line'
      file: string
      side: 'additions' | 'deletions'
      lineNumber: number
      text: string
      createdAt: string
    }
  | {
      id: string
      type: 'file'
      file: string
      text: string
      createdAt: string
    }
```

From ui/src/code-review/hooks/useCodeReviewAnnotations.ts (new file):

```typescript
export type CommentAction =
  | { type: 'ADD_COMMENT'; comment: CodeReviewComment }
  | { type: 'EDIT_COMMENT'; id: string; text: string }
  | { type: 'DELETE_COMMENT'; id: string }

export function reduceAnnotations(
  state: CodeReviewComment[],
  action: CommentAction,
): CodeReviewComment[]

export function useCodeReviewAnnotations(): {
  comments: CodeReviewComment[]
  addComment: (comment: CodeReviewComment) => void
  editComment: (id: string, text: string) => void
  deleteComment: (id: string) => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CodeReviewComment discriminated union to types.ts (RED-GREEN-REFACTOR — types first, drives test fixture compilation)</name>
  <files>ui/src/code-review/types.ts</files>
  <read_first>
    - ui/src/code-review/types.ts (current state — verify `FileDiff` and `Commit` exports, do not regress them)
    - .planning/phases/27-inline-comments/27-CONTEXT.md (D-07 — exact union shape)
    - .planning/phases/27-inline-comments/27-RESEARCH.md (Code Examples — Type Definitions)
    - .planning/phases/27-inline-comments/27-PATTERNS.md (`types.ts` section — adapt the analog union shape from reviewer-v2/types.ts)
  </read_first>
  <behavior>
    - Test 1: `import type { CodeReviewComment } from './types'` compiles without `any` casts.
    - Test 2: Both line and file variants assign to `CodeReviewComment`; discriminating on `type` narrows correctly (line variant has `side` and `lineNumber`; file variant does not).
    - Test 3: `FileDiff` and `Commit` exports remain present and unchanged.
  </behavior>
  <action>
    Append the `CodeReviewComment` discriminated union to ui/src/code-review/types.ts per D-07. The line variant has fields `id: string`, `type: 'line'`, `file: string`, `side: 'additions' | 'deletions'`, `lineNumber: number`, `text: string`, `createdAt: string`. The file variant has fields `id: string`, `type: 'file'`, `file: string`, `text: string`, `createdAt: string` (no `side`, no `lineNumber`). Do NOT touch existing `FileDiff` or `Commit` exports. Do NOT import anything from `reviewer-v2/`. Keep the existing snake_case JSON comments above `FileDiff` and `Commit` intact.
  </action>
  <verify>
    <automated>cd ui && npm run tsc --silent 2>&1 | grep -v "^npm" | head -20; npx tsc --noEmit -p . 2>&1 | grep -E "error TS" | grep "types.ts" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `ui/src/code-review/types.ts` contains string `export type CodeReviewComment`.
    - File contains both `type: 'line'` and `type: 'file'` discriminants in the same union.
    - File contains `side: 'additions' | 'deletions'` exactly once (line variant only).
    - File still contains `export interface FileDiff` and `export interface Commit` (existing exports preserved).
    - `cd ui && npx tsc --noEmit -p .` exits 0 (no TypeScript errors anywhere; existing tests still typecheck).
    - File does NOT contain the string `reviewer-v2`.
  </acceptance_criteria>
  <done>
    Union type added; tsc passes; existing exports untouched. Plan 27-02 can `import type { CodeReviewComment } from '../types'` against this contract.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — write reducer tests FIRST, then implement reduceAnnotations + useCodeReviewAnnotations</name>
  <files>
    ui/src/code-review/hooks/useCodeReviewAnnotations.test.ts,
    ui/src/code-review/hooks/useCodeReviewAnnotations.ts
  </files>
  <read_first>
    - ui/src/reviewer-v2/useAnnotations.test.ts (analog test file — copy the shape, NOT the imports)
    - ui/src/reviewer-v2/useAnnotations.ts (analog reducer — note: this analog wraps state in `{ annotations: [] }`; Phase 27 does NOT wrap — see PATTERNS.md "Critical difference from analog")
    - ui/src/code-review/types.ts (after Task 1 completes — `CodeReviewComment` is the fixture type)
    - .planning/phases/27-inline-comments/27-CONTEXT.md (D-08 — action names ADD_COMMENT/EDIT_COMMENT/DELETE_COMMENT)
    - .planning/phases/27-inline-comments/27-RESEARCH.md ("Code Examples" — Reducer section)
    - .planning/phases/27-inline-comments/27-PATTERNS.md ("`ui/src/code-review/hooks/useCodeReviewAnnotations.test.ts`" + ".ts" sections — required test cases)
  </read_first>
  <behavior>
    RED phase — write these tests FIRST in `useCodeReviewAnnotations.test.ts` (they MUST fail before the implementation file exists):
    - Test A: `reduceAnnotations([], { type: 'ADD_COMMENT', comment: lineFixture })` returns a 1-element array containing the line fixture (identity preserved on the object).
    - Test B: Adding then editing — `EDIT_COMMENT` updates `text` to the new value; `id`, `file`, `type`, `side`, `lineNumber`, `createdAt` all unchanged.
    - Test C: Adding then deleting — `DELETE_COMMENT` removes the matching id; result has length 0.
    - Test D: `EDIT_COMMENT` with unknown id — state is structurally equivalent (length unchanged, text unchanged).
    - Test E: `DELETE_COMMENT` with unknown id — state is structurally equivalent (length unchanged, original id still present).
    - Test F: Discriminated union — adding a `type: 'file'` fixture (no `side`, no `lineNumber`) compiles AND the result preserves the file variant; adding both line and file variants in sequence yields a 2-element array with the two variants distinguishable by `type`.
    - Test G: Reducer is pure — calling `reduceAnnotations(input, action)` does NOT mutate `input` (use `Object.freeze(input)` or compare reference identity to assert).

    GREEN phase — implement the reducer + hook to satisfy the tests:
    - Pure `reduceAnnotations(state: CodeReviewComment[], action: CommentAction): CodeReviewComment[]` using switch on `action.type`. All branches return new arrays (spread/map/filter — no mutation).
    - `CommentAction` union: `ADD_COMMENT { comment }`, `EDIT_COMMENT { id, text }`, `DELETE_COMMENT { id }`.
    - `useCodeReviewAnnotations()` hook wraps `useReducer(reduceAnnotations, [])`. Returns `{ comments, addComment, editComment, deleteComment }`. Each dispatch helper closes over `dispatch` (stable React reference — no `useCallback` needed because `dispatch` is already stable).

    Constraints:
    - No `@testing-library/react` import. Tests import `reduceAnnotations` and call it directly — no React renderer.
    - No mutation in reducer body (assert via `Object.freeze(state)` in at least one test).
    - No import from `reviewer-v2/`. Tests must compile under the ESLint `no-restricted-imports` rule.
    - Initial state is `[]` (flat array, NOT `{ annotations: [] }` — PATTERNS.md "Critical difference from analog").
  </behavior>
  <action>
    Create both files under `ui/src/code-review/hooks/`. Write the test file first (RED). Confirm it fails with `npm test` (module not found / `reduceAnnotations is not defined`). Then create `useCodeReviewAnnotations.ts` exporting (1) `CommentAction` union, (2) `reduceAnnotations` pure function with three branches (`ADD_COMMENT`, `EDIT_COMMENT`, `DELETE_COMMENT`), (3) `useCodeReviewAnnotations` hook using `useReducer(reduceAnnotations, [])` returning `{ comments, addComment, editComment, deleteComment }`. Re-run tests — they MUST pass (GREEN). Refactor only if a duplicate branch can be merged; keep the switch exhaustive. Use the exact action names from D-08 (uppercase, underscore — `ADD_COMMENT`, not `add`). Use `crypto.randomUUID()` is NOT this plan's concern — the caller supplies the full comment object including `id`. Reducer never generates IDs.

    Test file MUST follow PATTERNS.md test-pattern (vitest, no `@testing-library/react`, no `readFileSync` source-text matching — these are behavior tests against the pure function). Use two fixtures: a `lineFixture: CodeReviewComment` (type='line') and a `fileFixture: CodeReviewComment` (type='file'). Cover all seven behavior cases A-G above. Add at least one `Object.freeze` assertion to lock immutability.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run src/code-review/hooks/useCodeReviewAnnotations.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/hooks/useCodeReviewAnnotations.test.ts` exists and contains the strings `'ADD_COMMENT'`, `'EDIT_COMMENT'`, `'DELETE_COMMENT'`.
    - Test file contains a frozen-input assertion: `Object.freeze(` appears at least once.
    - Test file does NOT contain `@testing-library/react` (grep -v '^//' filtered out comments).
    - Test file does NOT contain `reviewer-v2`.
    - `ui/src/code-review/hooks/useCodeReviewAnnotations.ts` exists and exports `reduceAnnotations`, `useCodeReviewAnnotations`, and a type `CommentAction`.
    - Implementation file imports `useReducer` from `'react'` (exact string).
    - Implementation file imports `CodeReviewComment` type from `'../types'` (exact relative path).
    - Implementation does NOT contain the string `mutate` or array index assignment (`state[`); reducer body uses spread/map/filter only.
    - `cd ui && npm test -- --run src/code-review/hooks/useCodeReviewAnnotations.test.ts` exits 0 with at least 7 passing test cases.
    - `cd ui && npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>
    Reducer + hook + tests committed. Tests green. The contract for Plan 27-02 (`addComment(comment)`, `editComment(id, text)`, `deleteComment(id)`) is exported and verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Comment state is React in-memory, session-only, single-user, never leaves the browser tab. No network I/O. No serialization. No user input crosses a privilege boundary inside this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | reducer state mutation | mitigate | Reducer is a pure function returning new arrays/objects on every branch; Object.freeze assertion in test B locks immutability. React `useReducer` identity-checks results so mutation would silently break re-renders. |
| T-27-02 | Information disclosure | comment text (PII risk) | accept | Comments are user-typed strings rendered only back to the same local user in the same tab; never serialized or transmitted in this phase. Phase 28 will introduce the serialization boundary and a separate threat model applies there. |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH.md "Package Legitimacy Audit" confirms zero new packages. No install tasks in this plan. Slopcheck not applicable. |
</threat_model>

<verification>
- `cd ui && npm test -- --run src/code-review/hooks/useCodeReviewAnnotations.test.ts` — all reducer behavior tests pass.
- `cd ui && npx tsc --noEmit -p .` — TypeScript compiles with zero errors.
- `cd ui && npm run lint` — ESLint passes (verifies the `no-restricted-imports` reviewer-v2 block is honored).
- Source-grep gates listed in `<acceptance_criteria>` per task all hold (use `grep -v '^\s*//' file | grep -c token` for comment-aware counts).
</verification>

<success_criteria>
- `CodeReviewComment` discriminated union exported from `ui/src/code-review/types.ts`; consumers in plans 27-02 and 27-03 can import it without `any`.
- `reduceAnnotations` is a pure function — adding/editing/deleting comments returns new arrays; mutation tests pass under `Object.freeze`.
- `useCodeReviewAnnotations` hook exposes `{ comments, addComment, editComment, deleteComment }` ready for `CodeReviewApp` to consume in plan 27-03.
- Tests are React-renderer-free — they exercise the pure reducer directly; this proves COMMENT-03 (edit/delete logic) without UI coupling.
- All files live under `ui/src/code-review/`; ESLint coupling rule unbroken.
</success_criteria>

<output>
Create `.planning/phases/27-inline-comments/27-01-SUMMARY.md` per the template when done. Include:
- Files touched (types.ts diff range, new hook + test files)
- Test count delta (e.g., "+7 tests")
- Confirmation that `CodeReviewComment` and `useCodeReviewAnnotations` are now importable by downstream plans
</output>
