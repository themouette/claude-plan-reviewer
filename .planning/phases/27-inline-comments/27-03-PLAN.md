---
phase: 27-inline-comments
plan: 3
type: execute
wave: 3
depends_on:
  - 27-01
  - 27-02
files_modified:
  - ui/src/code-review/CodeReviewApp.tsx
  - ui/src/code-review/CodeReviewApp.test.ts
  - ui/src/code-review/FileListPane.tsx
  - ui/src/code-review/FileListPane.test.ts
autonomous: false
requirements:
  - COMMENT-01
  - COMMENT-02
  - COMMENT-03
  - COMMENT-04
tags:
  - react
  - state-integration
  - badges
  - code-review

must_haves:
  truths:
    - "CodeReviewApp owns the comments array via useCodeReviewAnnotations and passes comments + addLineComment + addFileComment + editComment + deleteComment to DiffPane."
    - "addLineComment / addFileComment generate a fresh id via crypto.randomUUID() and a createdAt timestamp via new Date().toISOString() before dispatching ADD_COMMENT."
    - "CodeReviewApp derives commentCounts: Record<string, number> from comments with useMemo([comments]); zero-comment files do not appear in the map (or appear with value 0)."
    - "FileListPane accepts commentCounts and renders a blue badge after the filename when count > 0; zero-comment files show NO badge."
    - "Comments survive switching between commits (selectedCommitShas changes do NOT reset the comments array)."
    - "Editing reopens the inline textarea in CommentBubble; saving updates only the text; deleting removes the bubble immediately with no confirmation dialog."
    - "Visual UI: a human verifies the four COMMENT-01..04 success criteria end-to-end in the browser."
  artifacts:
    - path: "ui/src/code-review/CodeReviewApp.tsx"
      provides: "Comments state ownership + commentCounts derivation + ID/timestamp generation for ADD_COMMENT"
      contains: "useCodeReviewAnnotations"
    - path: "ui/src/code-review/FileListPane.tsx"
      provides: "Comment count badge per file in the file list"
      contains: "commentCounts"
    - path: "ui/src/code-review/CodeReviewApp.test.ts"
      provides: "Source-text assertions for state wiring, useMemo derivation, randomUUID, prop passing"
      min_lines: 50
    - path: "ui/src/code-review/FileListPane.test.ts"
      provides: "Source-text assertions for badge rendering, count > 0 guard, accent color"
      min_lines: 30
  key_links:
    - from: "ui/src/code-review/CodeReviewApp.tsx"
      to: "ui/src/code-review/hooks/useCodeReviewAnnotations.ts"
      via: "useCodeReviewAnnotations() destructuring"
      pattern: "useCodeReviewAnnotations\\(\\)"
    - from: "ui/src/code-review/CodeReviewApp.tsx"
      to: "ui/src/code-review/DiffPane.tsx"
      via: "comments, onAddLineComment, onAddFileComment, onEditComment, onDeleteComment props"
      pattern: "onAddLineComment="
    - from: "ui/src/code-review/CodeReviewApp.tsx"
      to: "ui/src/code-review/FileListPane.tsx"
      via: "commentCounts prop"
      pattern: "commentCounts="
    - from: "ui/src/code-review/FileListPane.tsx"
      to: "ui/src/index.css var(--color-focus)"
      via: "badge background"
      pattern: "var\\(--color-focus\\)"
---

<objective>
Lift comment state into `CodeReviewApp`, derive `commentCounts`, wire badges into `FileListPane`, and verify the four COMMENT-01..04 success criteria end-to-end with a human checkpoint. After this plan ships, Phase 27 is feature-complete: users can add hunk comments, add file comments, edit and delete them, see counts in the file list, and comments persist across commit navigation.

Purpose: Closes the loop from the affordances built in plan 27-02 to actual persisted (session-scoped) state. The reducer from plan 27-01 finally backs real UI. Badge addition delivers COMMENT-04.
Output: Modified `CodeReviewApp`, modified `FileListPane`, all tests green, human-verified end-to-end behavior in the browser.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/27-inline-comments/27-CONTEXT.md
@.planning/phases/27-inline-comments/27-RESEARCH.md
@.planning/phases/27-inline-comments/27-PATTERNS.md
@.planning/phases/27-inline-comments/27-UI-SPEC.md

@.planning/phases/27-inline-comments/27-01-SUMMARY.md
@.planning/phases/27-inline-comments/27-02-SUMMARY.md
@ui/src/code-review/CodeReviewApp.tsx
@ui/src/code-review/CodeReviewApp.test.ts
@ui/src/code-review/FileListPane.tsx
@ui/src/code-review/FileListPane.test.ts
@ui/src/code-review/DiffPane.tsx
@ui/src/code-review/types.ts
@ui/src/code-review/hooks/useCodeReviewAnnotations.ts

<interfaces>
<!-- Existing contracts this plan consumes (from 27-01 + 27-02). -->

From ui/src/code-review/hooks/useCodeReviewAnnotations.ts (27-01):
```typescript
useCodeReviewAnnotations(): {
  comments: CodeReviewComment[]
  addComment: (comment: CodeReviewComment) => void
  editComment: (id: string, text: string) => void
  deleteComment: (id: string) => void
}
```

From ui/src/code-review/DiffPane.tsx (27-02 extended DiffPaneProps):
```typescript
comments?: CodeReviewComment[]
onAddLineComment?: (file: string, lineNumber: number, side: 'additions' | 'deletions', text: string) => void
onAddFileComment?: (file: string, text: string) => void
onEditComment?: (id: string, text: string) => void
onDeleteComment?: (id: string) => void
```

From ui/src/code-review/FileListPane.tsx (this plan extends FileListPaneProps):
```typescript
commentCounts?: Record<string, number>  // default {} — filename → count of submitted comments
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add commentCounts prop + badge rendering to FileListPane (+ tests)</name>
  <files>
    ui/src/code-review/FileListPane.tsx,
    ui/src/code-review/FileListPane.test.ts
  </files>
  <read_first>
    - ui/src/code-review/FileListPane.tsx (current — find `renderFileNode` and the basename span / change counts span; badge goes between them)
    - ui/src/code-review/FileListPane.test.ts (preserve all existing assertions)
    - .planning/phases/27-inline-comments/27-UI-SPEC.md ("File List Badge" section — background var(--color-focus), color #fff, borderRadius 10, fontSize 12, fontWeight 600, padding 2px 8px, marginLeft 8)
    - .planning/phases/27-inline-comments/27-PATTERNS.md ("FileListPane.tsx" section — badge insertion point)
    - .planning/phases/27-inline-comments/27-CONTEXT.md (D-10 — exact spec)
  </read_first>
  <action>
    A. Extend `FileListPaneProps` with `commentCounts?: Record<string, number>` (default `{}`). Default in destructuring: `commentCounts = {}`.

    B. In `renderFileNode`, after the basename `<span>` (the one wrapping `{basename}`) and BEFORE the change counts `<span>` (the one guarded by `showCounts`), insert:
    - A conditional `{(commentCounts[file.filename] ?? 0) > 0 && (<span aria-label={...} style={...}>{commentCounts[file.filename]}</span>)}`.
    - Style: `background: 'var(--color-focus)'`, `color: '#fff'`, `borderRadius: 10`, `fontSize: 12`, `fontWeight: 600`, `padding: '2px 8px'`, `marginLeft: 8`, `flexShrink: 0`.
    - No aria-label on the badge span (per UI-SPEC Copywriting Contract: "No aria-label on badge span — parent button's title carries full filename context"); the surrounding button already has a `title` attribute.

    C. Update `FileListPane.test.ts` — preserve EVERY existing assertion. Append Phase 27 assertions:
    - `expect(source).toContain('commentCounts')`
    - `expect(source).toContain('commentCounts[file.filename]')`
    - `expect(source).toContain('> 0')` (badge-guard condition)
    - `expect(source).toContain('var(--color-focus)')` (already present for active border; ensure badge style is in the same source)
    - `expect(source).toContain("borderRadius: 10")` (pill shape distinguishes badge from other elements)
    - `expect(source).toContain("padding: '2px 8px'")` (exact spec literal)
    - Source-order assertion: the badge span (`commentCounts[file.filename]`) must appear AFTER the basename span and BEFORE the change-counts (`showCounts`) span. Implement via `indexOf` checks (`expect(source.indexOf('commentCounts[file.filename]')).toBeGreaterThan(source.indexOf('{basename}'))` and `expect(source.indexOf('commentCounts[file.filename]')).toBeLessThan(source.indexOf('{showCounts'))` — adapt to the actual variable name as it appears).
  </action>
  <verify>
    <automated>cd ui && npm test -- --run src/code-review/FileListPane.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/FileListPane.tsx` contains the strings `commentCounts`, `commentCounts[file.filename]`, `> 0`.
    - File contains `borderRadius: 10` and `padding: '2px 8px'` (exact badge spec).
    - File contains `var(--color-focus)` AT LEAST twice (existing active-border + new badge background).
    - `FileListPaneProps` interface contains `commentCounts?: Record<string, number>` (or structurally equivalent).
    - `cd ui && npm test -- --run src/code-review/FileListPane.test.ts` exits 0; all existing assertions preserved.
    - `cd ui && npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>
    Badge rendering committed. FileListPane accepts `commentCounts` and renders blue pill counts where count > 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lift comments state into CodeReviewApp; derive commentCounts; wire DiffPane + FileListPane (+ tests)</name>
  <files>
    ui/src/code-review/CodeReviewApp.tsx,
    ui/src/code-review/CodeReviewApp.test.ts
  </files>
  <read_first>
    - ui/src/code-review/CodeReviewApp.tsx (current — locate `useState` declarations near the top, `useMemo` patterns, and the `<DiffPane .../>` + `<FileListPane .../>` JSX call sites)
    - ui/src/code-review/CodeReviewApp.test.ts (preserve all existing assertions; append Phase 27 assertions)
    - ui/src/code-review/hooks/useCodeReviewAnnotations.ts (27-01 contract — destructure `comments`, `addComment`, `editComment`, `deleteComment`)
    - ui/src/code-review/types.ts (`CodeReviewComment` discriminated union)
    - ui/src/code-review/DiffPane.tsx (27-02 — confirm Phase 27 prop names exactly: `comments`, `onAddLineComment`, `onAddFileComment`, `onEditComment`, `onDeleteComment`)
    - .planning/phases/27-inline-comments/27-CONTEXT.md (D-08, D-09, D-10)
    - .planning/phases/27-inline-comments/27-RESEARCH.md ("commentCounts derivation" + "Don't Hand-Roll" — use `crypto.randomUUID()`)
    - .planning/phases/27-inline-comments/27-PATTERNS.md ("CodeReviewApp.tsx" section — state + useMemo + prop passing)
  </read_first>
  <action>
    A. Imports. Add: `import { useMemo } from 'react'` to the existing `react` import (it currently imports `useEffect, useRef, useState`). Add: `import { useCodeReviewAnnotations } from './hooks/useCodeReviewAnnotations'`. Add: `import type { CodeReviewComment } from './types'`.

    B. Near the top of `CodeReviewApp` (after the existing `useState` declarations, before `useCommits()`), call the hook: `const { comments, addComment, editComment, deleteComment } = useCodeReviewAnnotations()`.

    C. Define two adder wrappers that generate `id` and `createdAt` BEFORE dispatching:
    - `function handleAddLineComment(file: string, lineNumber: number, side: 'additions' | 'deletions', text: string) { addComment({ id: crypto.randomUUID(), type: 'line', file, side, lineNumber, text, createdAt: new Date().toISOString() }) }`
    - `function handleAddFileComment(file: string, text: string) { addComment({ id: crypto.randomUUID(), type: 'file', file, text, createdAt: new Date().toISOString() }) }`

    D. Add `commentCounts` derivation with `useMemo`, dependency `[comments]`:
    ```
    const commentCounts = useMemo<Record<string, number>>(() => {
      const counts: Record<string, number> = {}
      for (const c of comments) {
        counts[c.file] = (counts[c.file] ?? 0) + 1
      }
      return counts
    }, [comments])
    ```

    E. Update the `<FileListPane ... />` JSX to add `commentCounts={commentCounts}` as a new prop (preserve all existing props).

    F. Update the `<DiffPane ... />` JSX to add five new props (preserve all existing props):
    - `comments={comments}`
    - `onAddLineComment={handleAddLineComment}`
    - `onAddFileComment={handleAddFileComment}`
    - `onEditComment={editComment}`
    - `onDeleteComment={deleteComment}`

    G. CONFIRM that nothing in the existing keyboard / commit-switching / selector logic resets the comments array. The reducer state is owned by `useCodeReviewAnnotations`'s `useReducer`, which is independent of `selectedCommitShas`, `files`, or `selector`. No code change needed for persistence — but DO NOT add any effect that clears `comments` on commit change. (This is the implementation of D-09.)

    H. Update `CodeReviewApp.test.ts` — preserve EVERY existing assertion. Append Phase 27 assertions:
    - `expect(source).toContain('useCodeReviewAnnotations')`
    - `expect(source).toContain("from './hooks/useCodeReviewAnnotations'")`
    - `expect(source).toContain('addComment')`
    - `expect(source).toContain('editComment')`
    - `expect(source).toContain('deleteComment')`
    - `expect(source).toContain('crypto.randomUUID()')` (id generation)
    - `expect(source).toContain('new Date().toISOString()')` (timestamp generation)
    - `expect(source).toContain('commentCounts')`
    - `expect(source).toContain('useMemo')`
    - `expect(source).toContain('counts[c.file]')` (loop body of commentCounts derivation)
    - `expect(source).toContain('commentCounts={commentCounts}')` (FileListPane prop)
    - `expect(source).toContain('comments={comments}')` (DiffPane prop)
    - `expect(source).toContain('onAddLineComment={handleAddLineComment}')`
    - `expect(source).toContain('onAddFileComment={handleAddFileComment}')`
    - `expect(source).toContain('onEditComment={editComment}')`
    - `expect(source).toContain('onDeleteComment={deleteComment}')`
    - Negative guard for persistence (D-09): `expect(source).not.toMatch(/(setComments|setComment)\s*\(\s*\[\s*\]\s*\)/)` — comments are never reset to empty by app code (only `DELETE_COMMENT` removes individual entries).
    - `expect(source).not.toContain('reviewer-v2')` (preserved).
  </action>
  <verify>
    <automated>cd ui && npm test -- --run src/code-review/CodeReviewApp.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/CodeReviewApp.tsx` contains `useCodeReviewAnnotations()`, `crypto.randomUUID()`, `new Date().toISOString()`, `useMemo`, `counts[c.file]`.
    - File contains `commentCounts={commentCounts}` exactly once (passed to FileListPane).
    - File contains all five DiffPane props: `comments={comments}`, `onAddLineComment={handleAddLineComment}`, `onAddFileComment={handleAddFileComment}`, `onEditComment={editComment}`, `onDeleteComment={deleteComment}`.
    - File does NOT contain a `setComments([])` or `setComment([])` reset call (D-09: comments survive commit navigation).
    - `cd ui && npm test -- --run src/code-review/CodeReviewApp.test.ts` exits 0; all existing (Phase 24/25/26/26.2) assertions preserved; Phase 27 assertions pass.
    - `cd ui && npx tsc --noEmit -p .` exits 0.
    - `cd ui && npm run lint` exits 0 (`code-review/`→`reviewer-v2/` block holds).
  </acceptance_criteria>
  <done>
    Comments state lifted to `CodeReviewApp`; `commentCounts` derived and wired to FileListPane; all five Phase 27 callbacks wired to DiffPane. Reducer survives commit navigation by construction.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite typecheck + lint + test sweep</name>
  <files>(no edits — verification only)</files>
  <read_first>
    - (none — this is a verification sweep)
  </read_first>
  <action>
    Run the full UI verification gate. If any of the three commands fail, fix the underlying source (do NOT modify tests to make them pass — these are existing contracts):
    1. `cd ui && npm run lint`
    2. `cd ui && npx tsc --noEmit -p .`
    3. `cd ui && npm test`

    Capture the final test count delta (compare to the count recorded in 27-02-SUMMARY).
  </action>
  <verify>
    <automated>cd ui && npm run lint && npx tsc --noEmit -p . && npm test 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui && npm run lint` exits 0 with no errors.
    - `cd ui && npx tsc --noEmit -p .` exits 0 with no errors.
    - `cd ui && npm test` exits 0 — all tests pass (existing + new Phase 27 tests).
    - Total test count is strictly greater than the count recorded in 27-02-SUMMARY (this plan adds at least the new FileListPane + CodeReviewApp Phase 27 assertions).
  </acceptance_criteria>
  <done>
    Code base is green end-to-end. Ready for human verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — COMMENT-01..04 end-to-end in browser</name>
  <files>ui/src/code-review/HunkCommentForm.tsx, ui/src/code-review/CommentBubble.tsx, ui/src/code-review/DiffPane.tsx, ui/src/code-review/FileListPane.tsx, ui/src/code-review/CodeReviewApp.tsx</files>
  <action>
    Pause here. Tasks 1-3 are complete (FileListPane badge, CodeReviewApp state lift + commentCounts derivation, full automated test gate green). The human now verifies the four Phase 27 success criteria (COMMENT-01..04) end-to-end in the browser using the steps in <how-to-verify> below. If anything fails, the executor patches and re-runs Tasks 1-3 before resuming this checkpoint.
  </action>
  <what-built>
    Phase 27 is complete in code. The user can now:
    - Hover a diff line in any non-binary file and see a `+` button in the gutter (COMMENT-01 affordance).
    - Click the `+` to open an inline `HunkCommentForm` anchored to that line.
    - Click `+ Comment` in any file header to open an inline `HunkCommentForm` above the diff body (COMMENT-02 affordance).
    - Submit a comment via the `Add Comment` button or `Cmd+Enter`; the form is replaced by a `CommentBubble` card with text, timestamp, and edit/delete icons.
    - Click the ✎ pencil to edit a comment inline (textarea re-appears with `Save Changes` / `Discard Changes`); click the × to delete immediately, no confirmation (COMMENT-03).
    - Switch between commits (single-click another commit in the drawer, or use ArrowLeft/ArrowRight) — comments persist (COMMENT-03 / D-09).
    - See a blue pill badge after each filename in the file list showing the comment count for that file; files with zero comments show NO badge (COMMENT-04).
  </what-built>
  <how-to-verify>
    Prerequisite: build the binary in dev mode so the UI is served fresh.

    1. From repo root, start the dev server (separate terminal):
       ```
       cd ui && npm run dev
       ```
       Note the localhost port Vite reports (typically 5173).

    2. In a SECOND terminal, start the Rust server pointing at the same repo so a real branch diff is fetched. Use any existing test command from this repo or the standard `cargo run -- review-hook` flow — whatever the developer normally uses to open the code review UI on this branch. (If unsure, the developer can just open the Vite dev server URL directly — `/api/diff/branch` will 404 against Vite but the empty-state UI is still navigable, and the developer can confirm affordances against the existing diff fixtures already in the codebase.)

    3. Navigate the browser to `http://127.0.0.1:5173/code-review` (or the URL the dev server reports). Confirm the diff loads.

    4. **COMMENT-01 (hunk anchor):**
       a. Hover any line in any non-binary file. Confirm a `+` button appears in the gutter on hover.
       b. Click the `+`. A `HunkCommentForm` should appear inline directly below that line, with an autofocused textarea labeled "Add a comment…".
       c. Type "test line comment" and press `Cmd+Enter` (macOS) or `Ctrl+Enter` (Linux).
       d. Confirm: the form is replaced by a `CommentBubble` containing "test line comment" + a timestamp + ✎ + × buttons.
       e. Confirm: the bubble has a 3px blue left border accent.

    5. **COMMENT-02 (file-level):**
       a. In the file header of any file (the row with the chevron + filename + change counts), look at the trailing area and confirm a `+ Comment` button appears.
       b. Click `+ Comment`. A `HunkCommentForm` should appear between the file header and the diff body.
       c. Type "test file comment" and click `Add Comment`.
       d. Confirm: a `CommentBubble` with "test file comment" appears above the diff body (NOT inside the diff lines).

    6. **COMMENT-03 (edit/delete):**
       a. Click the ✎ pencil on any comment bubble. The text is replaced by a textarea pre-filled with the current text, with `Save Changes` and `Discard Changes` buttons.
       b. Modify the text to "edited test" and press `Cmd+Enter` (or click `Save Changes`).
       c. Confirm: the bubble now shows "edited test".
       d. Click the × on the same bubble. Confirm: the bubble is removed immediately, no confirmation dialog.

    7. **COMMENT-04 (badge count):**
       a. With at least one comment on file A and zero comments on file B, look at the left file list sidebar.
       b. Confirm: file A's row shows a blue pill badge with the count after the filename.
       c. Confirm: file B's row shows NO badge.
       d. Add a second comment on file A. Confirm: the badge updates to `2`.

    8. **Persistence across commits (D-09):**
       a. Open the commits drawer (toolbar Commits toggle).
       b. With at least one comment on the current diff, click a different commit in the drawer.
       c. Confirm: the diff swaps to the new commit's files; comments from step 4-6 are still present (visible on the original file if it's part of this commit's diff; counted in the file list badge regardless).
       d. Click back to the original commit / all commits. Confirm comments still present.

    9. **Sanity:**
       a. Press `F12` (devtools) → Console. Confirm: NO React warnings or errors emitted while interacting.
       b. Press `F12` → Network. Confirm: NO new `/api/` calls fire when adding/editing/deleting comments (session state only — D-09).
       c. Refresh the page. Confirm: all comments are gone (session-only, no localStorage — D-09).
  </how-to-verify>
  <resume-signal>
    Type `approved` if all eight steps pass. Otherwise describe which step fails and what you observed; the planner/executor will iterate.
  </resume-signal>
  <verify>
    <human-check>User runs the 8-step browser walkthrough in &lt;how-to-verify&gt; and confirms (1) gutter `+` button appears on line hover and opens HunkCommentForm; (2) file-header `+ Comment` button opens HunkCommentForm above diff body; (3) submitted comments render as CommentBubble with edit/delete; (4) edit reopens textarea and Save Changes updates text; (5) delete removes bubble immediately with no confirmation; (6) file list badge appears after filenames with count &gt; 0 and is absent for count 0; (7) comments persist across commit navigation; (8) no React console warnings, no `/api/` calls on comment ops, comments cleared on page refresh.</human-check>
  </verify>
  <acceptance_criteria>
    - User responds `approved` (or equivalent explicit confirmation) after walking through all 8 steps in &lt;how-to-verify&gt;.
    - If any step fails, the user describes the failure; executor patches and re-runs Tasks 1-3 before re-issuing this checkpoint.
  </acceptance_criteria>
  <done>
    Human-confirmed: COMMENT-01..04 success criteria all met end-to-end in the browser. Phase 27 is feature-complete and ready for git commit / Phase 28 kickoff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→DOM | Comment text typed by the user is rendered back in `<p>{comment.text}</p>` (React-escaped). Pre-existing in 27-02. |
| (no network boundary in this plan) | Comments live in React state. No fetch / no clipboard / no localStorage. Phase 28 will introduce the serialization boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-XSS | Information disclosure / Tampering | CommentBubble text + timestamp rendering | mitigate | Inherited from 27-02 (React-escaped text content; no `dangerouslySetInnerHTML`). No change in this plan. |
| T-27-03-UUID | Spoofing | id collisions on rapid-add | mitigate | `crypto.randomUUID()` is collision-resistant (122 random bits). RESEARCH "Don't Hand-Roll" confirms availability in browser + jsdom. |
| T-27-03-COUNT | Tampering | commentCounts including pending state | mitigate | Per RESEARCH Pitfall 4, `commentCounts` is derived from the submitted `comments` array only. Pending state lives in `DiffPane`-local `pendingLineAnchor` / `pendingFileComment` and never enters the `comments` array (per RESEARCH Pattern 4 Option A). Acceptance criteria for 27-02 (`'__pending__'` sentinel) keeps pending out of the global array; this plan's `useMemo` iterates `comments` only. |
| T-27-03-PERSIST | Tampering | comments accidentally reset on commit nav | mitigate | The reducer state is owned by `useCodeReviewAnnotations`'s `useReducer`, which is independent of `selectedCommitShas` / `selector` / `files`. Negative source assertion in CodeReviewApp.test.ts forbids `setComments([])` or `setComment([])` resets. |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH "Package Legitimacy Audit" — zero new packages. No install tasks. |
</threat_model>

<verification>
- `cd ui && npm run lint && npx tsc --noEmit -p . && npm test` — all green (automated, Task 3).
- Human walks through the 8-step verification (Task 4) and types `approved` if all steps pass.
- Inspecting the DOM in step 9b confirms zero new `/api/` calls fire on add/edit/delete (proves D-09: session-only).
</verification>

<success_criteria>
- COMMENT-01: User can add a comment anchored to any diff hunk (verified step 4).
- COMMENT-02: User can add a comment at the whole-file level (verified step 5).
- COMMENT-03: User can edit or delete their own comments (verified step 6); comments persist across commit navigation (verified step 8).
- COMMENT-04: File list shows a comment count badge per file; zero-comment files show no badge (verified step 7).
- Session persistence: comments lost on page refresh, never written to disk or localStorage (verified step 9c).
- Architecture: zero imports from `reviewer-v2/`, zero new npm packages, full lint + tsc + test green.
</success_criteria>

<output>
Create `.planning/phases/27-inline-comments/27-03-SUMMARY.md` per the template when done. Include:
- Final test count (compare to 27-02-SUMMARY count)
- Confirmation of the human-verify outcome (paste the user's `approved` message or, if reworked, the issue + the patch)
- Note that Phase 27 closes COMMENT-01..04; Phase 28 will pick up SUBMIT-01..04 against the `comments: CodeReviewComment[]` array exposed here.
</output>
