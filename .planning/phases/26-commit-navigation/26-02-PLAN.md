---
phase: 26-commit-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - ui/src/code-review/CommitDrawer.tsx
  - ui/src/code-review/CommitDrawer.test.ts
  - ui/src/code-review/DiffPane.tsx
  - ui/src/code-review/DiffPane.test.ts
autonomous: true
requirements:
  - COMMIT-01
  - COMMIT-02
user_setup: []

must_haves:
  truths:
    - "CommitDrawer renders an aside overlay with role='navigation', aria-label='Branch commits', position:absolute, width 296px, zIndex 10"
    - "CommitDrawer renders one row per Commit: SHA chip (monospace), message (truncated), author · date metadata, and a checkbox"
    - "CommitRow active state uses borderLeft 2px solid var(--color-focus) and background var(--color-bg) (matches FileListPane active pattern)"
    - "Checkbox onChange and onClick call e.stopPropagation() so row-click navigation does not fire when the checkbox is clicked"
    - "CommitDrawer shows the same spinner (24x24 with @keyframes spin) as DiffPane when loading && commits.length === 0"
    - "CommitDrawer shows 'No commits on this branch' / 'This branch has no commits beyond the base branch.' empty state when commits.length === 0 && !loading"
    - "DiffPane accepts new optional props (viewMode, activeCommitSha, commits) and renders a commit title strip ({short_sha} — {message}\\n{author} · {date}) when viewMode === 'commit' and activeCommit is found in commits"
    - "DiffPane's existing four states (loading/error/empty/content) are unchanged; the title strip renders outside renderContent() and only when in 'commit' mode"
  artifacts:
    - path: "ui/src/code-review/CommitDrawer.tsx"
      provides: "Overlay drawer component listing commits with row click + checkbox interactions"
      contains: "export default function CommitDrawer"
    - path: "ui/src/code-review/CommitDrawer.test.ts"
      provides: "Source-text assertions covering structure, props, copy, and reviewer-v2 isolation"
      contains: "describe('CommitDrawer'"
    - path: "ui/src/code-review/DiffPane.tsx"
      provides: "DiffPaneProps extended with viewMode + activeCommitSha + commits; conditional title strip"
      contains: "viewMode: 'branch' | 'commit'"
    - path: "ui/src/code-review/DiffPane.test.ts"
      provides: "Existing tests preserved + new assertions for per-commit title strip"
      contains: "describe('DiffPane'"
  key_links:
    - from: "ui/src/code-review/CommitDrawer.tsx"
      to: "ui/src/code-review/types.ts"
      via: "type-only import of Commit"
      pattern: "from './types'"
    - from: "ui/src/code-review/DiffPane.tsx"
      to: "ui/src/code-review/types.ts"
      via: "Commit type used for activeCommit lookup"
      pattern: "Commit"
---

<objective>
Build the visible UI surface for commit navigation: the `CommitDrawer` overlay component (lists commits with row-click and checkbox interactions per D-05 / D-11 / D-13) AND extend `DiffPane` to render the per-commit title strip (D-06) when in `viewMode === 'commit'`.

Purpose: D-01 and D-11 require that CommitDrawer be a pure display component (state owned by CodeReviewApp, props flow down). D-06 mandates the per-commit indicator be a title strip in the DiffPane (not a toolbar badge). Both components must respect the established CSS variable tokens and Phase 25 visual contract.

Output: Two new files (CommitDrawer + test) and two modified files (DiffPane + test). Source-text assertion tests verify props shape, key tokens, and copy literals. No CodeReviewApp wiring yet — that lands in 26-03.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-commit-navigation/26-CONTEXT.md
@.planning/phases/26-commit-navigation/26-RESEARCH.md
@.planning/phases/26-commit-navigation/26-PATTERNS.md
@.planning/phases/26-commit-navigation/26-UI-SPEC.md
@.planning/phases/26-commit-navigation/26-01-SUMMARY.md

@ui/src/code-review/types.ts
@ui/src/code-review/FileListPane.tsx
@ui/src/code-review/DiffPane.tsx
@ui/src/code-review/DiffPane.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From ui/src/code-review/types.ts (created in 26-01):
```typescript
export interface Commit {
  sha: string
  short_sha: string
  message: string
  author: string
  email: string
  date: string
}
```

CommitDrawer props contract (the parent — CodeReviewApp — will pass these in 26-03):
```typescript
export interface CommitDrawerProps {
  commits: Commit[]
  loading: boolean
  error: string | null
  activeCommitSha: string | null
  checkedCommitShas: string[]
  onCommitClick: (sha: string) => void
  onCheckChange: (sha: string, checked: boolean) => void
}
```

DiffPane existing props (must be preserved AND extended):
```typescript
export interface DiffPaneProps {
  files: FileDiff[]
  loading: boolean
  error: string | null
  diffStyle: 'unified' | 'split'
  diffPaneRef: React.RefObject<HTMLDivElement | null>
  onReload: () => void
  // Phase 26 additions:
  viewMode?: 'branch' | 'commit'   // default 'branch' — preserves existing call sites
  activeCommitSha?: string | null  // default null
  commits?: Commit[]               // default [] — for title strip lookup
}
```

Active-row pattern from FileListPane.tsx (lines 104-119 — copy):
```typescript
borderLeft: isActive ? '2px solid var(--color-focus)' : '2px solid transparent',
background: isActive ? 'var(--color-bg)' : 'transparent',
color: isActive ? 'var(--color-text-primary)' : 'var(--color-text-secondary)',
fontWeight: isActive ? 600 : 400,
```

Loading spinner from DiffPane.tsx (lines 72-96 — copy):
```typescript
<div style={{
  width: 24, height: 24,
  border: '2px solid var(--color-border)',
  borderTopColor: 'var(--color-focus)',
  borderRadius: '50%',
  animation: 'spin 0.8s linear infinite',
}} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CommitDrawer.tsx with overlay + CommitRow + tests (source-text assertions)</name>
  <files>ui/src/code-review/CommitDrawer.tsx, ui/src/code-review/CommitDrawer.test.ts</files>
  <read_first>
    - ui/src/code-review/FileListPane.tsx (analog component — row layout, hover/active states, click handlers — copy patterns from lines 74-172)
    - ui/src/code-review/DiffPane.tsx (loading spinner block lines 72-96, empty state block lines 162-195 — copy spinner and empty layout)
    - ui/src/code-review/AppToolbar.test.ts (source-text test pattern — readFileSync + source.toContain assertions)
    - ui/src/code-review/types.ts (Commit interface — type-only import)
    - .planning/phases/26-commit-navigation/26-PATTERNS.md (lines 153-249 — CommitDrawer pattern map)
    - .planning/phases/26-commit-navigation/26-UI-SPEC.md (Component Inventory → CommitDrawer + CommitRow + Loading state sections)
    - .planning/phases/26-commit-navigation/26-RESEARCH.md (Pattern 5: CommitDrawer as Overlay; Code Examples → CommitRow structure)
  </read_first>
  <behavior>
    - Default export is a function component `CommitDrawer` accepting `CommitDrawerProps` (commits, loading, error, activeCommitSha, checkedCommitShas, onCommitClick, onCheckChange).
    - Root element is `<aside role="navigation" aria-label="Branch commits">` with inline style: `position: 'absolute'`, `top: 0`, `left: 0`, `width: 296`, `height: '100%'`, `background: 'var(--color-surface)'`, `borderRight: '1px solid var(--color-border)'`, `zIndex: 10`, `display: 'flex'`, `flexDirection: 'column'`, `overflowY: 'auto'`.
    - First child: a 12px semibold uppercase header containing the literal text `COMMITS` styled with `color: 'var(--color-text-secondary)'`, padding `'8px 16px'`.
    - Loading state (when `loading && commits.length === 0`): centered 24x24 spinner identical to DiffPane lines 72-96 (border, borderTopColor, borderRadius 50%, animation 'spin 0.8s linear infinite').
    - Empty state (when `!loading && commits.length === 0 && error === null`): two stacked elements — heading "No commits on this branch" (16px / 600 / var(--color-text-primary)) and body "This branch has no commits beyond the base branch." (14px / var(--color-text-secondary)) inside a centered padded container (padding '48px 32px').
    - Error state (when `error !== null`): single-line message "Could not load commits. Check server connection and reload." with `color: 'var(--color-accent-deny)'` (matches DiffPane error pattern).
    - Content state (otherwise): an `<ol>` (or `<ul>`) with one `<li>` per commit. Each row:
      - `padding: '8px 16px'`, flex column with gap 4.
      - Line 1 (flex row, gap 8, align center): an `<input type="checkbox">` (checked = checkedCommitShas.includes(commit.sha), onChange + onClick both call `e.stopPropagation()` then onCheckChange), an SHA chip span (monospace, fontFamily 'ui-monospace, "Cascadia Code", "Fira Code", monospace', fontSize 12, padding '4px 8px', borderRadius 4, border '1px solid var(--color-border)', background 'rgba(59,130,246,0.12)', text = commit.short_sha), a message span (flex: 1, fontSize 14, overflow hidden / textOverflow ellipsis / whiteSpace nowrap, `title={commit.message}`).
      - Line 2: `<div>` with fontSize 12 / color var(--color-text-secondary) containing `{commit.author} · {commit.date}`.
      - Whole row onClick fires `onCommitClick(commit.sha)`.
      - Active row (isActive = commit.sha === activeCommitSha): apply borderLeft 2px solid var(--color-focus), background var(--color-bg), color var(--color-text-primary), fontWeight 600.
      - Inactive row: borderLeft 2px solid transparent, background transparent, color var(--color-text-secondary), fontWeight 400.
    - CommitDrawer.test.ts performs source-text assertions:
      - `expect(typeof CommitDrawer).toBe('function')`
      - source contains `role="navigation"` AND `aria-label="Branch commits"`
      - source contains `width: 296` AND `zIndex: 10` AND `position: 'absolute'`
      - source contains the literal string `'COMMITS'`
      - source contains the literal `'No commits on this branch'`
      - source contains the literal `'This branch has no commits beyond the base branch.'`
      - source contains the literal `'Could not load commits. Check server connection and reload.'`
      - source contains `e.stopPropagation()` at least twice (onChange + onClick on the checkbox)
      - source contains `commit.short_sha` AND `commit.message` AND `commit.author` AND `commit.date`
      - source contains `var(--color-focus)` AND `var(--color-surface)` AND `var(--color-border)` AND `var(--color-bg)` AND `var(--color-text-secondary)`
      - source contains `'spin 0.8s linear infinite'` (spinner reuse)
      - source does NOT contain `reviewer-v2/`
      - source contains `onCommitClick(` AND `onCheckChange(`
  </behavior>
  <action>
    Step 1 — Create `ui/src/code-review/CommitDrawer.tsx`. Imports: `type { Commit } from './types'`. No useEffect / useState / useRef — CommitDrawer is pure display (D-11). Define and export `CommitDrawerProps` interface as in the `<interfaces>` block above. Default export `function CommitDrawer(props: CommitDrawerProps): React.JSX.Element`. Inside, branch on loading/error/empty/content states per the behavior block.

    Step 2 — Create `ui/src/code-review/CommitDrawer.test.ts`. Use the source-text pattern from `AppToolbar.test.ts`: `import { readFileSync } from 'fs'`, `import { resolve } from 'path'`, `import CommitDrawer from './CommitDrawer'`, `const source = readFileSync(resolve(__dirname, './CommitDrawer.tsx'), 'utf-8')`. Wrap assertions in `describe('CommitDrawer', () => { ... })`. Each behavior bullet in the test list above corresponds to one `it(...)` case.

    Step 3 — Run `cd ui && npx vitest run code-review/CommitDrawer.test.ts` and confirm all cases pass.

    Step 4 — Run `cd ui && npm run lint -- --max-warnings 0` and confirm ESLint passes (no `reviewer-v2/` import).
  </action>
  <verify>
    <automated>cd ui && npx vitest run code-review/CommitDrawer.test.ts 2>&1 | grep -E "passed|FAIL"</automated>
    <automated>grep -c "export default function CommitDrawer" ui/src/code-review/CommitDrawer.tsx</automated>
    <automated>grep -c "role=\"navigation\"" ui/src/code-review/CommitDrawer.tsx</automated>
    <automated>grep -c "aria-label=\"Branch commits\"" ui/src/code-review/CommitDrawer.tsx</automated>
    <automated>grep -c "width: 296" ui/src/code-review/CommitDrawer.tsx</automated>
    <automated>grep -c "zIndex: 10" ui/src/code-review/CommitDrawer.tsx</automated>
    <automated>grep -c "e.stopPropagation()" ui/src/code-review/CommitDrawer.tsx</automated>
    <automated>grep -c "reviewer-v2/" ui/src/code-review/CommitDrawer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `ui/src/code-review/CommitDrawer.tsx` exists
    - Source contains `export default function CommitDrawer` (exactly once)
    - Source contains `role="navigation"` AND `aria-label="Branch commits"`
    - Source contains `width: 296` (D-04 chosen midpoint)
    - Source contains `position: 'absolute'` AND `zIndex: 10`
    - Source contains `'COMMITS'` (drawer header literal)
    - Source contains `'No commits on this branch'`
    - Source contains `'This branch has no commits beyond the base branch.'`
    - Source contains `'Could not load commits. Check server connection and reload.'`
    - Source contains `e.stopPropagation()` at least twice
    - Source contains `commit.short_sha`, `commit.message`, `commit.author`, `commit.date`
    - Source contains `'spin 0.8s linear infinite'` (loading spinner reuse)
    - Source contains `borderLeft:` with active-row indicator using `var(--color-focus)`
    - Source does NOT contain `reviewer-v2/` (`grep -c "reviewer-v2/" ui/src/code-review/CommitDrawer.tsx` === 0)
    - `cd ui && npx vitest run code-review/CommitDrawer.test.ts` exits 0 with all assertions passing
    - `cd ui && npm run lint -- --max-warnings 0` exits 0
  </acceptance_criteria>
  <done>
    CommitDrawer + tests committed as `feat(26-02): add CommitDrawer overlay component`. All source-text assertions pass. ESLint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend DiffPane with viewMode + commit title strip; preserve all existing behavior</name>
  <files>ui/src/code-review/DiffPane.tsx, ui/src/code-review/DiffPane.test.ts</files>
  <read_first>
    - ui/src/code-review/DiffPane.tsx (entire file — modifications must NOT change loading/error/empty/content state machine in renderContent())
    - ui/src/code-review/DiffPane.test.ts (existing tests must remain green — new assertions ADDED, not replacing)
    - ui/src/code-review/types.ts (Commit type — add to existing FileDiff import)
    - .planning/phases/26-commit-navigation/26-PATTERNS.md (lines 454-515 — DiffPane modification pattern)
    - .planning/phases/26-commit-navigation/26-RESEARCH.md (Pattern 7: DiffPane Per-Commit Title Strip)
    - .planning/phases/26-commit-navigation/26-UI-SPEC.md (Component Inventory → DiffPane title section)
  </read_first>
  <behavior>
    - DiffPaneProps gets three new OPTIONAL props with safe defaults: `viewMode?: 'branch' | 'commit'` (default treated as 'branch'), `activeCommitSha?: string | null` (default null), `commits?: Commit[]` (default empty array).
    - When `viewMode === 'commit'` AND `activeCommitSha !== null` AND a commit with matching sha exists in `commits`: render a title strip ABOVE `renderContent()` but INSIDE the outer scroll container div (so it scrolls with content). Strip styling: `background: 'var(--color-surface)'`, `borderBottom: '1px solid var(--color-border)'`, `padding: '8px 16px'`. Two child divs:
      - First: `fontSize: 14, fontWeight: 600, color: 'var(--color-text-primary)'` containing `{activeCommit.short_sha} — {activeCommit.message}`.
      - Second: `fontSize: 12, color: 'var(--color-text-secondary)', marginTop: 2` containing `{activeCommit.author} · {activeCommit.date}`.
    - When `viewMode !== 'commit'` OR `activeCommitSha === null` OR no matching commit in `commits`: the title strip is NOT rendered (existing branch-view layout is byte-identical to today).
    - The existing renderContent() function (loading / error / empty / content states from lines 71-252) is NOT modified — the title strip wraps OUTSIDE renderContent().
    - The existing `files` prop, `loading`, `error`, `diffStyle`, `diffPaneRef`, `onReload`, and PatchDiff / FileDiffRenderer logic are unchanged.
    - DiffPane.test.ts retains ALL existing 13 assertions AND adds new assertions: source contains `viewMode`, `activeCommitSha`, `commits` in props, source contains the template literal `${activeCommit.short_sha} — ${activeCommit.message}` (or equivalent shape), source contains `${activeCommit.author} · ${activeCommit.date}`, source still does not import from `reviewer-v2/`.
    - DiffPane does NOT fetch per-commit data internally in this plan — fetch ownership is decided in 26-03 (CodeReviewApp passes `files` down already; per RESEARCH "State Architecture Detail" the cleaner separation is CodeReviewApp owning the fetch trigger). DiffPane only receives `files` and renders.
  </behavior>
  <action>
    Step 1 — Edit `ui/src/code-review/DiffPane.tsx`:
    1. Add `Commit` to the type import: `import type { FileDiff, Commit } from './types'`.
    2. Extend `DiffPaneProps` with three new optional fields as in `<interfaces>` block (viewMode, activeCommitSha, commits).
    3. Destructure them in the function signature with default values (`viewMode = 'branch'`, `activeCommitSha = null`, `commits = []`).
    4. Compute `const activeCommit = viewMode === 'commit' && activeCommitSha !== null ? commits.find((c) => c.sha === activeCommitSha) ?? null : null` near the top of the function body.
    5. In the JSX, BEFORE the existing `{renderContent()}` call, INSIDE the outer `<div ref={diffPaneRef}>`, conditionally render the title strip when `activeCommit !== null` using the styling specified in the behavior block.
    6. Do NOT alter renderContent() body — preserve all four existing states exactly as today.

    Step 2 — Edit `ui/src/code-review/DiffPane.test.ts`:
    1. Preserve ALL 13 existing `it(...)` cases unchanged.
    2. ADD new `it(...)` cases:
      - "extends DiffPaneProps with viewMode, activeCommitSha, and commits" — source contains literal `viewMode` AND `activeCommitSha` AND `commits` AND `'branch' | 'commit'`.
      - "imports Commit type from './types'" — source contains `Commit` and `from './types'`.
      - "renders commit title strip when viewMode === 'commit'" — source contains `activeCommit.short_sha` AND `activeCommit.message` AND `activeCommit.author` AND `activeCommit.date`.
      - "still does not import from reviewer-v2/" — source does NOT contain `reviewer-v2/`.

    Step 3 — Run `cd ui && npx vitest run code-review/DiffPane.test.ts` — confirm existing 13 + new 4 = 17 cases all pass.

    Step 4 — Run `cd ui && npm run lint -- --max-warnings 0` and `cd ui && npx tsc --noEmit` to confirm no regressions.

    NOTE: Existing call sites of DiffPane (CodeReviewApp.tsx) do NOT pass the new props — that's why the three are optional. Existing call sites compile unchanged. 26-03 will add the new props to CodeReviewApp.tsx.
  </action>
  <verify>
    <automated>cd ui && npx vitest run code-review/DiffPane.test.ts 2>&1 | grep -E "passed|FAIL"</automated>
    <automated>grep -c "viewMode" ui/src/code-review/DiffPane.tsx</automated>
    <automated>grep -c "activeCommit" ui/src/code-review/DiffPane.tsx</automated>
    <automated>grep -c "import type { FileDiff, Commit } from './types'" ui/src/code-review/DiffPane.tsx</automated>
    <automated>cd ui && npx tsc --noEmit 2>&1 | head -5</automated>
    <automated>cd ui && npx vitest run code-review/ 2>&1 | grep -E "Test Files|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/DiffPane.tsx` imports `Commit` from `./types` (literal `import type { FileDiff, Commit } from './types'` or equivalent)
    - DiffPaneProps source contains `viewMode` and `activeCommitSha` and `commits` (three new optional fields)
    - DiffPaneProps source contains `'branch' | 'commit'`
    - Source contains `activeCommit.short_sha` AND `activeCommit.message`
    - Source contains `activeCommit.author` AND `activeCommit.date`
    - Source contains the existing assertion targets (`disableWorkerPool={true}`, `expansionLineCount: 10`, `'spin 0.8s linear infinite'`, `'No changes on this branch'`, `'Could not load diff'`) — i.e., no existing test regressed
    - `cd ui && npx vitest run code-review/DiffPane.test.ts` exits 0 with at least 17 passing assertions (13 existing + 4 new)
    - `cd ui && npx vitest run code-review/` exits 0 across the whole code-review test suite (no other test file regressed)
    - `cd ui && npx tsc --noEmit` exits 0 (no TypeScript errors; existing CodeReviewApp call site still type-checks because new props are optional)
    - `cd ui && npm run lint -- --max-warnings 0` exits 0
  </acceptance_criteria>
  <done>
    DiffPane extended with viewMode + activeCommitSha + commits. Title strip renders when in per-commit mode. All existing tests + 4 new assertions pass. Committed as `feat(26-02): extend DiffPane with per-commit title strip`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → DOM rendering | Commit.message and Commit.author are server-supplied strings rendered as React children |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-T1 | Tampering / Injection | rendering commit.message and commit.author in React | accept | React escapes children by default; no `dangerouslySetInnerHTML` is used. Backend git2 already validates UTF-8. Local-only trust boundary. |
| T-26-02-T2 | Tampering | propagation of checkbox click into row navigation | mitigate | `e.stopPropagation()` on both `onChange` and `onClick` of the checkbox input prevents the row's onClick from firing — covered by acceptance criterion ">= 2 instances of e.stopPropagation()". |

No package-manager installs in this plan — Package Legitimacy Audit not applicable.
</threat_model>

<verification>
- `cd ui && npx vitest run code-review/` exits 0 — all code-review test files pass (CommitDrawer.test.ts, DiffPane.test.ts, plus existing AppToolbar/CodeReviewApp/FileListPane/useDiff/useCommits)
- `cd ui && npx tsc --noEmit` exits 0 (CommitDrawer compiles; DiffPane new props are optional so existing CodeReviewApp call site type-checks)
- `cd ui && npm run lint -- --max-warnings 0` exits 0
- Manual sanity (not blocking): visiting `/code-review` still renders identically to before this plan (no CommitDrawer is mounted because CodeReviewApp doesn't render it yet — that's 26-03)
</verification>

<success_criteria>
1. `CommitDrawer.tsx` exists with full overlay + row + checkbox + active state + spinner + empty state + error state semantics
2. `CommitDrawer.test.ts` source-text asserts all key tokens, copy literals, props shape, and ARCH-01 coupling rule
3. `DiffPane.tsx` extended with three new OPTIONAL props (viewMode, activeCommitSha, commits) + commit title strip; existing renderContent() byte-unchanged
4. `DiffPane.test.ts` preserves all 13 existing assertions and adds 4 new ones for the title strip and new props
5. All code-review tests pass; TypeScript compiles; ESLint clean
</success_criteria>

<output>
Create `.planning/phases/26-commit-navigation/26-02-SUMMARY.md` when done. Include commit SHAs, test counts, and any deviations from the spec.
</output>
