---
phase: 26-commit-navigation
plan: 03
type: execute
wave: 3
depends_on:
  - 26-01
  - 26-02
files_modified:
  - ui/src/code-review/AppToolbar.tsx
  - ui/src/code-review/AppToolbar.test.ts
  - ui/src/code-review/hooks/useDiff.ts
  - ui/src/code-review/hooks/useDiff.test.ts
  - ui/src/code-review/CodeReviewApp.tsx
  - ui/src/code-review/CodeReviewApp.test.ts
autonomous: false
requirements:
  - COMMIT-02
  - COMMIT-03
  - COMMIT-04
  - DIFF-05
user_setup: []

must_haves:
  truths:
    - "AppToolbar renders a 'Commits' toggle button driven by commitsOpen + onCommitsToggle props"
    - "Clicking the Commits toggle opens/closes the drawer; closing returns viewMode to 'branch' and activeCommitSha to null"
    - "Clicking a commit row in the drawer sets activeCommitSha to that sha and viewMode to 'commit'; DiffPane shows the per-commit diff"
    - "Left arrow navigates to previous commit; right arrow navigates to next; both ONLY when viewMode === 'commit' AND activeCommitSha !== null"
    - "Arrow keys stop at first/last commit (no wrap); up/down arrows are NOT wired (preserve diff pane scroll)"
    - "When viewMode === 'branch' and checkedCommitShas covers all loaded commits: GET /api/diff/branch supplies files (existing path)"
    - "When viewMode === 'branch' and checkedCommitShas is a proper subset: client-side union of FileDiff[] from /api/diff/commit/{sha} for each checked sha"
    - "When viewMode === 'commit': GET /api/diff/commit/{activeCommitSha} supplies files"
    - "checkedCommitShas is initialized to all commit shas when commits first load (D-08 opt-out model) via deferred setTimeout(0) pattern (avoids react-hooks/set-state-in-effect)"
    - "Per-commit and union fetches respect the contextLines (Expand All) value just like the existing branch fetch — calling refetch(999) while in commit mode re-fetches with ?context=999"
    - "No new backend routes — all interactions use existing /api/commits, /api/diff/branch, and /api/diff/commit/{sha} from Phase 24"
  artifacts:
    - path: "ui/src/code-review/AppToolbar.tsx"
      provides: "AppToolbarProps extended with commitsOpen + onCommitsToggle; Commits button rendered in right-controls group"
      contains: "commitsOpen"
    - path: "ui/src/code-review/hooks/useDiff.ts"
      provides: "useDiff extended to accept an optional fetchMode + activeCommitSha + checkedCommitShas selector; client-side union helper for DIFF-05"
      contains: "fetchFilteredBranchDiff"
    - path: "ui/src/code-review/CodeReviewApp.tsx"
      provides: "All commit state owned here (drawerOpen, viewMode, activeCommitSha, checkedCommitShas); CommitDrawer mounted inside aside; keyboard handler; toggle handler"
      contains: "drawerOpen"
  key_links:
    - from: "ui/src/code-review/CodeReviewApp.tsx"
      to: "ui/src/code-review/hooks/useCommits.ts"
      via: "useCommits() destructure"
      pattern: "useCommits()"
    - from: "ui/src/code-review/CodeReviewApp.tsx"
      to: "ui/src/code-review/CommitDrawer.tsx"
      via: "default import + conditional render inside aside"
      pattern: "<CommitDrawer"
    - from: "ui/src/code-review/CodeReviewApp.tsx"
      to: "/api/diff/commit/{sha}"
      via: "viewMode==='commit' OR DIFF-05 union triggers"
      pattern: "/api/diff/commit/"
    - from: "ui/src/code-review/AppToolbar.tsx"
      to: "ui/src/code-review/CodeReviewApp.tsx"
      via: "props commitsOpen + onCommitsToggle"
      pattern: "onCommitsToggle"
---

<objective>
Integrate Phase 26 components into the live UI: add the `Commits` toggle button to `AppToolbar`, lift all commit state into `CodeReviewApp`, mount `CommitDrawer` as an overlay in the existing aside, wire the keyboard handler (D-09 / D-10), implement DIFF-05 client-side union for subset-of-commits branch view, and reuse the existing `useDiff` fetch infrastructure for per-commit and union fetches.

Purpose: D-11 mandates all commit state lives in CodeReviewApp. D-07 says drawer close returns to branch view. D-09/D-10 specify arrow-key handler is gated on viewMode === 'commit'. RESEARCH Open Question 2 was resolved during planning in favor of CodeReviewApp owning fetch triggers (the cleaner data flow — files prop already lives in App) — DiffPane stays a pure renderer of `files`.

Output: AppToolbar extended; useDiff extended with mode-aware fetching; CodeReviewApp wired with all four state variables, drawer, keyboard, toggle handler, and per-commit/union fetch logic; tests updated; one blocking human-verify checkpoint after autonomous code lands.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-commit-navigation/26-CONTEXT.md
@.planning/phases/26-commit-navigation/26-RESEARCH.md
@.planning/phases/26-commit-navigation/26-PATTERNS.md
@.planning/phases/26-commit-navigation/26-UI-SPEC.md
@.planning/phases/26-commit-navigation/26-01-SUMMARY.md
@.planning/phases/26-commit-navigation/26-02-SUMMARY.md

@ui/src/code-review/AppToolbar.tsx
@ui/src/code-review/AppToolbar.test.ts
@ui/src/code-review/CodeReviewApp.tsx
@ui/src/code-review/CodeReviewApp.test.ts
@ui/src/code-review/hooks/useDiff.ts
@ui/src/code-review/hooks/useDiff.test.ts
@ui/src/code-review/hooks/useCommits.ts
@ui/src/code-review/CommitDrawer.tsx
@ui/src/code-review/DiffPane.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + plans 26-01/26-02. -->

AppToolbarProps after extension:
```typescript
export interface AppToolbarProps {
  diffStyle: 'unified' | 'split'
  contextExpanded: boolean
  contextLoading: boolean
  onDiffStyleChange: (style: 'unified' | 'split') => void
  onExpandAll: () => void
  // Phase 26 additions
  commitsOpen: boolean
  onCommitsToggle: () => void
}
```

useDiff after extension — adds a typed selector for what to fetch:
```typescript
export type DiffFetchSelector =
  | { mode: 'branch' }                                  // GET /api/diff/branch
  | { mode: 'commit'; sha: string }                     // GET /api/diff/commit/{sha}
  | { mode: 'branch-union'; shas: string[] }            // N x GET /api/diff/commit/{sha}, flatten

export interface UseDiffOptions {
  selector: DiffFetchSelector
}

export function useDiff(opts?: UseDiffOptions): UseDiffResult
```

`fetchFilteredBranchDiff(checkedShas, doFetch, contextLines?)` — exported standalone helper for testing:
- Returns `FileDiff[]` flattened from `Promise.all(checkedShas.map(sha => GET /api/diff/commit/{sha}?context=...))`.
- Each sub-fetch failure resolves to `[]` (no throw — same posture as fetchDiffOnce).

CodeReviewApp new state (per D-11, all four owned here):
```typescript
const [drawerOpen, setDrawerOpen] = useState(false)
const [viewMode, setViewMode] = useState<'branch' | 'commit'>('branch')
const [activeCommitSha, setActiveCommitSha] = useState<string | null>(null)
const [checkedCommitShas, setCheckedCommitShas] = useState<string[]>([])
const { commits, loading: commitsLoading, error: commitsError } = useCommits()
```

CodeReviewApp drawer-toggle handler (D-07 mandates drawer-close-returns-to-branch):
```typescript
function handleCommitsToggle() {
  if (drawerOpen) {
    setDrawerOpen(false)
    setViewMode('branch')
    setActiveCommitSha(null)
  } else {
    setDrawerOpen(true)
  }
}
```

Keyboard navigation (D-09 / D-10 / boundary = stop):
```typescript
useEffect(() => {
  function handleKeyDown(e: KeyboardEvent) {
    if (viewMode !== 'commit' || commits.length === 0 || activeCommitSha === null) return
    const idx = commits.findIndex((c) => c.sha === activeCommitSha)
    if (e.key === 'ArrowLeft' && idx > 0) setActiveCommitSha(commits[idx - 1].sha)
    else if (e.key === 'ArrowRight' && idx < commits.length - 1) setActiveCommitSha(commits[idx + 1].sha)
  }
  window.addEventListener('keydown', handleKeyDown)
  return () => window.removeEventListener('keydown', handleKeyDown)
}, [viewMode, activeCommitSha, commits])
```

Aside positioning (D-01 / Pitfall 3): the existing `<aside>` MUST gain `position: 'relative'` so the CommitDrawer (`position: absolute`) anchors inside it. Width 240 stays the same — drawer overlays at 296px, partially off the right edge of the aside, which is intentional (D-01 overlay model).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Commits toggle button to AppToolbar (props + render + tests)</name>
  <files>ui/src/code-review/AppToolbar.tsx, ui/src/code-review/AppToolbar.test.ts</files>
  <read_first>
    - ui/src/code-review/AppToolbar.tsx (entire file — the Expand All button block lines 103-125 is the analog; copy its structure)
    - ui/src/code-review/AppToolbar.test.ts (existing 11 assertions must remain green; new ones ADDED)
    - .planning/phases/26-commit-navigation/26-PATTERNS.md (lines 402-451 — AppToolbar modification pattern)
    - .planning/phases/26-commit-navigation/26-UI-SPEC.md (Component Inventory → AppToolbar extended section)
    - .planning/phases/26-commit-navigation/26-RESEARCH.md (Code Examples → AppToolbar Commits button addition)
  </read_first>
  <behavior>
    - AppToolbarProps gains exactly two new required fields: `commitsOpen: boolean` and `onCommitsToggle: () => void`. They are NOT optional — CodeReviewApp will provide them in Task 3.
    - The Commits button renders INSIDE the right-controls `<div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>` block, BEFORE the layout toggle group (Unified / Side-by-side). Place it after the reserved-slot `<div />` placeholder and before the layout `<div>` border-wrapped group.
    - Button styling mirrors the Expand All button (lines 103-125): height 32, padding '0 16px', border '1px solid var(--color-border)', borderRadius 6, fontSize 14, cursor 'pointer', outline 'none', background 'var(--color-surface)'. Active state (`commitsOpen === true`): `color: 'var(--color-text-primary)'` + `fontWeight: 600`. Inactive: `color: 'var(--color-text-secondary)'` + `fontWeight: 400`. Label text is literally `'Commits'` in both states.
    - Use existing `makeFocusHandlers('commits')` for the focus ring (passes the literal string 'commits' as id).
    - Button is NOT disabled while contextLoading — drawer toggle is orthogonal to the Expand All loading state.
    - AppToolbar.test.ts retains ALL 11 existing assertions AND adds:
      - "AppToolbarProps include commitsOpen and onCommitsToggle" — source contains `commitsOpen: boolean` AND `onCommitsToggle:`
      - "renders the 'Commits' label literally" — source contains `'Commits'`
      - "Commits button uses fontWeight 600 when commitsOpen is true" — source contains `commitsOpen ? 600 : 400` OR equivalent ternary on fontWeight tied to commitsOpen
      - "Commits button uses color-text-primary when commitsOpen and color-text-secondary otherwise" — source contains `commitsOpen ?` near `var(--color-text-primary)`
      - "Commits button uses makeFocusHandlers('commits')" — source contains `makeFocusHandlers('commits')`
      - "AppToolbar still does NOT import from reviewer-v2/" — source does NOT contain `reviewer-v2/`
  </behavior>
  <action>
    Step 1 — Edit `ui/src/code-review/AppToolbar.tsx`:
    1. Extend `AppToolbarProps` interface with `commitsOpen: boolean` and `onCommitsToggle: () => void` (both required, after the existing `onExpandAll` field).
    2. Destructure the two new props in the function signature.
    3. Insert a new `<button>` element in the right-controls group, BEFORE the layout toggle's bordered `<div>`. Style and label as in the behavior block. `onClick={onCommitsToggle}`. Spread `{...makeFocusHandlers('commits')}`.

    Step 2 — Edit `ui/src/code-review/AppToolbar.test.ts`:
    1. Keep all 11 existing `it(...)` cases verbatim.
    2. Add the six new `it(...)` cases listed in the behavior block.

    Step 3 — Run `cd ui && npx vitest run code-review/AppToolbar.test.ts` and confirm 17+ assertions pass. NOTE: existing call site in `CodeReviewApp.tsx` does NOT yet supply the new required props — TypeScript WILL fail on `npx tsc --noEmit` until Task 3 lands. This is expected; tests for AppToolbar.tsx are isolated source-text assertions and do not exercise the call site.
  </action>
  <verify>
    <automated>cd ui && npx vitest run code-review/AppToolbar.test.ts 2>&1 | grep -E "passed|FAIL"</automated>
    <automated>grep -c "commitsOpen: boolean" ui/src/code-review/AppToolbar.tsx</automated>
    <automated>grep -c "onCommitsToggle" ui/src/code-review/AppToolbar.tsx</automated>
    <automated>grep -c "'Commits'" ui/src/code-review/AppToolbar.tsx</automated>
    <automated>grep -c "makeFocusHandlers('commits')" ui/src/code-review/AppToolbar.tsx</automated>
    <automated>grep -c "reviewer-v2/" ui/src/code-review/AppToolbar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/AppToolbar.tsx` source contains `commitsOpen: boolean` AND `onCommitsToggle:` in the props interface
    - Source contains literal `'Commits'` (button label)
    - Source contains a ternary tying `fontWeight` to `commitsOpen` (e.g., `commitsOpen ? 600 : 400`)
    - Source contains a ternary tying `color` to `commitsOpen` involving both `var(--color-text-primary)` and `var(--color-text-secondary)`
    - Source contains `makeFocusHandlers('commits')`
    - Source still contains the 11 existing tokens asserted by the prior tests (`height: 48`, `'Code Review'`, `'Unified'`, `'Side-by-side'`, `'Expand All'`, `'Collapse'`, `'Loading...'`, `disabled={contextLoading}`, `var(--color-surface)`, `onFocus`, `onBlur`)
    - Source does NOT contain `reviewer-v2/`
    - `cd ui && npx vitest run code-review/AppToolbar.test.ts` exits 0 with all assertions passing
  </acceptance_criteria>
  <done>
    AppToolbar extended with Commits toggle button + tests. Committed as `feat(26-03): add Commits toggle to AppToolbar`. Note: TypeScript may not yet compile project-wide because CodeReviewApp call site is updated in Task 3 — this is intentional task sequencing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend useDiff with mode-aware fetching (commit + branch-union) and add helper + tests</name>
  <files>ui/src/code-review/hooks/useDiff.ts, ui/src/code-review/hooks/useDiff.test.ts</files>
  <read_first>
    - ui/src/code-review/hooks/useDiff.ts (entire file — the existing fetchDiffOnce pattern is the template for the new selector-driven fetch)
    - ui/src/code-review/hooks/useDiff.test.ts (existing 6 cases must remain green; new ones ADDED)
    - .planning/phases/26-commit-navigation/26-RESEARCH.md (Pattern 3: Client-Side FileDiff Union for DIFF-05, Pitfall 5: DiffPane fetching on every viewMode change)
    - .planning/phases/26-commit-navigation/26-CONTEXT.md (Specifics: "client-side union — no new backend param needed"; D-05 / D-13: gestures are orthogonal)
  </read_first>
  <behavior>
    - Add a new exported type `DiffFetchSelector` (discriminated union with three variants: `{mode:'branch'}`, `{mode:'commit',sha:string}`, `{mode:'branch-union',shas:string[]}`).
    - Add a new exported pure function `fetchFilteredBranchDiff(shas, doFetch, contextLines?) => Promise<FileDiff[]>`:
      - Fetches `/api/diff/commit/{sha}` for every sha in parallel via `Promise.all`.
      - When `contextLines !== undefined`, the URL is `/api/diff/commit/{sha}?context={contextLines}` (matches existing fetchDiffOnce ?context handling — including treating 0 as a real value, not absent).
      - Each sub-fetch failure (non-ok response or thrown error) resolves to `[]` for that sha (no throw escapes the function).
      - Returns the flat union of all per-commit FileDiff[] arrays (no dedup — per RESEARCH Open Question 1 recommendation: render duplicates as-is, which mirrors git history).
    - Add a new exported pure function `fetchCommitDiffOnce(sha, doFetch, contextLines?) => Promise<FetchDiffResult>` (returns `{ files, error }` matching existing fetchDiffOnce signature):
      - Calls `/api/diff/commit/{sha}` with optional `?context={contextLines}`.
      - 200 → `{ files: FileDiff[], error: null }`.
      - Non-ok → `{ files: [], error: 'fetch failed' }`.
      - Throws → `{ files: [], error: 'network error' }`.
    - `useDiff(opts?)` accepts an optional `{ selector }` argument:
      - If opts is omitted OR opts.selector.mode === 'branch': existing behavior (calls fetchDiffOnce).
      - If opts.selector.mode === 'commit': initial fetch calls fetchCommitDiffOnce(selector.sha, ..., contextLines). refetch(contextLines) re-fetches the same sha.
      - If opts.selector.mode === 'branch-union': initial fetch calls fetchFilteredBranchDiff(selector.shas, ..., contextLines). refetch behaves identically.
      - The selector is captured per-render — `useDiff` MUST re-trigger the fetch when selector.mode OR selector.sha (for 'commit') OR a sorted-join of selector.shas (for 'branch-union') changes. Use a stable string key derived from the selector to track changes (e.g., `${selector.mode}:${selector.mode === 'commit' ? selector.sha : selector.mode === 'branch-union' ? [...selector.shas].sort().join(',') : ''}`).
    - Existing useDiff() (no arg) call site in CodeReviewApp continues to compile because opts is optional and defaults to branch mode.
    - Existing 6 useDiff.test.ts cases continue to pass.
    - Add new test cases (in useDiff.test.ts):
      - `describe('fetchCommitDiffOnce'`:
        - "calls /api/diff/commit/{sha} and returns files on 200"
        - "returns { files: [], error: 'fetch failed' } on non-ok"
        - "returns { files: [], error: 'network error' } when fetch throws"
        - "appends ?context=999 when contextLines is 999"
      - `describe('fetchFilteredBranchDiff'`:
        - "returns flat union of files from each sha when all 200" — pass 2 shas, each returning 1 file; expect length 2
        - "returns [] for a single failing sha without throwing" — pass 2 shas where one returns 500; expect length 1 (the other succeeded)
        - "appends ?context=N to every per-sha URL when contextLines is set" — capture all fetched URLs and assert each contains `?context=`
  </behavior>
  <action>
    Step 1 — Edit `ui/src/code-review/hooks/useDiff.ts`:
    1. Export a new type `DiffFetchSelector` (discriminated union, three variants).
    2. Export `fetchCommitDiffOnce(sha, doFetch, contextLines?)` — mirror fetchDiffOnce structure but URL is `/api/diff/commit/${sha}` (with `?context=${n}` appended if contextLines !== undefined).
    3. Export `fetchFilteredBranchDiff(shas, doFetch, contextLines?)` — `Promise.all(shas.map(sha => fetchCommitDiffOnce(sha, doFetch, contextLines).then(r => r.files).catch(() => [])))`, then `.flat()`.
    4. Extend `useDiff()` to accept `opts?: { selector: DiffFetchSelector }`. Internally:
       - Compute selector key (string) for change tracking: `branch:'', commit:sha, branch-union:sorted-comma-joined`.
       - Wrap fetch dispatch in a helper that calls the right pure function based on selector.mode.
       - On mount AND on selector-key change, perform the fetch (set loading=true via the existing async pattern — the existing useEffect sets state on resolve only; that pattern works for branch but needs adaptation for selector-key as a useEffect dependency).
       - The mount useEffect deps array becomes `[selectorKey]`. To avoid `react-hooks/set-state-in-effect` violation when re-fetching, use the same setTimeout(0) pattern already used in CodeReviewApp for activeIndex reset (set `setLoading(true)` inside `setTimeout(0)` if needed, OR keep the existing pattern of NOT calling setLoading(true) inside the effect body and instead invoking refetch via setTimeout from an effect that watches the key).
    5. `refetch(contextLines?)` re-invokes the dispatcher with the current selector and the given contextLines.

    Step 2 — Edit `ui/src/code-review/hooks/useDiff.test.ts`:
    1. Keep all 6 existing `it(...)` inside `describe('fetchDiffOnce', ...)` unchanged.
    2. Add new `describe('fetchCommitDiffOnce', ...)` block with the 4 cases listed above.
    3. Add new `describe('fetchFilteredBranchDiff', ...)` block with the 3 cases listed above.

    Step 3 — Run `cd ui && npx vitest run code-review/hooks/useDiff.test.ts` and confirm all assertions (existing 6 + new 7 = 13) pass.

    Step 4 — Run `cd ui && npx tsc --noEmit`. Existing useDiff() call in CodeReviewApp must still compile (opts is optional).
  </action>
  <verify>
    <automated>cd ui && npx vitest run code-review/hooks/useDiff.test.ts 2>&1 | grep -E "passed|FAIL"</automated>
    <automated>grep -c "export type DiffFetchSelector" ui/src/code-review/hooks/useDiff.ts</automated>
    <automated>grep -c "export async function fetchCommitDiffOnce" ui/src/code-review/hooks/useDiff.ts</automated>
    <automated>grep -c "export async function fetchFilteredBranchDiff" ui/src/code-review/hooks/useDiff.ts</automated>
    <automated>grep -c "branch-union" ui/src/code-review/hooks/useDiff.ts</automated>
    <automated>grep -c "/api/diff/commit/" ui/src/code-review/hooks/useDiff.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source contains `export type DiffFetchSelector` discriminating on `mode: 'branch' | 'commit' | 'branch-union'`
    - Source contains `export async function fetchCommitDiffOnce` taking `(sha, doFetch, contextLines?)`
    - Source contains `export async function fetchFilteredBranchDiff` taking `(shas, doFetch, contextLines?)`
    - Source contains the URL literal pattern `/api/diff/commit/`
    - Source contains `'fetch failed'` AND `'network error'` (consistent error strings)
    - Source contains `Promise.all` (parallel per-sha fetching for union)
    - Source does NOT contain `reviewer-v2/`
    - `cd ui && npx vitest run code-review/hooks/useDiff.test.ts` exits 0 with at least 13 passing cases (6 existing + 7 new)
    - `cd ui && npx tsc --noEmit` exits 0 (existing `useDiff()` no-arg call in CodeReviewApp still compiles)
    - `cd ui && npm run lint -- --max-warnings 0 ui/src/code-review/hooks/useDiff.ts` exits 0
  </acceptance_criteria>
  <done>
    useDiff extended with selector pattern, fetchCommitDiffOnce, fetchFilteredBranchDiff; existing 6 tests still pass; 7 new tests pass. Committed as `feat(26-03): extend useDiff with per-commit and union fetching`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire CodeReviewApp — state, CommitDrawer, keyboard, mode switching, DIFF-05 union; update tests</name>
  <files>ui/src/code-review/CodeReviewApp.tsx, ui/src/code-review/CodeReviewApp.test.ts</files>
  <read_first>
    - ui/src/code-review/CodeReviewApp.tsx (entire file — existing state + JSX is the canvas)
    - ui/src/code-review/CodeReviewApp.test.ts (existing 18 assertions remain green; new ones ADDED)
    - ui/src/code-review/CommitDrawer.tsx (created in 26-02 — props contract)
    - ui/src/code-review/hooks/useCommits.ts (created in 26-01 — returns { commits, loading, error, truncated })
    - ui/src/code-review/hooks/useDiff.ts (extended in Task 2 — accepts optional { selector })
    - ui/src/code-review/AppToolbar.tsx (extended in Task 1 — now requires commitsOpen + onCommitsToggle)
    - ui/src/code-review/DiffPane.tsx (extended in 26-02 — accepts optional viewMode + activeCommitSha + commits)
    - .planning/phases/26-commit-navigation/26-PATTERNS.md (lines 282-398 — CodeReviewApp modification pattern)
    - .planning/phases/26-commit-navigation/26-RESEARCH.md (Pattern 4: Keyboard Navigation Handler; Pattern 6: Mode Switching — Drawer Close Resets to Branch; Pitfall 2, 3, 4, 6)
  </read_first>
  <behavior>
    - All four new state variables declared with exact initial values: `drawerOpen=false`, `viewMode='branch'`, `activeCommitSha=null`, `checkedCommitShas=[]`.
    - `useCommits()` destructured to `{ commits, loading: commitsLoading, error: commitsError }`.
    - `useDiff` called with a derived selector reflecting current state:
      - viewMode === 'commit' && activeCommitSha → `{ mode: 'commit', sha: activeCommitSha }`
      - viewMode === 'branch' && checkedCommitShas.length === commits.length && commits.length > 0 (all checked) → `{ mode: 'branch' }`
      - viewMode === 'branch' && commits.length > 0 && checkedCommitShas.length < commits.length → `{ mode: 'branch-union', shas: checkedCommitShas }`
      - viewMode === 'branch' && commits.length === 0 (commits not loaded yet) → `{ mode: 'branch' }` (default to branch — preserves existing behavior on initial load)
    - The aside element gets `position: 'relative'` added to its inline style (Pitfall 3 — CommitDrawer position:absolute anchors here).
    - Inside the aside, BEFORE the existing FileListPane, conditionally render `<CommitDrawer ... />` when `drawerOpen === true`. Props:
      - `commits={commits}`, `loading={commitsLoading}`, `error={commitsError}`
      - `activeCommitSha={activeCommitSha}`, `checkedCommitShas={checkedCommitShas}`
      - `onCommitClick={(sha) => { setActiveCommitSha(sha); setViewMode('commit') }}`
      - `onCheckChange={(sha, checked) => setCheckedCommitShas(prev => checked ? [...prev, sha] : prev.filter(s => s !== sha))}`
    - AppToolbar receives the two new props: `commitsOpen={drawerOpen}`, `onCommitsToggle={handleCommitsToggle}`.
    - DiffPane receives the three new optional props: `viewMode={viewMode}`, `activeCommitSha={activeCommitSha}`, `commits={commits}`.
    - handleCommitsToggle function: if drawerOpen, close drawer AND reset viewMode='branch' AND clear activeCommitSha=null (D-07). Else open drawer (don't touch viewMode/activeCommitSha).
    - checkedCommitShas seeding useEffect: when `commits.length > 0 && checkedCommitShas.length === 0`, schedule `setCheckedCommitShas(commits.map(c => c.sha))` via `setTimeout(0)` (Pitfall 6 — same deferral pattern as the activeIndex reset). Cleanup clears the timeout.
    - Keyboard handler useEffect listening to window 'keydown': active only when `viewMode === 'commit' && commits.length > 0 && activeCommitSha !== null`. ArrowLeft → previous (no wrap, stop at index 0). ArrowRight → next (no wrap, stop at last index). Cleanup removes listener. Deps array: `[viewMode, activeCommitSha, commits]` (Pitfall 2 — stale closure avoidance).
    - CodeReviewApp.test.ts retains ALL 18 existing assertions AND adds:
      - "imports CommitDrawer" — source contains `import CommitDrawer from './CommitDrawer'`
      - "imports useCommits" — source contains `from './hooks/useCommits'` AND `useCommits()`
      - "declares drawerOpen, viewMode, activeCommitSha, checkedCommitShas state" — source contains all four identifiers
      - "viewMode state is typed 'branch' | 'commit' with initial 'branch'" — source contains `useState<'branch' | 'commit'>('branch')`
      - "aside has position: 'relative'" — source contains `position: 'relative'` (D-01 prerequisite — Pitfall 3)
      - "passes commitsOpen and onCommitsToggle to AppToolbar" — source contains `commitsOpen={drawerOpen}` AND `onCommitsToggle={`
      - "passes viewMode, activeCommitSha, commits to DiffPane" — source contains `viewMode={viewMode}` AND `activeCommitSha={activeCommitSha}` AND `commits={commits}` (in the DiffPane JSX block)
      - "conditionally renders CommitDrawer when drawerOpen" — source contains `{drawerOpen &&` near `<CommitDrawer`
      - "handleCommitsToggle resets viewMode to 'branch' and activeCommitSha to null on close" — source contains both `setViewMode('branch')` and `setActiveCommitSha(null)` inside a function that also calls `setDrawerOpen(false)`
      - "keyboard handler listens for ArrowLeft and ArrowRight only" — source contains `'ArrowLeft'` AND `'ArrowRight'` AND does NOT contain `'ArrowUp'` AND does NOT contain `'ArrowDown'`
      - "keyboard handler is gated on viewMode === 'commit'" — source contains `viewMode !== 'commit'` (early-return guard)
      - "keyboard handler stops at boundaries (no wrap)" — source contains both `idx > 0` AND `idx < commits.length - 1` (guard conditions)
      - "checkedCommitShas seeded via setTimeout(0) deferral" — source contains `setTimeout(` near `setCheckedCommitShas(commits.map`
      - "still does not import from reviewer-v2/" — source does NOT contain `reviewer-v2/`
  </behavior>
  <action>
    Step 1 — Edit `ui/src/code-review/CodeReviewApp.tsx`:
    1. Imports: add `import CommitDrawer from './CommitDrawer'` and `import { useCommits } from './hooks/useCommits'`.
    2. Add the four new useState declarations after the existing three.
    3. Call `useCommits()` and destructure to `commits, commitsLoading, commitsError`.
    4. Compute the selector for useDiff based on viewMode + checkedCommitShas + commits (per behavior block). Call `useDiff({ selector })` — replacing the current bare `useDiff()`.
    5. Add the checkedCommitShas seeding useEffect (setTimeout(0) pattern, deps `[commits.length]`).
    6. Add the keyboard handler useEffect (window.addEventListener('keydown', ...), deps `[viewMode, activeCommitSha, commits]`).
    7. Add the `handleCommitsToggle` function (resets viewMode + activeCommitSha when closing).
    8. Add `position: 'relative'` to the existing aside inline style.
    9. Inside the aside, BEFORE `<FileListPane ... />`, add the conditional `{drawerOpen && <CommitDrawer ... />}` block.
    10. Update the `<AppToolbar ... />` JSX to add `commitsOpen={drawerOpen}` and `onCommitsToggle={handleCommitsToggle}`.
    11. Update the `<DiffPane ... />` JSX to add `viewMode={viewMode}`, `activeCommitSha={activeCommitSha}`, `commits={commits}`.

    Step 2 — Edit `ui/src/code-review/CodeReviewApp.test.ts`:
    1. Keep all 18 existing `it(...)` cases verbatim — EXCEPT the assertion `it('declares useDiff() and destructures files, loading, error, refetch')`: the existing test asserts `expect(source).toContain('useDiff()')` (the call site shape). Update this specific assertion to `expect(source).toContain('useDiff(')` (without the empty parens — useDiff now receives an argument). The destructure of `{ files, loading, error, refetch }` is unchanged.
    2. Add the 14 new `it(...)` cases listed above in the behavior block.

    Step 3 — Run `cd ui && npx vitest run code-review/CodeReviewApp.test.ts` — confirm all assertions pass.

    Step 4 — Run `cd ui && npx vitest run code-review/` — confirm the full code-review suite passes (useCommits, useDiff, AppToolbar, CommitDrawer, DiffPane, FileListPane, CodeReviewApp).

    Step 5 — Run `cd ui && npx tsc --noEmit` — confirm zero TypeScript errors project-wide (AppToolbar's new required props are now satisfied; DiffPane's new optional props are passed; CommitDrawer is mounted with full prop contract).

    Step 6 — Run `cd ui && npm run lint -- --max-warnings 0` — confirm zero ESLint errors/warnings.

    Step 7 — Run `cargo build --release` from repo root — confirm the embedded UI build (build.rs invokes `npm run build`) succeeds.
  </action>
  <verify>
    <automated>cd ui && npx vitest run code-review/ 2>&1 | grep -E "Test Files|Tests"</automated>
    <automated>cd ui && npx tsc --noEmit 2>&1 | head -5</automated>
    <automated>cd ui && npm run lint -- --max-warnings 0 2>&1 | tail -5</automated>
    <automated>grep -c "import CommitDrawer from './CommitDrawer'" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -c "from './hooks/useCommits'" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -c "useState<'branch' | 'commit'>" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -c "drawerOpen" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -c "activeCommitSha" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -c "checkedCommitShas" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -c "position: 'relative'" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -c "'ArrowLeft'" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -c "'ArrowRight'" ui/src/code-review/CodeReviewApp.tsx</automated>
    <automated>grep -E "'ArrowUp'|'ArrowDown'" ui/src/code-review/CodeReviewApp.tsx; echo "exit=$?"</automated>
    <automated>cargo build --release 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/CodeReviewApp.tsx` contains `import CommitDrawer from './CommitDrawer'`
    - Source contains `from './hooks/useCommits'` AND `useCommits()`
    - Source contains `useState(false)` (for drawerOpen) AND `useState<'branch' | 'commit'>('branch')`
    - Source contains all four identifiers: `drawerOpen`, `viewMode`, `activeCommitSha`, `checkedCommitShas`
    - Source contains `position: 'relative'` (aside positioning prerequisite — Pitfall 3)
    - Source contains `<CommitDrawer` rendered conditionally with `drawerOpen &&`
    - Source contains `commitsOpen={drawerOpen}` AND `onCommitsToggle={` passed to AppToolbar
    - Source contains `viewMode={viewMode}` AND `activeCommitSha={activeCommitSha}` AND `commits={commits}` passed to DiffPane
    - Source contains `'ArrowLeft'` AND `'ArrowRight'`
    - Source does NOT contain `'ArrowUp'` (`grep -c "'ArrowUp'" ... === 0`)
    - Source does NOT contain `'ArrowDown'` (`grep -c "'ArrowDown'" ... === 0`)
    - Source contains `viewMode !== 'commit'` (keyboard gate guard)
    - Source contains `idx > 0` AND `idx < commits.length - 1` (no-wrap boundary guards)
    - Source contains `setTimeout(` adjacent to `setCheckedCommitShas(commits.map(` (deferred seeding pattern)
    - Source contains a handler that calls `setDrawerOpen(false)`, `setViewMode('branch')`, AND `setActiveCommitSha(null)` together (D-07)
    - Source does NOT contain `reviewer-v2/`
    - `cd ui && npx vitest run code-review/` exits 0 (all test files pass: useCommits, useDiff, AppToolbar, CommitDrawer, DiffPane, FileListPane, CodeReviewApp)
    - `cd ui && npx tsc --noEmit` exits 0 — zero type errors
    - `cd ui && npm run lint -- --max-warnings 0` exits 0 — zero ESLint errors/warnings
    - `cargo build --release` succeeds (UI embedding via build.rs works)
  </acceptance_criteria>
  <done>
    CodeReviewApp wired with all Phase 26 features. Tests, types, lint, and full release build pass. Committed as `feat(26-03): wire CodeReviewApp with commit drawer + keyboard + DIFF-05 union`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — manual smoke test across all five Phase 26 requirements</name>
  <files>(human verification — no files modified; logs / screenshots may be attached to the SUMMARY)</files>
  <read_first>
    - .planning/phases/26-commit-navigation/26-CONTEXT.md (Decisions D-01 through D-13 — the truth source for expected behavior)
    - .planning/phases/26-commit-navigation/26-UI-SPEC.md (Component Inventory + Interaction Contract — the visual contract being verified)
    - .planning/phases/26-commit-navigation/26-03-PLAN.md Task 3 acceptance_criteria (the automated checks that already passed)
  </read_first>
  <what-built>
    Phase 26 commit navigation is now wired:
    - AppToolbar shows a "Commits" toggle button in the right controls
    - Clicking it opens a 296px-wide drawer overlaying the file list aside, listing all branch commits (SHA chip + truncated message + author · date + checkbox)
    - Clicking a commit row switches the diff pane to that commit's diff and shows a title strip ({short_sha} — {message} / {author} · {date}) above the file list
    - Pressing left/right arrows navigates between commits while in per-commit mode (stop at boundaries — no wrap)
    - Up/down arrows continue to scroll the diff pane (no commit-nav binding)
    - Closing the drawer (clicking "Commits" again) returns to full-branch view
    - Unchecking commits in the drawer while in branch view rebuilds the diff as a union of the checked commits (via /api/diff/commit/{sha} × N)
    - All commit state lives in CodeReviewApp; backend changes were not needed (Phase 24 endpoints are reused)
  </what-built>
  <how-to-verify>
    Build and launch the binary against a non-trivial branch with multiple commits and run the eight checks below.

    1. Build + run: `cargo build --release` then `./target/release/plan-reviewer code-review`. If the `code-review` subcommand is not yet wired in main.rs (Phase 29), use `cd ui && npm run dev` and visit `http://127.0.0.1:5173/code-review` with a backend running, OR navigate to `http://127.0.0.1:{port}/code-review` after invoking the existing hook entrypoint.

    2. COMMIT-01 — Commit list renders: Click the "Commits" button. Verify a 296px panel appears anchored to the left edge of the file-list aside, containing a "COMMITS" header and one row per commit. Each row shows: a checkbox (checked), a monospace SHA chip (7 chars), the commit message truncated with ellipsis, and "author · date" on a second line. Verify rows scroll independently if many.

    3. COMMIT-02 — Per-commit view on click: Click a commit row (NOT the checkbox). Verify the diff pane re-renders with that commit's diff. A title strip appears at the top of the diff pane showing {short_sha} — {message} on line 1 and {author} · {date} on line 2. The clicked row shows a 2px left border in --color-focus and bolder text.

    4. COMMIT-04 — Keyboard navigation: With a commit clicked, press the right arrow. Verify the active row moves to the next commit and the diff updates. Press left to go back. At index 0 press left → nothing happens (boundary stop). At the last index press right → nothing happens. Press up/down → no commit-nav behavior (diff scrolls normally).

    5. COMMIT-03 — Mode toggle via drawer close: In per-commit view, click the "Commits" toggle. Verify the drawer closes, the title strip disappears, and the diff returns to the full branch diff. The active row indicator is gone (state cleared).

    6. DIFF-05 — Checkbox filtering: Re-open the drawer. Uncheck one or two commits. Verify the diff updates to show only the union of FileDiff[] from the still-checked commits. Files touched only by unchecked commits no longer appear. Re-check → returns to /api/diff/branch result.

    7. D-13 — Orthogonal gestures: Click a commit row to enter per-commit view. Uncheck the active commit's checkbox. Verify the diff pane CONTINUES showing the clicked commit's diff (unchecking has no immediate effect in per-commit mode). Close and re-open the drawer; in branch view, the unchecked commit is now excluded from the union.

    8. Visual / styling spot checks: Drawer right edge has 1px border in --color-border. Active row left edge has 2px blue border (--color-focus). SHA chip has a slight blue-tint background. Empty / loading states: if no commits exist, drawer shows "No commits on this branch" + "This branch has no commits beyond the base branch."

    If ANY check fails, capture the failure (devtools console error text, visual deviation, or screenshot path) and paste into the resume signal.
  </how-to-verify>
  <action>
    Pause autonomous execution and present the eight-check verification script above to the user. Do NOT proceed to any subsequent plan until the user confirms with "approved" or describes failures. Record the user's response verbatim in the SUMMARY for this plan.
  </action>
  <verify>
    <automated>echo "human verify — no automated check; user must type approved in resume signal"; test -f .planning/phases/26-commit-navigation/26-03-PLAN.md</automated>
    <human-check>User has typed "approved" in the resume signal after running all 8 checks above</human-check>
  </verify>
  <acceptance_criteria>
    - User has typed "approved" (or equivalent confirmation) in response to the resume signal
    - For any check that did NOT pass, the user has described the failure mode in writing (devtools error, visual deviation, or attached screenshot path)
    - The SUMMARY for this plan records the resume-signal text verbatim
    - All five Phase 26 requirements (COMMIT-01, COMMIT-02, COMMIT-03, COMMIT-04, DIFF-05) are reported as PASS by the user
    - D-13 orthogonality check is reported as PASS
  </acceptance_criteria>
  <done>
    User confirms all 8 manual checks pass. Resume signal text captured in the SUMMARY. No code changes occur in this task.
  </done>
  <resume-signal>
    Type "approved" if all 8 checks pass. Otherwise, describe which check(s) failed and what you observed. Plan 26-03 cannot be considered complete until all five requirement-level checks (COMMIT-01..04, DIFF-05) and the D-13 orthogonality check pass.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → `/api/diff/commit/{sha}` | SHA path param flows to backend; backend validates via `git2::Oid::from_str` (Phase 24 — already mitigated, returns 400 on invalid hex) |
| window `keydown` events → setState | Global keyboard event handler in CodeReviewApp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-T1 | Tampering | SHA passed in URL path to /api/diff/commit/{sha} | accept (already mitigated upstream) | Backend `src/diff_api.rs` line 313 validates via `git2::Oid::from_str(&sha)` returning HTTP 400 on non-hex / wrong-length input. Frontend only passes SHAs originally fetched from `/api/commits` (server-provided, not user-typed). |
| T-26-03-D1 | Denial of Service | DIFF-05 union triggers N parallel /api/diff/commit/{sha} calls | mitigate | COMMIT_LIMIT in Rust = 500. Worst case: 500 parallel local-loopback requests against a single-binary server — well within axum/tokio capacity for a single-user local tool. No additional rate-limiting needed. |
| T-26-03-D2 | Resource leak | window 'keydown' listener attached in useEffect | mitigate | The useEffect returns `() => window.removeEventListener('keydown', handleKeyDown)`. Acceptance criterion asserts presence of the cleanup. Pitfall 2 in RESEARCH (stale closure) addressed by including `[viewMode, activeCommitSha, commits]` in deps. |
| T-26-03-I1 | Information Disclosure | drawer renders commit author email | accept | Email is git metadata already visible to the user via local `git log`. No new exposure beyond what /api/commits already returns; rendered via React text node (no HTML injection vector). |

No package-manager installs in this plan — Package Legitimacy Audit not applicable.
</threat_model>

<verification>
- `cd ui && npx vitest run code-review/` exits 0 — entire code-review test suite green
- `cd ui && npx tsc --noEmit` exits 0
- `cd ui && npm run lint -- --max-warnings 0` exits 0
- `cargo build --release` succeeds (UI is embedded via rust-embed build pipeline)
- `cargo test` exits 0 — no Rust regressions (no Rust files modified in this phase)
- Human verification: all 8 manual smoke-test checks pass (COMMIT-01, COMMIT-02, COMMIT-03, COMMIT-04, DIFF-05, D-13 orthogonality, plus visual styling spot checks)
</verification>

<success_criteria>
1. AppToolbar renders a "Commits" toggle button that drives drawerOpen
2. CommitDrawer mounts inside the existing aside (positioned absolute, overlaying FileListPane) when drawerOpen === true
3. Clicking a commit row → viewMode='commit' + activeCommitSha=sha → DiffPane fetches /api/diff/commit/{sha} and renders title strip (COMMIT-02)
4. Closing the drawer → viewMode='branch' + activeCommitSha=null → DiffPane returns to full branch view (COMMIT-03)
5. Arrow keys navigate prev/next only in per-commit mode, stop at boundaries (COMMIT-04)
6. Unchecking commits in branch mode triggers client-side union of /api/diff/commit/{sha} responses; per-commit mode is unaffected by unchecks (DIFF-05 + D-13)
7. checkedCommitShas seeded to all shas on first commits load (D-08 opt-out)
8. No backend changes; tests + types + lint + release build all pass
9. Human-verify checkpoint approves the integrated UX
</success_criteria>

<output>
Create `.planning/phases/26-commit-navigation/26-03-SUMMARY.md` when done. Include:
- The four commit SHAs (AppToolbar, useDiff, CodeReviewApp, and any fixups)
- Final test counts for all test files in code-review/
- The user's "approved" signal from Task 4
- Any deviations from spec captured during human verify
</output>
