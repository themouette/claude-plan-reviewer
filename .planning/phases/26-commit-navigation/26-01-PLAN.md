---
phase: 26-commit-navigation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - ui/src/code-review/types.ts
  - ui/src/code-review/hooks/useCommits.ts
  - ui/src/code-review/hooks/useCommits.test.ts
autonomous: true
requirements:
  - COMMIT-01
user_setup: []

must_haves:
  truths:
    - "Codebase exports a Commit TypeScript interface mirroring the Rust JSON shape (snake_case short_sha)"
    - "fetchCommitsOnce(doFetch) is a pure injectable function that resolves to { commits, truncated, error } and never throws"
    - "useCommits() hook returns { commits, loading, error, truncated }; loading starts true, becomes false on resolve"
    - "Hook cleanup sets cancelledRef so post-unmount setState is skipped"
    - "Tests pass without any React renderer — fetchCommitsOnce is exercised directly"
    - "ESLint coupling rule still holds: no import from reviewer-v2/ in any new file"
  artifacts:
    - path: "ui/src/code-review/types.ts"
      provides: "Commit interface (sha, short_sha, message, author, email, date) alongside existing FileDiff"
      contains: "export interface Commit"
    - path: "ui/src/code-review/hooks/useCommits.ts"
      provides: "fetchCommitsOnce pure function + useCommits React hook"
      exports: ["fetchCommitsOnce", "useCommits", "DoFetch", "FetchCommitsResult", "UseCommitsResult"]
    - path: "ui/src/code-review/hooks/useCommits.test.ts"
      provides: "Vitest tests for fetchCommitsOnce covering 200, non-ok, and throw paths"
      contains: "describe('fetchCommitsOnce'"
  key_links:
    - from: "ui/src/code-review/hooks/useCommits.ts"
      to: "/api/commits"
      via: "doFetch in fetchCommitsOnce"
      pattern: "/api/commits"
    - from: "ui/src/code-review/hooks/useCommits.ts"
      to: "ui/src/code-review/types.ts"
      via: "type-only import of Commit"
      pattern: "from '../types'"
---

<objective>
Build the foundation layer for commit navigation: the `Commit` TypeScript interface (mirrors Rust `Commit` struct exactly — snake_case `short_sha` preserved per the existing `FileDiff.previous_filename` convention) and the `useCommits` hook (structural mirror of `useDiff` — pure `fetchCommitsOnce(doFetch)` for direct testing + React `useCommits()` with `cancelledRef`).

Purpose: D-12 mandates injectable `doFetch` pattern so tests can call the pure function directly without a React renderer. This plan is TDD per CLAUDE.md test coverage requirement — write failing test cases first, then implement.

Output: A typed `Commit` model, a tested data-fetching hook for `/api/commits`, and a Vitest test file (three behavior cases) that passes when the implementation is correct.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-commit-navigation/26-CONTEXT.md
@.planning/phases/26-commit-navigation/26-RESEARCH.md
@.planning/phases/26-commit-navigation/26-PATTERNS.md
@.planning/phases/26-commit-navigation/26-UI-SPEC.md

@ui/src/code-review/types.ts
@ui/src/code-review/hooks/useDiff.ts
@ui/src/code-review/hooks/useDiff.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/diff_api.rs (Rust serializes via serde Serialize with NO rename_all — snake_case is JSON-wire):
```rust
pub struct Commit {
    pub sha: String,
    pub short_sha: String,
    pub message: String,
    pub author: String,
    pub email: String,
    pub date: String, // ISO 8601 / RFC 3339
}

pub struct CommitList {
    pub commits: Vec<Commit>,
    pub truncated: bool,
}
```

From ui/src/code-review/types.ts (existing — `previous_filename` proves snake_case convention is correct for this codebase):
```typescript
export interface FileDiff {
  filename: string
  previous_filename?: string  // snake_case matches Rust JSON output (no rename_all on struct)
  status: 'added' | 'removed' | 'modified' | 'renamed' | 'copied'
  // ... other fields
}
```

From ui/src/code-review/hooks/useDiff.ts (the analog `useCommits` must mirror structurally):
```typescript
export type DoFetch = (url: string) => Promise<Response>

export interface FetchDiffResult {
  files: FileDiff[]
  error: string | null
}

export async function fetchDiffOnce(doFetch: DoFetch, contextLines?: number): Promise<FetchDiffResult>
export interface UseDiffResult { files, loading, error, refetch }
export function useDiff(): UseDiffResult
```

`useDiff` initializes `loading: true` and never calls `setLoading(true)` inside the mount `useEffect` body — this avoids the `react-hooks/set-state-in-effect` rule. `useCommits` MUST do the same.

`useCommits` has NO `refetch` method (commits don't change during a session — D-12 / RESEARCH says no refetch needed; useDiff's refetch exists because Expand All re-fetches the branch diff).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Commit interface to types.ts and write failing useCommits tests (RED)</name>
  <files>ui/src/code-review/types.ts, ui/src/code-review/hooks/useCommits.test.ts</files>
  <read_first>
    - ui/src/code-review/types.ts (current FileDiff interface — Commit interface appended below it; preserve snake_case convention)
    - ui/src/code-review/hooks/useDiff.test.ts (analog test file — mirror its structure exactly for useCommits.test.ts)
    - .planning/phases/26-commit-navigation/26-PATTERNS.md (Pattern Assignments → useCommits.test.ts section, lines 95-150)
    - .planning/phases/26-commit-navigation/26-RESEARCH.md (Pattern 1: useCommits Hook — Injectable doFetch)
    - src/diff_api.rs lines 30-38 (Rust Commit struct — JSON shape is the canonical source of truth)
  </read_first>
  <behavior>
    - types.ts exports a new `Commit` interface with exactly these fields, all `string`: sha, short_sha, message, author, email, date. Place it AFTER the existing FileDiff interface; do NOT modify FileDiff.
    - useCommits.test.ts has a `describe('fetchCommitsOnce', () => { ... })` block with three `it` cases:
      - "resolves to { commits, error: null } on a 200 response" — doFetch returns Response with JSON body `{ commits: [sampleCommit], truncated: false }`, expect result.error === null, result.commits.length === 1, result.truncated === false
      - "resolves to { commits: [], error: 'fetch failed' } on non-ok response" — doFetch returns Response with status 500, expect result.commits.length === 0, result.error === 'fetch failed'
      - "resolves to { commits: [], error: 'network error' } when doFetch throws" — doFetch returns Promise.reject, expect result.commits.length === 0, result.error === 'network error'
    - The test file uses a `sampleCommit: Commit` fixture with all six fields populated (sha: 'abc1234567890abcdef', short_sha: 'abc1234', message: 'Fix login bug', author: 'Alice', email: 'alice@example.com', date: '2026-05-24T10:00:00Z').
    - The test file imports from './useCommits' (the file does not exist yet — `npx vitest run` MUST fail with module-not-found or with the three test failures).
  </behavior>
  <action>
    Step 1 — Modify `ui/src/code-review/types.ts`: append a new `export interface Commit` after the existing `FileDiff` interface. Fields (all string, all required, snake_case to match Rust JSON wire format): `sha`, `short_sha`, `message`, `author`, `email`, `date`. Add a short comment `// snake_case matches Rust JSON output (no rename_all on struct)` consistent with the existing FileDiff convention.

    Step 2 — Create `ui/src/code-review/hooks/useCommits.test.ts` modeled on `useDiff.test.ts` (3 test cases — no `?context=` cases because useCommits accepts no parameters). Imports: `describe`, `it`, `expect` from `'vitest'`; `fetchCommitsOnce` from `'./useCommits'`; `type Commit` from `'../types'`. Define `sampleCommit: Commit` literal at the top. Wrap all three cases in `describe('fetchCommitsOnce', () => { ... })`.

    Step 3 — Confirm RED state: run `cd ui && npx vitest run hooks/useCommits.test.ts` — expect the test runner to fail with a module-resolution error or three failing tests because `./useCommits` does not yet exist. Record the failure in your summary; do NOT proceed to task 2 with a passing test.
  </action>
  <verify>
    <automated>cd ui && npx vitest run code-review/hooks/useCommits.test.ts 2>&1 | grep -E "FAIL|Error|Cannot find module|0 passed" || true</automated>
    <automated>grep -c "export interface Commit" ui/src/code-review/types.ts</automated>
    <automated>grep -c "describe('fetchCommitsOnce'" ui/src/code-review/hooks/useCommits.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/types.ts` contains the literal string `export interface Commit`
    - `ui/src/code-review/types.ts` contains all six fields named exactly: `sha`, `short_sha`, `message`, `author`, `email`, `date` (each `: string`)
    - `ui/src/code-review/types.ts` STILL contains `export interface FileDiff` (FileDiff was not removed or modified)
    - `ui/src/code-review/hooks/useCommits.test.ts` contains `describe('fetchCommitsOnce'`
    - `ui/src/code-review/hooks/useCommits.test.ts` contains exactly three `it(` cases corresponding to the three behavior cases above
    - `ui/src/code-review/hooks/useCommits.test.ts` contains `from './useCommits'`
    - `npx vitest run code-review/hooks/useCommits.test.ts` FAILS (RED state confirmed — exit code nonzero or output contains "FAIL" / "Cannot find module")
  </acceptance_criteria>
  <done>
    types.ts exports Commit interface alongside FileDiff. useCommits.test.ts exists with three failing test cases. Vitest run confirms RED state. Task 1 must be committed as `test(26-01): add failing useCommits tests + Commit type` before Task 2 begins.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fetchCommitsOnce + useCommits to make tests GREEN</name>
  <files>ui/src/code-review/hooks/useCommits.ts</files>
  <read_first>
    - ui/src/code-review/hooks/useDiff.ts (the structural template — copy import shape, DoFetch type, FetchDiffResult interface, useEffect cancelledRef pattern, loading-init-true convention)
    - ui/src/code-review/hooks/useCommits.test.ts (created in Task 1 — the implementation must make all three cases pass)
    - .planning/phases/26-commit-navigation/26-PATTERNS.md (Pattern Assignments → useCommits.ts section, lines 25-91)
    - .planning/phases/26-commit-navigation/26-RESEARCH.md (Pattern 1, lines 178-225; Common Pitfalls section)
  </read_first>
  <behavior>
    - fetchCommitsOnce(doFetch) returns Promise<FetchCommitsResult> where FetchCommitsResult is `{ commits: Commit[]; truncated: boolean; error: string | null }`.
    - Successful 200 response with JSON `{ commits, truncated }` → `{ commits, truncated, error: null }`.
    - Non-ok response (e.g., status 500) → `{ commits: [], truncated: false, error: 'fetch failed' }` (matches useDiff exact string).
    - doFetch throws → `{ commits: [], truncated: false, error: 'network error' }` (matches useDiff exact string).
    - useCommits() returns `{ commits, loading, error, truncated }`. NO `refetch` method (commits don't change in a session — see Patterns Map "Key constraint" note).
    - Initial state: `loading: true`, `commits: []`, `truncated: false`, `error: null`. cancelledRef pattern matches useDiff.
    - The mount useEffect MUST NOT call `setLoading(true)` synchronously (loading is already true on init — react-hooks/set-state-in-effect rule).
  </behavior>
  <action>
    Create `ui/src/code-review/hooks/useCommits.ts`. Imports: `useEffect, useRef, useState` from `'react'`; `type Commit` from `'../types'`.

    Exports in order:
    1. `export type DoFetch = (url: string) => Promise<Response>`
    2. `export interface FetchCommitsResult { commits: Commit[]; truncated: boolean; error: string | null }`
    3. `export async function fetchCommitsOnce(doFetch: DoFetch): Promise<FetchCommitsResult>` — calls `doFetch('/api/commits')`, returns `{ commits, truncated, error: null }` on ok, `{ commits: [], truncated: false, error: 'fetch failed' }` on non-ok, `{ commits: [], truncated: false, error: 'network error' }` in catch.
    4. `export interface UseCommitsResult { commits: Commit[]; loading: boolean; error: string | null; truncated: boolean }`
    5. `export function useCommits(): UseCommitsResult` — initial state `loading: true`, `commits: []`, `truncated: false`, `error: null`. Mount useEffect calls `fetchCommitsOnce(globalThis.fetch.bind(globalThis))`, sets state on resolve guarded by cancelledRef. Cleanup sets cancelledRef.current = true. No refetch method exported.

    Do NOT use `JSON.stringify` or any non-fetch dependency. The implementation is a structural copy of `useDiff.ts` minus the `?context=` URL builder and minus `refetch`.

    After writing, run `npx vitest run code-review/hooks/useCommits.test.ts` and confirm GREEN (all three cases pass). This is the GREEN state for TDD — commit as `feat(26-01): implement useCommits hook` separately from Task 1's test commit.
  </action>
  <verify>
    <automated>cd ui && npx vitest run code-review/hooks/useCommits.test.ts 2>&1 | tee /tmp/26-01-test.log; grep -E "3 passed|Tests +3 passed" /tmp/26-01-test.log</automated>
    <automated>grep -c "export async function fetchCommitsOnce" ui/src/code-review/hooks/useCommits.ts</automated>
    <automated>grep -c "export function useCommits" ui/src/code-review/hooks/useCommits.ts</automated>
    <automated>grep -c "cancelledRef" ui/src/code-review/hooks/useCommits.ts</automated>
    <automated>grep -c "refetch" ui/src/code-review/hooks/useCommits.ts</automated>
    <automated>grep -c "reviewer-v2/" ui/src/code-review/hooks/useCommits.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `ui/src/code-review/hooks/useCommits.ts` exists
    - Source contains `export async function fetchCommitsOnce` (exactly once)
    - Source contains `export function useCommits` (exactly once)
    - Source contains `export type DoFetch`
    - Source contains `export interface FetchCommitsResult`
    - Source contains `export interface UseCommitsResult`
    - Source contains the literal URL `'/api/commits'`
    - Source contains `cancelledRef` (mount-safety pattern)
    - Source contains `'fetch failed'` and `'network error'` string literals (exact matches to useDiff)
    - Source does NOT contain `refetch` (per RESEARCH "Key constraint" — commits don't change per session)
    - Source does NOT contain `reviewer-v2/`
    - `cd ui && npx vitest run code-review/hooks/useCommits.test.ts` exits 0 with "3 passed"
    - `cd ui && npm run lint -- --max-warnings 0 ui/src/code-review/hooks/useCommits.ts` reports no errors (ESLint coupling rule respected)
  </acceptance_criteria>
  <done>
    fetchCommitsOnce and useCommits implemented as structural mirror of useDiff (minus refetch and ?context). All three test cases GREEN. Task committed as `feat(26-01): implement useCommits hook`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server `/api/commits` | Request initiated by frontend; response shape is server-controlled |
| server `/api/commits` → frontend state | JSON deserialized via `res.json()` then placed in React state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-T1 | Tampering | response JSON shape | accept | Backend is local (127.0.0.1 only — single-binary, no remote). Local-only trust boundary already established for v0.7.0 milestone. No additional client-side schema validation needed; type assertion via `as { commits: Commit[]; truncated: boolean }` mirrors existing `useDiff` pattern. |
| T-26-01-I1 | Information Disclosure | commit author email | accept | The email is git metadata already visible to the user via `git log` on the same repo. No new exposure. |
| T-26-01-D1 | Denial of Service | malformed response causing throw | mitigate | `try/catch` wraps `await doFetch + res.json()`; failure returns `{ commits: [], error: 'network error' }` instead of throwing into React. Already covered by Task 1 test case 3. |

No package-manager installs in this plan — Package Legitimacy Audit not applicable.
</threat_model>

<verification>
- `cd ui && npx vitest run code-review/hooks/useCommits.test.ts` exits 0, "3 passed"
- `cd ui && npm run lint -- --max-warnings 0` exits 0 (ESLint no-restricted-imports rule for reviewer-v2 coupling holds)
- `cd ui && npx tsc --noEmit` exits 0 (TypeScript compiles — Commit interface used correctly)
- Source of truth: `grep -c "export interface Commit" ui/src/code-review/types.ts` === 1
</verification>

<success_criteria>
1. `Commit` interface added to types.ts with all six fields named exactly as per Rust JSON wire format (snake_case)
2. `useCommits.test.ts` contains three behavior cases covering 200 / non-ok / throw paths
3. `useCommits.ts` exports fetchCommitsOnce + useCommits and passes the test suite
4. No `reviewer-v2/` imports introduced (ARCH-01 coupling rule preserved)
5. Two atomic commits exist: one for failing tests (RED) and one for implementation (GREEN)
</success_criteria>

<output>
Create `.planning/phases/26-commit-navigation/26-01-SUMMARY.md` when done. Include the two commit SHAs and the final test output (3 passed).
</output>
