---
phase: 07.3-insert-phase-b-from-planning-research-integration-plugin-rew
plan: "01"
subsystem: cli-dispatch
tags: [rust, cli, hooks, subcommand, deprecation]
dependency_graph:
  requires: [07.2-04]
  provides: [review-hook-subcommand, updated-hook-command-strings]
  affects: [src/main.rs, src/integrations/claude.rs, src/integrations/gemini.rs, src/update.rs]
tech_stack:
  added: []
  patterns: [clap-subcommand-dispatch, eprintln-deprecation-warning]
key_files:
  created: []
  modified:
    - src/main.rs
    - src/integrations/claude.rs
    - src/integrations/gemini.rs
    - src/update.rs
decisions:
  - "ReviewHook variant added as first variant in Commands enum for discoverability"
  - "Deprecation warning uses eprintln! (stderr only) — stdout reserved for hook JSON output"
  - "--plan-file path (opencode flow) does NOT receive deprecation warning"
  - "name field in Gemini hooks.json stays 'plan-reviewer'; only command field changed"
metrics:
  duration_seconds: 205
  completed_date: "2026-04-10T23:53:20Z"
  tasks_completed: 2
  files_modified: 4
requirements:
  - INTEG-05
---

# Phase 07.3 Plan 01: Add review-hook Subcommand Summary

Add explicit `plan-reviewer review-hook` subcommand and update all hook command strings from bare `"plan-reviewer"` to `"plan-reviewer review-hook"` across all four write locations.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add Commands::ReviewHook variant and deprecation warning in main.rs | 4215739 | src/main.rs |
| 2 | Update all four hook command strings to "plan-reviewer review-hook" | 55935d0 | src/integrations/claude.rs, src/integrations/gemini.rs, src/update.rs |

## What Was Built

### Task 1: Commands::ReviewHook subcommand (src/main.rs)

- Added `ReviewHook` variant as the first variant in the `Commands` enum, with doc comment explaining its purpose and the deprecation of the bare invocation
- Added dispatch arm `Some(Commands::ReviewHook) => run_hook_flow(cli.no_browser, cli.port)` before `Install` in the match block
- Added deprecation warning in `None` arm's else branch using `eprintln!` (stderr only) — stdout reserved for hook JSON output
- The `--plan-file` path (opencode flow) is unaffected — no deprecation warning
- Added `Commands` to the test module `use super::` imports
- Added 4 tests: `test_cli_review_hook_subcommand_parses`, `test_cli_review_hook_with_no_browser_flag`, `test_cli_review_hook_with_port_flag`, `test_cli_no_subcommand_is_none`

### Task 2: Hook command string updates (3 files)

- `src/integrations/claude.rs`: `install()` now writes `"command": "plan-reviewer review-hook"` in hooks/hooks.json; existing test updated to assert the new string; new `install_writes_hook_command_with_hook_subcommand` test added
- `src/integrations/gemini.rs`: `install()` raw string literal updated to `"command":"plan-reviewer review-hook"`; existing test updated; new `install_writes_hook_command_with_hook_subcommand` test added
- `src/update.rs`: Both `write_claude_plugin_files` and `write_gemini_extension_files` updated; two new tests `test_write_claude_plugin_files_uses_hook_subcommand` and `test_write_gemini_extension_files_uses_hook_subcommand` added

## Verification Results

```
cargo fmt --check    — clean (exit 0)
cargo clippy -- -D warnings  — zero warnings (exit 0)
cargo test           — 90 unit tests + 13 integration tests: all pass (exit 0)
grep Commands::ReviewHook src/main.rs   — found (enum variant + dispatch arm + 3 test assertions)
grep 'deprecation warning' src/main.rs  — found in eprintln! inside None arm else branch
grep 'plan-reviewer review-hook' src/integrations/claude.rs  — found (3 occurrences)
grep 'plan-reviewer review-hook' src/integrations/gemini.rs  — found (4 occurrences)
grep -c 'plan-reviewer review-hook' src/update.rs  — 6 occurrences
```

## Deviations from Plan

None — plan executed exactly as written.

The plan noted: "If Phase 07.2 has NOT yet executed, these functions will not exist and this task cannot proceed." Phase 07.2 had executed — `write_claude_plugin_files` and `write_gemini_extension_files` were present in `src/update.rs` as expected.

## Known Stubs

None. All hook command strings are wired to the real `"plan-reviewer review-hook"` value. No placeholders remain.

## Threat Flags

No new security-relevant surface introduced. The deprecation warning is a static string literal written to stderr. Hook command strings are hardcoded literals. No user input touches any of the modified code paths.

## Self-Check: PASSED

- src/main.rs contains `ReviewHook,` variant: FOUND
- src/main.rs contains `Some(Commands::ReviewHook) =>` dispatch: FOUND
- src/main.rs contains `deprecation warning` in eprintln!: FOUND
- src/integrations/claude.rs contains `plan-reviewer review-hook`: FOUND
- src/integrations/gemini.rs contains `plan-reviewer review-hook`: FOUND
- src/update.rs contains `plan-reviewer review-hook` (6 occurrences): FOUND
- Commit 4215739 exists: VERIFIED (git log)
- Commit 55935d0 exists: VERIFIED (git log)
- cargo test exits 0 (103 tests total): VERIFIED
