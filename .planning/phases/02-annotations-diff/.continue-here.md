---
context: phase
phase: 02-annotations-diff
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-04-09T20:07:42.864Z
---

<current_state>
Plan 02-04 human verification. Task 1 done (build). Task 2 is extended UX bug-fix across multiple sessions. All code changes built and in working tree — not yet committed. User needs to test the binary.
</current_state>

<completed_work>
- Task 1: Build binary — Done (commit 21a804e)
- Task 2 (in progress):
  - Fixed Range collapse: offset-based tracking (getRangeOffsets/rangeFromOffsets) in useTextSelection.ts
  - Fixed annotation highlights: annotationOffsetsRef + useLayoutEffect (no deps) rebuilds from offsets
  - Fixed hover text→sidebar card: document mousemove + caretRangeFromPoint (was broken because planRef.current null at mount)
  - Added auto-focus textarea: useLayoutEffect queries [data-annotation-id] textarea on focusAnnotationId change
  - Added aligned card layout: computeAndApplyLayout() imperative DOM positioning + scroll sync
</completed_work>

<remaining_work>
- Task 2: User confirms all scenarios pass:
  1. Selection highlight persists after clicking Comment
  2. Annotation cards align to selection Y level in sidebar
  3. Sidebar cards scroll in sync with plan scroll
  4. Hover over highlighted text highlights matching sidebar card
  5. Clicking Comment auto-focuses textarea
- Commit all changes
- Write 02-04-SUMMARY.md
- Update STATE.md and ROADMAP.md
- Run /gsd:code-review 2 and /gsd:verify-work
</remaining_work>

<decisions_made>
- offset-based Range tracking (not live Range) — live Ranges collapse when React reconciles dangerouslySetInnerHTML
- annotationOffsetsRef Map<id,{start,end}> — same reason
- useLayoutEffect (no deps) for annotation highlights — useEffect([annotations]) ran after paint
- document mousemove listener (not planRef) — planRef.current null at mount time
- computeAndApplyLayout() imperative (no React state) — scroll-driven updates can't trigger re-renders
- Estimated heights: delete=100px, comment/replace=212px for collision resolution
- data-cards-scroll overflow:hidden, scrollTop synced to planTab — cards move with plan text
</decisions_made>

<blockers>
None.
</blockers>

<next_action>
Ask user to test the binary with a plan that has multiple annotations. If aligned layout heights are off, adjust estimates in computeAndApplyLayout() in App.tsx. When all scenarios pass, commit, write summary, verify.
</next_action>
