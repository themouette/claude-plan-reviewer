---
phase: 28
plan: 03
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified:
  - ui/src/code-review/CodeReviewSubmitPopover.tsx
  - ui/src/code-review/CodeReviewSubmitPopover.test.ts
  - ui/src/code-review/AppToolbar.tsx
  - ui/src/code-review/AppToolbar.test.ts
  - ui/src/code-review/CodeReviewApp.tsx
  - ui/src/code-review/CodeReviewApp.test.ts
autonomous: false
requirements: [SUBMIT-01, SUBMIT-02, SUBMIT-03, SUBMIT-04]

must_haves:
  truths:
    - "The Approve button in the AppToolbar is disabled when comments.length > 0 (gate D-06, SUBMIT-01)"
    - "The Request Changes button in the AppToolbar is disabled when comments.length === 0 (gate D-06, SUBMIT-04)"
    - "Clicking Approve while online opens the CodeReviewSubmitPopover with an optional Global Instruction textarea (SUBMIT-02)"
    - "Confirming the popover (with or without a typed instruction) calls `fetch('/api/decide', { method: 'POST', body: buildCodeReviewPayload('approved', comments, globalInstruction?) })`"
    - "Clicking Request Changes submits immediately (NO popover) with body `buildCodeReviewPayload('changes_requested', comments)`"
    - "When `useHeartbeat()` returns 'offline', both submit handlers write the same JSON to `navigator.clipboard` instead of POSTing — clipboard fallback is preserved (SUBMIT-03 success criterion 5)"
    - "On clipboard write failure, a read-only textarea with the JSON payload appears with a Dismiss button"
    - "After a successful confirmed_approve or confirmed_request_changes state, the tab attempts to close after 500ms via window.close()"
    - "CodeReviewApp.tsx calls `useHeartbeat()` (imported from '../shared/useHeartbeat') and passes `connectivity` plus `comments` to AppToolbar"
    - "CodeReviewApp.test.ts no longer contains the `does NOT call useHeartbeat` negative assertion; it contains positive assertions that useHeartbeat is imported from shared/ and called"
    - "`cd ui && npm run lint && npm test -- --run` exits 0"
  artifacts:
    - path: "ui/src/code-review/CodeReviewSubmitPopover.tsx"
      provides: "Approve confirmation popover with optional global instruction textarea"
      contains: "aria-label=\"Approve review\""
    - path: "ui/src/code-review/CodeReviewSubmitPopover.test.ts"
      provides: "Source-assertion + behavioral tests for popover dismiss + submit semantics"
    - path: "ui/src/code-review/AppToolbar.tsx"
      provides: "AppToolbar with submit controls (Approve, Request Changes, status messages, popover trigger)"
      contains: "'Approve'"
    - path: "ui/src/code-review/CodeReviewApp.tsx"
      provides: "Calls useHeartbeat() and passes connectivity + comments + onApprove + onRequestChanges to AppToolbar"
      contains: "useHeartbeat"
  key_links:
    - from: "ui/src/code-review/AppToolbar.tsx"
      to: "ui/src/code-review/buildCodeReviewPayload.ts"
      via: "import { buildCodeReviewPayload, shouldUseClipboard } from './buildCodeReviewPayload'"
      pattern: "buildCodeReviewPayload"
    - from: "ui/src/code-review/AppToolbar.tsx"
      to: "ui/src/code-review/CodeReviewSubmitPopover.tsx"
      via: "import CodeReviewSubmitPopover from './CodeReviewSubmitPopover'"
      pattern: "CodeReviewSubmitPopover"
    - from: "ui/src/code-review/AppToolbar.tsx"
      to: "ui/src/shared/connectivity.ts"
      via: "import type { ConnectivityStatus } from '../shared/connectivity'"
      pattern: "from '\\.\\./shared/connectivity'"
    - from: "ui/src/code-review/CodeReviewApp.tsx"
      to: "ui/src/shared/useHeartbeat.ts"
      via: "import { useHeartbeat } from '../shared/useHeartbeat'"
      pattern: "from '\\.\\./shared/useHeartbeat'"
    - from: "ui/src/code-review/CodeReviewApp.tsx"
      to: "ui/src/code-review/AppToolbar.tsx"
      via: "passes connectivity={connectivity} comments={comments} onApprove={...} onRequestChanges={...}"
      pattern: "connectivity=\\{connectivity\\}"
---

<objective>
Wire the full code-review submission flow into the UI. Adds a new `CodeReviewSubmitPopover` component (parallel to `reviewer-v2/SubmitPopover` but copy-only — no imports), extends `AppToolbar.tsx` with submit controls + the submit-state machine + clipboard fallback, and modifies `CodeReviewApp.tsx` to call `useHeartbeat()` and pass `connectivity` + `comments` + submit callbacks down. Updates `CodeReviewApp.test.ts` to replace the negative useHeartbeat assertion with positive ones, and extends `AppToolbar.test.ts` with the new Phase 28 source assertions. Closes the human verification checkpoint by visually exercising every state in the submit-state machine in a real browser.

Purpose: Phase 28 success criteria all live in this plan — gate logic (D-06), Approve popover (D-05), structured JSON output via the typed serializer from plan 28-02, and offline clipboard preservation. The shared/ infrastructure from plan 28-01 is the precondition that lets `CodeReviewApp` import `useHeartbeat` from `'../shared/useHeartbeat'` without violating the ESLint code-review boundary rule.

Output: Two new files, four modified files, all lint + tests passing, and a human-verified browser walkthrough of the submission flow.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-review-submission/28-CONTEXT.md
@.planning/phases/28-review-submission/28-RESEARCH.md
@.planning/phases/28-review-submission/28-PATTERNS.md
@.planning/phases/28-review-submission/28-UI-SPEC.md
@.planning/phases/28-review-submission/28-01-SUMMARY.md
@.planning/phases/28-review-submission/28-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Available from plan 28-02 (buildCodeReviewPayload.ts) -->

```typescript
export type ReviewDecision = 'approved' | 'changes_requested'
export function buildCodeReviewPayload(
  decision: ReviewDecision,
  comments: CodeReviewComment[],
  globalInstruction?: string,
): string
export function shouldUseClipboard(status: ConnectivityStatus): boolean
```

<!-- Available from plan 28-01 (ui/src/shared/) -->

```typescript
// from '../shared/connectivity'
export type ConnectivityStatus = 'online' | 'offline'
// from '../shared/useHeartbeat'
export function useHeartbeat(): ConnectivityStatus
```

<!-- Available from Phase 27 (ui/src/code-review/types.ts) -->

```typescript
export type CodeReviewComment =
  | { id: string; type: 'line'; file: string; side: 'additions' | 'deletions'; lineNumber: number; endLineNumber?: number; text: string; createdAt: string }
  | { id: string; type: 'file'; file: string; text: string; createdAt: string }
```

<!-- Current AppToolbarProps (ui/src/code-review/AppToolbar.tsx lines 1-13) — Phase 28 adds 4 props -->

```typescript
export interface AppToolbarProps {
  diffStyle: 'unified' | 'split'
  contextExpanded: boolean
  contextLoading: boolean
  onDiffStyleChange: (style: 'unified' | 'split') => void
  onExpandAll: () => void
  commitsOpen: boolean
  onCommitsToggle: () => void
  allFilesExpanded: boolean
  filesCount: number
  onToggleAllFiles: () => void
  // Phase 28 ADDS:
  comments: CodeReviewComment[]
  connectivity: ConnectivityStatus
  onApprove: (globalInstruction?: string) => void
  onRequestChanges: () => void
}
```

<!-- Reference pattern for submit state machine (ui/src/reviewer-v2/SubmitControls.tsx, lines 13-19) — copy semantics, NOT imports -->

```typescript
type SubmitState =
  | 'idle'
  | 'popover_open'
  | 'confirmed_approve'             // (renamed from confirmed_allow)
  | 'confirmed_request_changes'     // (renamed from confirmed_deny)
  | 'clipboard_confirmed'
  | 'clipboard_error'
```

<!-- Reference pattern for popover dismiss + textarea (ui/src/reviewer-v2/SubmitPopover.tsx, lines 22-99) — copy semantics, NOT imports -->

Pattern: outside-click + Escape dismiss; sync-during-render reset of textarea when `open` flips false; Cmd/Ctrl+Enter submits; autoFocus textarea; `position: absolute; top: 40; right: 0; minWidth: 320; zIndex: 50; background: var(--color-surface); border: 1px solid var(--color-border); borderRadius: 8; padding: 16; boxShadow: '0 8px 24px rgba(0,0,0,0.3)'`.

<!-- Existing CodeReviewApp.test.ts negative assertion that MUST be removed (lines 85-87) -->

```typescript
it('does NOT call useHeartbeat (RESEARCH Open Question 1)', () => {
  expect(source).not.toContain('useHeartbeat')
})
```

<!-- Copywriting contract (UI-SPEC Copywriting Contract section) — verbatim required -->

| Element | Exact copy |
|---------|------------|
| Approve button label | `Approve` |
| Request Changes button label | `Request Changes` |
| Approve disabled title | `Cannot approve while comments exist` |
| Request Changes disabled title | `Add at least one comment before requesting changes` |
| Popover aria-label | `Approve review` |
| Popover textarea aria-label | `Global instruction (optional)` |
| Popover textarea placeholder | `Leave an instruction for the agent (optional)` |
| Popover submit button label | `Confirm Approve` |
| Confirmed approve label | `Approved` |
| Confirmed approve body | `You can close this tab.` |
| Confirmed request changes label | `Review submitted` |
| Confirmed request changes body | `You can close this tab.` |
| Clipboard confirmed label | `Copied to clipboard` |
| Clipboard confirmed body | `Paste into your Claude conversation.` |
| Clipboard error label | `Clipboard write failed` |
| Clipboard error dismiss button | `Dismiss` |
| Clipboard error textarea aria-label | `JSON payload — copy and paste into Claude` |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CodeReviewSubmitPopover component (popover for optional global instruction on Approve)</name>
  <files>ui/src/code-review/CodeReviewSubmitPopover.tsx, ui/src/code-review/CodeReviewSubmitPopover.test.ts</files>
  <read_first>
    - ui/src/reviewer-v2/SubmitPopover.tsx (full file — reference pattern only; do NOT import from it)
    - ui/src/code-review/AppToolbar.test.ts (source-assertion test pattern at top of file)
    - .planning/phases/28-review-submission/28-UI-SPEC.md (Copywriting Contract section)
    - .planning/phases/28-review-submission/28-PATTERNS.md (CodeReviewSubmitPopover pattern section)
  </read_first>
  <behavior>
    - Test 1: Component default export is a function.
    - Test 2: Source contains `aria-label="Approve review"` literal (NOT "Send feedback").
    - Test 3: Source contains the textarea label string `'Global instruction (optional)'`.
    - Test 4: Source contains the textarea placeholder `'Leave an instruction for the agent (optional)'`.
    - Test 5: Source contains the submit button label `'Confirm Approve'`.
    - Test 6: Source contains `var(--color-accent-approve)` (the submit button background — green, NOT red `--color-accent-deny`).
    - Test 7: Source does NOT contain `messageRequired` (the prop is removed in this version — message is always optional).
    - Test 8: Source contains `position: 'absolute'` and `top: 40` and `right: 0` and `minWidth: 320` (visual shell pinning per UI-SPEC).
    - Test 9: Source contains Escape-key + outside-click handlers — assert the source contains both `'Escape'` and `rootRef.current` (the outside-click guard).
    - Test 10: Source contains Cmd+Enter shortcut — assert `(e.metaKey || e.ctrlKey) && e.key === 'Enter'`.
    - Test 11: Source contains `autoFocus` on the textarea.
    - Test 12: Source does not import from `reviewer-v2/` (negative grep).
  </behavior>
  <action>
    Create `ui/src/code-review/CodeReviewSubmitPopover.tsx`. Component contract:

    Props interface `CodeReviewSubmitPopoverProps` exports `open: boolean`, `onDismiss: () => void`, `onConfirm: (globalInstruction?: string) => void`. Default export is the function `CodeReviewSubmitPopover(props): React.JSX.Element | null`.

    Behavior to implement: local state `message: string` + `prevOpen: boolean`; sync-during-render reset of `message` to `''` when `open` flips false (the SubmitPopover.tsx lines 13-18 pattern); one `useEffect` (gated on `open`) that attaches document-level `keydown` (Escape -> onDismiss) and `mousedown` (outside-click via `rootRef.current.contains(e.target as Node)` negation -> onDismiss); `return null` when `!open`.

    Root element: `<div ref={rootRef} role="dialog" aria-modal="true" aria-label="Approve review">` with these exact inline styles (sourced verbatim from UI-SPEC Spacing/Color sections): `position: 'absolute', top: 40, right: 0, minWidth: 320, zIndex: 50, background: 'var(--color-surface)', border: '1px solid var(--color-border)', borderRadius: 8, padding: 16, boxShadow: '0 8px 24px rgba(0, 0, 0, 0.3)'`.

    Textarea: `aria-label="Global instruction (optional)"`, `placeholder="Leave an instruction for the agent (optional)"`, `autoFocus`, `rows={4}`, `value={message}`, with these inline styles: width 100%, minHeight 80, fontSize 14, fontFamily inherit, background `var(--color-bg)`, color `var(--color-text-primary)`, border `1px solid var(--color-border)`, borderRadius 4, padding 12, resize vertical, boxSizing border-box, outline none. Wire `onChange` to `setMessage(e.target.value)`. On `(e.metaKey || e.ctrlKey) && e.key === 'Enter'`, call `onConfirm(message.trim() || undefined)` and `e.preventDefault()`. Imperative focus ring via inline onFocus/onBlur setting `outline: '2px solid var(--color-focus)'` with `outlineOffset: '2px'`.

    Submit button: label literal `Confirm Approve`, NO disabled state (message always optional), height 32, padding `0 16` (paddingLeft/Right 16), borderRadius 6, border none, background `var(--color-accent-approve)`, color `#fff`, fontSize 14, fontWeight 600, cursor pointer, opacity 1. Click handler: `onConfirm(message.trim() || undefined)`. Same imperative focus-ring pattern as the textarea.

    Create `ui/src/code-review/CodeReviewSubmitPopover.test.ts` using the source-assertion pattern: import `readFileSync` from `'fs'`, read `./CodeReviewSubmitPopover.tsx` into a `source` const, default-import the component for the function-export check, then add the 12 it() blocks listed in the behavior section above.

    Hard constraints: do NOT import anything from `ui/src/reviewer-v2/` (ESLint blocks this). Do NOT extend or wrap SubmitPopover.tsx — copy the pattern, change the copy, drop `messageRequired`. All text literals come verbatim from UI-SPEC's Copywriting Contract.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run CodeReviewSubmitPopover && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `ui/src/code-review/CodeReviewSubmitPopover.tsx` exists with a default-exported function
    - File `ui/src/code-review/CodeReviewSubmitPopover.test.ts` has at least 12 it() cases (one per behavior point above) and all pass
    - `grep -c 'aria-label="Approve review"' ui/src/code-review/CodeReviewSubmitPopover.tsx` returns at least 1
    - `grep -c "Confirm Approve" ui/src/code-review/CodeReviewSubmitPopover.tsx` returns at least 1
    - `grep -c "var(--color-accent-approve)" ui/src/code-review/CodeReviewSubmitPopover.tsx` returns at least 1
    - `grep -c "var(--color-accent-deny)" ui/src/code-review/CodeReviewSubmitPopover.tsx` returns 0 (the popover never uses the red accent)
    - `grep -c "messageRequired" ui/src/code-review/CodeReviewSubmitPopover.tsx` returns 0
    - `grep -c "reviewer-v2" ui/src/code-review/CodeReviewSubmitPopover.tsx ui/src/code-review/CodeReviewSubmitPopover.test.ts` returns 0
    - `cd ui && npm test -- --run CodeReviewSubmitPopover` exits 0
    - `cd ui && npm run lint` exits 0
    - `cd ui && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Popover component compiles, lints, passes its source-assertion suite, and contains every verbatim string from the UI-SPEC Copywriting Contract</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend AppToolbar with submit controls + state machine + clipboard fallback (and extend AppToolbar.test.ts)</name>
  <files>ui/src/code-review/AppToolbar.tsx, ui/src/code-review/AppToolbar.test.ts</files>
  <read_first>
    - ui/src/code-review/AppToolbar.tsx (full file — Phase 28 ADDS to this; existing exports stay)
    - ui/src/code-review/AppToolbar.test.ts (existing source-assertion patterns at lines 60-108 — Phase 28 appends similar blocks)
    - ui/src/reviewer-v2/SubmitControls.tsx (reference pattern only — semantics, NOT imports)
    - ui/src/code-review/buildCodeReviewPayload.ts (from plan 28-02; consumer for both online and offline paths)
    - ui/src/code-review/CodeReviewSubmitPopover.tsx (from Task 1 — Approve flow consumes it)
    - .planning/phases/28-review-submission/28-UI-SPEC.md (Copywriting Contract, Color, Gate Logic sections)
  </read_first>
  <behavior>
    Source assertions to add (alongside the existing 23 Phase 25/26 cases — APPEND, do not replace):
    - Test A: `AppToolbarProps includes comments: CodeReviewComment[]`
    - Test B: `AppToolbarProps includes connectivity: ConnectivityStatus`
    - Test C: `AppToolbarProps includes onApprove:` and `onRequestChanges:`
    - Test D: source contains `'Approve'` literal (the button label)
    - Test E: source contains `'Request Changes'` literal (the button label)
    - Test F: source contains `'Cannot approve while comments exist'` (disabled title — D-06)
    - Test G: source contains `'Add at least one comment before requesting changes'` (disabled title — D-06)
    - Test H: source contains `var(--color-accent-approve)` (Approve background)
    - Test I: source contains `var(--color-accent-deny)` (Request Changes background)
    - Test J: source contains the import `from './buildCodeReviewPayload'`
    - Test K: source contains the import `from '../shared/connectivity'` for `ConnectivityStatus`
    - Test L: source contains the import for `CodeReviewSubmitPopover from './CodeReviewSubmitPopover'`
    - Test M: source contains the SubmitState union literal `'confirmed_approve'`
    - Test N: source contains the SubmitState union literal `'confirmed_request_changes'`
    - Test O: source contains `'clipboard_confirmed'` and `'clipboard_error'`
    - Test P: source contains `window.close()` (auto-close after confirmed)
    - Test Q: source contains the auto-reset literal `3000` (clipboard_confirmed -> idle)
    - Test R: source contains the auto-close literal `500` (confirmed -> window.close)
    - Test S: source contains `comments.length === 0` (canApprove gate D-06)
    - Test T: source contains `comments.length > 0` (canRequestChanges gate D-06)
    - Test U: source contains `shouldUseClipboard(connectivity)` (offline branch entry)
    - Test V: source contains `buildCodeReviewPayload('approved'` AND `buildCodeReviewPayload('changes_requested'`
    - Test W: source contains `fetch('/api/decide'` (the POST endpoint)
    - Test X: source contains `'Approved'` literal (confirmed_approve label) AND `'Review submitted'` (confirmed_request_changes label)
    - Test Y: source contains `'Copied to clipboard'` AND `'Paste into your Claude conversation.'`
    - Test Z: source contains `'Clipboard write failed'` AND `'Dismiss'` AND `'JSON payload — copy and paste into Claude'`
    - Test AA: source does NOT contain `'reviewer-v2/'` (preserves the ESLint boundary)
    - Test BB: existing tests still pass (Phase 25/26 assertions — confirmed by running the entire file)
  </behavior>
  <action>
    Extend `ui/src/code-review/AppToolbar.tsx` (do NOT rewrite — preserve all existing Phase 25/26.2 props and buttons exactly):

    1) Add imports at the top of the file: `useEffect`, `useState` from `'react'`; `CodeReviewComment` from `'./types'` (type-only); `ConnectivityStatus` from `'../shared/connectivity'` (type-only); `buildCodeReviewPayload`, `shouldUseClipboard` from `'./buildCodeReviewPayload'`; default `CodeReviewSubmitPopover` from `'./CodeReviewSubmitPopover'`.

    2) Extend `AppToolbarProps` with four new fields (appended at the END of the existing interface so existing assertion line numbers do not shift): `comments: CodeReviewComment[]`, `connectivity: ConnectivityStatus`, `onApprove: (globalInstruction?: string) => void`, `onRequestChanges: () => void`. Destructure them in the function signature.

    3) Inside the function body, ABOVE the existing `makeFocusHandlers` helper, declare:
       - `type SubmitState = 'idle' | 'popover_open' | 'confirmed_approve' | 'confirmed_request_changes' | 'clipboard_confirmed' | 'clipboard_error'`
       - `const [submitState, setSubmitState] = useState<SubmitState>('idle')`
       - `const [clipboardJson, setClipboardJson] = useState('')`
       - `const canApprove = comments.length === 0`
       - `const canRequestChanges = comments.length > 0`

    4) Add two useEffect hooks (after the state declarations):
       - Auto-close: when `submitState === 'confirmed_approve' || submitState === 'confirmed_request_changes'`, set a 500ms `window.setTimeout` that calls `try { window.close() } catch { /* browser may block — ignore */ }`. Clean up on unmount via the returned function.
       - Auto-reset: when `submitState === 'clipboard_confirmed'`, set a 3000ms `window.setTimeout` that calls `setSubmitState('idle')`. Clean up on unmount.

    5) Add two async handlers `handleApprove(globalInstruction?: string)` and `handleRequestChanges()`. Both follow the SubmitControls.tsx pattern adapted for code-review:
       - If `shouldUseClipboard(connectivity)`: build the payload via `buildCodeReviewPayload(...)`, call `navigator.clipboard.writeText(json).then(() => setSubmitState('clipboard_confirmed')).catch(() => { setClipboardJson(json); setSubmitState('clipboard_error') })`. Return early.
       - Else: build the body via `buildCodeReviewPayload(...)`. Wrap in try/catch. Call `fetch('/api/decide', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body })`. If `res.ok || res.status === 409`, set the appropriate confirmed_* state. Else fall through to `setClipboardJson(body); setSubmitState('clipboard_error')`. On a thrown fetch error, do the same fallback (call `buildCodeReviewPayload` again to capture the JSON, set clipboardJson, set clipboard_error).
       - `handleApprove` uses decision literal `'approved'` and passes the `globalInstruction` through to `buildCodeReviewPayload`.
       - `handleRequestChanges` uses decision literal `'changes_requested'` and does NOT pass a global instruction (call `buildCodeReviewPayload('changes_requested', comments)` with only two arguments).

    6) Add the submit controls UI inside the existing `{/* Right: controls */}` div, REPLACING the existing `<div />` placeholder on line ~69 (the one preceded by `{/* Reserved: help / GitHub / theme — empty in Phase 25 (D-03) */}`). Wrap the new controls in a `<div style={{ position: 'relative', display: 'flex', alignItems: 'center', gap: 8 }}>` container that becomes the popover's positioning parent (so `position: absolute; top: 40; right: 0` on the popover anchors correctly).

       Inside this container, render conditionally on `submitState`:
       - When `submitState === 'idle' || submitState === 'popover_open'`: render `<button>Approve</button>` and `<button>Request Changes</button>` side by side, with the popover trailing as a sibling.
         - Approve button: `className="submit-btn"`, `disabled={!canApprove}`, `title={!canApprove ? 'Cannot approve while comments exist' : undefined}`. On click: `if (shouldUseClipboard(connectivity)) { void handleApprove(); } else { setSubmitState('popover_open'); }`. Visual: height 32, paddingLeft/Right 16, borderRadius 6, border none, background `var(--color-accent-approve)`, color `#fff`, fontSize 14, fontWeight 600, cursor `canApprove ? 'pointer' : 'default'`, opacity `canApprove ? 1 : 0.4`, outline none. onMouseOver/Out toggle background to `var(--color-accent-approve-hover)` only when canApprove. Use `makeFocusHandlers('approve')` for focus ring.
         - Request Changes button: `className="submit-btn"`, `disabled={!canRequestChanges}`, `title={!canRequestChanges ? 'Add at least one comment before requesting changes' : undefined}`, onClick `() => { void handleRequestChanges() }`. Same visual shape as Approve but `background: var(--color-accent-deny)`. Use `makeFocusHandlers('request-changes')`.
         - `<CodeReviewSubmitPopover open={submitState === 'popover_open'} onDismiss={() => setSubmitState('idle')} onConfirm={(gi) => { void handleApprove(gi) }} />` as a sibling of the buttons (inside the relative-positioned wrapper so its absolute positioning anchors correctly).
       - When `submitState === 'confirmed_approve'`: render `<div role="status" aria-live="polite" style={{display:'flex', alignItems:'center', gap:8}}>` containing `<span style={{fontSize:14, fontWeight:600, color:'var(--color-accent-approve)'}}>Approved</span>` and `<span style={{fontSize:14, color:'var(--color-text-secondary)'}}>You can close this tab.</span>`.
       - When `submitState === 'confirmed_request_changes'`: same shape as confirmed_approve, but label text `Review submitted` and body `You can close this tab.`.
       - When `submitState === 'clipboard_confirmed'`: same shape, label `Copied to clipboard` (color `var(--color-accent-approve)`), body `Paste into your Claude conversation.`.
       - When `submitState === 'clipboard_error'`: a column layout (`flexDirection: 'column'`) with a row containing `<span color: var(--color-accent-deny)>Clipboard write failed</span>` and a `Dismiss` button (height/padding small, border 1px solid var(--color-border), background var(--color-surface), color var(--color-text-secondary); click calls `setSubmitState('idle')`), plus a `<textarea readOnly value={clipboardJson} aria-label="JSON payload — copy and paste into Claude" onClick={(e) => (e.target as HTMLTextAreaElement).select()}>` with monospace styles (width 320, height 80, fontFamily monospace, fontSize 12, padding 8, borderRadius 4, border `1px solid var(--color-border)`, background `var(--color-bg)`, color `var(--color-text-primary)`, resize vertical, cursor text).

       Placement: this entire submit block goes WHERE the `<div />` placeholder is now (between the `{/* Reserved ... */}` comment and the `Commits` button). The existing AppToolbar.test.ts case at line ~46 asserts `/Reserved|D-03/` (regex) — keep an inline comment such as `{/* Phase 28 submit controls (replaces D-03 Reserved slot) */}` so that assertion still matches the `D-03` token.

    7) Append the new Phase 28 source-assertion `it()` cases (Tests A through AA above) to `ui/src/code-review/AppToolbar.test.ts` AT THE END of the existing `describe` block, following the exact pattern used by Phase 26 additions (lines 60-108). Do NOT remove or modify existing assertions.

    Hard constraints:
    - Do NOT import from `ui/src/reviewer-v2/` anywhere (ESLint blocks this).
    - Do NOT change the existing Phase 25/26 button order, props, or visual styles — only ADD.
    - All copy must match UI-SPEC's Copywriting Contract verbatim — no synonyms.
    - If extracting the submit logic into a sub-component inside AppToolbar.tsx (a local `SubmitBar()` function) helps readability, that is allowed per CONTEXT D-02 Claude's-discretion, but the JSX literals in the source file must still satisfy every source-assertion grep check.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run AppToolbar && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/AppToolbar.tsx` defines a `SubmitState` type union whose members include `'confirmed_approve'`, `'confirmed_request_changes'`, `'clipboard_confirmed'`, `'clipboard_error'` (each `grep -c` returns at least 1)
    - `AppToolbarProps` interface contains `comments: CodeReviewComment[]`, `connectivity: ConnectivityStatus`, `onApprove:`, `onRequestChanges:` (all four grep checks return at least 1)
    - `grep -c "comments.length === 0" ui/src/code-review/AppToolbar.tsx` returns at least 1 (canApprove gate)
    - `grep -c "comments.length > 0" ui/src/code-review/AppToolbar.tsx` returns at least 1 (canRequestChanges gate)
    - `grep -c "fetch('/api/decide'" ui/src/code-review/AppToolbar.tsx` returns at least 1
    - `grep -c "buildCodeReviewPayload" ui/src/code-review/AppToolbar.tsx` returns at least 2 (used in both handleApprove and handleRequestChanges)
    - `grep -c "shouldUseClipboard" ui/src/code-review/AppToolbar.tsx` returns at least 1
    - `grep -c "navigator.clipboard.writeText" ui/src/code-review/AppToolbar.tsx` returns at least 1
    - `grep -c "window.close()" ui/src/code-review/AppToolbar.tsx` returns at least 1
    - `grep -c "reviewer-v2" ui/src/code-review/AppToolbar.tsx` returns 0 (no boundary violation)
    - `grep -v '^#' ui/src/code-review/AppToolbar.test.ts | grep -c "it("` returns at least 38 (23 existing cases + 15+ new Phase 28 cases)
    - `cd ui && npm test -- --run AppToolbar` exits 0 with all assertions passing (both old and new)
    - `cd ui && npm run lint` exits 0
    - `cd ui && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AppToolbar carries the submit-state machine, gate logic, online POST, offline clipboard, status messages, and popover trigger; all 38+ source assertions pass; ESLint boundary preserved</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire CodeReviewApp.tsx (useHeartbeat + props), replace negative test assertion with positives</name>
  <files>ui/src/code-review/CodeReviewApp.tsx, ui/src/code-review/CodeReviewApp.test.ts</files>
  <read_first>
    - ui/src/code-review/CodeReviewApp.tsx (lines 1-12 imports, lines 232-303 the JSX block — Phase 28 ADDS to this; do NOT remove Phase 27 wiring)
    - ui/src/code-review/CodeReviewApp.test.ts (lines 85-87 — the negative assertion to remove; lines 213-274 — Phase 27 assertion patterns to mimic)
    - .planning/phases/28-review-submission/28-RESEARCH.md (Pitfall 3 — the negative useHeartbeat assertion)
    - .planning/phases/28-review-submission/28-PATTERNS.md (CodeReviewApp.tsx section + CodeReviewApp.test.ts section)
  </read_first>
  <behavior>
    Test changes in CodeReviewApp.test.ts:
    - REMOVE the existing `it('does NOT call useHeartbeat (RESEARCH Open Question 1)', ...)` block (lines 85-87) — the assertion would otherwise fail because Phase 28 ADDS useHeartbeat.
    - ADD `it('Phase 28: imports useHeartbeat from shared/', ...)`: `expect(source).toContain("from '../shared/useHeartbeat'")` AND `expect(source).toContain('useHeartbeat')`.
    - ADD `it('Phase 28: assigns useHeartbeat() result to connectivity', ...)`: `expect(source).toMatch(/const\s+connectivity\s*=\s*useHeartbeat\(\)/)`.
    - ADD `it('Phase 28: passes connectivity and comments to AppToolbar', ...)`: `expect(source).toContain('connectivity={connectivity}')` AND `expect(source).toContain('comments={comments}')`.
    - ADD `it('Phase 28: passes onApprove and onRequestChanges to AppToolbar', ...)`: `expect(source).toContain('onApprove=')` AND `expect(source).toContain('onRequestChanges=')`.
    - PRESERVE all existing assertions (Phase 25/26/27). The "does NOT import from reviewer-v2/" assertion must continue to pass — Phase 28 uses only `'./'` and `'../shared/'` paths.

    Source changes in CodeReviewApp.tsx:
    - Add import: `import { useHeartbeat } from '../shared/useHeartbeat'`.
    - At the top of the `CodeReviewApp()` function body (alongside the other state hooks; placement near the existing `useCodeReviewAnnotations()` line is appropriate), add `const connectivity = useHeartbeat()`.
    - In the JSX `<AppToolbar ... />` element, add four new props on new lines after `onToggleAllFiles={handleToggleAllFiles}`: `comments={comments}`, `connectivity={connectivity}`, `onApprove={() => {}}`, `onRequestChanges={() => {}}`.

    Important design note: per CONTEXT.md "handleApprove/handleRequestChanges location choice" — the actual submit logic was placed inside AppToolbar (Task 2). CodeReviewApp's role here is solely to (a) call `useHeartbeat()`, (b) propagate `connectivity` and `comments` down, and (c) provide stub callback props. AppToolbar owns the entire submit flow internally and does not call back up. Therefore the `onApprove`/`onRequestChanges` props at the CodeReviewApp layer are no-op stubs (`() => {}`). Do NOT move the submit logic to CodeReviewApp — doing so would break Task 2's source assertions on AppToolbar.tsx.
  </behavior>
  <action>
    Apply the changes described in the behavior block, in this order:

    1) Edit `ui/src/code-review/CodeReviewApp.tsx`:
       - Add the line `import { useHeartbeat } from '../shared/useHeartbeat'` near the existing `useCodeReviewAnnotations` import (line ~9).
       - Inside the function body, after the existing `const { comments, addComment, editComment, deleteComment } = useCodeReviewAnnotations()` line (around line 27), insert `const connectivity = useHeartbeat()`.
       - In the existing `<AppToolbar ... />` JSX (lines 234-245), add the four Phase 28 props on new lines after `onToggleAllFiles={handleToggleAllFiles}` and before the closing `/>`: `comments={comments}`, `connectivity={connectivity}`, `onApprove={() => {}}`, `onRequestChanges={() => {}}`.

    2) Edit `ui/src/code-review/CodeReviewApp.test.ts`:
       - Delete the entire `it('does NOT call useHeartbeat (RESEARCH Open Question 1)', () => { expect(source).not.toContain('useHeartbeat') })` block at lines 85-87.
       - Append the 4 new Phase 28 assertions described in the behavior block at the end of the existing `describe('CodeReviewApp', ...)` block, after the Phase 27 cases (around line 273).

    3) Run lint, tsc, and tests; commit on green.

    Hard constraints:
    - Do NOT touch any Phase 27 wiring (commentCounts, handleAddLineComment, etc.) — those props stay as-is.
    - Do NOT remove or modify any other existing assertions in CodeReviewApp.test.ts beyond the single deletion at lines 85-87.
    - The "does NOT import from reviewer-v2/" assertion at line ~89 MUST still pass after edits.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run CodeReviewApp && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/code-review/CodeReviewApp.tsx` contains `import { useHeartbeat } from '../shared/useHeartbeat'` exactly once
    - `ui/src/code-review/CodeReviewApp.tsx` contains `const connectivity = useHeartbeat()` exactly once
    - `ui/src/code-review/CodeReviewApp.tsx` JSX contains all four new prop assignments: `comments={comments}`, `connectivity={connectivity}`, `onApprove=`, `onRequestChanges=` (each verified via grep returning at least 1)
    - `grep -c "does NOT call useHeartbeat" ui/src/code-review/CodeReviewApp.test.ts` returns 0 (negative assertion deleted)
    - `grep -c "Phase 28:" ui/src/code-review/CodeReviewApp.test.ts` returns at least 4 (four new positive assertions added)
    - `grep -c "from '\\.\\./shared/useHeartbeat'" ui/src/code-review/CodeReviewApp.test.ts` returns at least 1
    - `grep -c "reviewer-v2" ui/src/code-review/CodeReviewApp.tsx` returns 0 (boundary preserved)
    - `cd ui && npm test -- --run CodeReviewApp` exits 0 with all assertions passing (including the preserved Phase 25/26/27 cases)
    - `cd ui && npm run lint` exits 0
    - `cd ui && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CodeReviewApp calls useHeartbeat and propagates connectivity + comments to AppToolbar; the negative useHeartbeat assertion is gone; four positive assertions verify the new wiring</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — exercise every submit state in a real browser</name>
  <files>(no files modified — human verification of the running UI from Tasks 1-3)</files>
  <what-built>
    Phase 28 review submission flow is fully wired in the code-review reviewer at `/code-review`. AppToolbar carries Approve + Request Changes buttons with gate logic (D-06) and a popover for optional global instruction. Both buttons fall back to clipboard when the server is offline. Status messages render on success/failure. CodeReviewApp calls useHeartbeat() from the new shared/ module.
  </what-built>
  <action>
    Pause for the human to exercise the submit flow in a real browser following the steps in the `<how-to-verify>` block below. The executor must wait for the human to type "approved" before continuing to the next plan. If the human reports any deviation, the executor records the issue and either revises this plan or creates a follow-up plan via `/gsd:plan-phase --gaps 28`.
  </action>
  <how-to-verify>
    Prerequisite: from the repo root, run `cargo run -- review-hook < tests/fixtures/sample_plan_hook.json &` (or the equivalent local invocation that starts the server at 127.0.0.1:8080 and opens the browser). Navigate to `/code-review`.

    1. Gate verification with no comments (SUBMIT-01 / SUBMIT-04 / D-06):
       - On a branch with at least one changed file, observe the Approve button is enabled (full opacity, green background) and Request Changes is disabled (opacity ~0.4, default cursor).
       - Hover the disabled Request Changes button — the tooltip "Add at least one comment before requesting changes" appears.

    2. Add a comment (Phase 27 flow), then verify the gates flip (D-06):
       - Click the `+` on a hovered diff line, type "test", submit.
       - Observe that Approve is now disabled with tooltip "Cannot approve while comments exist", and Request Changes is enabled with red background.

    3. Approve flow + popover (SUBMIT-02):
       - Delete the comment to re-enable Approve.
       - Click Approve. A popover appears anchored under the button with the textarea labeled "Global instruction (optional)" and placeholder "Leave an instruction for the agent (optional)".
       - Press Escape — the popover dismisses, state returns to idle.
       - Click Approve again. Click outside the popover — it dismisses.
       - Click Approve again. Type "Please also update CHANGELOG.md". Press Cmd+Enter (or Ctrl+Enter on Linux). The popover closes and the status "Approved — You can close this tab." appears briefly before the tab closes (or the close attempt is silently blocked by the browser; the status message must still appear).
       - In the terminal where you ran the binary, observe stdout JSON containing `"decision":"approved"` and `"global_instruction":"Please also update CHANGELOG.md"`.

    4. Request Changes flow (SUBMIT-03):
       - Re-open the browser tab via the same hook invocation.
       - Add at least one line comment AND one file-level comment.
       - Click Request Changes (no popover). Status "Review submitted — You can close this tab." appears; tab attempts close.
       - In the terminal stdout, observe JSON containing `"decision":"changes_requested"`, an array `"comments"` with at least two entries, with `line` and `side` for the line comment and only `file`+`text` for the file comment. No `global_instruction` key.

    5. Offline clipboard fallback (SUBMIT-03 success criterion 5):
       - Kill the server binary (Ctrl+C in the terminal).
       - Within ~15 seconds (after the heartbeat has logged 3 failures), the offline state takes effect. You may invoke a hide/show of the browser tab to force a heartbeat tick.
       - Click Approve (no popover in offline mode — clipboard fires directly per UI-SPEC). Status "Copied to clipboard — Paste into your Claude conversation." appears.
       - Paste into a text editor — confirm the clipboard contains exactly `{"decision":"approved"}` (no comments, no global_instruction key) if comments are cleared.
       - Repeat with a comment present: click Request Changes; verify clipboard contains `{"decision":"changes_requested","comments":[...]}`.

    6. Clipboard error fallback (defensive UX):
       - Open browser devtools, Application > Permissions, block "Clipboard". Click Approve (online or offline). Status "Clipboard write failed" + read-only textarea appears with the JSON payload selectable.
       - Re-enable clipboard permissions for subsequent runs.

    7. Cosmetic / a11y spot checks:
       - Tab through all controls — focus rings appear on Approve, Request Changes, Confirm Approve, textarea, Dismiss button.
       - In dark mode, all colors use CSS variables (no white-on-white or black-on-black). Toggle dark/light if your build supports it.
       - Status messages have `aria-live="polite"` (verified via devtools, Elements panel, search for `aria-live`).
  </how-to-verify>
  <resume-signal>
    Reply "approved" if all steps 1-7 pass.

    If any step fails, reply with the step number(s) and the observed-vs-expected delta. The executor will create a follow-up plan or revise this one. Specifically watch for: missing copy strings, wrong colors (red on Approve, green on Request Changes), gate logic inversion, popover failing to dismiss on Escape, or the clipboard fallback writing the wrong shape.
  </resume-signal>
  <verify>
    <human-check>Human types "approved" after completing all 7 verification steps in a real browser. No automated test substitutes for this — the gate is blocking by design.</human-check>
  </verify>
  <acceptance_criteria>
    - All 7 verification steps in `<how-to-verify>` are confirmed by the human user
    - Steps 1-2 confirm gate logic (D-06): Approve disabled with comments, Request Changes disabled without comments
    - Step 3 confirms the Approve popover dismisses on Escape and outside-click, and that the global instruction trims and serializes correctly
    - Step 4 confirms the structured JSON shape from D-01 (decision, comments array with file/line/side or file-only entries, no global_instruction)
    - Step 5 confirms the offline clipboard write produces a byte-identical payload to the online POST body
    - Step 6 confirms the clipboard_error state surfaces the JSON in a selectable textarea
    - Step 7 confirms a11y (focus rings, aria-live status messages)
    - Human types `approved` exactly to resume autonomous execution
  </acceptance_criteria>
  <done>Human verification approved; all 7 walkthrough steps observed as passing. Any failed step generates a follow-up plan via gap-closure.</done>
</task>

</tasks>

<verification>
After all four tasks complete:
- `cd ui && npm run lint` exits 0
- `cd ui && npm test -- --run` exits 0; all suites pass including the four affected (CodeReviewSubmitPopover, AppToolbar, CodeReviewApp, ReviewerV2Shell)
- `cd ui && npx tsc --noEmit` exits 0
- `cargo fmt && cargo clippy -- -D warnings` from the repo root exits 0 (CLAUDE.md requirement; this plan does not touch Rust but the pre-commit hook still runs)
- `grep -rn "from '\.\./reviewer-v2" ui/src/code-review/` returns 0 matches
- Browser walkthrough Task 4 steps 1-7 all observed as passing
</verification>

<success_criteria>
- All four phase requirement IDs (SUBMIT-01, SUBMIT-02, SUBMIT-03, SUBMIT-04) are satisfied by code that ships in this plan
- Approve gate (D-06, SUBMIT-01): Approve button disabled when comments exist — visible in UI, asserted in source
- Optional instruction (SUBMIT-02): popover textarea exists, optional, trimmed before serialization — verified by buildCodeReviewPayload tests (plan 28-02) + browser walkthrough step 3
- Structured JSON output (SUBMIT-03): both online POST and offline clipboard paths serialize via `buildCodeReviewPayload` — verified by source-assertion tests + browser walkthrough step 4 (stdout JSON inspection) + step 5 (clipboard inspection)
- Request Changes gate (SUBMIT-04, D-06): button disabled when no comments — visible in UI, asserted in source
- Clipboard fallback (SUBMIT-03 criterion 5): triggered by `shouldUseClipboard(connectivity)`, uses `buildCodeReviewPayload`, no separate clipboard implementation — verified in source + browser walkthrough step 5
- CodeReviewApp calls `useHeartbeat()` from shared/ — asserted; negative Phase 25 assertion removed
- No ESLint boundary violations — verified by `npm run lint` exit 0
- Human verification approved
</success_criteria>

<output>
Create `.planning/phases/28-review-submission/28-03-SUMMARY.md` when done. Include:
- Confirmation of all four SUBMIT-XX requirements with the source file + line where each is implemented
- Notes from the human verification walkthrough (any deviations, any browser-specific clipboard behavior observed)
- Confirmation that the submit handlers live inside AppToolbar (the planned approach) for Phase 29's reference when wiring `/api/decide` server-side
</output>
