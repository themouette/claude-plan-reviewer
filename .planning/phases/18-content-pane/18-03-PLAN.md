---
phase: 18-content-pane
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - ui/src/reviewer-v2/PlanContent.tsx
  - ui/src/reviewer-v2/PlanContent.test.ts
  - ui/src/reviewer-v2/ContentPane.tsx
  - ui/src/reviewer-v2/ContentPane.test.ts
  - ui/src/reviewer-v2/ReviewerV2.tsx
  - ui/src/reviewer-v2/ReviewerV2Shell.tsx
  - ui/src/index.css
autonomous: false
requirements:
  - LAYOUT-02
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03

must_haves:
  truths:
    - "Navigating to /v2 renders formatted markdown (tables, task lists, strikethrough, code blocks) inside the center column"
    - "Hovering a paragraph in /v2 shows a subtle background and a + icon at its right edge"
    - "Selecting text in /v2 hides the hover icon and shows a fixed-positioned pill toolbar anchored to the selection end"
    - "Toolbar clicks fire the onAction callback with the selected text (the selection is not cleared by mousedown); annotation persistence is Phase 21 — handleAction in this phase only calls resetTextSelection()"
    - "ReviewerV2Shell's center <main> now mounts <ContentPane /> instead of the placeholder span"
  artifacts:
    - path: "ui/src/reviewer-v2/PlanContent.tsx"
      provides: "Markdown host with paragraph hover detection via event delegation"
      exports: ["default"]
    - path: "ui/src/reviewer-v2/ContentPane.tsx"
      provides: "Container that fetches /api/plan, renders PlanContent + SelectionToolbar"
      exports: ["default"]
    - path: "ui/src/reviewer-v2/ContentPane.test.ts"
      provides: "Tests for fetch state machine (loading/ready/error)"
    - path: "ui/src/reviewer-v2/PlanContent.test.ts"
      provides: "Test that selectedText non-empty suppresses hover state"
  key_links:
    - from: "ui/src/reviewer-v2/ContentPane.tsx"
      to: "GET /api/plan"
      via: "fetch in useEffect with renderMarkdown(data.plan_md)"
      pattern: "fetch\\('/api/plan'\\)"
    - from: "ui/src/reviewer-v2/ContentPane.tsx"
      to: "ui/src/reviewer-v2/utils/markdownRenderer.ts"
      via: "import { renderMarkdown }"
      pattern: "renderMarkdown"
    - from: "ui/src/reviewer-v2/PlanContent.tsx"
      to: "ui/src/reviewer-v2/GutterIcon.tsx"
      via: "import GutterIcon"
      pattern: "GutterIcon"
    - from: "ui/src/reviewer-v2/ReviewerV2Shell.tsx"
      to: "ui/src/reviewer-v2/ContentPane.tsx"
      via: "import ContentPane; <ContentPane /> mounted in <main>"
      pattern: "<ContentPane"
---

<objective>
Wire the content pane end-to-end. Compose `PlanContent` (markdown host with paragraph-hover delegation) and `ContentPane` (data fetch + selection orchestration), then replace the placeholder `<span>Content</span>` in `ReviewerV2Shell` with `<ContentPane />`. Add the paragraph-hover CSS rules to `ui/src/index.css` and import the highlight.js stylesheet from the v2 entry to ensure the github-dark code-block theme is bundled on the `/v2` route.

Purpose: This plan converts the foundation built in Plans 01 and 02 into a working content pane. After this plan, the four success criteria from ROADMAP Phase 18 are observable in a real browser at `/v2`.

Output: PlanContent + ContentPane components with tests, a Shell wiring change, an index.css addition for the paragraph-hover background, an updated ReviewerV2 entry that imports the highlight.js stylesheet for code-block colors, and a human-verify checkpoint that confirms the four ROADMAP success criteria.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-content-pane/18-RESEARCH.md
@.planning/phases/18-content-pane/18-UI-SPEC.md
@.planning/phases/18-content-pane/18-PATTERNS.md

@ui/src/App.tsx
@ui/src/reviewer-v2/ReviewerV2.tsx
@ui/src/reviewer-v2/ReviewerV2Shell.tsx
@ui/src/index.css
@ui/src/reviewer-v2/useAnnotations.ts

<interfaces>
<!-- Contract this plan honors. Consumers expect these exports. -->

From ui/src/reviewer-v2/hooks/useTextSelection.ts (Plan 01):

```typescript
export function rangeFromOffsets(container, start, end): Range | null
export function useTextSelection(containerRef): [string, () => void, () => {start,end}|null]
```

From ui/src/reviewer-v2/utils/markdownRenderer.ts (Plan 01):

```typescript
export function renderMarkdown(md: string): string
```

From ui/src/reviewer-v2/GutterIcon.tsx (Plan 02):

```typescript
export default function GutterIcon(props: {
  paragraph: HTMLElement
  containerRef: RefObject<HTMLDivElement | null>
  onAdd: () => void
}): React.JSX.Element
```

From ui/src/reviewer-v2/SelectionToolbar.tsx (Plan 02):

```typescript
export default function SelectionToolbar(props: {
  offsets: { start: number; end: number }
  selectedText: string
  containerRef: RefObject<HTMLDivElement | null>
  onAction: (type: AnnotationType, anchorText: string, prefillComment?: string) => void
}): React.JSX.Element | null
```

From ui/src/reviewer-v2/types.ts (existing):

```typescript
export type AnnotationType = 'comment' | 'delete' | 'replace'
```

The server endpoint /api/plan returns `{ plan_md: string }` — verified against ui/src/App.tsx line 696-702.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PlanContent with paragraph hover delegation + index.css addition</name>
  <files>ui/src/reviewer-v2/PlanContent.tsx, ui/src/reviewer-v2/PlanContent.test.ts, ui/src/index.css</files>
  <read_first>
    - ui/src/App.tsx (lines 1262-1279 — the existing plan-prose div with dangerouslySetInnerHTML)
    - .planning/phases/18-content-pane/18-PATTERNS.md (PlanContent.tsx section — event delegation pattern with closest('p, li, blockquote, h1..h6'))
    - .planning/phases/18-content-pane/18-UI-SPEC.md (Paragraph Hover State section)
    - ui/src/reviewer-v2/GutterIcon.tsx (the component this composes — created in Plan 02)
    - ui/src/index.css (existing .plan-prose rules from lines 64-219 — confirm structure before appending)
  </read_first>
  <behavior>
    - PlanContent renders a single `<div className="plan-prose">` with dangerouslySetInnerHTML={{ __html: planHtml }}
    - The div has `position: relative` (anchors absolute-positioned GutterIcon) and `maxWidth: 720, margin: '0 auto'`
    - onMouseMove on the div: identifies hovered paragraph via target.closest('p, li, blockquote, h1, h2, h3, h4, h5, h6')
    - When selectedText is non-empty, handleMouseMove returns early — no hover state is set (selection supersedes hover)
    - When a paragraph hovered changes, the previous paragraph's inline background style is cleared and the new one's is set to rgba(255,255,255,0.04) with borderRadius 4px and transition background 0.1s ease
    - On mouseLeave: clears hoveredParagraph state and clears the inline background on the last hovered element
    - When hoveredParagraph is set AND selectedText is empty, GutterIcon is rendered as a sibling with paragraph={hoveredParagraph} containerRef={planRef} onAdd={onAdd}
    - PlanContent also accepts and forwards planRef from the parent (so the parent can pass it to useTextSelection)
    - index.css gains a `.plan-prose .paragraph-hovered` rule (and a light-scheme override) — present even when implementation uses inline styles, because future themes may need the class hook
  </behavior>
  <action>
    Create ui/src/reviewer-v2/PlanContent.tsx with the following structure:

      - Imports: `import { useState, type RefObject } from 'react'`, `import GutterIcon from './GutterIcon'`.
      - Props type: `{ planHtml: string; planRef: RefObject<HTMLDivElement | null>; selectedText: string; onAdd: () => void }`.
      - useState `hoveredParagraph: HTMLElement | null` initialized to null.
      - Function `handleMouseMove(e: React.MouseEvent<HTMLDivElement>)`: if `selectedText` is non-empty, return immediately. Read `target = e.target as Element`. Compute `para = target.closest('p, li, blockquote, h1, h2, h3, h4, h5, h6')`. If `para && planRef.current?.contains(para)`, setHoveredParagraph(para as HTMLElement). Else setHoveredParagraph(null).
      - Function `handleMouseLeave()`: setHoveredParagraph(null).
      - Apply hover background imperatively via a useEffect that fires when `hoveredParagraph` changes: on the new element set `style.background = 'rgba(255,255,255,0.04)'`, `style.borderRadius = '4px'`, `style.transition = 'background 0.1s ease'`. Cleanup function clears these three inline styles back to empty strings.
      - Render JSX wrapper `<div style={{ position: 'relative', maxWidth: 720, margin: '0 auto' }}>` containing:
        - `<div ref={planRef} className="plan-prose" onMouseMove={handleMouseMove} onMouseLeave={handleMouseLeave} dangerouslySetInnerHTML={{ __html: planHtml }} />`
        - `{hoveredParagraph && !selectedText && (<GutterIcon paragraph={hoveredParagraph} containerRef={planRef} onAdd={onAdd} />)}`

    Append to ui/src/index.css (after the existing .plan-prose blocks ending around line 219). Do NOT modify any existing rule. Append a block that declares the class `.plan-prose .paragraph-hovered` with background rgba(255,255,255,0.04), border-radius 4px, and transition background 0.1s ease; then a `@media (prefers-color-scheme: light)` override that sets the background to rgba(0,0,0,0.03) for the same selector. Include a comment line above the block reading "Phase 18: paragraph hover background — applied via inline style in PlanContent.tsx; class hook reserved for future theme overrides."

    Create ui/src/reviewer-v2/PlanContent.test.ts using pure Vitest. Tests (assertions on the source file's structural invariants, since the project does not use @testing-library/react):
      1. The exported default is a function: `expect(typeof PlanContent).toBe('function')`.
      2. Read the source via `readFileSync` and assert it contains `selectedText` early-return pattern: `expect(source).toMatch(/if \(selectedText\) return/)`.
      3. Source contains `target.closest('p, li, blockquote, h1, h2, h3, h4, h5, h6')` (Pattern 2 anchor).
      4. Source contains the GutterIcon conditional `hoveredParagraph && !selectedText` so that hover affordance is suppressed during selection.
      5. Source contains `dangerouslySetInnerHTML` — confirms markdown injection is used.
      6. Source contains `className="plan-prose"` — confirms it picks up the existing prose styles.

    Do NOT import from any `../` path. Do NOT touch ui/src/App.tsx.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run reviewer-v2/PlanContent && npm run lint && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/reviewer-v2/PlanContent.tsx exists
    - `grep -c "export default function PlanContent" ui/src/reviewer-v2/PlanContent.tsx` returns 1
    - `grep -c "target.closest('p, li, blockquote, h1, h2, h3, h4, h5, h6')" ui/src/reviewer-v2/PlanContent.tsx` returns 1
    - `grep -c "if (selectedText) return" ui/src/reviewer-v2/PlanContent.tsx` returns 1
    - `grep -c "hoveredParagraph && !selectedText" ui/src/reviewer-v2/PlanContent.tsx` returns 1
    - `grep -c "dangerouslySetInnerHTML" ui/src/reviewer-v2/PlanContent.tsx` returns 1
    - `grep -c "className=\"plan-prose\"" ui/src/reviewer-v2/PlanContent.tsx` returns 1
    - `grep -E "from '\\.\\./" ui/src/reviewer-v2/PlanContent.tsx` finds no matches
    - File ui/src/reviewer-v2/PlanContent.test.ts exists
    - `grep -v '^[[:space:]]*\\*' ui/src/index.css | grep -c "paragraph-hovered"` returns at least 2 (dark scheme + light scheme rule)
    - `cd ui && npx vitest run src/reviewer-v2/PlanContent.test.ts` exits 0
    - `cd ui && npm run lint` exits 0
    - `cd ui && npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>
    PlanContent compiles, tests pass, index.css updated, lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ContentPane with /api/plan fetch + wire into ReviewerV2Shell + ReviewerV2</name>
  <files>ui/src/reviewer-v2/ContentPane.tsx, ui/src/reviewer-v2/ContentPane.test.ts, ui/src/reviewer-v2/ReviewerV2Shell.tsx, ui/src/reviewer-v2/ReviewerV2.tsx</files>
  <read_first>
    - ui/src/App.tsx (lines 676-710 — the /api/plan fetch state machine and lines 1250-1279 — the FloatingAnnotationAffordance render conditional)
    - ui/src/reviewer-v2/ReviewerV2.tsx (current contents — must be updated to import highlight.js theme)
    - ui/src/reviewer-v2/ReviewerV2Shell.tsx (current contents — the `<main>` placeholder span at lines 62-70 must be replaced)
    - ui/src/reviewer-v2/PlanContent.tsx (created in Task 1 of this plan)
    - ui/src/reviewer-v2/SelectionToolbar.tsx (created in Plan 02)
    - ui/src/reviewer-v2/utils/markdownRenderer.ts (created in Plan 01 — the renderMarkdown function)
    - ui/src/reviewer-v2/hooks/useTextSelection.ts (created in Plan 01 — the hook tuple)
  </read_first>
  <behavior>
    - ContentPane fetches GET /api/plan on mount; sets status to 'ready' on success, 'error' on failure
    - On success: parses { plan_md: string }, calls renderMarkdown(plan_md), stores result in planHtml state
    - status state union: 'loading' | 'ready' | 'error'
    - useTextSelection(planRef) is called once at the component top
    - When status is 'loading': renders a span with text 'Loading…'
    - When status is 'error': renders a span with the copy from the UI-SPEC: 'Could not render plan — The markdown content is unavailable. Reload the page to retry.'
    - When status is 'ready': renders <PlanContent ... /> plus, when selectedText is non-empty AND offsets are present, <SelectionToolbar ... />
    - onAction handler is supplied (stub for Phase 21): logs nothing, calls reset() to clear the selection, no other side effects in this phase (Phase 21 will wire to useAnnotations)
    - onAdd handler (for the GutterIcon path): same stub behavior — calls reset() (no selection to clear, but it's idempotent)
    - ReviewerV2Shell mounts <ContentPane /> in place of the previous <span>Content</span> placeholder; `<main>`'s padding is set to 0 because ContentPane owns its own padding of 32px
    - ReviewerV2.tsx adds the side-effect import `import 'highlight.js/styles/github-dark.css'` so code-block tokens are styled when only the v2 entry loads
  </behavior>
  <action>
    Create ui/src/reviewer-v2/ContentPane.tsx with the following structure:

      - Imports: `import { useEffect, useRef, useState } from 'react'`, `import { useTextSelection } from './hooks/useTextSelection'`, `import { renderMarkdown } from './utils/markdownRenderer'`, `import PlanContent from './PlanContent'`, `import SelectionToolbar from './SelectionToolbar'`, `import type { AnnotationType } from './types'`.
      - Inside the component body:
        - `const planRef = useRef<HTMLDivElement>(null)`
        - `const [planHtml, setPlanHtml] = useState<string>('')`
        - `const [status, setStatus] = useState<'loading' | 'ready' | 'error'>('loading')`
        - `const [selectedText, resetTextSelection, getOffsets] = useTextSelection(planRef)`
        - useEffect with empty deps that does `fetch('/api/plan').then(res => { if (!res.ok) throw new Error('HTTP ' + res.status); return res.json() }).then((data: { plan_md: string }) => { setPlanHtml(renderMarkdown(data.plan_md)); setStatus('ready') }).catch(() => setStatus('error'))`
        - `const offsets = selectedText ? getOffsets() : null` — note: using inline `getOffsets()` here instead of `useLayoutEffect` (as documented in PATTERNS.md for the legacy App.tsx path). The inline form is intentionally simpler for v2: `getOffsets()` is a synchronous snapshot of the hook's stored offsets and runs on every render, so the toolbar always reads the latest selection. `useLayoutEffect` would add a reactive layer App.tsx needed for scroll-relative positioning, which the v2 `position: fixed` toolbar does not require. Do not switch to `useLayoutEffect`.
        - `function handleAction(_type: AnnotationType, _anchorText: string, _prefill?: string) { resetTextSelection() }` — stub that clears the selection. Phase 21 will replace this with the real annotation dispatch. Prefix unused params with underscore so eslint no-unused-vars is satisfied without a disable comment.
        - `function handleAdd() { resetTextSelection() }` — stub for the gutter-icon click in Phase 21.
      - Wrap the return in `<div style={{ position: 'relative', padding: 32 }}>` (the xl spacing token; ContentPane owns its own padding).
      - When status === 'loading': render `<span style={{ fontSize: 14, color: 'var(--color-text-secondary)' }}>Loading…</span>`.
      - When status === 'error': render `<span style={{ fontSize: 14, color: 'var(--color-text-secondary)' }}>Could not render plan — The markdown content is unavailable. Reload the page to retry.</span>`.
      - When status === 'ready': render `<PlanContent planHtml={planHtml} planRef={planRef} selectedText={selectedText} onAdd={handleAdd} />` followed by `{selectedText && offsets && (<SelectionToolbar offsets={offsets} selectedText={selectedText} containerRef={planRef} onAction={handleAction} />)}`.

    Create ui/src/reviewer-v2/ContentPane.test.ts using pure Vitest. Tests (source-assertions on the production file):
      1. `typeof ContentPane === 'function'`.
      2. Source contains `fetch('/api/plan')` (the data link to the server endpoint).
      3. Source contains `renderMarkdown` (uses the v2 utility, not App.tsx's marked.use()).
      4. Source contains the literal string `'Could not render plan — The markdown content is unavailable. Reload the page to retry.'` (matches UI-SPEC Copywriting Contract).
      5. Source contains `'Loading…'` (the ellipsis character — copy from UI-SPEC empty/loading states).
      6. Source contains both `<PlanContent` and `<SelectionToolbar` JSX tags.
      7. Source contains `padding: 32` (xl token, ContentPane-owned padding).
      8. Source contains `resetTextSelection` inside the `handleAction` definition — proves the Phase 18 stub clears the selection after a toolbar click. Specifically, the test must extract the body of the `handleAction` function (e.g. `const handleActionBody = source.match(/function handleAction[^{]*{[^}]*}/s)?.[0]`) and assert `expect(handleActionBody).toMatch(/resetTextSelection/)`. This anchors the truth in Plan 03's must_haves that toolbar clicks clear the selection without persisting any annotation.

    Update ui/src/reviewer-v2/ReviewerV2Shell.tsx:
      - Add `import ContentPane from './ContentPane'` at the top of the file.
      - In the `<main>` element on the existing line range 53-71, change `padding: 32` to `padding: 0`.
      - Replace the placeholder `<span ...>Content</span>` (lines 62-70 in the current file) with `<ContentPane />`.
      - Do NOT change anything else in the file (header, left/right asides, flex container).

    Update ui/src/reviewer-v2/ReviewerV2.tsx:
      - At the top of the file, add side-effect import `import 'highlight.js/styles/github-dark.css'` so the github-dark tokens are bundled into the v2 chunk even when App.tsx is not loaded (Open Question #2 from 18-RESEARCH.md).
      - Do NOT change the existing useHeartbeat / useAnnotations calls or the ReviewerV2Shell mount.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run reviewer-v2/ContentPane && npm run lint && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/reviewer-v2/ContentPane.tsx exists
    - `grep -c "export default function ContentPane" ui/src/reviewer-v2/ContentPane.tsx` returns 1
    - `grep -c "fetch('/api/plan')" ui/src/reviewer-v2/ContentPane.tsx` returns 1
    - `grep -c "renderMarkdown(data.plan_md)" ui/src/reviewer-v2/ContentPane.tsx` returns 1
    - `grep -c "useTextSelection(planRef)" ui/src/reviewer-v2/ContentPane.tsx` returns 1
    - `grep -c "Could not render plan" ui/src/reviewer-v2/ContentPane.tsx` returns 1
    - `grep -c "padding: 32" ui/src/reviewer-v2/ContentPane.tsx` returns 1
    - `grep -E "from '\\.\\./" ui/src/reviewer-v2/ContentPane.tsx` finds no matches
    - File ui/src/reviewer-v2/ContentPane.test.ts exists
    - `grep -c "resetTextSelection" ui/src/reviewer-v2/ContentPane.test.ts` returns at least 1 (proves the handleAction stub assertion is wired)
    - `grep -c "<ContentPane />" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 1
    - `grep -c "import ContentPane from './ContentPane'" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 1
    - `grep -c "Content$" ui/src/reviewer-v2/ReviewerV2Shell.tsx` returns 0 (the placeholder span text 'Content' is gone)
    - `grep -c "import 'highlight.js/styles/github-dark.css'" ui/src/reviewer-v2/ReviewerV2.tsx` returns 1
    - `cd ui && npx vitest run src/reviewer-v2/ContentPane.test.ts` exits 0
    - `cd ui && npm test` exits 0 (full reviewer-v2 suite still passes — no Phase 17 regressions)
    - `cd ui && npm run lint` exits 0
    - `cd ui && npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>
    ContentPane renders plan markdown and mounts SelectionToolbar conditionally; ReviewerV2Shell mounts ContentPane in the center column; ReviewerV2 imports the highlight.js theme; all tests pass; lint and typecheck clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Phase 18 ROADMAP success criteria in browser</name>
  <files>none (verification-only)</files>
  <read_first>
    - .planning/phases/18-content-pane/18-UI-SPEC.md (every visible behavior in steps 2-6 traces back to a contract here)
    - .planning/ROADMAP.md (Phase 18 Success Criteria section — the four observable truths this checkpoint validates)
  </read_first>
  <action>
    Pause execution and present the human verification flow described in `<how-to-verify>` to the user. Do not proceed past this task until the user replies with `approved` or describes a specific failure. If the user describes a failure, summarize the gap and stop — the orchestrator will route the failure into a follow-up planning cycle.

    The executor's role in this checkpoint is purely orchestrational: print the verification steps, wait for the resume-signal, and record the user's response. No code changes are made by this task.
  </action>
  <what-built>
    Phase 18 Content Pane is wired end-to-end. Navigating to `/v2` should:
    1. Render the plan markdown with GFM features (tables, task checkboxes, strikethrough, syntax-highlighted code blocks).
    2. Show a subtle background highlight and a `+` icon at the right edge when the mouse hovers over any paragraph.
    3. Replace the hover affordance with a fixed pill toolbar (Comment / Delete / Replace / ▾ more) anchored to the selection end when text is selected.
    4. Serialize selections as character offsets — verifiable via React DevTools or by inspecting the toolbar's `offsets` prop in the rendered DOM.
  </what-built>
  <how-to-verify>
    Start the binary so the browser opens at `/v2`. Concrete steps:

      1. From the repository root, in one terminal run the Rust binary with a sample plan via the existing hook flow. Pipe a JSON payload containing a markdown plan with all GFM features (heading, paragraph with inline code, pipe table with 2 columns, two task list items - one unchecked one checked, strikethrough span, and a fenced js code block) into `cargo run -- review-hook`. The browser should open. Manually navigate to `http://localhost:<port>/v2` (port shown in stdout) — append `/v2` to the URL.
      2. In the center column, verify the markdown rendered: heading, paragraph, table grid with cell borders, two checkbox list items (one checked), strikethrough text rendered with strikethrough, and a code block with syntax-highlighted tokens (you should see different colors for `const`, identifiers, operators, numbers).
      3. Move the mouse onto the first paragraph. Verify: (a) subtle background appears on that paragraph, (b) a `+` icon appears at the right edge of the paragraph. Move to the next paragraph — the previous highlight clears and the new one appears. Move the mouse out of the content area — both clear.
      4. Click-drag to select a few words inside any paragraph. Verify: (a) the paragraph background highlight is gone, (b) the selected text shows a blue selection-lock highlight, (c) a pill toolbar appears anchored to the bottom-right of the selection, with three colored pills (blue/red/amber) and a `▾ more` expander. Verify the toolbar stays in place when you scroll the content pane (`position: fixed` behavior).
      5. Click the `▾ more` expander. Verify a dropdown menu appears with six items: `clarify this`, `needs test`, `give me an example`, `out of scope`, `search internet`, `search codebase`.
      6. Open browser DevTools, inspect the SelectionToolbar element, verify the React props in DevTools include `offsets={{ start: <number>, end: <number> }}` and `selectedText="<exact selection>"` — confirming character-offset serialization.
      7. Click any pill (e.g. Comment). Verify the selection clears (the toolbar disappears, the highlight is gone). This is the Phase 21 hook point — no annotation is added in Phase 18.

    After completing all 7 steps, type `approved` to continue. If any step fails, describe the failure (which paragraph, which browser, screenshot if possible).
  </how-to-verify>
  <verify>
    <human-check>User has reported `approved` after running all 7 verification steps in the browser at `/v2`. No JavaScript errors in DevTools console. All four ROADMAP Phase 18 success criteria visibly observed.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe issues encountered.</resume-signal>
  <acceptance_criteria>
    - All four ROADMAP Phase 18 success criteria observed in the browser (GFM rendering, paragraph hover affordance, selection toolbar, character-offset serialization)
    - The hover-to-selection transition is clean (hover affordance disappears when selection starts)
    - No JavaScript errors in the browser console during the verification flow
    - User responds `approved` (or course-corrects with specific failure details that lead to a follow-up plan)
  </acceptance_criteria>
  <done>
    User has approved that the Phase 18 ROADMAP success criteria are observable in the browser.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Browser (`GET /api/plan`) | Untrusted markdown content flows from the local Rust server into the browser, is parsed by `marked` (Plan 01), and injected via `dangerouslySetInnerHTML` in `PlanContent`. |
| Browser DOM → JS event handlers | User mouse events drive paragraph hover and text selection; payloads are filtered against `planRef.current.contains(...)`. |
| React state → DOM mutation (imperative hover background) | `PlanContent` directly mutates `element.style.background` on the hovered paragraph; cleanup must clear the inline style to avoid leaking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Tampering | `dangerouslySetInnerHTML` in `PlanContent` receives `marked`-parsed HTML | accept | This is the same risk profile as the existing `App.tsx` `.plan-prose` div. The plan content originates from a local file on the user's machine routed through the plan-reviewer binary; the threat model treats it as trusted. Sanitization (DOMPurify) is a milestone-level concern, not Phase 18. |
| T-18-09 | Tampering | `target.closest('p, li, blockquote, h1, h2, h3, h4, h5, h6')` could match elements outside `planRef` | mitigate | The `planRef.current?.contains(para)` guard ensures only descendants of the prose container set the hover state. Verified by the source-assertion test in PlanContent.test.ts. |
| T-18-10 | Repudiation | Phase 21 will record annotations; Phase 18 stub silently discards `onAction` calls | accept | The stub explicitly clears the selection so user actions visibly conclude even when no record is created. Phase 21 will replace the stub with the real reducer dispatch — tracked by the requirement chain. |
| T-18-11 | Information Disclosure | Plan content rendered to DOM could leak via screen-share or extension | accept | Local-only tool per project core value. The plan was already visible in the existing reviewer; the v2 path does not change exposure surface. |
| T-18-12 | Denial of Service | Pathological markdown (extremely large tables, deeply nested lists) could freeze rendering | accept | `marked@18` performance is the same as the existing reviewer. No additional mitigation needed in this phase. |
| T-18-13 | Tampering | Imperative style mutations leak when the hovered element unmounts | mitigate | The useEffect cleanup function explicitly resets `style.background`, `style.borderRadius`, and `style.transition` to empty strings when the dep changes. |
| T-18-SC | Tampering | npm package installs | accept | No new packages installed in Phase 18 (audit performed in 18-RESEARCH.md: marked@18.0.0, marked-highlight@2.2.4, highlight.js@11.11.1 already in package.json). |
</threat_model>

<verification>
- `cd ui && npm test` exits 0 (full vitest suite — Phase 17 tests still pass, Plan 01/02/03 tests all green)
- `cd ui && npm run lint` exits 0
- `cd ui && npx tsc -b --noEmit` exits 0
- `cargo fmt --check && cargo clippy -- -D warnings` exit 0 (no Rust changes in this plan, but the pre-commit hook will run)
- Human verification of all 7 steps in Task 3 confirmed by `approved` response
</verification>

<success_criteria>
1. `/v2` route renders plan markdown with GFM formatting — tables as grids, task-list checkboxes, strikethrough, syntax-highlighted code blocks (ROADMAP success criterion 1)
2. Hovering a paragraph reveals subtle background + `+` icon at the right edge (ROADMAP success criterion 2)
3. Selecting text replaces hover with selection-lock highlight and shows the fixed pill toolbar anchored to the selection end (ROADMAP success criterion 3)
4. Text selections serialize to `{ start, end }` character offsets, inspectable via React DevTools on the SelectionToolbar's `offsets` prop (ROADMAP success criterion 4)
5. The Shell's center `<main>` now mounts `<ContentPane />` — the placeholder span is gone
6. No file outside `reviewer-v2/` and `ui/src/index.css` is modified; the ESLint isolation boundary is preserved (no `../` imports)
</success_criteria>

<output>
Create `.planning/phases/18-content-pane/18-03-SUMMARY.md` when done.
</output>
