---
context: phase
phase: 18-content-pane
task: post-execution-gates
status: in_progress
last_updated: 2026-05-20T10:53:24.005Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No DOM mutation inside dangerouslySetInnerHTML children — any classList/style mutation on elements inside the planRef div is wiped by React reconciliation. All visual overlays must be sibling elements outside the innerHTML tree.
- [ ] CONSTRAINT: No React state updates mid-drag — calling setSelectedText (or any setState) inside mousedown/mousemove handlers causes a re-render that resets the browser's native drag-selection anchor. Visual-only changes (removeHighlight, classList) are safe; setState is only safe on mouseup.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| classList mutation on dangerouslySetInnerHTML children | Adding CSS classes to elements inside the innerHTML div — React reconciliation wipes them on every render | blocking | Use absolutely-positioned sibling overlays (MarkdownView is now memo'd to prevent even reconciliation touching that subtree) |
| setState on mousedown during drag | setSelectedText('') on mousedown triggers a re-render mid-drag, resetting the browser's selection anchor — all drags start at offset 0 | blocking | Only call setSelectedText in mouseup handlers (capture); use removeHighlight() for visual-only mid-drag changes |

<current_state>
Phase 18 post-execution gates in progress. Code review done (REVIEW.md committed). Verifier ran and returned `human_needed` — 6 UAT items saved to 18-HUMAN-UAT.md. User has been testing in browser and confirming items one by one.

**UAT items status (as of end of session):**
1. ✅ GFM rendering at /v2 — confirmed working
2. ✅ Paragraph hover affordance — confirmed working (after overlay sibling fix + MarkdownView memo)
3. ⏳ Selection toolbar and hover suppression — selection now works (outside-click clear fixed), pending final confirmation
4. ⏳ Character-offset serialization — not yet explicitly confirmed
5. ⏳ More expander with 6 quick actions — not yet explicitly confirmed
6. ⏳ Selection clears after pill click — not yet explicitly confirmed

All four bug fixes this session:
- `0c93112` — overlay sibling instead of classList on innerHTML children
- `443385a` — onMouseOut + relatedTarget guard (onMouseLeave geometric hit-testing)
- `060d2f5` — no setSelectedText on mousedown (prevents mid-drag re-render)
- `2bb0012` — e.buttons !== 0 guard (suppress hover during drag)
- `6b9a505` — MarkdownView memoized (structural fix for innerHTML reconciliation)
- `1cb6a66` — clear selection on any outside click (not just inside container)
</current_state>

<completed_work>
- All 3 phase plans executed (18-01, 18-02, 18-03) — SUMMARYs exist
- Code review run → 18-REVIEW.md committed (2 critical, 4 warnings, 2 info — advisory)
- Verification run → status: human_needed → 18-VERIFICATION.md committed
- 18-HUMAN-UAT.md created and committed with 6 test items
- 6 bug fixes committed addressing hover flicker and selection anchor issues
- 99/99 tests passing, clean build
</completed_work>

<remaining_work>
1. User completes remaining UAT items 3-6 in browser
2. User types "approved" (or reports issues)
3. If approved: run `gsd-sdk query phase.complete 18` → update ROADMAP/STATE → commit
4. Close the phase with offer_next routing
</remaining_work>

<decisions_made>
- Hover highlight implemented as absolutely-positioned sibling div (not classList on innerHTML children) — React owns the sibling, never touches innerHTML
- MarkdownView extracted as memo'd sub-component — hard bail-out prevents any reconciliation of the innerHTML subtree during hover state changes
- e.buttons !== 0 guard retained as a UX rule (no hover jitter during drag), complementary to memoization
- setSelectedText cleared from mousedown — visual state (removeHighlight) is separated from React state updates
- Outside-click now clears selection — the previous containment guard was too conservative for Phase 18 (Phase 21 can re-add exclusions for annotation pill interactions)
</decisions_made>

<blockers>
- None technical. Waiting for human UAT sign-off.
- GPG key times out — user needs to unlock before commits.
</blockers>

## Required Reading (in order)
1. `ui/src/reviewer-v2/PlanContent.tsx` — MarkdownView memo pattern, overlay approach
2. `ui/src/reviewer-v2/hooks/useTextSelection.ts` — isDraggingRef, onMouseDown without setState
3. `.planning/phases/18-content-pane/18-HUMAN-UAT.md` — remaining UAT items

## Infrastructure State
- Branch: `gsd/v0.6.0-markdown-annotator-v2`
- Build: clean (`npm run build` passes)
- Tests: 99/99 passing
- Binary: needs `cargo run -- review-hook < /tmp/test-hook-input.json` to serve at /v2

<context>
Phase 18 is functionally complete. All plans have SUMMARYs. Code review and verification have run. The only remaining gate is human UAT sign-off (6 items). Items 1-2 are confirmed. Items 3-6 need browser verification. Once the user types "approved", the phase can be closed with gsd-sdk query phase.complete 18.

The series of selection bugs were all rooted in the same underlying issue: React's reconciliation and state updates interfering with the browser's native selection mechanism. The final architecture (MarkdownView memo + no setState on mousedown + e.buttons guard) is solid.
</context>

<next_action>
Present the UAT checkpoint to the user again with current status. Ask them to test items 3-6:
- Select text → confirm toolbar appears pinned to selection end, hover background disappears
- Check React DevTools for offsets={{ start: N, end: N }} on SelectionToolbar
- Click "More" button → confirm 6-item dropdown
- Click a pill → confirm toolbar disappears and selection clears

Then: `gsd-sdk query phase.complete 18` → commit → offer_next routing
</next_action>
