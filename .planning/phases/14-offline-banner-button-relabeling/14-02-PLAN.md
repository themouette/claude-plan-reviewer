---
phase: 14-offline-banner-button-relabeling
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - ui/src/App.tsx
autonomous: true
requirements:
  - OFX-01
  - OFX-02
must_haves:
  truths:
    - "An amber non-dismissable banner reading the two locked copy lines appears between <PageHeader> and the appState block when connectivity is offline"
    - "The banner disappears immediately when useHeartbeat() returns 'online' — no transition"
    - "Approve button shows 'Copy to clipboard — approve' while offline, reverts to approveLabel on recovery"
    - "Outer Deny button shows 'Copy to clipboard — deny' while offline, reverts to denyLabel on recovery, and continues to toggle the deny form (no behavior change)"
    - "Inner Submit Denial button shows 'Copy to clipboard' while offline, reverts to 'Submit Denial' on recovery"
    - "The banner is visible across ALL appState values (loading, error, reviewing, confirmed) — it is NOT inside any appState branch"
    - "useHeartbeat() is called exactly ONCE at the top of App — one interval, one visibility listener"
    - "The approve/deny/submit click handlers are NOT modified — buttons still POST to /api/decide in Phase 14"
    - "Annotation UI remains fully interactive while banner is shown — no overlay, no pointer-events: none"
  artifacts:
    - path: "ui/src/App.tsx"
      provides: "OfflineBanner colocated sub-component + useHeartbeat() call + banner mount + three label ternaries"
      contains: "OfflineBanner"
    - path: "ui/src/App.tsx"
      provides: "Single useHeartbeat() call in App body"
      contains: "useHeartbeat"
    - path: "ui/src/App.tsx"
      provides: "Three render-time label ternaries"
      contains: "approveButtonLabel"
  key_links:
    - from: "ui/src/App.tsx"
      to: "ui/src/hooks/useHeartbeat.ts"
      via: "import { useHeartbeat } from './hooks/useHeartbeat'"
      pattern: "useHeartbeat"
    - from: "ui/src/App.tsx"
      to: "ui/src/utils/offlineLabels.ts"
      via: "import { ..., approveButtonLabel, denyButtonLabel, submitDenialButtonLabel } from './utils/offlineLabels'"
      pattern: "from './utils/offlineLabels'"
    - from: "OfflineBanner"
      to: "ui/src/index.css"
      via: "var(--color-banner-bg) and var(--color-banner-text) in inline style"
      pattern: "color-banner"
---

<objective>
Wire Plan 01's helper module and CSS tokens into App.tsx: add one colocated OfflineBanner sub-component, call useHeartbeat() once at the top of App, mount the banner conditionally between PageHeader and the appState block, and replace the three button label literals with render-time helper-function calls.

Purpose: Delivers the complete Phase 14 user-visible behavior — amber offline banner and relabeled buttons — by integrating the already-tested offlineLabels.ts contracts into the React tree. This plan makes zero changes to submit-path logic; click handlers continue to POST to /api/decide (Phase 15 owns the clipboard write).

Output: A modified App.tsx with five targeted edits (imports, OfflineBanner function, hook call, banner mount point, three label sites). No other files changed.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/PROJECT.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/ROADMAP.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/STATE.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-CONTEXT.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-RESEARCH.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-UI-SPEC.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-PATTERNS.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01 — use these directly, do not explore. -->

From ui/src/utils/offlineLabels.ts (created by Plan 01):
```typescript
import type { ConnectivityStatus } from './connectivity'

export const OFFLINE_BANNER_LINE_1: string  // 'Server connection lost — working offline.'
export const OFFLINE_BANNER_LINE_2: string  // "When you're done, copy your decision to the clipboard and paste it back into Claude."
export const OFFLINE_APPROVE_LABEL: string  // 'Copy to clipboard — approve'
export const OFFLINE_DENY_LABEL: string     // 'Copy to clipboard — deny'
export const OFFLINE_SUBMIT_DENIAL_LABEL: string  // 'Copy to clipboard'

export function approveButtonLabel(status: ConnectivityStatus, defaultLabel: string): string
export function denyButtonLabel(status: ConnectivityStatus, defaultLabel: string): string
export function submitDenialButtonLabel(status: ConnectivityStatus): string
```

From ui/src/hooks/useHeartbeat.ts (Phase 13 — unchanged):
```typescript
export function useHeartbeat(): ConnectivityStatus
// Returns 'online' | 'offline'. Single call per component tree.
// Manages its own 5s interval, AbortSignal.timeout(3000), and visibilitychange listener.
// Calling it twice creates two intervals — FORBIDDEN by D-12.
```

From ui/src/index.css (Plan 01 additions):
```css
/* Both theme blocks now contain: */
--color-banner-bg: /* #f59e0b (dark) / #d97706 (light) */
--color-banner-text: /* #0f1117 (dark) / #0f172a (light) */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add imports, OfflineBanner colocated sub-component, and useHeartbeat() hook call to App.tsx</name>
  <files>
    ui/src/App.tsx
  </files>
  <read_first>
    - ui/src/App.tsx (full read to confirm line numbers before editing — especially lines 1-15 for import block, ~89-150 for existing colocated sub-components, ~537-561 for hook-call region)
    - .planning/phases/14-offline-banner-button-relabeling/14-RESEARCH.md (§"Code Examples → Verified pattern: exact App.tsx integration sites"; §"Architecture Patterns → Pattern 3"; §"Common Pitfalls → Pitfall 2, 6, 8")
    - .planning/phases/14-offline-banner-button-relabeling/14-PATTERNS.md ("Edit 1: New imports", "Edit 2: OfflineBanner sub-component", "Edit 3: hook call")
    - .planning/phases/14-offline-banner-button-relabeling/14-CONTEXT.md (D-09 role="status"; D-12 single hook call; D-13 colocation in App.tsx not components/)
    - .planning/phases/14-offline-banner-button-relabeling/14-UI-SPEC.md (§Layout — banner element spec; §Color — var reference)
  </read_first>
  <action>
**Step A — Add imports.** Locate the existing import block at the top of ui/src/App.tsx (lines 1-~12). After the existing `import { useTextSelection, rangeFromOffsets } from './hooks/useTextSelection'` line, add these two import statements on adjacent lines:

```ts
import { useHeartbeat } from './hooks/useHeartbeat'
import {
  OFFLINE_BANNER_LINE_1,
  OFFLINE_BANNER_LINE_2,
  approveButtonLabel,
  denyButtonLabel,
  submitDenialButtonLabel,
} from './utils/offlineLabels'
```

Do NOT import `OFFLINE_APPROVE_LABEL`, `OFFLINE_DENY_LABEL`, or `OFFLINE_SUBMIT_DENIAL_LABEL` directly — the helper functions already return the right values; importing the constants would be redundant.

**Step B — Add OfflineBanner sub-component.** Locate `function ErrorView()` in App.tsx (around line 113-150). Immediately AFTER the closing `}` of `ErrorView`, insert the new `OfflineBanner` function declaration. Follow the exact same shape as `ErrorView` and `LoadingSpinner`: function declaration (not arrow), zero props, inline `style={{...}}` object, CSS variable references via `var(--color-*)`.

Exact body to insert:

```tsx
function OfflineBanner() {
  return (
    <div
      role="status"
      style={{
        background: 'var(--color-banner-bg)',
        color: 'var(--color-banner-text)',
        padding: '16px 32px',
        display: 'flex',
        flexDirection: 'column',
        gap: '8px',
        alignItems: 'flex-start',
        fontSize: '14px',
        fontWeight: 400,
        lineHeight: 1.5,
        flexShrink: 0,
      }}
    >
      <div>{OFFLINE_BANNER_LINE_1}</div>
      <div>{OFFLINE_BANNER_LINE_2}</div>
    </div>
  )
}
```

Critical rules for OfflineBanner (per D-09, D-12, D-13, Pitfall 6, UI-SPEC §Layout):
- `role="status"` on the outer div — NOT `role="alert"`.
- `flexShrink: 0` MUST be present (Pitfall 2 — PageHeader at App.tsx:51 uses the same; the parent is display:flex flex-direction:column and without it the banner height is clipped on small viewports).
- Do NOT call `useHeartbeat()` inside OfflineBanner — that would create a second interval (Pitfall 6, D-12). Banner renders from the App-level value passed via conditional render.
- Do NOT add `tabindex`, `onClick`, `onHover`, or any interactive prop — the banner is non-interactive (UI-SPEC §Tab Order).
- No `position: sticky`, no `position: fixed` — normal flow only (UI-SPEC §Spacing).
- No close button, no aria-label on the div, no border, no border-radius — solid full-bleed bar per D-07.
- Both `<div>` children use the already-imported constants OFFLINE_BANNER_LINE_1 and OFFLINE_BANNER_LINE_2 — do NOT inline the copy strings here.

**Step C — Add useHeartbeat() hook call in App body.** Locate the App function body hooks region (around line 537-561). Find the `theme` state initialization line:

```tsx
const [theme, setTheme] = useState<'dark' | 'light'>(() => { ... })
```

And the `function handleThemeToggle()` declaration that follows. After `handleThemeToggle` and before the `appState` state declaration (around line 551), insert:

```tsx
const connectivity = useHeartbeat()
```

This placement keeps the derived signal adjacent to the other state/hook declarations, before the data-loading effects, and ensures `connectivity` is in scope across the entire App function body including the JSX return (which starts around line 1067). The name `connectivity` must match exactly — it will be referenced in four JSX sites added in Task 2.

**Do NOT add `connectivity` to any existing `useCallback`, `useMemo`, or `useEffect` dependency array.** The variable is read at render time only, never inside a memoized closure. Adding it to an existing dep array would be a mistake (Pitfall 7 from RESEARCH.md).

**Step D — Verify structural invariants after edits:**
- Run: grep -c "useHeartbeat()" ui/src/App.tsx — must return exactly 1 (single call site, D-12).
- Run: grep -c 'role="status"' ui/src/App.tsx — must return exactly 1.
- Run: grep -c 'role="alert"' ui/src/App.tsx — must return exactly 0.
- Run: grep -c "from './utils/offlineLabels'" ui/src/App.tsx — must return exactly 1.
- Run: grep -c "from './hooks/useHeartbeat'" ui/src/App.tsx — must return exactly 1.
- Run: cd ui && npm run lint — ESLint must exit 0. If react-hooks/exhaustive-deps warns about `connectivity`, it means it was captured in a memoized callback — move the read to render time.

**Forbidden:**
- Calling `useHeartbeat()` inside `OfflineBanner` — doubles ping rate (Pitfall 6).
- Placing `useHeartbeat()` call inside a condition or loop — Rules of Hooks violation.
- Using `role="alert"` — too disruptive for a non-destructive status (D-09, UI-SPEC §Live-Region).
- Adding `tabindex="0"` or any child `<button>` / `<a>` to OfflineBanner — banner must be non-focusable (UI-SPEC §Tab Order).
- Using `position: sticky` or `position: fixed` — normal flow only (UI-SPEC §Spacing).
- Extracting OfflineBanner to ui/src/components/ — D-13 forbids it.
- Animating banner mount/unmount — deferred (CONTEXT.md &lt;deferred&gt;).
- Importing the raw OFFLINE_* label constants for direct use in JSX — use the helper functions in Task 2; importing them here is unused.
  </action>
  <verify>
    <automated>cd /Users/julien.muetton/Projects/lab/claude-plan-reviewer/ui &amp;&amp; [ "$(grep -c "useHeartbeat()" src/App.tsx)" = "1" ] &amp;&amp; [ "$(grep -c 'role="status"' src/App.tsx)" = "1" ] &amp;&amp; [ "$(grep -c 'role="alert"' src/App.tsx)" = "0" ] &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "useHeartbeat()" ui/src/App.tsx returns exactly 1.
    - grep -c 'role="status"' ui/src/App.tsx returns exactly 1.
    - grep -c 'role="alert"' ui/src/App.tsx returns exactly 0.
    - grep -c "from './utils/offlineLabels'" ui/src/App.tsx returns exactly 1.
    - grep -c "from './hooks/useHeartbeat'" ui/src/App.tsx returns exactly 1.
    - grep -c "function OfflineBanner" ui/src/App.tsx returns exactly 1.
    - grep -c "flexShrink: 0" ui/src/App.tsx returns at least 2 (PageHeader at line ~51 + new OfflineBanner).
    - grep -c "const connectivity = useHeartbeat()" ui/src/App.tsx returns exactly 1.
    - cd ui && npm run lint exits 0 with no new errors or warnings.
  </acceptance_criteria>
  <done>
    OfflineBanner function is colocated after ErrorView in App.tsx. Imports for useHeartbeat and offlineLabels helpers are in the import block. const connectivity = useHeartbeat() is in the App body hook region. Lint is clean. No interactive props on the banner. role="status" is present. flexShrink: 0 is present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount banner JSX and wire three button-label ternaries in App.tsx</name>
  <files>
    ui/src/App.tsx
  </files>
  <read_first>
    - ui/src/App.tsx (read specifically: line ~1075 PageHeader mount, ~1078 appState block open, ~1297 approveLabel site, ~1335 denyLabel site, ~1418 Submit Denial site — use anchor-string search if line numbers have shifted)
    - .planning/phases/14-offline-banner-button-relabeling/14-RESEARCH.md (§"Code Examples → Verified pattern: exact App.tsx integration sites" — Edits 4a through 4d)
    - .planning/phases/14-offline-banner-button-relabeling/14-PATTERNS.md ("Edit 4a Banner mount point", "Edit 4b/c/d label sites", "Pattern D: Render-time conditional")
    - .planning/phases/14-offline-banner-button-relabeling/14-UI-SPEC.md (§Layout &amp; Interaction Contract — DOM order diagram; §Copywriting Contract — online/offline label table)
    - .planning/phases/14-offline-banner-button-relabeling/14-CONTEXT.md (D-01 two buttons; D-02 approve offline; D-03 outer deny offline; D-04 inner submit offline; D-05 render-time not state mutation; D-10 banner outside appState branches; D-11 banner coexists with ErrorView)
  </read_first>
  <action>
**Step A — Mount OfflineBanner between PageHeader and the appState block.**

Locate the JSX in App's return value. Find the `<PageHeader ... />` line (anchor: `activeTab={activeTab} onTabChange={setActiveTab}`). Immediately AFTER `<PageHeader ... />` and BEFORE the `{/* Non-reviewing states */}` comment and its associated `{appState !== 'reviewing' && (` block, insert:

```tsx
{connectivity === 'offline' && <OfflineBanner />}
```

The resulting DOM structure must be:

```tsx
<PageHeader activeTab={activeTab} onTabChange={setActiveTab} theme={theme} onThemeToggle={handleThemeToggle} />
{connectivity === 'offline' && <OfflineBanner />}

{/* Non-reviewing states: loading, error, confirmed */}
{appState !== 'reviewing' && (
  ...
)}
```

**Critical placement rule (Pitfall 1 from RESEARCH.md, D-10):** The `{connectivity === 'offline' && <OfflineBanner />}` line MUST be a sibling of the `{appState !== 'reviewing' && ...}` block and the `{appState === 'reviewing' && ...}` block — NOT inside either of them. If the banner is placed inside `{appState !== 'reviewing' && ...}` it will disappear when the user is in the reviewing state. If placed inside `{appState === 'reviewing' && ...}` it disappears on loading/error/confirmed. It must be visible for ALL four appState values (D-10).

After inserting, verify DOM nesting with: grep -n "connectivity.*offline.*OfflineBanner\|appState.*reviewing" ui/src/App.tsx — the connectivity line must appear BEFORE both appState branches, with no appState conditionals wrapping it.

**Step B — Replace approve button label (anchor: `{approveLabel}`).**

Locate the Approve button in the action bar JSX. The anchor is the exact JSX expression `{approveLabel}` on its own (around line 1297). Replace ONLY the `{approveLabel}` token with:

```tsx
{approveButtonLabel(connectivity, approveLabel)}
```

The surrounding `<span>↵ Enter</span>` keyboard hint (lines after) MUST remain — do not remove or modify it. Only the label JSX expression is changed.

**Step C — Replace outer Deny button label (anchor: `{denyLabel}`).**

Locate the outer Deny button (the one that toggles the deny form open/closed). The anchor is the exact JSX expression `{denyLabel}` (around line 1335). Replace ONLY this token with:

```tsx
{denyButtonLabel(connectivity, denyLabel)}
```

The button's `onClick` handler that toggles the deny form MUST remain unchanged — this phase does NOT modify click handler bodies (CONTEXT.md domain section, D-03).

**Step D — Replace inner Submit Denial button label (anchor: `Submit Denial` bare text node).**

Locate the inner submit denial button (inside the deny form, fires the `deny()` function). The anchor is the bare text `Submit Denial` inside that button (around line 1418). Because this is currently bare JSX text (not a JSX expression), the replacement must wrap it in braces:

```tsx
{submitDenialButtonLabel(connectivity)}
```

Replace the bare text `Submit Denial` with the expression above. Do NOT modify the button's onClick or disabled condition — the click handler calls `deny()` only if `denyMessageValid` (unchanged in Phase 14).

**Step E — Run full verification suite:**

1. `cd ui && npm test -- --run` — all tests pass (including Plan 01's 15 offlineLabels tests). No regressions.
2. `cd ui && npm run lint` — ESLint exits 0. No `react-hooks/exhaustive-deps` warnings for `connectivity`.
3. `cd ui && npm run build` — TypeScript compile + Vite bundle exits 0.
4. `grep -c "useHeartbeat()" ui/src/App.tsx` — returns exactly 1.
5. `grep -c 'role="status"' ui/src/App.tsx` — returns exactly 1.
6. `grep -c 'role="alert"' ui/src/App.tsx` — returns exactly 0.
7. `grep -c "approveButtonLabel(connectivity, approveLabel)" ui/src/App.tsx` — returns exactly 1.
8. `grep -c "denyButtonLabel(connectivity, denyLabel)" ui/src/App.tsx` — returns exactly 1.
9. `grep -c "submitDenialButtonLabel(connectivity)" ui/src/App.tsx` — returns exactly 1.
10. `grep -c "connectivity === 'offline' && &lt;OfflineBanner" ui/src/App.tsx` — returns exactly 1.
11. `find ui/src/components -name 'OfflineBanner*' 2>/dev/null | wc -l` — returns 0 (banner stays in App.tsx).
12. `grep -v '^//' ui/src/App.tsx | grep -c '{approveLabel}'` — returns 0 (bare approveLabel is fully replaced).
13. `grep -v '^//' ui/src/App.tsx | grep -c '{denyLabel}'` — returns 0 (bare denyLabel is fully replaced — only function-call form remains).

**Forbidden (all from CONTEXT.md domain, D-05, Pitfall 1, RESEARCH.md Anti-Patterns):**
- Modifying approve(), deny(), or any click handler body — Phase 14 must NOT touch submit-path logic.
- Using `useEffect` to set approveLabel/denyLabel state on connectivity transitions — forbidden by D-05. Render-time ternary via helper functions is the only correct shape.
- Placing the banner inside any appState branch — forbidden by D-10 (Pitfall 1).
- Adding `approveLabel`/`denyLabel` React state mutations (`setApproveLabel`, `setDenyLabel`) anywhere in this plan — D-05 prohibits state mutation for connectivity-derived label changes.
- Extracting ActionBar as a sub-component to "clean up" the inline JSX — A3/Pattern 4 in RESEARCH.md; action bar stays inline.
- Setting `pointer-events: none` on any element while offline — success criterion 5 requires full annotation interactivity.
  </action>
  <verify>
    <automated>cd /Users/julien.muetton/Projects/lab/claude-plan-reviewer/ui &amp;&amp; npm test -- --run &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; [ "$(grep -c "approveButtonLabel(connectivity, approveLabel)" src/App.tsx)" = "1" ] &amp;&amp; [ "$(grep -c "denyButtonLabel(connectivity, denyLabel)" src/App.tsx)" = "1" ] &amp;&amp; [ "$(grep -c "submitDenialButtonLabel(connectivity)" src/App.tsx)" = "1" ] &amp;&amp; [ "$(grep -c "useHeartbeat()" src/App.tsx)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - cd ui && npm test -- --run exits 0 with all tests passing (no regressions on existing tests; Plan 01's 15 offlineLabels tests still pass).
    - cd ui && npm run lint exits 0 with no new errors or warnings.
    - cd ui && npm run build exits 0.
    - grep -c "approveButtonLabel(connectivity, approveLabel)" ui/src/App.tsx returns exactly 1.
    - grep -c "denyButtonLabel(connectivity, denyLabel)" ui/src/App.tsx returns exactly 1.
    - grep -c "submitDenialButtonLabel(connectivity)" ui/src/App.tsx returns exactly 1.
    - grep -c "connectivity === 'offline' && &lt;OfflineBanner" ui/src/App.tsx returns exactly 1.
    - grep -c "useHeartbeat()" ui/src/App.tsx returns exactly 1.
    - grep -c 'role="status"' ui/src/App.tsx returns exactly 1.
    - grep -c 'role="alert"' ui/src/App.tsx returns exactly 0.
    - grep -v '^//' ui/src/App.tsx | grep -c '{approveLabel}' returns 0 (bare approveLabel usage fully replaced).
    - grep -v '^//' ui/src/App.tsx | grep -c '{denyLabel}' returns 0 (bare denyLabel usage fully replaced).
    - find ui/src/components -name 'OfflineBanner*' 2>/dev/null returns no files.
    - The OfflineBanner JSX mount point is a sibling of — NOT inside — the appState conditional blocks.
  </acceptance_criteria>
  <done>
    Banner renders conditionally between PageHeader and the appState block. Three button labels now call approveButtonLabel/denyButtonLabel/submitDenialButtonLabel(connectivity, ...) at render time. No click handlers modified. No state mutations added. Full test suite passes. Lint clean. Build passes. Phase 14 is functionally complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useHeartbeat() → App | The connectivity signal is client-side derived; no user input crosses this boundary |
| button label display | Display strings are constants from offlineLabels.ts; no user data is rendered in the banner or relabeled buttons |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | OfflineBanner copy | accept | Banner copy is a hardcoded constant; no user data or server data is rendered in the banner. Zero disclosure surface. |
| T-14-02 | Spoofing | Button label flip | accept | Labels are derived from a pure function on a local signal; no network input shapes the label strings. No spoofing surface introduced. |
| T-14-03 | Denial of Service | Second useHeartbeat() call | mitigate | Pitfall 6 — plan explicitly forbids calling useHeartbeat() inside OfflineBanner; grep -c "useHeartbeat()" App.tsx === 1 gate enforces it. A second call would double ping rate and CPU load. |
</threat_model>

<verification>
**Plan-level verification gates** (all must pass before declaring this plan complete):

1. `cd ui && npm test -- --run` — full Vitest suite passes (0 failures, Plan 01's 15 offlineLabels tests still green).
2. `cd ui && npm run lint` — ESLint exits 0 with no new warnings. Specifically: no react-hooks/exhaustive-deps warning for `connectivity` (if it fires, connectivity was captured in a memoized callback — move to render time).
3. `cd ui && npm run build` — TypeScript compile + Vite production bundle exits 0.
4. `grep -c "useHeartbeat()" ui/src/App.tsx` returns exactly 1.
5. `grep -c 'role="status"' ui/src/App.tsx` returns exactly 1.
6. `grep -c 'role="alert"' ui/src/App.tsx` returns exactly 0.
7. `grep -c "approveButtonLabel(connectivity, approveLabel)" ui/src/App.tsx` returns exactly 1.
8. `grep -c "denyButtonLabel(connectivity, denyLabel)" ui/src/App.tsx` returns exactly 1.
9. `grep -c "submitDenialButtonLabel(connectivity)" ui/src/App.tsx` returns exactly 1.
10. `grep -v '^//' ui/src/App.tsx | grep -c '{approveLabel}'` returns 0.
11. `grep -v '^//' ui/src/App.tsx | grep -c '{denyLabel}'` returns 0.
12. `find ui/src/components -name 'OfflineBanner*' 2>/dev/null | wc -l` returns 0.

Manual smoke (cannot be automated — record in SUMMARY):
- Trigger offline via DevTools Network → Offline: after ~15s amber banner appears with two lines; buttons relabel. Toggle back: banner disappears after ~5s, labels revert.
- Theme toggle while offline: banner palette switches correctly (dark: #f59e0b bg / #0f1117 text; light: #d97706 bg / #0f172a text).
- Keyboard tab order: banner is not in tab stop sequence.
- Annotation interactivity: text selection, comment textarea, deny textarea all remain usable while banner is shown.
- Banner is visible across all appState values (force loading/error/confirmed states with banner present).

If any automated gate fails, STOP and surface to user before marking complete.
</verification>

<success_criteria>
- ui/src/App.tsx imports useHeartbeat and the three label helper functions from offlineLabels.ts.
- OfflineBanner colocated function component exists in App.tsx with role="status", flexShrink:0, inline var(--color-banner-bg)/var(--color-banner-text) styles, and the two locked copy lines from OFFLINE_BANNER_LINE_1/OFFLINE_BANNER_LINE_2.
- const connectivity = useHeartbeat() is called exactly once in the App function body.
- {connectivity === 'offline' && &lt;OfflineBanner /&gt;} is placed between &lt;PageHeader /&gt; and the appState conditional blocks in App's JSX return — not inside any appState branch.
- The three button label sites use render-time helper calls: approveButtonLabel(connectivity, approveLabel), denyButtonLabel(connectivity, denyLabel), submitDenialButtonLabel(connectivity).
- No click handler body is modified (approve/deny still POST to /api/decide).
- No approveLabel/denyLabel React state mutations on connectivity transitions.
- All automated verification gates pass (test, lint, build, grep invariants).
- Phase 14 ROADMAP success criteria 1-5 are fully satisfied:
  1. Banner appears between header and columns when offline.
  2. Banner absent when online.
  3. Submit buttons relabeled offline, reverted online.
  4. Banner disappears on recovery (single successful ping).
  5. Annotation UI fully interactive while banner shown.
</success_criteria>

<output>
After completion, create .planning/phases/14-offline-banner-button-relabeling/14-02-SUMMARY.md per the standard SUMMARY template.

Required SUMMARY content:
- Files modified: ui/src/App.tsx (edits: 1 import block, 1 OfflineBanner function, 1 useHeartbeat() call, 1 banner mount, 3 label ternaries)
- Files unchanged: all other files
- Test count delta: 0 new tests (pure-helper tests live in Plan 01)
- Paste outputs of npm test --run, npm run lint, npm run build, and the 12 automated grep gates
- Record manual smoke test results for the 4 DevTools scenarios
- Note: Phase 14 is now fully complete. Phase 15 (Clipboard Submit Path) owns the next change — wiring the relabeled buttons to navigator.clipboard.writeText() on click.
</output>
