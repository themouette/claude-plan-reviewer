---
context: phase
phase: 14-offline-banner-button-relabeling
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-05-07T10:18:33.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: PERFORMANCE REGRESSION — A user-reported performance regression (every interaction takes several seconds) was discovered mid-session and was NOT resolved before pause. Do not mark Phase 14 complete or advance to Phase 15 until this is diagnosed and fixed.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Advancing with unresolved perf regression | Phase 14 introduced a performance regression (several-second delays on text selection, tab switching). Root cause not conclusively identified. | blocking | Run isolation test first (replace `useHeartbeat()` with `'online' as const`). Only advance after confirmed fix + all 39 tests still pass. |
| Worktree merge with staged planning files | Staged `.planning/` changes on main blocked the worktree merge twice this session, requiring manual intervention to commit staged files first, then retry merge. | advisory | Before spawning executor agents, ensure the main branch has no staged changes in `.planning/` — commit or reset staging area first. |

<current_state>
Phase 14 (offline-banner-button-relabeling) is code-complete: both plans executed, 39/39 tests pass, code reviewed, VERIFICATION.md and HUMAN-UAT.md committed. However, a performance regression was reported by the user partway through the session, and the phase was NOT marked complete in ROADMAP/STATE due to this open issue.

The verification status is `human_needed` — 5 browser UAT items remain (in 14-HUMAN-UAT.md).
</current_state>

<completed_work>

- **Plan 14-01** ✓ — `ui/src/utils/offlineLabels.ts` (5 constants, 3 label functions), `offlineLabels.test.ts` (15 Vitest tests), CSS theme tokens `--color-banner-bg` / `--color-banner-text` in both themes. 39/39 tests pass.
- **Plan 14-02** ✓ — `ui/src/App.tsx`: `OfflineBanner` sub-component, `useHeartbeat()` call, banner mount between `<PageHeader>` and appState blocks, 3 label ternaries for approve/deny/submit buttons.
- **Code review** ✓ — `14-REVIEW.md` committed. Findings: WR-01 (Enter shortcut shows while offline, fires failing POST), WR-02 ("Submit Denial" hardcoded vs exported constant), IN-01 (dead inlineChips code, pre-existing), IN-02 (test naming style).
- **Verification** ✓ — `14-VERIFICATION.md` committed (status: human_needed, 8/9 must-haves automated).
- **HUMAN-UAT.md** ✓ — 5 browser UAT items committed.
</completed_work>

<remaining_work>

1. **BLOCKING: Diagnose and fix performance regression** — User reports every interaction (text selection popover, tab switching) takes several seconds. Isolation test: in `ui/src/App.tsx`, replace `const connectivity = useHeartbeat()` with `const connectivity: ConnectivityStatus = 'online'` and check if slowness disappears. If yes, the issue is that `useHeartbeat()` in the top-level App component causes re-renders that trigger the no-dep `useLayoutEffect` (DOM walk per annotation). Fix: memoize or lift the heartbeat state out of App.
2. **Human UAT** — Run the 5 items in `14-HUMAN-UAT.md` in a browser with the running binary. Update pass/fail results.
3. **Mark phase complete** — Once perf is fixed and UAT passes, run `gsd-sdk query phase.complete "14"` and update ROADMAP/STATE.
4. **Optional: Address WR-01/WR-02** — Fix the Enter shortcut and "Submit Denial" constant. These are non-blocking but worth a quick fix.
</remaining_work>

<decisions_made>

- Light-theme `--color-banner-text` uses UI-SPEC override `#0f172a` (5.60:1 contrast), not D-08 literal `#f8fafc` (which fails AA at 3.04:1).
- Two pre-existing bare `{approveLabel}` / `{denyLabel}` usages (ConfirmationView done-state) intentionally NOT wrapped in helpers — offline label logic does not apply post-decision.
- 14-01 tasks committed atomically (both in one commit) to keep lint clean — the imports/hook-call from Task 1 produce unused-var errors until Task 2 wires the JSX.
</decisions_made>

<blockers>

- **Performance regression** (blocking): Every interaction takes several seconds after Phase 14. Suspected cause: `useHeartbeat()` state living in App's top-level body causes re-renders that trigger the no-dep `useLayoutEffect` annotation DOM walk. Isolation test is the first step.
- **Human UAT** (human action required): 5 browser tests in `14-HUMAN-UAT.md`. Must be done with binary running, DevTools offline simulation.
</blockers>

## Required Reading (in order)
1. `.planning/phases/14-offline-banner-button-relabeling/14-REVIEW.md` — WR-01 and WR-02 findings to fix
2. `.planning/phases/14-offline-banner-button-relabeling/14-HUMAN-UAT.md` — 5 pending browser UAT items
3. `.planning/phases/14-offline-banner-button-relabeling/14-VERIFICATION.md` — full verification status

## Critical Anti-Patterns (do NOT repeat these)

- **STAGED PLANNING FILES BLOCK WORKTREE MERGE**: Before spawning executor agents, check `git status` and ensure no `.planning/` files are staged. Staged files block the post-wave worktree merge. Fix: `git commit` the staged files first, then spawn agents.
- **RESCUED SUMMARY.md BLOCKS MERGE**: The worktree cleanup script rescues SUMMARY.md to the working tree, then the merge fails because it's untracked. Fix: `rm` the rescued copy (it's identical to the worktree version), let the merge bring it in.

## Infrastructure State

- Rust binary: `cargo run` from repo root — serves `http://localhost:<dynamic-port>`
- Dev frontend: `cd ui && npm run dev` — requires `/api/*` proxy to binary port (NOT configured in `vite.config.ts` — set up manually if needed)
- All 39 Vitest tests: `cd ui && npm test -- --run`

<context>
Both plans executed cleanly. The performance regression appeared when the user tested the running app. From code inspection, the most plausible cause is `useHeartbeat()` at App's top level causing extra renders that trigger the expensive no-dep `useLayoutEffect` (which rebuilds all CSS annotation highlights by walking the DOM for every annotation, on every render). Before Phase 14, renders only happened on user interactions. After Phase 14, the initial heartbeat tick + periodic polls may add extra render triggers.

The GSD framework files in `.claude/` have large unstaged changes (a GSD update) — these are pre-existing and completely unrelated to Phase 14 work. Do not commit them as part of Phase 14 cleanup.
</context>

<next_action>
Start with: Run the isolation test — open `ui/src/App.tsx`, find `const connectivity = useHeartbeat()` (around line 583), temporarily change to `const connectivity: ConnectivityStatus = 'online'` and import `ConnectivityStatus` from `'./utils/connectivity'`. Build and test: does the slowness disappear? This confirms or rules out `useHeartbeat` as root cause.
</next_action>
