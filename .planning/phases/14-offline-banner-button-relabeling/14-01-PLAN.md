---
phase: 14-offline-banner-button-relabeling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/utils/offlineLabels.ts
  - ui/src/utils/offlineLabels.test.ts
  - ui/src/index.css
autonomous: true
requirements:
  - OFX-01
  - OFX-02
must_haves:
  truths:
    - "A pure helper module exports the five offline copy constants byte-for-byte"
    - "Three pure label-selection functions return offline strings when status is offline and the default otherwise"
    - "Two new CSS variables --color-banner-bg and --color-banner-text are defined in both :root (dark) and [data-theme=light] blocks"
    - "Vitest unit tests cover constants byte-equality plus the online/offline branches of every label function (15 tests)"
    - "All UI gates pass: npm test, npm run lint, npm run build (under ui/)"
  artifacts:
    - path: "ui/src/utils/offlineLabels.ts"
      provides: "Pure helper module: 5 string constants + 3 label functions"
      exports: ["OFFLINE_BANNER_LINE_1", "OFFLINE_BANNER_LINE_2", "OFFLINE_APPROVE_LABEL", "OFFLINE_DENY_LABEL", "OFFLINE_SUBMIT_DENIAL_LABEL", "approveButtonLabel", "denyButtonLabel", "submitDenialButtonLabel"]
      contains: "ConnectivityStatus"
    - path: "ui/src/utils/offlineLabels.test.ts"
      provides: "Vitest unit tests for offlineLabels module"
      contains: "describe"
    - path: "ui/src/index.css"
      provides: "Theme tokens including --color-banner-bg and --color-banner-text in both theme blocks"
      contains: "--color-banner-bg"
  key_links:
    - from: "ui/src/utils/offlineLabels.ts"
      to: "ui/src/utils/connectivity.ts"
      via: "import type ConnectivityStatus"
      pattern: "from './connectivity'"
    - from: "ui/src/utils/offlineLabels.test.ts"
      to: "ui/src/utils/offlineLabels.ts"
      via: "named imports of constants and functions"
      pattern: "from './offlineLabels'"
---

<objective>
Foundation plan for Phase 14. Create the pure helper module ui/src/utils/offlineLabels.ts that owns all five locked copy strings and three label-selection functions, sibling Vitest test file, and the two new CSS theme tokens (--color-banner-bg and --color-banner-text) in both :root and [data-theme="light"] blocks of ui/src/index.css.

Purpose: Encapsulates the offline copy contract in a single testable module so Plan 02's App.tsx wiring is reduced to thin call sites. Satisfies the CLAUDE.md test coverage gate via pure-helper Vitest tests (no @testing-library/react), matching the Phase 13 / connectivity.ts pattern. Adds the theme-aware banner palette (locked WCAG AA contrast pairs from UI-SPEC §Color) that Plan 02's OfflineBanner consumes via var(...).

Output: A new helper module + sibling test (15 cases) + two new CSS variables in two theme blocks. Touches NO React component code; Plan 02 owns all App.tsx edits.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/PROJECT.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/ROADMAP.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/STATE.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-CONTEXT.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-RESEARCH.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-UI-SPEC.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.planning/phases/14-offline-banner-button-relabeling/14-PATTERNS.md

# Analog files — pattern source of truth (read these before coding)
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/ui/src/utils/connectivity.ts
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/ui/src/utils/connectivity.test.ts
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/ui/src/utils/serializeAnnotations.test.ts
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/ui/src/index.css

The signal type ConnectivityStatus is exported by ui/src/utils/connectivity.ts as the string-literal union 'online' | 'offline'. Import it with the `type` qualifier — do NOT redefine it locally.

The existing CSS variable convention in ui/src/index.css declares all dark-theme tokens inside the `:root { ... }` block (the last token before the closing brace today is `--color-link: #60a5fa;`) and all light-theme tokens inside the `[data-theme="light"] { ... }` block (the last token before the closing brace today is `--color-link: #1d4ed8;` with a trailing comment). Both blocks must redeclare any token that varies by theme — CSS custom properties do not inherit across re-scoping selectors.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create offlineLabels pure helper module + Vitest tests (RED then GREEN)</name>
  <files>
    ui/src/utils/offlineLabels.ts (new)
    ui/src/utils/offlineLabels.test.ts (new)
  </files>
  <read_first>
    - ui/src/utils/connectivity.ts (analog: pure-function utility, ConnectivityStatus type source)
    - ui/src/utils/connectivity.test.ts (analog: Vitest describe/it/expect pattern, "Test N: ..." naming)
    - ui/src/utils/serializeAnnotations.test.ts (second analog confirming the no-mock pure-helper test style)
    - .planning/phases/14-offline-banner-button-relabeling/14-RESEARCH.md (Pattern 1 in "Architecture Patterns" — full helper signature; "Code Examples" — full test body lines 499-585)
    - .planning/phases/14-offline-banner-button-relabeling/14-UI-SPEC.md (§Copywriting Contract — locked copy strings; §Testability Contract — recommended cases)
    - .planning/phases/14-offline-banner-button-relabeling/14-CONTEXT.md (D-02, D-03, D-04, D-06 — locked label/banner copy)
    - ui/package.json (confirm @testing-library/react is NOT present — must remain absent)
  </read_first>
  <behavior>
    Test 1: OFFLINE_BANNER_LINE_1 equals exactly: Server connection lost — working offline. (em dash U+2014, period at end, no trailing whitespace)
    Test 2: OFFLINE_BANNER_LINE_2 equals exactly: When you're done, copy your decision to the clipboard and paste it back into Claude. (ASCII U+0027 apostrophe, period at end)
    Test 3: OFFLINE_APPROVE_LABEL equals exactly: Copy to clipboard — approve (em dash U+2014)
    Test 4: OFFLINE_DENY_LABEL equals exactly: Copy to clipboard — deny (em dash U+2014)
    Test 5: OFFLINE_SUBMIT_DENIAL_LABEL equals exactly: Copy to clipboard
    Test 6: approveButtonLabel('online', 'Approve') returns 'Approve'
    Test 7: approveButtonLabel('online', 'No issues') returns 'No issues' (Phase 11.1 custom label preserved when online)
    Test 8: approveButtonLabel('offline', 'Approve') returns OFFLINE_APPROVE_LABEL
    Test 9: approveButtonLabel('offline', 'No issues') returns OFFLINE_APPROVE_LABEL (offline overrides any custom default)
    Test 10: denyButtonLabel('online', 'Deny') returns 'Deny'
    Test 11: denyButtonLabel('online', 'Leave feedback') returns 'Leave feedback' (custom default preserved online)
    Test 12: denyButtonLabel('offline', 'Deny') returns OFFLINE_DENY_LABEL
    Test 13: denyButtonLabel('offline', 'Leave feedback') returns OFFLINE_DENY_LABEL (offline overrides custom)
    Test 14: submitDenialButtonLabel('online') returns 'Submit Denial'
    Test 15: submitDenialButtonLabel('offline') returns OFFLINE_SUBMIT_DENIAL_LABEL
  </behavior>
  <action>
**Step A — RED: Write the failing test file FIRST at ui/src/utils/offlineLabels.test.ts** with the exact body shown in 14-RESEARCH.md "Code Examples" section, lines 499-585. Do NOT improvise — copy the body verbatim. The test file imports from './offlineLabels' (which does not yet exist).

Run: cd ui && npm test -- --run offlineLabels

Vitest MUST fail with "Cannot find module './offlineLabels'" or equivalent. This RED step is required before implementation — proves the test actually exercises the module under test.

**Step B — GREEN: Create ui/src/utils/offlineLabels.ts** with EXACTLY this content (per RESEARCH.md "Architecture Patterns → Pattern 1" lines 232-260 and PATTERNS.md "Pure function pattern"):

```ts
import type { ConnectivityStatus } from './connectivity'

export const OFFLINE_BANNER_LINE_1 = 'Server connection lost — working offline.'
export const OFFLINE_BANNER_LINE_2 =
  "When you're done, copy your decision to the clipboard and paste it back into Claude."

export const OFFLINE_APPROVE_LABEL = 'Copy to clipboard — approve'
export const OFFLINE_DENY_LABEL = 'Copy to clipboard — deny'
export const OFFLINE_SUBMIT_DENIAL_LABEL = 'Copy to clipboard'

export function approveButtonLabel(
  status: ConnectivityStatus,
  defaultLabel: string,
): string {
  return status === 'offline' ? OFFLINE_APPROVE_LABEL : defaultLabel
}

export function denyButtonLabel(
  status: ConnectivityStatus,
  defaultLabel: string,
): string {
  return status === 'offline' ? OFFLINE_DENY_LABEL : defaultLabel
}

export function submitDenialButtonLabel(status: ConnectivityStatus): string {
  return status === 'offline' ? OFFLINE_SUBMIT_DENIAL_LABEL : 'Submit Denial'
}
```

**Critical character-level rules** (Pitfalls 3 + 4 from 14-RESEARCH.md):
- The em dash — is Unicode U+2014 (NOT two hyphens, NOT a hyphen-minus, NOT en dash U+2013). Appears in three constants: OFFLINE_BANNER_LINE_1, OFFLINE_APPROVE_LABEL, OFFLINE_DENY_LABEL. macOS shortcut: Option+Shift+Hyphen.
- The apostrophe in you're MUST be ASCII U+0027 (straight), NOT curly U+2019. Disable smart-quotes in your editor before typing OR copy from this plan body literally (which uses straight ASCII apostrophes throughout).
- No trailing whitespace on either banner line.

Use `import type { ConnectivityStatus } from './connectivity'` (with the `type` qualifier — verified pattern in useHeartbeat.ts:2-8). Do NOT redefine ConnectivityStatus.

**Step C — Verify GREEN:**
- Run: cd ui && npm test -- --run offlineLabels — all 15 tests must pass.
- Run: cd ui && npm test -- --run — full suite (existing 15 connectivity/heartbeat/serialize tests + 15 new = 30) must pass with 0 failures.

**Step D — Character-level audit:**
- Run: grep -cP '\x{2014}' ui/src/utils/offlineLabels.ts — must return exactly 3 (banner line 1 + approve label + deny label).
- Run: grep -c $'’' ui/src/utils/offlineLabels.ts — must return 0 (no curly apostrophe).

**Step E — Lint gate:** Run cd ui && npm run lint — exit 0, no warnings or errors.

**Forbidden** (per RESEARCH.md anti-patterns and PATTERNS.md "Anti-Patterns"):
- Adding @testing-library/react to ui/package.json — bundle-size policy, no React renderer in tests.
- Using vi.mock(...) or renderHook — pure helpers do not need mocks.
- Defining ConnectivityStatus locally instead of importing from ./connectivity — single source of truth.
- Switch statements — ConnectivityStatus is a binary union; ternary is the idiomatic shape (PATTERNS.md "Why ternary not switch").
- Two hyphens or hyphen-minus in place of em dash — Pitfall 3.
- Curly apostrophe in you're — Pitfall 4.
- Adding any non-pure-helper test (e.g. mounting App.tsx via jsdom) — UI-SPEC §Testability Contract "Banned test approaches".
  </action>
  <verify>
    <automated>cd /Users/julien.muetton/Projects/lab/claude-plan-reviewer/ui &amp;&amp; npm test -- --run offlineLabels &amp;&amp; [ "$(grep -cP '\x{2014}' src/utils/offlineLabels.ts)" = "3" ] &amp;&amp; [ "$(grep -c $'’' src/utils/offlineLabels.ts)" = "0" ] &amp;&amp; [ "$(grep -c '@testing-library' package.json)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/utils/offlineLabels.ts exists.
    - File ui/src/utils/offlineLabels.test.ts exists.
    - cd ui && npm test -- --run offlineLabels exits 0 with 15 passing tests.
    - cd ui && npm test -- --run (full suite) exits 0 with no failures.
    - grep -cP '\x{2014}' ui/src/utils/offlineLabels.ts returns exactly 3.
    - grep -c $'’' ui/src/utils/offlineLabels.ts returns exactly 0.
    - grep -c "from './connectivity'" ui/src/utils/offlineLabels.ts returns exactly 1.
    - grep -c "@testing-library" ui/package.json returns 0 (no new dependency).
    - cd ui && npm run lint exits 0 with no errors and no new warnings.
  </acceptance_criteria>
  <done>
    Pure helper module with 5 named-export string constants and 3 named-export label functions exists. Vitest test file with 15 cases is green. Em dash and apostrophe character checks pass. No new npm dependencies. Lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --color-banner-bg and --color-banner-text tokens to both theme blocks of index.css</name>
  <files>
    ui/src/index.css
  </files>
  <read_first>
    - ui/src/index.css (full file — locate the `:root` block closing brace and the `[data-theme="light"]` block closing brace; existing last token in each block is --color-link)
    - .planning/phases/14-offline-banner-button-relabeling/14-UI-SPEC.md (§Color — WCAG contrast verification table; §Color override rationale for the light-theme #0f172a value)
    - .planning/phases/14-offline-banner-button-relabeling/14-CONTEXT.md (D-08 — note the UI-SPEC override of D-08's literal #f8fafc to #0f172a for AA contrast; UI-SPEC supersedes D-08 literal)
    - .planning/phases/14-offline-banner-button-relabeling/14-PATTERNS.md (`ui/src/index.css` section — exact insertion shape, comment convention)
  </read_first>
  <action>
**Step A — Locate insertion points** in ui/src/index.css:
- Find the `:root {` block (begins around line 3). Its last variable declaration before the closing `}` is --color-link: #60a5fa; (around line 21).
- Find the `[data-theme="light"] {` block (begins around line 24). Its last variable declaration before the closing `}` is --color-link: #1d4ed8; with a trailing comment (around line 42).
- Line numbers are illustrative; anchor on the literal --color-link token in each block.

**Step B — Add the two new variables INSIDE the `:root` block, on the line(s) immediately after the existing --color-link declaration and before the closing brace:**

```
  --color-banner-bg: #f59e0b;
  --color-banner-text: #0f1117;
```

(Two-space indentation matches existing tokens. No trailing inline comment in dark theme — matches the surrounding style.)

**Step C — Add the two new variables INSIDE the `[data-theme="light"]` block, on the line(s) immediately after the existing --color-link declaration and before the closing brace:**

```
  --color-banner-bg: #d97706;
  --color-banner-text: #0f172a; /* dark on amber, 5.60:1 — UI-SPEC override of D-08 literal */
```

**Locked values (do NOT alter):**
- Dark --color-banner-bg: #f59e0b (amber-500). Source: D-08 + UI-SPEC §Color.
- Dark --color-banner-text: #0f1117 (matches --color-bg in dark theme). 8.79:1 contrast — passes AAA.
- Light --color-banner-bg: #d97706 (amber-600). Source: D-08 + UI-SPEC §Color.
- Light --color-banner-text: #0f172a — UI-SPEC override of D-08 literal #f8fafc for AA contrast (5.60:1 vs the failing 3.04:1 of #f8fafc against amber-600). The trailing comment documents the override per UI-SPEC §Color override rationale.

**Step D — Verify with grep gates:**
- grep -cE '\-\-color-banner-(bg|text)' ui/src/index.css MUST return exactly 4 (two vars × two themes).
- grep -c '#f59e0b' ui/src/index.css MUST return exactly 1 (dark banner bg only — value not used elsewhere in file).
- grep -c '#d97706' ui/src/index.css MUST return exactly 1 (light banner bg only — value not used elsewhere).
- grep -q '#0f172a' ui/src/index.css MUST succeed (the light banner-text value; this hex may already appear elsewhere as --color-text-primary light value — that is fine).
- The two `:root` additions MUST sit between the last existing :root token and the :root closing brace. The two `[data-theme="light"]` additions MUST sit between the last existing light token and that block's closing brace. Verify by reading lines around each block after edit.

**Step E — Build gate:**
Run cd ui && npm run build and confirm Vite/TS compile exits 0 with no warnings introduced by these changes. CSS files are not type-checked but the build pipeline verifies CSS validity.

**Forbidden:**
- Defining either variable in only one theme block — both blocks MUST redeclare (CSS custom properties do not inherit across [data-theme="light"] re-scoping; PATTERNS.md "Why both blocks must redeclare").
- Substituting the light-theme text value with D-08's original #f8fafc — fails WCAG AA (3.04:1). Use UI-SPEC's #0f172a (5.60:1).
- Adding a new CSS class for the banner (e.g. .offline-banner) — App.tsx uses inline style objects exclusively. Plan 02 will reference these tokens via var(--color-banner-bg) and var(--color-banner-text) from inline styles.
- Renaming the variables — the names --color-banner-bg and --color-banner-text are referenced by Plan 02. Any rename here cascades into a Plan 02 break.
  </action>
  <verify>
    <automated>cd /Users/julien.muetton/Projects/lab/claude-plan-reviewer/ui &amp;&amp; [ "$(grep -cE '\-\-color-banner-(bg|text)' src/index.css)" = "4" ] &amp;&amp; [ "$(grep -c '#f59e0b' src/index.css)" = "1" ] &amp;&amp; [ "$(grep -c '#d97706' src/index.css)" = "1" ] &amp;&amp; grep -q '#0f172a' src/index.css &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE '\-\-color-banner-(bg|text)' ui/src/index.css returns exactly 4.
    - grep -c '#f59e0b' ui/src/index.css returns exactly 1.
    - grep -c '#d97706' ui/src/index.css returns exactly 1.
    - grep -q '#0f172a' ui/src/index.css succeeds (returns at least one match).
    - The two new dark-theme tokens are positioned BEFORE the :root block's closing brace, not after.
    - The two new light-theme tokens are positioned BEFORE the [data-theme="light"] block's closing brace, not after.
    - cd ui && npm run build exits 0.
    - cd ui && npm run lint exits 0 with no new errors or warnings.
  </acceptance_criteria>
  <done>
    Both --color-banner-bg and --color-banner-text exist in :root (dark theme: #f59e0b / #0f1117) and in [data-theme="light"] (light theme: #d97706 / #0f172a, with override comment). Vite build passes. Lint passes. Tokens are ready to be referenced by Plan 02's OfflineBanner inline styles via var(--color-banner-bg) / var(--color-banner-text).
  </done>
</task>

</tasks>

<verification>
**Plan-level verification gates** (all must pass before declaring this plan complete):

1. cd ui && npm test -- --run — full Vitest suite passes (30 tests: 15 existing + 15 new).
2. cd ui && npm run lint — ESLint exits 0 with no new warnings.
3. cd ui && npm run build — TypeScript compile + Vite production bundle exits 0.
4. grep -cP '\x{2014}' ui/src/utils/offlineLabels.ts returns exactly 3 (em dash count in the helper module).
5. grep -c $'’' ui/src/utils/offlineLabels.ts returns exactly 0 (no curly apostrophe).
6. grep -cE '\-\-color-banner-(bg|text)' ui/src/index.css returns exactly 4 (two vars × two themes).
7. grep -c '@testing-library' ui/package.json returns 0 (no new dependency added).

If any gate fails, STOP and surface to the user — do not proceed to Plan 02.
</verification>

<success_criteria>
- ui/src/utils/offlineLabels.ts exists with 5 string constants and 3 label functions, all named exports.
- ui/src/utils/offlineLabels.test.ts exists with 15 Vitest cases, all passing.
- ui/src/index.css has --color-banner-bg and --color-banner-text declared in BOTH :root and [data-theme="light"] blocks with the locked values.
- All character-level audits pass (em dash count = 3, curly apostrophe count = 0).
- All UI build gates pass (test, lint, build).
- ROADMAP success criterion 4 (banner disappears on recovery) and the label-flip-on-recovery contract are pre-validated by the helper's online-branch tests (tests 6, 7, 10, 11, 14) — Plan 02 wires the consumer.
</success_criteria>

<output>
After completion, create .planning/phases/14-offline-banner-button-relabeling/14-01-SUMMARY.md per the standard SUMMARY template.

Required SUMMARY content:
- Files created: ui/src/utils/offlineLabels.ts, ui/src/utils/offlineLabels.test.ts
- Files modified: ui/src/index.css
- Test count delta: +15 passing
- Verification command results (paste outputs of npm test --run, npm run lint, npm run build, the four grep gates)
- Note: This plan introduces zero runtime behavior on its own. The consumer wiring lives in 14-02-PLAN.md. The contracts (helper module exports, CSS token names) are now stable and consumed by 14-02.
</output>
