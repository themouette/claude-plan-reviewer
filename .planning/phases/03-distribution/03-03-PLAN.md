---
phase: 03-distribution
plan: 03
type: execute
wave: 1
depends_on:
  - 03-01
  - 03-02
files_modified:
  - install.sh
autonomous: false
gap_closure: true
requirements:
  - DIST-01
  - DIST-02

must_haves:
  truths:
    - "install.sh line 9 contains REPO='themouette/claude-plan-reviewer' (not OWNER placeholder)"
    - "install.sh line 4 comment contains 'themouette' (not OWNER placeholder)"
    - "A semver git tag (v0.1.0) has been pushed to trigger the release workflow"
  artifacts:
    - path: "install.sh"
      provides: "Working curl | sh installer with correct GitHub repo URL"
      contains: "themouette/claude-plan-reviewer"
  key_links:
    - from: "install.sh"
      to: "https://api.github.com/repos/themouette/claude-plan-reviewer/releases/latest"
      via: "REPO variable used in curl URL construction"
      pattern: "REPO=\"themouette/claude-plan-reviewer\""
---

<objective>
Close two gaps identified in 03-VERIFICATION.md that prevent the phase goal from being fully achieved:

1. Replace the OWNER placeholder in install.sh with the actual GitHub org "themouette" so download URLs resolve correctly
2. Guide the user to push a v0.1.0 git tag to trigger the cargo-dist release workflow and publish binaries

Purpose: Without these fixes, the install script constructs incorrect URLs and no release artifacts exist on GitHub. Both must be resolved for DIST-01 (binaries published) and DIST-02 (curl | sh works end-to-end).

Output:
- `install.sh` with correct `REPO="themouette/claude-plan-reviewer"` on line 9 and updated comment on line 4
- User pushes `v0.1.0` tag to trigger release pipeline
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-distribution/03-VERIFICATION.md
@.planning/phases/03-distribution/03-01-SUMMARY.md

<interfaces>
<!-- Current install.sh state (lines needing change) -->
From install.sh:
```sh
# Line 4: Usage: curl -fsSL https://raw.githubusercontent.com/OWNER/claude-plan-reviewer/main/install.sh | sh
# Line 9: REPO="OWNER/claude-plan-reviewer"
```

<!-- Authoritative source for correct owner -->
From Cargo.toml line 5:
```toml
repository = "https://github.com/themouette/claude-plan-reviewer"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace OWNER placeholder in install.sh with themouette</name>
  <files>install.sh</files>
  <read_first>
    - install.sh (read full file to confirm current state -- lines 4 and 9 contain OWNER placeholder)
    - Cargo.toml (confirm repository = "https://github.com/themouette/claude-plan-reviewer" as authoritative source)
  </read_first>
  <action>
Edit install.sh to replace the OWNER placeholder with the actual GitHub username "themouette" in exactly two locations:

**Line 4** (comment with usage URL):
Change:
```
# Usage: curl -fsSL https://raw.githubusercontent.com/OWNER/claude-plan-reviewer/main/install.sh | sh
```
To:
```
# Usage: curl -fsSL https://raw.githubusercontent.com/themouette/claude-plan-reviewer/main/install.sh | sh
```

**Line 6** (NOTE comment about replacing OWNER):
Remove or update this line. Since OWNER is no longer a placeholder, the note is obsolete. Change:
```
# NOTE: Replace OWNER with the actual GitHub username/org before publishing a release.
```
To:
```
# Repository: https://github.com/themouette/claude-plan-reviewer
```

**Line 9** (REPO variable):
Change:
```
REPO="OWNER/claude-plan-reviewer"
```
To:
```
REPO="themouette/claude-plan-reviewer"
```

No other lines need changes. The rest of install.sh is verified correct per 03-VERIFICATION.md.
  </action>
  <verify>
    <automated>
      grep -n 'OWNER' /Users/julien.muetton/Projects/lab/claude-plan-reviewer/install.sh; test $? -ne 0 &amp;&amp; echo "PASS: No OWNER placeholders remain" || echo "FAIL: OWNER placeholder still present" &amp;&amp;
      grep -n 'themouette/claude-plan-reviewer' /Users/julien.muetton/Projects/lab/claude-plan-reviewer/install.sh &amp;&amp;
      sh -n /Users/julien.muetton/Projects/lab/claude-plan-reviewer/install.sh &amp;&amp; echo "Shell syntax OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep 'OWNER' install.sh returns NO matches (exit code 1)
    - grep 'themouette/claude-plan-reviewer' install.sh returns matches on lines 4 and 9
    - sh -n install.sh exits 0 (valid shell syntax preserved)
    - No other lines in install.sh are modified
  </acceptance_criteria>
  <done>
    - install.sh contains REPO="themouette/claude-plan-reviewer" on line 9
    - install.sh usage comment on line 4 references themouette (not OWNER)
    - No OWNER placeholders remain anywhere in install.sh
    - Shell syntax remains valid
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push v0.1.0 git tag to trigger release workflow</name>
  <read_first>
    - .github/workflows/release.yml (confirm the workflow triggers on tag push matching the pattern)
  </read_first>
  <action>
The release pipeline is fully configured but has never been triggered because no git tag has been pushed. The user must push a semver tag to trigger the cargo-dist release workflow.

**Before tagging:** Ensure the OWNER fix from Task 1 is committed and pushed to main. The tag should point to a commit that includes the corrected install.sh.

**Steps for the user:**

1. Verify all changes are committed and pushed:
   ```bash
   git status
   git push origin main
   ```

2. Create and push the tag:
   ```bash
   git tag v0.1.0
   git push origin v0.1.0
   ```

3. Monitor the release workflow:
   - Visit https://github.com/themouette/claude-plan-reviewer/actions
   - The "Release" workflow should trigger automatically on the tag push
   - Wait for all jobs to complete (plan, build-local-artifacts, build-global-artifacts, host, announce)

4. Verify the release:
   - Visit https://github.com/themouette/claude-plan-reviewer/releases
   - Confirm v0.1.0 release exists with four archive assets:
     - plan-reviewer-v0.1.0-aarch64-apple-darwin.tar.xz
     - plan-reviewer-v0.1.0-x86_64-apple-darwin.tar.xz
     - plan-reviewer-v0.1.0-x86_64-unknown-linux-musl.tar.xz
     - plan-reviewer-v0.1.0-aarch64-unknown-linux-musl.tar.xz
  </action>
  <verify>
    <automated>MISSING -- requires GitHub Actions CI execution and manual release inspection</automated>
  </verify>
  <acceptance_criteria>
    - git tag v0.1.0 exists locally (git tag -l v0.1.0 returns "v0.1.0")
    - Tag is pushed to origin (git ls-remote --tags origin shows v0.1.0)
    - GitHub Actions "Release" workflow completes successfully
    - GitHub Releases page shows v0.1.0 with four platform archives
  </acceptance_criteria>
  <done>
    - v0.1.0 tag pushed to origin
    - GitHub Actions release workflow triggered and completed
    - Four platform binaries published to GitHub Releases (DIST-01)
    - curl -fsSL .../install.sh | sh can now resolve and download binaries (DIST-02 unblocked)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh REPO variable -> GitHub API | Correcting OWNER to themouette ensures the script hits the correct GitHub API endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing | install.sh OWNER placeholder | mitigate | Replace OWNER with "themouette" per Cargo.toml authoritative source. This closes the placeholder gap identified in the original threat model. |
</threat_model>

<verification>
Phase 3, Plan 03 (gap closure) is complete when:

1. `grep 'OWNER' install.sh` returns zero matches (placeholder fully replaced)
2. `grep 'themouette/claude-plan-reviewer' install.sh` returns matches on lines 4 and 9
3. `sh -n install.sh` exits 0 (shell syntax valid)
4. `git tag -l v0.1.0` returns "v0.1.0" (tag exists locally)
5. GitHub Releases page shows v0.1.0 with four platform archives (human-verified)
</verification>

<success_criteria>
- install.sh uses "themouette/claude-plan-reviewer" as the REPO value (Gap 2 from VERIFICATION.md closed)
- v0.1.0 release published to GitHub Releases with binaries for all four targets (Gap 1 from VERIFICATION.md closed)
- DIST-01 and DIST-02 requirements fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-distribution/03-03-SUMMARY.md` using the summary template.
</output>
