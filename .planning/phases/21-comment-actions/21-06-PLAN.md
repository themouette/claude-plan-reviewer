---
phase: 21-comment-actions
plan: 06
type: execute
wave: 1
depends_on: ["21-05"]
files_modified:
  - ui/src/reviewer-v2/SelectionToolbar.tsx
  - ui/src/reviewer-v2/SelectionToolbar.test.ts
  - ui/src/reviewer-v2/ContentPane.tsx
  - ui/src/reviewer-v2/ContentPane.test.ts
  - ui/src/reviewer-v2/AnnotationForm.tsx
autonomous: true
gap_closure: true
requirements: [COMMENT-04, COMMENT-05, OUTLINE-04]

must_haves:
  truths:
    - "Delete pill directly creates a delete bubble — no textarea popup opens"
    - "All 6 predefined quick actions directly create comment bubbles — no textarea popup opens"
    - "Replace pill still opens a form, and that form has an orange/amber accent (border: 1px solid #f59e0b) indicating replace type"
    - "QUICK_ACTIONS[4] is 'Search the web' (was 'search internet')"
    - "All vitest tests pass with zero failures (cd ui && npm test -- --run)"
  artifacts:
    - path: "ui/src/reviewer-v2/SelectionToolbar.tsx"
      provides: "QUICK_ACTIONS with corrected 'Search the web' label at index 4"
      contains: "Search the web"
    - path: "ui/src/reviewer-v2/ContentPane.tsx"
      provides: "handleAction with delete/predefined bypass paths and D-04-correct form-open path"
      contains: "type === 'delete' || prefillComment !== undefined"
    - path: "ui/src/reviewer-v2/AnnotationForm.tsx"
      provides: "Replace-type orange border accent on the container"
      contains: "formState.type === 'replace'"
  key_links:
    - from: "ui/src/reviewer-v2/ContentPane.tsx"
      to: "ui/src/reviewer-v2/SelectionToolbar.tsx"
      via: "onAction callback"
      pattern: "onAction={handleAction}"
    - from: "ui/src/reviewer-v2/ContentPane.tsx"
      to: "ui/src/reviewer-v2/AnnotationForm.tsx"
      via: "formState prop"
      pattern: "formState={formState}"

threat_model:
  threats: []
  note: "Gap closure for local-only UI behavior. No new attack surface — all changes are pure in-browser state mutations with no network calls, no persistent storage, no new DOM injection beyond what phase 21 already established."
---

<objective>
Fix three UAT failures from 21-HUMAN-UAT.md in a single plan:

**Failure 2 — Delete quick action:** Delete pill should bypass the textarea popup and directly create a delete bubble. Confirmed by UAT: "Delete should directly create a bubble in the comment column without opening a textarea popup."

**Failure 3 — Predefined actions:** All 6 predefined actions (expandable menu) should bypass the textarea popup and directly create comment bubbles. Confirmed by UAT: "Predefined actions should behave like Delete: no popup, create a comment bubble directly on the sidebar."

**Label discrepancy — VERIFICATION.md SC-2:** `'search internet'` in QUICK_ACTIONS does not match the REQUIREMENTS.md requirement `"Search the web"`. Fix to `'Search the web'`.

Also fix Replace color: when Replace pill IS used (it still opens a form, unlike Delete), the AnnotationForm should show an orange/amber accent border so the type is visually distinct.

Output:
- `SelectionToolbar.tsx`: 'search internet' → 'Search the web'
- `ContentPane.tsx`: bypass paths in handleAction for delete and predefined actions
- `AnnotationForm.tsx`: orange border when formState.type === 'replace'
- `SelectionToolbar.test.ts`: update tuple test for 'Search the web'
- `ContentPane.test.ts`: update D-04 test to reflect bypass semantics
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-comment-actions/21-CONTEXT.md
@.planning/phases/21-comment-actions/21-HUMAN-UAT.md
@.planning/phases/21-comment-actions/21-VERIFICATION.md
@ui/src/reviewer-v2/SelectionToolbar.tsx
@ui/src/reviewer-v2/ContentPane.tsx
@ui/src/reviewer-v2/AnnotationForm.tsx
</context>

---

<task id="06-T1">
<title>Fix QUICK_ACTIONS label: 'search internet' → 'Search the web'</title>

<read_first>
- ui/src/reviewer-v2/SelectionToolbar.tsx — current QUICK_ACTIONS array (the `'search internet'` entry is index 4)
- ui/src/reviewer-v2/SelectionToolbar.test.ts — the test that asserts the exact tuple; it hardcodes `'search internet'` and must be updated
</read_first>

<action>
In `SelectionToolbar.tsx`, change the 5th entry of `QUICK_ACTIONS` from `'search internet'` to `'Search the web'`.

In `SelectionToolbar.test.ts`, update the `'equals the exact 6-label tuple from REQUIREMENTS.md COMMENT-04'` test: replace `'search internet'` with `'Search the web'` in the `.toEqual(...)` array.

No other test changes needed for this task.
</action>

<acceptance_criteria>
- `grep -n "search internet" ui/src/reviewer-v2/SelectionToolbar.tsx` returns no matches
- `grep -n "Search the web" ui/src/reviewer-v2/SelectionToolbar.tsx` returns one match in QUICK_ACTIONS
- `grep -n "search internet" ui/src/reviewer-v2/SelectionToolbar.test.ts` returns no matches
- `grep -n "Search the web" ui/src/reviewer-v2/SelectionToolbar.test.ts` returns one match in the tuple assertion
- `cd ui && npm test -- --run SelectionToolbar` exits 0
</acceptance_criteria>
</task>

<task id="06-T2">
<title>Add delete and predefined-action bypass paths to handleAction</title>

<read_first>
- ui/src/reviewer-v2/ContentPane.tsx — read the full `handleAction` function carefully; note: (a) the D-03 auto-submit block at the end, (b) the prefill determination block, (c) the `setFormState` call, (d) the comment "CRITICAL: Do NOT call resetTextSelection() here"
- ui/src/reviewer-v2/types.ts — `AnnotationType` union (`'comment' | 'delete' | 'replace'`)
</read_first>

<action>
Restructure `handleAction` in `ContentPane.tsx` to add two bypass paths. The new control flow is:

1. Get offsets — same as current (`const offsets = getOffsets()`)
2. D-03 auto-submit — move to happen BEFORE bypass/form decision; use `latestFormValueRef.current || formState.prefill` as the comment (fallback to prefill when user hasn't typed)
3. **Bypass path** (new): `if (type === 'delete' || prefillComment !== undefined)` → call `onAddAnnotation` directly with:
   - `comment`: `type === 'delete' ? 'Delete' : prefillComment!`
   - `type`: as passed
   - `anchorStart: offsets.start`, `anchorEnd: offsets.end`, `anchorText`, `id: crypto.randomUUID()`
   - Then `setFormState(null)`, `latestFormValueRef.current = ''`, `resetTextSelection()`, `return`
4. **Form path** (existing — now only reached for `comment` and `replace` types): reconstruct range for positioning, compute prefill (`type === 'replace' ? 'Replace' : ''`), call `setFormState(...)` — do NOT call `resetTextSelection()` here (D-04 still applies to the form-open path)

Remove the comment "CRITICAL: Do NOT call resetTextSelection() here" from the top of the function and replace it with a comment in the form path explaining D-04 still applies there.

Do NOT change the function signature: `function handleAction(type: AnnotationType, anchorText: string, prefillComment?: string)`
</action>

<acceptance_criteria>
- `grep -n "type === 'delete' || prefillComment !== undefined" ui/src/reviewer-v2/ContentPane.tsx` returns one match
- `grep -n "setFormState({" ui/src/reviewer-v2/ContentPane.tsx` returns one match (only the form path)
- `grep -n "resetTextSelection()" ui/src/reviewer-v2/ContentPane.tsx` — returns matches including one in the bypass block
- The delete bypass does NOT call `setFormState`; trace: if `type === 'delete'`, execution returns before reaching `setFormState`
- `grep -n "|| formState.prefill" ui/src/reviewer-v2/ContentPane.tsx` returns one match in the D-03 block
- `cd ui && npm test -- --run ContentPane` exits 0 (test file also updated in T3)
</acceptance_criteria>
</task>

<task id="06-T3">
<title>Update ContentPane.test.ts D-04 test to reflect bypass semantics</title>

<read_first>
- ui/src/reviewer-v2/ContentPane.test.ts — read the `'handleAction body does NOT call resetTextSelection (D-04)'` test case; it will fail after T2 because `handleAction` now calls `resetTextSelection()` in bypass paths
</read_first>

<action>
In `ContentPane.test.ts`, replace the existing `'handleAction body does NOT call resetTextSelection (D-04)'` test with two new tests:

```
it('handleAction bypasses form and calls resetTextSelection for delete type and predefined actions', () => {
  expect(source).toContain("type === 'delete' || prefillComment !== undefined")
  expect(source).toContain('resetTextSelection()')
})

it('handleAction opens form (setFormState) for comment and replace types without clearing selection (D-04)', () => {
  expect(source).toContain('setFormState({')
  expect(source).toMatch(/type.*'replace'.*\?.*'Replace'/)
})
```

No other test changes needed for T3.
</action>

<acceptance_criteria>
- `grep -n "handleAction body does NOT call resetTextSelection" ui/src/reviewer-v2/ContentPane.test.ts` returns no matches (old test removed)
- Two new test cases are present: one asserting bypass path, one asserting form path
- `cd ui && npm test -- --run ContentPane` exits 0
</acceptance_criteria>
</task>

<task id="06-T4">
<title>Add orange/amber accent to AnnotationForm for 'replace' type</title>

<read_first>
- ui/src/reviewer-v2/AnnotationForm.tsx — read the container `<div>` style (currently `border: '1px solid var(--color-border)'`); this is the border to change conditionally
- ui/src/reviewer-v2/SelectionToolbar.tsx — replace pill color is `#f59e0b` (amber); use this exact hex for consistency
</read_first>

<action>
In `AnnotationForm.tsx`, change the container div's `border` style from the fixed `'1px solid var(--color-border)'` to a conditional:

```
border: formState.type === 'replace' ? '1px solid #f59e0b' : '1px solid var(--color-border)'
```

No other style changes. No prop signature changes.
</action>

<acceptance_criteria>
- `grep -n "formState.type === 'replace'" ui/src/reviewer-v2/AnnotationForm.tsx` returns one match
- `grep -n "#f59e0b" ui/src/reviewer-v2/AnnotationForm.tsx` returns one match (the replace border color)
- `grep -n "var(--color-border)" ui/src/reviewer-v2/AnnotationForm.tsx` still returns a match (fallback for non-replace types)
- `cd ui && npm test -- --run AnnotationForm` exits 0
</acceptance_criteria>
</task>

<task id="06-T5">
<title>Full vitest suite green check</title>

<read_first>
- All modified files above
</read_first>

<action>
Run the full vitest suite to confirm all 5 modified files pass and no regressions in other tests.
</action>

<acceptance_criteria>
- `cd ui && npm test -- --run` exits 0 with 0 failed tests
- Test count equals or exceeds the pre-gap-closure baseline of 312 tests (new tests added in T3 may increase this slightly)
</acceptance_criteria>
</task>

<verification>
Run `cd ui && npm test -- --run` — all tests must pass.

Manual smoke check (if dev server running):
1. Select text → click "Delete" pill → bubble appears immediately in sidebar, NO popup
2. Select text → click "▾ more" → click "Search the web" → bubble appears immediately in sidebar, NO popup  
3. Select text → click "Search the web" → bubble comment text is "Search the web" (not "search internet")
4. Select text → click "Replace" → popup opens with orange border, "Replace" pre-filled
</verification>
