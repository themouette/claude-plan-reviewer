---
phase: 21-comment-actions
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - ui/src/reviewer-v2/ReviewerV2Shell.tsx
  - ui/src/reviewer-v2/ReviewerV2Shell.test.ts
  - ui/src/reviewer-v2/OutlinePane.tsx
  - ui/src/reviewer-v2/OutlinePane.test.ts
autonomous: false
requirements: [COMMENT-05, OUTLINE-04]
tags: [react-19, shell-state-lift, integration, css-custom-properties, human-verify]

must_haves:
  truths:
    - "Submitting an annotation via AnnotationForm causes a bubble to appear in the right pane"
    - "Clicking the pencil icon on a bubble switches the bubble to edit mode (textarea visible)"
    - "Saving an edit updates annotation.comment in the store; the bubble returns to display mode"
    - "Clicking × removes the bubble immediately; no confirmation dialog appears"
    - "Each outline item with comments shows a count badge; sections with no comments show no badge"
    - "Adding a comment causes the badge for the owning section to update immediately"
  artifacts:
    - path: "ui/src/reviewer-v2/ReviewerV2Shell.tsx"
      provides: "editingId state + useSectionAnnotationCounts call + complete prop forwarding"
      contains: "editingId"
    - path: "ui/src/reviewer-v2/OutlinePane.tsx"
      provides: "annotationCounts prop + per-section count badge rendering"
      contains: "annotationCounts"
  key_links:
    - from: "ui/src/reviewer-v2/ReviewerV2Shell.tsx"
      to: "ui/src/reviewer-v2/hooks/useSectionAnnotationCounts.ts"
      via: "import { useSectionAnnotationCounts }"
      pattern: "import.*useSectionAnnotationCounts.*from.*hooks/useSectionAnnotationCounts"
    - from: "ui/src/reviewer-v2/ReviewerV2Shell.tsx"
      to: "ui/src/reviewer-v2/CommentPane.tsx"
      via: "props editingId, onEdit, onRemove, onCancelEdit"
      pattern: "editingId=\\{editingId\\}"
    - from: "ui/src/reviewer-v2/ReviewerV2Shell.tsx"
      to: "ui/src/reviewer-v2/OutlinePane.tsx"
      via: "prop annotationCounts={annotationCounts}"
      pattern: "annotationCounts=\\{annotationCounts\\}"
---

<objective>
Integrate every Phase 21 component built in Plans 01-03 by:
- Lifting `editingId: string | null` state into `ReviewerV2Shell` and wiring the three new callbacks (`onEdit`, `onRemove`, `onCancelEdit`) through to `CommentPane`.
- Destructuring `editAnnotation` and `removeAnnotation` from `useAnnotations` (already exported, never previously consumed).
- Adding the `useSectionAnnotationCounts(sections, annotations, planRef)` call in the Shell and threading the resulting `Map<string, number>` into `OutlinePane` via a new `annotationCounts` prop.
- Rendering the per-section count badge inside each `OutlinePane` button per UI-SPEC.
- Extending the existing Escape-key handler to also clear `editingId` (so users can cancel inline edits via Escape from anywhere on the page).
- A human-verification checkpoint at the end of the wave, since Phase 21 introduces user-visible behavior across three new flows (create / edit / delete) plus the outline badge.

Purpose: This plan closes Phase 21. After it lands, all three requirements (COMMENT-04, COMMENT-05, OUTLINE-04) are fully satisfied. It is autonomous: false because the checkpoint is part of the plan.

Output:
- `ReviewerV2Shell.tsx`: `editingId` state, `editAnnotation`/`removeAnnotation` destructuring, `useSectionAnnotationCounts` call, prop forwarding to both `CommentPane` and `OutlinePane`, Escape handler extension.
- `OutlinePane.tsx`: `annotationCounts?: Map<string, number>` prop + inline badge `<span>` rendered when `count > 0`.
- Test updates covering all the wiring patterns.
- A `checkpoint:human-verify` task driving the user through all three flows + the badge update.
</objective>

<execution_context>
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-comment-actions/21-CONTEXT.md
@.planning/phases/21-comment-actions/21-RESEARCH.md
@.planning/phases/21-comment-actions/21-PATTERNS.md
@.planning/phases/21-comment-actions/21-UI-SPEC.md
@.planning/phases/21-comment-actions/21-01-PLAN.md
@.planning/phases/21-comment-actions/21-02-PLAN.md
@.planning/phases/21-comment-actions/21-03-PLAN.md
@ui/src/reviewer-v2/ReviewerV2Shell.tsx
@ui/src/reviewer-v2/ReviewerV2Shell.test.ts
@ui/src/reviewer-v2/OutlinePane.tsx
@ui/src/reviewer-v2/OutlinePane.test.ts
@ui/src/reviewer-v2/CommentPane.tsx
@ui/src/reviewer-v2/useAnnotations.ts
@ui/src/reviewer-v2/types.ts

<interfaces>
<!-- Contracts this plan consumes from Plans 01 and 03. -->

From `./hooks/useSectionAnnotationCounts` (Plan 01):
```typescript
export function useSectionAnnotationCounts(
  sections: Section[],
  annotations: Annotation[],
  planRef: RefObject<HTMLDivElement | null>,
): Map<string, number>
```

From `./CommentPane` (Plan 03, NEW required props):
```typescript
editingId: string | null
onEdit: (id: string, newComment?: string) => void
onRemove: (id: string) => void
onCancelEdit: () => void
```

From `./useAnnotations` (existing, never previously destructured):
```typescript
const { annotations, addAnnotation, editAnnotation, removeAnnotation } = useAnnotations()
```

Shell wires CommentPane like this (the combined open-edit / save-edit callback):
```typescript
onEdit={(id, newComment) => {
  if (newComment !== undefined) {
    editAnnotation(id, newComment)
    setEditingId(null)
  } else {
    setEditingId(id)
  }
}}
onRemove={removeAnnotation}
onCancelEdit={() => setEditingId(null)}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OutlinePane annotationCounts prop + badge rendering</name>
  <files>
    ui/src/reviewer-v2/OutlinePane.tsx,
    ui/src/reviewer-v2/OutlinePane.test.ts
  </files>
  <read_first>
    ui/src/reviewer-v2/OutlinePane.tsx,
    ui/src/reviewer-v2/OutlinePane.test.ts,
    ui/src/index.css,
    .planning/phases/21-comment-actions/21-UI-SPEC.md,
    .planning/phases/21-comment-actions/21-PATTERNS.md
  </read_first>
  <behavior>
    - New optional prop: `annotationCounts?: Map<string, number>`. When undefined or count <= 0 for a given section, NO badge is rendered (badge is absent from the DOM, not hidden via opacity).
    - When count > 0: render a `<span aria-label="{count} comments">` inside the existing button after `{section.text}`. The badge uses `font-size: 11px`, `font-weight: 600`, `line-height: 1`, `min-width: 16px`, `height: 16px`, `padding: 0 4px`, `border-radius: 8px`, `display: 'inline-flex'`, `align-items: 'center'`, `justify-content: 'center'`, `margin-left: 8px`.
    - Color switching based on `section.id === activeId`:
      - Active: `background: 'var(--color-focus)'`, `color: '#fff'`
      - Inactive: `background: 'rgba(59, 130, 246, 0.25)'`, `color: 'var(--color-focus)'`
    - All existing IntersectionObserver, click-to-scroll, and auto-scroll behaviors UNCHANGED.
  </behavior>
  <action>
    Edit `ui/src/reviewer-v2/OutlinePane.tsx`:
    - Add to the prop interface (lines 4-14): `annotationCounts?: Map<string, number>`. Destructure in the function signature.
    - Inside the `<button>` element (current lines 58-92), AFTER `{section.text}` (line 91) and BEFORE the button's closing tag, insert: `{(annotationCounts?.get(section.id) ?? 0) > 0 && (<span aria-label={\`${annotationCounts!.get(section.id)} comments\`} style={{ fontSize: 11, fontWeight: 600, lineHeight: 1, minWidth: 16, height: 16, padding: '0 4px', borderRadius: 8, display: 'inline-flex', alignItems: 'center', justifyContent: 'center', marginLeft: 8, background: section.id === activeId ? 'var(--color-focus)' : 'rgba(59, 130, 246, 0.25)', color: section.id === activeId ? '#fff' : 'var(--color-focus)' }}>{annotationCounts!.get(section.id)}</span>)}`.
    - Note: the `annotationCounts!.get(...)` non-null assertions are safe because the surrounding `(annotationCounts?.get(...) ?? 0) > 0` check guarantees the Map exists and the key is present with a positive value.

    Edit `ui/src/reviewer-v2/OutlinePane.test.ts`: ADD a new `describe('OutlinePane annotation count badge (Phase 21)', ...)` block with these source-as-text assertions:
    - source contains `annotationCounts`
    - source contains `annotationCounts?.get(section.id)` (the count lookup)
    - source contains `aria-label={\`${annotationCounts!.get(section.id)} comments\`}` OR a regex-tolerant variant — use `expect(source).toMatch(/aria-label=.*comments/)` so minor literal variations pass while structural content is pinned
    - source contains `var(--color-focus)` (badge active background)
    - source contains `rgba(59, 130, 246, 0.25)` (badge inactive background)
    - source contains `borderRadius: 8` (badge pill radius)
    - source contains `minWidth: 16` (badge min size)
    - source contains `marginLeft: 8` (badge spacing from section text)
    Keep all existing assertions intact.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run --reporter=verbose OutlinePane</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/reviewer-v2/OutlinePane.tsx` prop interface contains `annotationCounts?: Map<string, number>`
    - `ui/src/reviewer-v2/OutlinePane.tsx` contains the conditional `(annotationCounts?.get(section.id) ?? 0) > 0` guard
    - `ui/src/reviewer-v2/OutlinePane.tsx` badge `<span>` uses `var(--color-focus)` (active) and `rgba(59, 130, 246, 0.25)` (inactive)
    - `ui/src/reviewer-v2/OutlinePane.tsx` badge has `aria-label` containing the literal text `comments`
    - `ui/src/reviewer-v2/OutlinePane.test.ts` Phase 21 describe block exists with all listed assertions
    - `cd ui && npm test -- --run OutlinePane` exits 0
    - `cd ui && npm run lint` reports no new errors
  </acceptance_criteria>
  <done>
    OutlinePane renders the OUTLINE-04 count badge inline with each section button when the count is > 0, with active/inactive color switching tied to `activeId`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReviewerV2Shell integration — editingId state, useSectionAnnotationCounts call, prop forwarding</name>
  <files>
    ui/src/reviewer-v2/ReviewerV2Shell.tsx,
    ui/src/reviewer-v2/ReviewerV2Shell.test.ts
  </files>
  <read_first>
    ui/src/reviewer-v2/ReviewerV2Shell.tsx,
    ui/src/reviewer-v2/ReviewerV2Shell.test.ts,
    ui/src/reviewer-v2/CommentPane.tsx,
    ui/src/reviewer-v2/OutlinePane.tsx,
    ui/src/reviewer-v2/useAnnotations.ts,
    ui/src/reviewer-v2/hooks/useSectionAnnotationCounts.ts,
    .planning/phases/21-comment-actions/21-CONTEXT.md,
    .planning/phases/21-comment-actions/21-RESEARCH.md,
    .planning/phases/21-comment-actions/21-PATTERNS.md
  </read_first>
  <behavior>
    - Shell destructures the full `useAnnotations()` API: `const { annotations, addAnnotation, editAnnotation, removeAnnotation } = useAnnotations()`.
    - Shell holds new state: `const [editingId, setEditingId] = useState<string | null>(null)`, placed parallel to `hoveredCommentId` / `focusedCommentId`.
    - Shell calls `const annotationCounts = useSectionAnnotationCounts(sections, annotations, planRef)` once per render (the hook is memoized via `useMemo` internally per Plan 01).
    - The existing Escape keydown handler (lines 17-23) is extended: inside the `if (e.key === 'Escape')` branch, ALSO call `setEditingId(null)`. Both `focusedCommentId` and `editingId` clear on Escape.
    - `OutlinePane` receives the new prop: `annotationCounts={annotationCounts}`.
    - `CommentPane` receives 4 new props:
      - `editingId={editingId}`
      - `onEdit={(id, newComment) => { if (newComment !== undefined) { editAnnotation(id, newComment); setEditingId(null) } else { setEditingId(id) } }}`
      - `onRemove={removeAnnotation}`
      - `onCancelEdit={() => setEditingId(null)}`
    - No other behavior changes. ContentPane wiring (Plan 02 territory) is untouched.
  </behavior>
  <action>
    Edit `ui/src/reviewer-v2/ReviewerV2Shell.tsx`:
    - Add import: `import { useSectionAnnotationCounts } from './hooks/useSectionAnnotationCounts'`.
    - Replace `const { annotations, addAnnotation } = useAnnotations()` (line 13) with `const { annotations, addAnnotation, editAnnotation, removeAnnotation } = useAnnotations()`.
    - Add new state below existing `focusedCommentId` declaration (line 15): `const [editingId, setEditingId] = useState<string | null>(null)`.
    - Add the hook call below the new state: `const annotationCounts = useSectionAnnotationCounts(sections, annotations, planRef)`.
    - Modify the Escape handler (lines 17-23): inside the existing `if (e.key === 'Escape')` block, add `setEditingId(null)` alongside `setFocusedCommentId(null)`.
    - On the `<OutlinePane>` JSX element (current lines 64-69): add `annotationCounts={annotationCounts}` as a new prop.
    - On the `<CommentPane>` JSX element (current lines 100-109): add four new props:
      - `editingId={editingId}`
      - `onEdit={(id, newComment) => { if (newComment !== undefined) { editAnnotation(id, newComment); setEditingId(null) } else { setEditingId(id) } }}`
      - `onRemove={removeAnnotation}`
      - `onCancelEdit={() => setEditingId(null)}`

    Edit `ui/src/reviewer-v2/ReviewerV2Shell.test.ts`: ADD a new `describe('ReviewerV2Shell editingId + annotationCounts wiring (Phase 21)', ...)` block with these source-as-text assertions:
    - source contains `editAnnotation`
    - source contains `removeAnnotation`
    - source contains `useState<string | null>(null)` AT LEAST twice (one for `focusedCommentId`, one for `editingId`) — use `const matches = source.match(/useState<string \| null>\(null\)/g) ?? []; expect(matches.length).toBeGreaterThanOrEqual(2)`
    - source contains `useSectionAnnotationCounts(sections, annotations, planRef)`
    - source contains `annotationCounts={annotationCounts}`
    - source contains `editingId={editingId}`
    - source contains `onCancelEdit={() => setEditingId(null)}` (or an equivalent inline closure — use `expect(source).toMatch(/onCancelEdit=\{.*setEditingId\(null\).*\}/)`)
    - source contains `setEditingId(null)` (Escape extension)
    - source contains `editAnnotation(id, newComment)` (the commit branch of the onEdit callback)
    - source contains `import { useSectionAnnotationCounts } from './hooks/useSectionAnnotationCounts'`
    Keep all existing assertions intact.
  </action>
  <verify>
    <automated>cd ui && npm test -- --run --reporter=verbose ReviewerV2Shell &amp;&amp; cd ui && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/reviewer-v2/ReviewerV2Shell.tsx` imports `useSectionAnnotationCounts` from `./hooks/useSectionAnnotationCounts`
    - `ui/src/reviewer-v2/ReviewerV2Shell.tsx` destructures `editAnnotation` and `removeAnnotation` from `useAnnotations()`
    - `ui/src/reviewer-v2/ReviewerV2Shell.tsx` declares `const [editingId, setEditingId] = useState<string | null>(null)`
    - `ui/src/reviewer-v2/ReviewerV2Shell.tsx` calls `useSectionAnnotationCounts(sections, annotations, planRef)`
    - `ui/src/reviewer-v2/ReviewerV2Shell.tsx` Escape handler calls BOTH `setFocusedCommentId(null)` AND `setEditingId(null)`
    - `ui/src/reviewer-v2/ReviewerV2Shell.tsx` `<OutlinePane>` receives `annotationCounts={annotationCounts}`
    - `ui/src/reviewer-v2/ReviewerV2Shell.tsx` `<CommentPane>` receives `editingId`, `onEdit`, `onRemove`, `onCancelEdit` props
    - `ui/src/reviewer-v2/ReviewerV2Shell.tsx` `onEdit` callback handles BOTH the "no arg → setEditingId(id)" and "arg → editAnnotation + clear" branches
    - `ui/src/reviewer-v2/ReviewerV2Shell.test.ts` Phase 21 describe block exists with all assertions
    - `cd ui && npm test -- --run ReviewerV2Shell` exits 0
    - `cd ui && npm run lint` reports no new errors
    - `cd ui && npm run build` succeeds — this proves the whole tree compiles after Phase 21 (Plans 01-03 leave it in a non-buildable state because CommentPane requires the new props, which only this plan supplies)
  </acceptance_criteria>
  <done>
    All Plan 21 pieces are wired together via the Shell. The full annotation lifecycle (create, edit, delete) is now functional end-to-end, and the OUTLINE-04 count badge updates on every annotation change because `useSectionAnnotationCounts` memoizes on `annotations`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — full create / edit / delete / badge flow</name>
  <files>(no files — verification only)</files>
  <read_first>
    .planning/phases/21-comment-actions/21-UI-SPEC.md,
    .planning/phases/21-comment-actions/21-CONTEXT.md
  </read_first>
  <what-built>
    Phase 21 complete: AnnotationForm popover, gutter-icon programmatic paragraph selection, CommentBubble edit/delete with inline textarea edit mode, sticky pinning for editing bubble, OutlinePane per-section count badges, full ReviewerV2Shell wiring. All COMMENT-04, COMMENT-05, and OUTLINE-04 requirements should now be observable in the browser.
  </what-built>
  <how-to-verify>
    Start the dev server: `cd ui && npm run dev` and `cargo run -- review fixtures/sample-plan.md` in another terminal (or use any existing fixture you ran with Phase 20 — same flow). Then in the browser tab opened at `http://127.0.0.1:{port}/v2`:

    1. **Comment pill flow (COMMENT-04 / D-01 / D-02):**
       - Select a sentence in the center pane.
       - Click the "Comment" pill in the SelectionToolbar.
       - VERIFY: SelectionToolbar disappears; a fixed-position popover with a textarea + "Post Comment" + "Dismiss" buttons appears at the same screen location.
       - VERIFY: textarea has focus (cursor blinking inside it) and the placeholder reads "Add a comment…".
       - VERIFY: the blue selection-lock highlight on the selected text stays visible while the form is open (D-04).
       - Type "test comment 1" and press Cmd+Enter (or Ctrl+Enter on Linux/Windows).
       - VERIFY: the form closes; the selection-lock highlight clears; a new bubble appears in the right pane with the comment text "test comment 1".

    2. **Delete / Replace pre-fill (COMMENT-04 prefills):**
       - Select another sentence.
       - Click the "Delete" pill.
       - VERIFY: textarea opens pre-filled with the word "Delete".
       - Click "Dismiss" (or press Escape).
       - VERIFY: form closes; no bubble is created.
       - Repeat with the "Replace" pill — VERIFY: textarea opens pre-filled with "Replace".
       - Click "Dismiss" again.

    3. **Predefined-actions menu (COMMENT-04 expandable menu):**
       - Select more text.
       - Click the "▾ more" expander in the SelectionToolbar.
       - Click "clarify this".
       - VERIFY: textarea opens pre-filled with "clarify this".
       - Submit by clicking the "Post Comment" button. VERIFY: new bubble appears with comment "clarify this".

    4. **Gutter-icon paragraph selection (COMMENT-04 / D-06):**
       - Hover a paragraph; the `+` gutter icon should appear on the right edge of the paragraph.
       - Click `+`.
       - VERIFY: the ENTIRE paragraph becomes visually selected (blue selection-lock highlight covers the whole paragraph) and the SelectionToolbar appears anchored to the end of the paragraph with all 3 pills + the "▾ more" expander.
       - Click any pill to confirm the same flow continues.

    5. **Auto-submit on conflict (D-03):**
       - Start a new annotation: select text, click "Comment", type "first comment".
       - WITHOUT submitting, select different text and click "Delete".
       - VERIFY: a bubble for "first comment" appears in the right pane (auto-submitted) AND the new annotation form is now open with prefill "Delete".
       - Dismiss the new form.

    6. **Edit flow (COMMENT-05 / D-08, D-10, D-12):**
       - Click any bubble in the right pane to focus it (bubble expands; type tag appears).
       - VERIFY: a pencil icon `✎` and an `×` icon appear at the bottom-right of the bubble.
       - Click the pencil.
       - VERIFY: the comment text is replaced by a textarea pre-filled with the existing comment; textarea has focus; "Save Changes" and "Discard Changes" buttons replace the icon row.
       - VERIFY: as you scroll the page, the editing bubble stays visible (sticky pinning, D-12).
       - Edit the text and press Cmd+Enter.
       - VERIFY: the textarea closes; the bubble shows the updated comment text; the bubble returns to expanded display mode.
       - Re-focus the bubble, click pencil, edit text, press Escape.
       - VERIFY: textarea closes; the original comment text is restored (Discard).

    7. **Delete flow (COMMENT-05 / D-09):**
       - Focus a bubble; click the `×` icon.
       - VERIFY: the bubble disappears immediately with no confirmation dialog. No browser confirm() or modal appears.

    8. **Section count badges (OUTLINE-04 / D-13, D-14):**
       - Verify the document has at least 2 H1/H2 headings. Look at the left outline panel.
       - VERIFY: each section that contains at least one comment shows a small numeric badge next to the section title.
       - VERIFY: sections with no comments show NO badge (count = 0 means no DOM element).
       - VERIFY: the badge for the currently-active section uses solid blue background (`var(--color-focus)`); inactive sections use translucent blue (`rgba(59, 130, 246, 0.25)`).
       - Add a new comment in a section. VERIFY: that section's badge count increments immediately (no page refresh needed).
       - Delete a comment. VERIFY: the badge count decrements; if it reaches 0, the badge disappears.

    9. **Escape clears edit mode globally:**
       - Click pencil on a bubble to enter edit mode.
       - Click anywhere outside the bubble (without clicking another bubble).
       - Press Escape.
       - VERIFY: edit mode exits; bubble returns to display mode.

    If any step fails, describe which step (1-9) and what you observed.
  </how-to-verify>
  <action>Pause execution and present the verification steps in <how-to-verify> to the user. Wait for an explicit approval signal before marking Phase 21 complete. This is a checkpoint:human-verify gate — no autonomous file edits.</action>
  <verify>
    <automated>cd ui && npm run dev (background) then open http://127.0.0.1:{port}/v2 in the browser opened by `cargo run -- review fixtures/sample-plan.md` — the verification itself is performed by the user reporting results for steps 1-9</automated>
  </verify>
  <done>User explicitly types "approved" in response to the verification prompt. If any step fails, the user describes the failing step(s); the executor opens a gap-closure ticket rather than marking Phase 21 complete.</done>
  <resume-signal>Type "approved" if all 9 steps pass; otherwise describe the failing step(s) and observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell editingId state -> CommentPane wrapper position | State transition controls global UI layout |
| useSectionAnnotationCounts -> OutlinePane render | Section badge values come from a memoized hook that walks DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-14 | Tampering | Combined onEdit callback (no-arg vs with-arg branches) | mitigate | The callback uses `if (newComment !== undefined)` (not truthy-check) so an empty-string save commits an empty comment as the user intended. Acceptance criterion asserts the literal `newComment !== undefined` is present. |
| T-21-15 | Denial of Service | useSectionAnnotationCounts recomputes on every render | mitigate | Plan 01 memoizes the hook on `[sections, annotations, planRef]`. Shell-side renders that change neither sections nor annotations skip the computation. Acceptance criterion for Plan 01 verifies the useMemo wrapping. |
| T-21-16 | Repudiation | Escape clears BOTH focusedCommentId and editingId | accept | Documented as part of the Escape contract per D-02 and the human-verification step 9. User expectations align — Escape is universally "get out of this state". |
| T-21-17 | Information Disclosure | annotationCounts Map<string, number> includes section ids | accept | Section ids come from the user's plan markdown (slugified headings) and are not sensitive; they already appear in the OutlinePane buttons and URL anchors. |
| T-21-SC | Tampering | npm/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
After all tasks complete:

```bash
cd ui && npm test -- --run
cd ui && npm run lint
cd ui && npm run build
cargo test --workspace
```

All checks pass. The build succeeds because Plan 04 closes the prop-interface loop opened by Plan 03. Human verification step (Task 3) is the authoritative behavioral acceptance gate.
</verification>

<success_criteria>
- All four files (`ReviewerV2Shell.tsx`, `ReviewerV2Shell.test.ts`, `OutlinePane.tsx`, `OutlinePane.test.ts`) modified per the actions above
- Full UI build succeeds (`cd ui && npm run build` exits 0)
- All vitest unit tests pass (`cd ui && npm test -- --run` exits 0)
- All Rust tests pass (`cargo test --workspace` exits 0 — no Rust changes in Phase 21 means this should be a no-op confirmation)
- Human verification step 9-of-9 passes
- All three Phase 21 requirements (COMMENT-04, COMMENT-05, OUTLINE-04) are observable in the running app
</success_criteria>

<output>
Create `.planning/phases/21-comment-actions/21-04-SUMMARY.md` when done following @/Users/julien.muetton/Projects/lab/claude-plan-reviewer/.claude/get-shit-done/templates/summary.md
</output>
